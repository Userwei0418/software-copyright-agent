# 软件说明书正式生成管线

本计划以 `copyright-doc` 技能的输出契约为验收标准。最终产物是正式软件说明书 DOCX、可编辑 Draw.io 图表、SVG/PNG 预览与必要的真实界面截图；Markdown 只允许作为内部中间材料。

## 产品原则

1. 规则负责索引、证据追踪、格式校验和风险提示，不作为 AI 生成的硬门禁。
2. AI 先依据结构化线索定向研究代表性源码，再生成带证据引用的章节内容。
3. 图表从章节文意与项目证据共同派生，并绑定到具体章节位置。
4. 每个阶段独立持久化、独立重试；部分失败不得抹去已成功产物。
5. 用户只需要一个主流程入口，内部步骤放在进度与高级诊断中。
6. 最终 DOCX 必须经过渲染和逐页视觉检查后才能标记为可交付。

## 阶段与验收门

### 1. 生成任务与结构化数据模型

- 每次生成创建独立版本任务。
- 研究、正文、图表、截图、组装、质检分别记录状态、耗时和错误。
- 支持成功步骤复用与失败步骤重试。
- 旧 Markdown 草稿保留为 legacy 产物，不再视为最终说明书。

### 2. 项目线索图谱与 AI 研究

- 本地提取入口、模块、路由、服务、实体、接口、状态、部署和页面线索。
- 为每章选择代表性源码片段，禁止将整个仓库无差别发送给模型。
- AI 研究笔记必须区分已验证事实、合理推断和待确认信息。
- 当前状态：后端执行器、版本化研究产物、证据引用校验及阶段重试已完成；前端将在一键生成入口接入完整流水线时展示摘要与进度。

### 3. 结构化章节正文

- 正文保存为段落、列表、表格、图表请求等结构化内容块。
- 每个内容块保留证据引用和推断说明。
- 支持章节级生成、重新生成、人工编辑与版本比较。
- 当前状态：结构化正文生成、单章重试、部分成功保留、人工确认版本及历史读取后端已完成；待一键主流程接入页面。

### 4. 章节驱动的专业图表

- 根据正文决定架构图、模块图、流程图、时序图、ER 图或部署图。
- 每张图输出 `.drawio`、`.svg` 和高分辨率 `.png`。
- 图表必须绑定章节锚点并经过布局验证与视觉复查。
- 当前状态：正文驱动的图表语义、通用 Draw.io 构建、SVG/高分辨率 PNG、本地结构校验、版本历史与预览读取后端已完成；最终文档质检阶段仍会对实际项目图进行逐页视觉复查。

### 5. 界面截图

- 仅在项目可安全本地启动时自动截图，禁止为了截图触发真实付费调用或外部副作用。
- 截图记录页面用途、入口、操作流程、后台交互、结果和异常恢复。
- 不可启动时允许用户导入截图并明确标记来源。
- 当前状态：安全评估、人工导入、图片净化与完整性校验、章节锚点、六维说明及无界面/无适配器时的非阻塞跳过已完成；后续可替换接入 Playwright 等受控浏览器适配器。

### 6. DOCX 组装与资产管理

- 使用真实 Word 标题、段落、表格、图注、页眉页脚和页码。
- 图表 PNG 和截图插入到绑定章节，保留可编辑源文件。
- 程序内提供文档预览、版本、导出和文件夹定位。
- 当前状态：正式 Word 装配、一键主流程、程序内结构预览、版本历史、资产页、自选路径导出及跨平台文件夹定位已完成。导出文档内嵌开源 Noto CJK 中文字体，避免 Windows/macOS 因字体环境不同出现方框字；章节采用连续排版，不再强制每章另起一页。

### 7. 渲染与质量检查

- 将 DOCX 渲染为 PDF/页面图片并逐页检查。
- 检查中文字体、空白页、分页、表格断裂、图文错位、裁切和页面密度。
- 只有通过质量门的版本才能标记为“可交付”。
- 当前状态：运行时确定性 A4 分页器、PNG/PDF 版本产物、OOXML/字体/图片/页密度交叉检查、`qa_passed/qa_failed` 状态和导出门禁已完成。一键主流程自动执行该阶段，程序内按页查看检查所依据的预览；高级入口仍可只重试质检。运行时预览明确标注为与 DOCX 同源的 companion render，不冒充 Word 原生渲染。确定性六页 fixture 同时经过内置分页器和真实 LibreOffice 全页回归，两边均为 6 页且无空白、裁切、重叠、断表或中文字体替换。

### 8. 生成后修订与重新交付

- 正文生成后允许按章编辑段落、列表和表格，保存为新修订，不覆盖 AI 原稿或既有 DOCX。
- 允许用户显式要求 AI 仅重新生成当前章节；图表请求变化时在装配前同步章节图表。
- 尚未单独保存的本地章节修改会在装配前统一落库，再生成新的 DOCX 版本并自动执行逐页质检。
- 图表请求在正文编辑器中保持只读，避免随意改 key 导致正文锚点与 Draw.io/SVG/PNG 资产断联；具体图表内容在图表资产工作台修改。
- 当前状态：章节编辑、单章 AI 重写、未保存状态提示、批量保存、新版本装配、自动质检和旧版本保留的桌面闭环已完成。下一步接入正式说明书图表资产的专门编辑页和 AI 对话修改。

## 留痕约定

每个阶段完成时同步更新 `PROGRESS.md`，运行对应测试和构建，并以独立 Git 提交推送。提交说明必须对应单一可验收阶段。
