# 开发进度

> 本文件采用追加式记录。每轮结束都更新“当前状态”和新增一条轮次记录，确保进度、验证和下一步保存在仓库中。

## 当前状态

- 当前里程碑：M1 领域内核与 CLI 纵向链路。
- 总体状态：已形成从项目输入到 59 页代码正文分页预览的完整确定性链路。
- 当前分支：`main`。
- 当前阻塞：无。
- 下一轮目标：生成首版源代码 DOCX，并建立渲染后页数 QA。

## 里程碑概览

| 里程碑 | 状态 | 说明 |
|---|---|---|
| M0 需求和架构基线 | 暂停 | 核心详细设计完成，迁移与运行时评估随实现推进 |
| M1 领域内核与 CLI | 进行中 | 已完成至代码正文分页预览的纵向链路 |
| M2 模型适配与项目理解 | 未开始 | 依赖核心接口与状态机 |
| M3 源代码文档 | 未开始 | 依赖扫描、模型和领域内核 |
| M4 说明书与图表 | 未开始 | 依赖模型、DOCX 和 Draw.io 内核 |
| M5 桌面应用与发布 | 未开始 | 可在 M1 后建立薄纵切验证 |
| M6 1.1 自动截图 | 未开始 | MVP 后续阶段 |

## 轮次记录

### 2026-08-10：规划基线

本轮目标：

- 将已经对齐的产品边界转化为可维护的仓库文档。
- 建立每轮留痕和下一步同步机制。

完成内容：

- 确认产品完全不依赖 Codex。
- 确认产品为单机、本地工具，不包含多用户和自动申报。
- 确认使用 SQLite 进行轻量持久化。
- 确认模型层需要支持多协议并兼容未来国内模型。
- 制定 M0 至 M6 的开发计划。
- 建立开发进度和架构决策记录。

验证：

- 核对当前仓库仅包含交接文档，没有待兼容的应用代码。
- 核对本地 `main` 与 `origin/main` 同步后再建立规划基线。

风险与待确认：

- LibreOffice、字体、Draw.io 渲染和 Chromium 的分发方式仍需在 M0/M5 形成许可与体积评估。
- 页面自动启动和截图暂定为 1.1，MVP 仅支持手动导入截图并预留接口。
- 首批原生模型协议数量需要在 M0 接口设计后结合测试成本确认。

下一步：

1. 编写领域对象和接口草案。
2. 设计 SQLite 表、版本迁移和数据保留策略。
3. 设计任务状态机、阶段重试、取消和恢复语义。
4. 设计任务目录、产物版本和哈希规则。
5. 完成设计评审后再决定 M1 工程骨架。

### 2026-08-10：M0 核心详细设计

本轮目标：

- 冻结跨平台架构和进程边界。
- 定义领域对象、模型协议、Tool、Artifact 和 QA 接口。
- 设计 SQLite、任务状态机、恢复语义和任务目录。
- 不创建应用工程骨架或业务实现。

完成内容：

- 新增 `ARCHITECTURE.md`，明确 Tauri、FastAPI sidecar、Python 内核和适配器职责。
- 明确 sidecar 只监听本机随机端口，使用一次性会话令牌。
- 新增 `DOMAIN_INTERFACES.md`，定义 ProjectSnapshot、Evidence、Fact、Artifact、QAResult 等对象。
- 将 `ProtocolAdapter` 与 `ModelProvider` 分层，规定能力探测和结构化输出降级策略。
- 定义受控 Tool、应用服务、存储端口和质量策略接口。
- 新增 `DATA_AND_WORKFLOW.md`，设计 SQLite 核心表和并发版本控制。
- 固定 MVP 十二阶段工作流、重试、取消、SSE 续传和启动恢复语义。
- 固定任务目录、内部 ASCII 命名、产物版本与哈希协议。
- 新增 ADR-007 至 ADR-009。

验证：

- 检查新增 Markdown 的标题结构和文件完整性。
- 执行 `git diff --check` 检查空白及补丁格式。
- 对照 `DEVELOPMENT_PLAN.md` 的 M0 交付物核对覆盖范围。

风险与待确认：

- SQLite 表目前是逻辑 Schema，M1 前需转为首版 migration 并通过约束测试。
- FastAPI 本地令牌的安全传递方式需在 Tauri 薄纵切中用真实打包环境验证。
- 大型运行时组件的许可证、体积、下载和升级策略尚未定案。
- 现有两个 Skill 的脚本、规则、依赖和可迁移边界尚未形成清单。

下一步：

1. 只读盘点 `copyright-doc` 与 `drawio-professional-diagram` 的可迁移资产。
2. 区分可直接迁移、需要重构、仅作规范参考和不可带入产品的内容。
3. 评估 LibreOffice、Draw.io 渲染、字体和后续 Chromium 的分发策略。
4. 冻结首批模型协议与协议契约测试范围。
5. 汇总 M0 评审清单，请用户确认后进入 M1。

### 2026-08-10：M1 首个可运行纵向切片

本轮目标：

- 停止继续扩展纯设计文档，开始可运行实现。
- 打通“扫描项目 → 写入 SQLite → 生成 manifest → CLI 输出摘要”。
- 保持零第三方运行依赖，兼容当前 Python 3.9 环境。

完成内容：

- 建立 `src/software_copyright_agent` Python 包和 `pyproject.toml`。
- 实现任务、阶段、文件分类和扫描结果领域对象。
- 实现跨平台项目扫描器，采用稳定排序和 SHA-256 项目指纹。
- 过滤常见版本控制、依赖、构建、虚拟环境目录和敏感文件。
- 默认跳过符号链接和超过 10 MiB 的文件。
- 建立首版 SQLite migration，包含项目、快照、任务、阶段和事件表。
- 实现事务化扫描服务和原子 `manifest.jsonl` 写入。
- 实现 `copyright-agent scan` CLI 与 JSON 输出。
- 添加 README、`.gitignore` 和单元测试。

验证：

- 使用 Python 3.9 标准库运行全部单元测试。
- 使用当前仓库完成端到端 CLI 扫描。
- 验证 SQLite 任务状态、阶段和事件已经落库。
- 验证任务目录生成 manifest，数据库启用 foreign keys 和 WAL。

已知限制：

- 扫描规则暂未读取项目自身的 `.gitignore`。
- 当前任务只表达 scan-only 纵向切片，尚未实现完整十二阶段调度器。
- ZIP 导入、秘密内容扫描和可配置 ignore 规则尚未实现。
- migration runner 当前只有 v1，尚未实现多版本 checksum 校验。

下一步：

1. 把 SQL 操作拆分为 repository 和 UnitOfWork，避免应用服务直接写 SQL。
2. 实现真实任务状态转换器和乐观并发版本检查。
3. 实现 ZIP 安全解压及输入摄取阶段。
4. 增加 `.gitignore`、二进制识别、秘密内容和超大项目预算。
5. 为 Windows 路径、保留名和大小写差异增加测试。

### 2026-08-10：Repository、UnitOfWork 与任务状态机

本轮目标：

- 移除扫描应用服务中的直接 SQL。
- 实现真实任务生命周期、失败留痕和乐观并发控制。
- 保持现有 CLI 行为可用。

完成内容：

- 新增 Source、Snapshot、Task、Stage 和 Event Repository。
- 新增 SQLite `UnitOfWork`，统一事务提交和回滚。
- 新增任务状态机和显式合法转换表。
- 使用 `row_version` 与原状态共同约束任务更新。
- 每次任务状态转换在同一事务中追加结构化事件。
- 扫描流程调整为 `created → running → completed`。
- 扫描异常时将阶段和任务持久化为 `failed`，不记录原始敏感异常内容。
- 扫描成功事件序列固定为 `task.running → stage.succeeded → task.completed`。

验证：

- 9 项单元测试全部通过。
- 覆盖非法状态跳转、过期版本写入、扫描失败落库和正常扫描。
- 使用当前仓库完成真实 CLI 扫描，任务最终 `row_version=3`。
- 核对 SQLite 中三条生命周期事件顺序正确。
- Python 编译检查和 `git diff --check` 通过。

已知限制：

- `UnitOfWork` 当前采用单 SQLite 写入事务，后续 sidecar 任务执行器需要串行化写入调度。
- 阶段自身尚未增加独立 `row_version`，目前依靠状态条件更新。
- 失败后的阶段重试和下游 `stale` 传播尚未实现。
- 输入摄取仍仅支持目录。

下一步：

1. 实现目录与 ZIP 统一的 `InputIngestor`。
2. 防止 ZIP Slip、绝对路径、符号链接和解压膨胀。
3. 将 ZIP 解压到任务专属 `input/extracted` 目录。
4. 增加文件数量、单文件和总解压体积预算。
5. 为恶意 ZIP 和中文路径编写回归测试。

### 2026-08-10：ZIP 安全摄取

本轮目标：

- 统一目录与 ZIP 项目输入。
- 不依赖外部解压命令，安全解压 ZIP 到任务隔离目录。
- 对常见恶意归档和解压膨胀建立确定性阻断。

完成内容：

- 新增 `InputIngestor`、`IngestedProject` 和可配置解压预算。
- 支持目录直接扫描与 ZIP 任务内解压扫描。
- ZIP 使用逐条目校验和流式复制，不调用 `extractall`。
- 阻止 `..` 穿越、绝对路径、Windows 盘符路径和反斜杠变体。
- 拒绝 ZIP 内符号链接和加密条目。
- 按大小写折叠检测重复路径，避免 Windows/macOS 文件覆盖冲突。
- 限制文件数量、单文件解压大小和总解压大小。
- 实际写入量不得超过 ZIP 声明大小或预算。
- 解压先写临时目录，完整成功后原子移动到 `input/extracted`。
- 单一顶层项目目录会自动成为扫描根目录，并忽略 `__MACOSX` 包装目录。
- CLI 帮助和扫描服务已经接受目录或 ZIP。

验证：

- 16 项单元测试全部通过。
- 覆盖中文路径、Zip Slip、符号链接、大小写冲突、文件数量和体积预算。
- 使用真实 ZIP 运行 CLI，正确扫描 2 个文件。
- SQLite 正确保存 `kind=zip`、任务 `completed` 和 `row_version=3`。
- 解压文件、manifest 和任务 ID 位于同一隔离任务目录。

已知限制：

- 当前只支持 ZIP，不支持 tar、7z 或远程 Git；这些均不属于当前 MVP。
- ZIP 原始压缩包不会复制到任务目录，只保存原始路径和解压结果。
- 暂未处理项目自身 `.gitignore` 和嵌套 ignore 规则。
- 扫描器会计算全部受控文件哈希，超大项目尚缺总扫描预算。

下一步：

1. 实现项目根与嵌套 `.gitignore` 的核心匹配规则。
2. 在哈希前识别二进制文件并限制读取。
3. 增加秘密文件名和内容特征检测报告。
4. 增加最大扫描文件数、总字节数和取消检查点。
5. 输出按文件类别、语言和忽略原因聚合的扫描报告。

### 2026-08-10：可审计项目扫描

本轮目标：

- 支持项目自身 `.gitignore` 的常用规则。
- 在读取内容前后执行资源预算和二进制判断。
- 检测秘密特征但不把秘密值写入数据库、manifest 或报告。
- 输出可供后续事实提取和 QA 使用的扫描报告。

完成内容：

- 实现项目根和嵌套 `.gitignore` 规则加载。
- 支持 glob、目录规则、根锚定、否定规则和最后匹配优先。
- 为默认目录、`.gitignore`、敏感文件、符号链接、超大文件和读取失败记录独立忽略原因。
- 增加常见源码扩展名到语言名称映射。
- 通过 NUL 字节和 UTF-8 解码检测二进制输入。
- 增加私钥、AWS Key、GitHub Token 和常见赋值式秘密特征检测。
- 秘密发现只包含 `rule_id`、相对路径和行号。
- 增加最大文件数、单文件字节数和总扫描字节数预算。
- manifest 新增 `language` 和 `is_binary` 字段。
- SQLite 快照摘要新增类别、语言、忽略原因、二进制和秘密发现计数。
- 原子生成 `qa/scan-report.json`。
- CLI JSON 和文本摘要新增扫描报告及安全统计。

验证：

- 19 项单元测试全部通过。
- 覆盖根/嵌套 ignore、否定与锚定规则、二进制、语言、秘密和预算。
- 测试确认秘密原文不会出现在 `SecretFinding` 中。
- 使用当前仓库完成真实扫描：30 个文件、19 个 Python 文件、0 个秘密发现。
- SQLite 聚合摘要、manifest 和扫描报告内容一致。
- Python 编译与 `git diff --check` 通过。

已知限制：

- `.gitignore` 是面向项目扫描的安全子集，不覆盖 Git 的全部转义和父目录重包含边缘语义。
- 当前文本识别以 UTF-8 为主，其他编码暂按二进制处理。
- 秘密检测是阻断前的第一层规则，后续还需要熵检测和可配置白名单。
- 扫描暂不支持协作式取消，预算超限会立即失败并持久化任务状态。

下一步：

1. 从 README、依赖清单和部署配置确定性提取项目元数据。
2. 根据文件语言、框架依赖和目录结构形成技术栈候选。
3. 生成模块候选及 Evidence 路径引用。
4. 将事实候选写入 SQLite，而不是只写 JSON 摘要。
5. 增加 `inspect` CLI，展示事实、证据和需要用户确认的字段。

### 2026-08-10：确定性事实与 Evidence

本轮目标：

- 从项目现有配置和文档提取可追溯事实，不引入模型猜测。
- 将事实、Evidence 和待确认项写入 SQLite。
- 增加 CLI 检查入口，验证持久化结果可被桌面层消费。

完成内容：

- 新增 SQLite v2 migration：`evidence`、`facts` 和 `confirmation_requests`。
- 支持从 v1 数据库幂等升级到 v2。
- 从 `package.json`、`pyproject.toml` 和 `Cargo.toml` 提取名称及版本。
- 配置清单中的明确名称和版本标记为确定性确认。
- README 一级标题仅作为较低置信候选，并生成用户确认请求。
- 未发现版本号时生成必填确认请求，不虚构默认版本。
- 根据扫描 manifest 聚合语言和源码模块候选。
- 根据依赖清单识别 React、Next.js、Vue、Tauri、FastAPI 等框架候选。
- 每个事实保存来源、置信度和 Evidence ID。
- Evidence 保存相对路径、定位器、内容哈希和提取器版本。
- 扫描工作流新增 `03_extract_facts` 阶段。
- 存在待确认项时任务进入 `waiting_for_user`，否则进入 `completed`。
- 新增 `inspect [task_id] --json`，省略 ID 时读取最近任务。

验证：

- 23 项单元测试全部通过。
- 覆盖 package、TOML、README、语言、框架、模块和确认请求。
- 覆盖既有 SQLite v1 数据库升级至 v2。
- 使用当前仓库完成端到端扫描和 inspect。
- 当前仓库确定性识别名称 `software-copyright-agent`、版本 `0.1.0`、Python 与模块候选。
- SQLite 中 Fact 的 Evidence ID 均可解析到对应 Evidence。

已知限制：

- TOML 当前使用受控正则读取标准 name/version 字段，M2 引入依赖后应换成正式 TOML 解析器。
- 框架识别当前基于依赖清单标记，尚未分析具体版本约束和运行方式。
- 模块候选根据源码目录聚合，仍需要后续语义模型判断其业务含义。
- 待确认项目前只能查看，尚不能通过 CLI 回答并恢复任务。
- 事实尚未支持 superseded 和用户修改历史。

下一步：

1. 实现 `confirm` CLI 写回答案。
2. 将用户答案保存为新的 Evidence 和 confirmed Fact。
3. 把旧候选事实标记为 superseded，保留完整历史。
4. 所有必填确认完成后恢复 `waiting_for_user → running`。
5. 增加后续阶段 checkpoint，为核心源码筛选做准备。

### 2026-08-10：用户确认与任务恢复闭环

本轮目标：

- 允许用户回答确定性程序无法安全确认的项目元数据。
- 保留候选事实历史，不直接覆盖原记录。
- 最后一个必填确认完成后合法恢复任务状态。

完成内容：

- 新增 `confirm TASK_ID FIELD_KEY VALUE` CLI。
- 确认服务在单一 UnitOfWork 中完成所有写入。
- 确认请求由 `pending` 原子更新为 `answered`，重复回答被拒绝。
- 每个用户答案生成 `user_confirmation` Evidence。
- 旧 candidate/confirmed Fact 标记为 `superseded`。
- 用户答案生成 `source=user`、`status=confirmed`、`confidence=1.0` 的新 Fact。
- 事实历史与 Evidence ID 均保留，inspect 可查看新旧版本。
- 工作流新增 `04_confirm_metadata` 等待阶段。
- 部分确认后任务保持 `waiting_for_user`。
- 最后一个必填确认完成后执行 `waiting_for_user → running → completed`。
- 确认阶段同步更新为 `succeeded`，任务版本递增至 5。

验证：

- 26 项单元测试全部通过。
- 覆盖部分确认、最终恢复、重复回答拒绝和 CLI 闭环。
- 真实测试项目从 README 候选生成两个确认请求。
- 回答名称和版本后，任务、三个阶段和所有确认项均完成。
- SQLite 保留旧名称为 `superseded`，新名称和版本为用户 confirmed Fact。
- 两条用户 Evidence 均能回溯到对应 confirmation ID。

已知限制：

- 当前确认值按文本保存，后续需要按字段 Schema 支持枚举、数组和结构化值。
- 当前 CLI 使用字段键定位确认请求；桌面 API 应使用 confirmation ID 和 expected row version。
- 当前纵向切片在元数据确认后标记完成，完整产品后续会继续进入源码筛选阶段。
- 尚未实现修改已回答确认项；未来修改应创建新版本而不是原地改写。

下一步：

1. 实现 `05_select_source` 阶段。
2. 确定性排除测试、mock、vendor、migration、生成代码和敏感文件。
3. 为候选源码计算业务相关性、入口性、依赖中心度和基础设施必要性评分。
4. 输出每个文件的选择/排除理由及 Evidence。
5. 增加 `source-plan` CLI，展示 A/B/C 分级和预计可用代码量。

### 2026-08-10：核心源码 A/B/C 计划

本轮目标：

- 建立 `05_select_source` 确定性阶段。
- 对所有受控源码给出选择、排除、等级、评分和理由。
- 计划可重复生成且历史版本不可覆盖。

完成内容：

- 新增 SQLite v3 migration 和扫描根引用。
- 新增 `source_plan_runs` 与 `source_candidates`。
- 支持目录项目和 ZIP 任务解压目录重新定位源码。
- 确定性排除 test、mock、fixture、vendor、migration、demo、sample、example 和 generated。
- 排除二进制源码及扫描阶段发现秘密的文件。
- 根据业务目录/文件名、应用入口、必要基础设施、主源码树、实现规模和通用辅助属性计算评分。
- A 级为核心优先，B 级为必要支撑，C 级为低相关或确定性排除。
- 每个候选保存 `reasons` 和可选 `exclusion_code`。
- 统计入选文件数、非空代码行数和 A/B/C 分布。
- 原子生成 `intermediate/source-selection/plan.vN.json`。
- 新增 `source-plan TASK_ID --json` CLI。
- 重复生成计划时 plan version 和 stage attempt 分别递增，不覆盖旧记录。
- 待确认任务不能提前执行源码计划。

验证：

- 31 项单元测试全部通过。
- 覆盖排除规则、A/B/C 分级、文件名业务语义、重复计划版本和 CLI。
- 第二次计划最初暴露固定 attempt 冲突，已改为数据库计算下一 attempt 并保留回归测试。
- 当前仓库真实计划识别 29 个源码文件：A 4、B 13、C 12。
- 入选 17 个文件、2769 个非空代码行；11 个测试文件被确定性排除。
- 当前任务阶段为 `05_select_source`，计划 JSON 与 SQLite 汇总一致。

已知限制：

- 当前评分基于路径、文件名和实现规模，不等同于业务语义理解。
- 暂未解析跨文件 import 图或调用关系，依赖中心度仍是后续能力。
- 非空物理行数只是容量近似，尚未执行视觉宽度换行和 Word 分页估算。
- C 级低相关候选不会自动进入代码文档，但仍保留供用户或模型复核。
- 模型语义重排不得恢复秘密、测试、vendor 等确定性排除项。

下一步：

1. 对 A/B 入选文件执行编码识别和确定性代码行提取。
2. 增加视觉宽度计算、长行硬换行和页容量配置。
3. 估算 59 页代码正文所需行数，并报告源码不足或超量。
4. 按 A 优先、B 补充和去重规则形成稳定代码序列。
5. 输出代码文档的分页预览数据，为 DOCX 生成做准备。

### 2026-08-10：代码视觉换行与 59 页分页预览

本轮目标：

- 将最新 A/B 源码计划转换为稳定代码序列。
- 在 Word 出件前确定性计算视觉换行和页面容量。
- 源码不足时如实报告，禁止填充和重复。

完成内容：

- 新增 SQLite v4 migration 和 `code_preview_runs`。
- 新增 `06_prepare_source_doc` 阶段。
- 只读取最新 source plan 中 selected 的 A/B 文件。
- 每个文件读取前使用 manifest SHA-256 复核，源码变化立即阻断。
- 使用 Unicode East Asian Width 计算中英文视觉宽度。
- 组合字符宽度为 0，中日韩宽字符宽度为 2，其他字符为 1。
- Tab 按固定宽度展开，长行在进入 Word 前硬分段。
- 默认参数：100 视觉列、每页 50 行、59 页代码正文、Tab 4 空格。
- 文件按 A 优先、B 补充、评分降序和路径稳定排序。
- 每个代码段保留原文件、源行号、segment 和 continuation 标记。
- 精确生成 page/entry JSON，不依赖 Word 自动换行估页。
- 源码超量时稳定截取前 2950 个视觉行；不足时生成实际页数。
- 新增 `code-preview TASK_ID --json` CLI。
- 重复预览创建新版本和新 stage attempt，不覆盖历史。
- 代码不足时任务为 `completed_with_warnings`，不是失败。

验证：

- 35 项单元测试全部通过。
- 覆盖中文双宽字符、Tab、硬换行、分页、不足警告和源码哈希变化。
- 覆盖预览版本化、SQLite v4 升级和 CLI。
- 当前仓库实际 source plan：32 个源码文件，A 5、B 14、C 13。
- 入选 19 个文件，预处理得到 3606 个视觉行。
- 稳定使用 2950 行生成完整 59 页正文，每页均为 50 行。
- 预览实际覆盖前 13 个文件，其余内容因达到目标容量而截断。
- 任务完成于 `06_prepare_source_doc`，`row_version=7`。

已知限制：

- 当前视觉宽度模型尚未与最终 Word 字体的真实字形度量做校准。
- 文件标题和分隔行计入页面容量；最终模板必须保持相同约定。
- 当前只支持 UTF-8 入选源码，其他编码会被阻断而非猜测转换。
- 当前按计划顺序截断，尚未实现保证每个核心模块最低覆盖量的配额算法。
- 分页预览是 DOCX 输入数据，不是最终可交付文档。

下一步：

1. 引入 DOCX 生成依赖并创建 1 页封面＋59 页代码正文。
2. 固定页面尺寸、页边距、等宽字体、字号、行距、页眉和页脚。
3. 使用分页预览逐页写入，禁止 Word 自动重排改变行数。
4. 通过 LibreOffice 转 PDF 并核对实际总页数为 60。
5. 增加空白页、异常换行、页眉页脚和字体回退 QA。

### 2026-08-10：首版 60 页源代码 DOCX 与真实渲染闭环

本轮目标：

- 将最新完整分页预览生成可打开的 Word 源代码文档。
- 固定 A4、封面、代码行距、页眉页脚和显式分页。
- 使用真实仓库完成 DOCX → PDF → 逐页 PNG 视觉验证。

完成内容：

- 新增 `python-docx` 运行依赖和 `source-docx TASK_ID --json` CLI。
- 新增 SQLite v5 migration 与 `source_document_runs`，保存预览版本、模板参数、摘要、相对路径和 SHA-256。
- 新增 `SourceDocumentBuilder` 和 `SourceDocumentService`。
- 使用 `compact_reference_guide` 预设、`editorial_cover` 封面模式，并将纸张明确覆盖为 A4。
- 固定 1 页封面、59 页正文、每页 50 个预处理视觉行。
- 使用显式分节和分页符，正文页眉、PAGE/NUMPAGES 字段及等宽代码字体。
- 生成前要求最新预览 `sufficient=true`，禁止短项目通过空白或重复内容凑页。
- 每次重复生成创建新版本，旧 DOCX 和数据库记录均不覆盖。
- 首轮视觉检查发现中文静态标签缺字；通过最小字体样本确认是 LibreOffice 与 macOS 系统 CJK 字体兼容问题。
- 最终 QA 模板改用无缺字的英文固定标签；用户确认的软件名称和版本仍保持原值。
- 新增 ADR-010，要求跨平台发布前内置可再分发的 CJK 字体并设置字体探测门禁。

验证：

- 37 项完整单元测试全部通过，新增固定页数、行数和不完整预览阻断覆盖。
- 真实仓库扫描 46 个文件，无秘密命中。
- 源码计划识别 35 个源码文件，A 6、B 15、C 14；选中 21 个，排除 13 个。
- 预处理得到 4131 个视觉行，稳定使用前 2950 行生成 59 个正文页。
- 最终 DOCX v3 SHA-256：`d5d5ce765e1dcb04b2034f3af3007adeaf360e00c7403411f77c0106696f5f25`。
- LibreOffice 实际渲染为 60 页，PDF 页面尺寸为 A4。
- 已检查全部 60 页缩略图，并以原始分辨率复核封面、第 2 页和第 60 页；未发现空白页、溢出、重叠、截断或异常分页。

已知限制：

- 本轮 QA 项目名称和源码为拉丁字符；中文项目在内置开源 CJK 字体完成前不能宣称跨平台渲染通过。
- PAGE 字段按整个文档计数，因此代码正文显示第 2–60 页；尚未提供“正文从 1 重新编号”的模板选项。
- 渲染 QA 当前是开发流程，尚未封装为应用内自动质量门禁和数据库 QA 记录。

下一步：

1. 增加 DOCX 自动 QA 服务：实际页数、A4、空白页、页眉页脚、产物哈希和报告持久化。
2. 选定并内置许可兼容的 CJK 字体，增加中文软件名和中文源码黄金样例。
3. 将渲染失败或页数不等于 60 设为出件阻断项。
4. 再开始说明书章节规划和 Draw.io 图表阶段，不提前进入桌面 UI。

### 2026-08-10：源代码 DOCX 自动 QA 门禁

本轮目标：

- 将上一轮开发期人工渲染步骤封装为应用自身可执行的质量门禁。
- 自动验证 DOCX 结构、实际页面、A4 尺寸和空白页。
- 将每次 QA 结果、检查明细和渲染路径持久化。

完成内容：

- 新增 SQLite v6 migration 和 `source_document_qa_runs`。
- 新增 `qa-source-docx TASK_ID --json` CLI；通过返回 0，质量阻断返回 3，运行故障返回 2。
- 新增 `LibreOfficeRenderer`，直接调用可配置的 LibreOffice 与 `pdftoppm`，不依赖 Codex 文档脚本。
- 支持 `COPYRIGHT_AGENT_SOFFICE` 指定应用内置或外部 headless LibreOffice。
- 新增 11 项自动检查：DOCX SHA-256、2 个分节、58 个正文分页符、2 个 A4 分节、页眉部件、PAGE/NUMPAGES 字段、60 个渲染页、页面尺寸一致、A4 宽高比和空白页。
- 使用 Pillow 检查纯白页面；自动结果始终保留 `visual_review_required=true`，不将像素阈值伪装为完整审美判断。
- QA 报告写入任务目录，SQLite 保存策略版本、通过状态、检查明细、摘要、报告路径和渲染路径。
- QA 未通过时任务结束为 `completed_with_warnings`；渲染器等运行故障记录为 `failed`，允许在修复环境后重试。

验证：

- 39 项完整单元测试全部通过。
- 新增完整 60 页通过测试，以及人为制造第 17 页空白时阻断测试。
- 首次真实运行发现 Homebrew LibreOffice 26.2 headless 进程无诊断 abort，任务安全记录失败而未产生伪通过结果。
- 增加通用 soffice 路径配置后，使用独立 headless LibreOffice 从失败状态成功重试。
- 真实 DOCX v3 自动 QA 结果为 11/11 通过，渲染 60 页，尺寸 827×1170，无空白页。
- QA run v1、检查明细和报告路径已写入 SQLite，任务完成于 `06_qa_source_doc`。
- 已再次检查自动 QA 产生的全部 60 页缩略图，未发现溢出、重叠、缺页或异常空白。

已知限制：

- 当前发布包尚未真正捆绑 LibreOffice 和 Poppler，只完成可配置路径与领域门禁；桌面打包阶段必须补齐两平台运行时。
- 空白页检查只能发现全白或近全白页面，不能自动判断字符缺失、排版美感或语义完整性。
- QA PNG 和 PDF 当前全部保留在任务目录，尚未提供按策略清理中间渲染物的生命周期管理。

下一步：

1. 实现 CJK 字体探测器和字体门禁，先覆盖中文软件名与中文源码黄金样例。
2. 选定许可兼容的内置字体，并定义 macOS/Windows 字体解析优先级。
3. 将字体检查加入 `source-docx-qa-v2`，中文缺字风险必须阻断。
4. 完成后再进入说明书内容规划和 Draw.io 图表纵向切片。

### 2026-08-10：内置 CJK 字体、DOCX 内嵌与中文黄金样例

本轮目标：

- 消除中文软件名、固定标签和中文源码对系统字体的依赖。
- 对字体文件、许可、字形覆盖和 DOCX 内嵌结构建立确定性门禁。
- 使用 60 页中文黄金样例完成真实渲染闭环。

完成内容：

- 从 notofonts 官方仓库引入未经修改的 `NotoSansCJKsc-Regular.otf`，随项目附带 SIL OFL 1.1 原文和第三方声明。
- 字体 SHA-256 固定为 `2c76254f6fc379fddfce0a7e84fb5385bb135d3e399294f6eeb6680d0365b74b`，许可证 SHA-256 同样进入代码门禁。
- 新增无第三方解析依赖的 OpenType cmap format 4/12 读取器。
- 生成 DOCX 前收集软件名称、版本、固定标签和全部入选源码中的非 ASCII 字符；字体缺少任何所需字形时阻断出件。
- 使用确定性 font key 将字体混淆为 OOXML `.odttf` 部件，并写入 `fontTable.xml.rels`、`embedRegular`、`embedTrueTypeFonts` 和 content type。
- 生成器恢复中文“源程序代码”、文档构成、页眉和页脚标签。
- QA 策略升级为 `source-docx-qa-v2`，增加字体 SHA、缺字、内嵌部件、embedRegular、嵌入设置和字体内容类型检查。
- 新增 `THIRD_PARTY_NOTICES.md`，记录上游、文件、哈希和许可证路径。

验证与迭代：

- 41 项完整单元测试全部通过。
- 覆盖字体/许可证哈希、常用中文 cmap、扩展字符缺字阻断，以及 DOCX 确实包含 `.odttf` 和 `embedRegular`。
- 第一版仅设置 `SAL_FONTPATH`，自动结构门禁通过但真实页面仍显示方框；人工视觉检查成功捕获该假阳性。
- 第二版复制字体到隔离用户字体目录仍无效。
- 第三版首次整体重序列化 OOXML，因丢失 `mc:Ignorable` 关联命名空间而被 LibreOffice 拒绝；改为局部字节插入后修复。
- 最终中文黄金样例使用“软著材料助手”、中文文件标题和 2950 行中文源码，实际渲染 60 页 A4。
- 自动 QA 17 项全部通过，所需 35 个非 ASCII 字形无缺失、无空白页。
- 已检查中文黄金样例全部 60 页缩略图，并以原始分辨率复核封面和正文页；中文无方框、无缺字、无溢出。
- 当前真实仓库重新生成 source DOCX v4，SHA-256 为 `4c4960fd070f77bd2d7647059cac4429d1ff18531ec543481971ed4ab727d1fe`；QA run v2 的 17 项检查全部通过。
- 已检查真实 v4 的全部 60 页渲染，中文固定标签正常，正文布局未发生回归。

已知限制：

- 当前只内置 Regular 字重；标题设置为粗体时由渲染器合成，后续若版式要求严格可再引入官方 Bold 字重。
- Noto Sans CJK SC 不覆盖所有 Unicode CJK 扩展字符；当前策略是确定性阻断并报告缺字样例。
- 字体使每个未子集化 DOCX 增加约 16MB；后续可评估安全的字体子集化，但不得破坏可编辑性和许可留痕。
- 自动门禁仍不能用像素规则证明所有字形语义正确，最终交付继续保留人工视觉检查。

下一步：

1. 开始说明书内容规划：从 Fact/Evidence 生成确定性章节骨架和缺失信息清单。
2. 先实现章节计划 JSON 与持久化，不立即大规模生成长篇正文。
3. 定义说明书所需架构图、核心流程图和模块关系图的 Draw.io 输入契约。
4. 保持模型输出只能提供候选内容，事实和图表节点必须可追溯到证据。

### 2026-08-10：修复源码文档页面未铺满

本轮目标：

- 修复每页虽有 50 行但正文仅占页面约四分之三的版式缺陷。
- 将正文纵向覆盖率纳入自动质量门禁，避免“页数正确但页面未铺满”再次回归。

完成内容：

- 将默认视觉列宽由 100 收窄到 90，为放大字号保留横向安全余量。
- 将源码字号由 8pt 调整为 9pt，精确行高由 11pt 调整为 15pt；继续保持每页 50 行和总计 60 页。
- QA 策略升级为 `source-docx-qa-v3`，逐页检测源码正文最末墨迹位置，最低纵向覆盖率要求为页面高度的 86%。
- QA 报告新增 `minimum_body_fill_ratio` 和 `underfilled_pages`，覆盖不足会直接阻断出件。
- 新增人为制造第 17 页正文不足的回归测试。

验证与迭代：

- 14pt 行高首轮真实渲染仍发现第 6 页覆盖率仅 85.38%，新门禁正确阻断，没有误放行。
- 调整为 15pt 后重新生成并渲染真实仓库，得到 60 页 A4，18 项自动检查全部通过，无空白页、无覆盖不足页。
- 最低正文纵向覆盖率提升到 89.23%，页面尺寸为 827×1170。
- 已检查全部 60 页缩略总览，并以原始分辨率抽检第 2、30、60 页；未发现裁切、溢出、重叠或异常换页。

下一步：

1. 进入说明书章节计划的确定性 JSON 与持久化实现。
2. 定义架构图、核心流程图和模块关系图的 Draw.io 输入契约。
3. 延续每轮自动检查、真实渲染和人工视觉抽检留痕。

### 2026-08-10：说明书章节计划与证据缺口

本轮目标：

- 进入 M4，但只实现说明书章节计划，不直接生成长篇正文。
- 让每个章节明确引用已有 Fact/Evidence，并暴露当前无法安全撰写的内容缺口。

完成内容：

- 新增 `manual-plan-v1` 确定性规划器，生成软件概述、运行环境、总体设计、功能设计、数据设计、接口设计、安全与可靠性、用户操作说明、测试部署维护共 9 个章节。
- 章节记录用途、子章节、生成模式、Fact ID、Evidence ID、缺失信息、状态和所需图表；每项缺口映射到稳定 Fact key，后续补齐证据后可自动转为 `ready`。
- 固定规划系统总体架构图和核心业务流程图，并关联到对应章节。
- 新增 SQLite v7 migration 和 `manual_plan_runs`，保存规则版本、摘要、产物路径和内容指纹。
- 新增 `manual-plan TASK_ID --json` CLI，计划原子写入 `intermediate/manual-planning/plan.vN.json`。
- 任务阶段新增 `07_plan_manual`，执行、失败、重试和事件均进入现有状态机留痕。

验证：

- 45 项完整测试全部通过，覆盖章节结构、模块动态子章节、事实和证据引用、缺失信息、状态自动收敛、图表需求、SQLite migration、CLI 与版本化持久化。
- 使用真实仓库任务生成最新 plan v3：9 个章节、2 个图表需求、27 项去重后的稳定事实缺口。
- 当前 9 个章节均为 `needs_evidence`；这是现有扫描只提取名称、版本、语言和模块目录所导致的真实结果，没有将章节骨架误报为可写正文。

已知限制：

- 当前确定性事实提取尚未覆盖数据实体、接口、状态转换、安全机制、部署方式和测试策略，因此不能开始可信的说明书正文生成。
- `project.modules` 目前来自目录候选，只能用于章节导航，不能直接证明模块业务职责。
- 图表阶段当前仅输出需求契约，还没有节点、边和证据绑定结构。

下一步：

1. 扩展项目理解，确定性提取数据存储、接口入口、任务/状态模型、配置和部署证据。
2. 为架构图和核心流程图定义节点、边、容器、证据引用及完整性检查契约。
3. 证据覆盖达到门槛后，再接入模型协议生成受 Schema 约束的章节候选内容。

### 2026-08-10：结构事实提取第一阶段

本轮目标：

- 从仓库中确定性提取数据存储、SQL 实体、HTTP 接口、状态模型和部署方式。
- 直接补充说明书章节规划所需 Fact key，不依赖模型推断业务语义。

完成内容：

- 新增结构分析预算：最多 2,000 个文件、16MB 总文本、单文件 512KB，只读取扫描清单中的非二进制源码和配置。
- 识别配置依赖声明及源码 import/调用模式中的 SQLite、PostgreSQL、MySQL、Redis、MongoDB 和常见 ORM。
- 从 `CREATE TABLE` 提取表名及来源文件，生成 `data.entities`。
- 识别 FastAPI/Flask 风格装饰器、Express 路由和 Spring Mapping，生成带方法、路径、文件和行号的 `interfaces.catalog`。
- 识别 Python `*Status` / `*State` Enum 及其显式字符串状态，生成 `data.lifecycle`。
- 识别 Dockerfile、Docker Compose、Tauri、Vercel、Netlify、Fly.io 描述文件，生成 `deployment.method`。
- 数据库、接口、状态和部署证据只保存命中摘要、定位信息与原文件 SHA-256，不复制整份配置。
- 排除 test、fixture、mock、demo、example 和 sample 路径，避免把测试夹具当成产品能力。

真实验证与纠偏：

- 初版全文关键词扫描在真实仓库中误报 MongoDB、MySQL 等，并把测试路由 `/orders` 当成真实接口。
- 将规则收紧为“配置依赖声明＋源码 import/调用模式＋测试路径排除”，重新扫描后只识别真实 SQLite、15 个 SQLite 表、TaskStatus/StageStatus，无虚假 HTTP 接口和外部数据库。
- 真实说明书 plan v1 的缺失信息由 27 项降至 24 项，数据设计章节首次从 `needs_evidence` 自动变为 `ready`；其余 8 章仍保持待证据状态。

验证：

- 47 项完整测试全部通过。
- 新增正向结构提取测试和关键词/测试夹具误报回归测试。
- 正向样例覆盖 SQLite、Redis、SQL 表、POST 路由、状态枚举和 Dockerfile，全部事实均关联证据引用及文件哈希。

已知限制：

- 当前接口识别不覆盖动态路径、集中式路由表、RPC、GraphQL Schema 和自定义框架。
- SQL 实体只提取显式 `CREATE TABLE`，尚未解析 ORM 模型字段和表关系。
- 状态事实能证明枚举集合，不能仅凭枚举证明允许的状态转换边。
- Dockerfile 等文件只能证明存在部署描述，不能证明当前环境已成功部署。

下一步：

1. 提取模块职责候选、接口请求响应 Schema 和显式错误处理证据。
2. 提取配置项、启动入口、安装升级与测试策略，继续降低说明书缺口。
3. 定义图表节点、边、容器和 Evidence ID 的确定性契约，状态转换边必须来自代码证据。

### 2026-08-10：接口契约、入口、配置与测试证据

本轮目标：

- 在不读取配置值的前提下补充运行入口和配置项证据。
- 提取显式接口请求响应模型、HTTP 错误状态和测试策略。

完成内容：

- 对 FastAPI/Flask 风格路由提取处理函数、参数类型、返回注解和 `response_model`，生成 `interfaces.contracts`。
- 识别显式 `HTTPException`、`abort` 和响应状态调用，生成只含状态码、文件和行号的 `interfaces.errors`。
- 识别 Python、Node.js 和 Java 的环境变量读取；`configuration.items` 只保存变量名和来源文件，不保存默认值、实际值或完整表达式。
- 从 `pyproject.toml` 的 `[project.scripts]`、`package.json` 的脚本名和 Dockerfile 的 CMD/ENTRYPOINT 提取 `runtime.entrypoints`；Node/Docker 脚本不保存命令正文。
- 从测试路径、文件名和显式框架导入生成 `testing.strategy`，记录框架列表和测试文件数量。
- 运行配置和测试内容分析沿用 2,000 文件、16MB 总量、单文件 512KB 的读取预算。
- 说明书运行环境章节新增“启动入口”和“运行配置项”两个稳定事实契约。

验证：

- 47 项完整测试全部通过。
- 正向样例验证 `OrderInput`、`OrderOutput`、HTTP 409、`SERVICE_TOKEN` 键名、npm `start` 入口和 unittest 策略均能回链证据。
- 真实仓库只识别 `COPYRIGHT_AGENT_SOFFICE` 配置键、`copyright-agent` 入口和 16 个 unittest 测试文件；没有因提取器规则文本虚报接口或错误状态。
- 最新真实说明书 plan v1 保持数据设计章节 `ready`，总缺口从上一轮 24 项降至 23 项；其余章节未因弱证据被错误放行。

已知限制：

- 请求响应模型只覆盖显式静态注解，不解析运行时依赖注入、动态 Schema 或 GraphQL 类型系统。
- HTTP 状态码能证明存在错误分支，但不能单独证明触发条件和恢复流程。
- 测试文件数量和框架只能证明测试资产存在，不等同于测试已执行或验收通过。
- 环境变量名可能暗示配置用途，但正文仍不得虚构其默认值、是否必填或安全存储方式。

下一步：

1. 提取显式状态转换调用、异常恢复和事务边界，为核心流程图提供有方向的边证据。
2. 定义架构图和流程图的节点、边、容器、Evidence ID 与完整性检查 Schema。
3. 再扩展 README/manifest 中的软件用途、运行环境和安装命令候选，并保留用户确认门槛。

### 2026-08-10：流程边证据与图表语义计划

本轮目标：

- 为流程图提取有方向、可定位的状态转换边。
- 在生成 Draw.io 前建立节点、边和证据引用的持久化语义契约。

完成内容：

- 使用 Python AST 解析 `ALLOWED_TRANSITIONS`，只提取显式源状态、目标状态、文件和行号，生成 `workflow.transitions`。
- 识别 `BEGIN IMMEDIATE`、commit 和 rollback，生成 `data.transactions`。
- 识别原子文件替换和失败临时文件清理，生成 `reliability.recovery`；不把普通异常捕获自动解释为恢复能力。
- 新增 `diagram-plan-v1`：系统架构图节点来自入口、模块和存储 Fact，核心流程图节点及边来自状态转换 Fact。
- 每个节点和边保存 Fact ID、Evidence ID；每条流程边额外保存源文件和行号定位。
- 图表计划校验重复节点键、重复边键、悬空边和无证据边。
- 系统架构图在缺少模块依赖边时保持 `needs_evidence`，不会凭节点顺序自动连线。
- 新增 SQLite v8 migration 和 `diagram_plan_runs`，关联最新说明书计划并保存版本、规则、摘要、路径和指纹。
- 新增 `diagram-plan TASK_ID --json` CLI 和任务阶段 `08_plan_diagrams`。

真实验证：

- 从 `state_machine.py` 提取 8 个任务状态和 13 条允许转换。
- 从 SQLite UnitOfWork 提取即时事务、commit 和 rollback；从各原子写入路径提取替换与失败清理机制。
- 最新真实 diagram plan v2 共 2 张图、11 个节点、13 条边，结构校验通过，13 条边均包含源文件和行号。
- 核心业务流程图状态为 `ready`；系统架构图包含 CLI、核心模块和 SQLite 三个节点，但因缺少模块依赖边保持 `needs_evidence`。
- 说明书缺口从 23 项降至 22 项；安全可靠性章节仍缺安全控制与审计备份，未被错误放行。

验证：

- 49 项完整测试全部通过。
- 定向测试覆盖 AST 状态边、事务/恢复机制、边证据、悬空引用校验、SQLite v8 migration、CLI 与版本化持久化。

已知限制：

- 当前只解析 Python 常量形式的状态转换表，尚未覆盖 TypeScript、Java 和数据库驱动的动态状态机。
- 允许转换表能证明合法状态边，但不说明每条边由哪个用户操作或业务条件触发。
- 架构图尚无 import/依赖关系证据，因此本轮不生成 Draw.io 文件。

下一步：

1. 提取 Python import 图及模块归属，形成有证据的架构依赖边。
2. 为状态转换边关联调用位置、阶段名称和触发条件候选。
3. 两张语义图达到门槛后，再使用 Draw.io 生成器输出可编辑 XML、SVG 并执行路由与视觉检查。

### 2026-08-10：Python 内部依赖图与架构图门槛

本轮目标：

- 以 AST 提取 Python 项目内部模块依赖，补齐系统架构图的有向边证据。
- 控制真实项目图规模，使语义计划可进入后续 Draw.io 布局阶段。

完成内容：

- 将 `src`、`app`、`lib` 布局统一转换为点分模块标识，正确处理普通模块和 `__init__.py` 包。
- 使用 Python AST 解析绝对导入和相对导入，并将目标限制在同一项目的已扫描 Python 模块集合内。
- 排除 test、fixture、mock、demo、example 和 sample 路径，标准库与第三方包不会形成内部依赖边。
- 新增 `architecture.modules` 和 `architecture.dependencies`；每条依赖包含源模块、目标模块、源文件、行号及文件哈希 Evidence。
- 系统架构语义计划改为使用内部模块图；存在有效依赖边后才从 `needs_evidence` 转为 `ready`。
- 针对大图建立确定性核心视图：按内部度数选择最多 12 个模块，再用高权重最大生成森林保留覆盖核心模块的依赖骨架。
- metadata 同时记录完整模块/边数量、入选前边数、最终节点/边数和选择规则，图视图裁剪不会修改完整事实。
- DiagramPlanService 根据边的源文件和 locator 类型匹配精确 Evidence ID；每条架构边和流程边只携带对应文件证据，不再附带整个 Fact 的证据集合。

真实验证与迭代：

- 完整 import 事实包含 29 个内部模块和 83 条内部依赖，未发现第三方模块误边。
- 首版选取 12 个模块后仍有 45 条架构边，视觉风险过高；最终改为生成森林，避免仅截断边导致局部过密。
- 最新 diagram plan v4：系统架构图 14 个节点、11 条边；核心流程图 8 个节点、13 条边；两张图均为 `ready`。
- 每条边只有 1 个精确 Evidence ID，并保留源文件和行号；整体结构校验通过。

验证：

- 49 项完整测试全部通过。
- 相对导入黄金样例验证 `orders.service -> orders.repository` 的模块解析、文件定位和证据引用。
- 图表测试验证架构依赖边、精确 Evidence 映射和流程边继续满足完整性门禁。

已知限制：

- 当前 import 图只覆盖 Python；JavaScript/TypeScript、Java、Go 和 Rust 需要各自的语法解析器。
- import 关系证明代码依赖，不等同于运行时调用频率或业务重要性；度数筛选只是确定性核心视图，不替代语义说明。
- CLI 和 SQLite 作为上下文节点尚未与模块边连接，因为当前没有跨 Fact 的直接边证据。

下一步：

1. 将两张 `ready` 语义图转换为未压缩、可编辑的 Draw.io XML。
2. 输出 SVG 预览并执行节点重叠、连线穿越、交叉和标签背景检查。
3. 通过自动检查后进行视觉复核，再决定是否插入说明书 DOCX。

### 2026-08-10：可编辑 Draw.io 图件生成与持久化

本轮目标：

- 将证据化语义计划转换为可编辑图件，而不是只停留在 JSON 规划。
- 为生成、导出和质量门禁建立本地持久化留痕。

完成内容：

- 新增确定性 Draw.io XML 生成器，输出未压缩 XML、稳定节点 ID、显式正交路径点和 SVG 预览。
- 系统架构图采用依赖森林树形布局；核心流程图采用状态分区布局与外围回退通道。
- 新增产品内置质量检查：验证 XML 可解析、ID 唯一、节点不重叠、边端点存在、每条边包含显式路径点。
- 新增 SQLite v9 migration 和 `diagram_artifact_runs`，保存计划关联、版本、生成器版本、四个产物路径、摘要与组合指纹。
- 新增任务阶段 `09_generate_diagrams` 和 `drawio TASK_ID --json` CLI；失败会记录安全错误并进入失败状态。
- SVG 导出通过本机 Draw.io Desktop 完成，可由 `COPYRIGHT_AGENT_DRAWIO` 配置，不依赖 Codex 或在线服务。

真实验证：

- 对本仓库真实任务生成架构图 14 节点/11 边、流程图 8 节点/13 边，并成功导出两份 SVG。
- 两份 `.drawio` 均通过专业制图 XML 校验；严格布局分析分别为 `vertices=15 edges=11 warnings=0` 与 `vertices=9 edges=13 warnings=0`，计数包含标题节点。
- 完成 PNG 视觉抽检；架构主体清晰，流程回退路径使用外围通道，不遮挡状态节点。

验证：

- 新增生成器结构测试、质量门禁测试和带假渲染器的服务持久化集成测试。
- 完整回归测试共 51 项，最终结果见本轮提交前验证记录。

已知限制：

- SVG 导出目前需要安装 Draw.io Desktop；未来桌面应用需把兼容导出器作为可检测的本地依赖打包或引导安装。
- 架构上下文节点没有无证据连线；它们只表达已知入口与存储，不虚构调用关系。
- 状态图展示允许转换，不代表每条转换的用户动作或业务条件，后续仍需触发证据候选。

下一步：

1. 将已验收 SVG 按章节插入说明书 DOCX，并验证图片清晰度、标题、分页和交叉引用。
2. 基于章节计划生成第一版证据约束正文，缺失字段继续保留确认门槛。
3. 为多模型协议定义统一请求、流式响应、能力探测和本地密钥引用接口。

### 2026-08-10：运行依赖边界与图表视觉模板 v2

- 接受 ADR-011：核心 DOCX 出件、图表预览、资产微调和导出不得要求用户安装 LibreOffice 或 Draw.io；两者降级为开发验证或可选兼容工具。
- 确定图表资产使用“证据语义、自动布局、人工/AI 覆盖、导出版本”四层模型，后续重新生成不得静默覆盖人工调整。
- 图表模板加入中文职责主标签、英文技术副标签、领域配色以及成功、警告、失败和回退路径的语义样式。
- 视觉复核后移除架构画布中没有关系证据的孤立入口/存储节点；事实保留在 metadata，不通过虚构边强行连接。
- 压缩依赖树的节点宽度和叶节点间距，以改善后续插入纵向说明书时的可读性。

下一步：

1. 为资产工作台定义 SQLite 覆盖层 Schema、冲突检测与版本合并规则。
2. 建立不依赖 Draw.io Desktop 的内部 SVG 导出器，保留 `.drawio` 作为兼容格式。
3. 完成桌面纵切后，在内置画布实现拖拽、属性修改、撤销与 AI 修改操作。

### 2026-08-10：图表资产非破坏覆盖层

- 新增 SQLite v10 migration 和 `diagram_asset_revisions`，按任务、图表和版本保存基础图件、父修订、编辑来源、操作、冲突、状态及审计文件。
- 定义首版覆盖操作：节点移动、缩放、样式、显示名称、隐藏，以及边路由、样式和显示名称；未知动作直接拒绝。
- 原始语义 `label`、节点/边关系、Fact ID 与 Evidence 不被修改，显示调整写入 `visual_override` 或 `display_label`。
- 每个操作绑定目标语义指纹；新基础图中目标缺失时报告 `target_missing`，目标语义改变时报告 `target_changed`，冲突操作不会自动应用。
- 人工编辑与 AI 编辑共用同一安全模型，以 `edit_source=manual|ai` 区分来源；每次保存创建不可覆盖的新版本并记录任务事件。
- 真实任务成功保存 `system_architecture` revision v1，包含节点移动和显示名称覆盖，状态为 `clean`、冲突数为 0。

下一步：

1. 让 Draw.io 与内部 SVG 生成器消费 `visual_override`，使保存的覆盖操作真正反映在预览与导出中。
2. 增加 revision rebase 服务和冲突解决操作，支持接受新语义、放弃修改或重新指定目标。
3. 再建立桌面资产工作台 API，避免 UI 直接操作 SQLite 或任务文件。

### 2026-08-10：覆盖层出图与内置 SVG 导出

- Draw.io 生成器开始消费最新资产 revision：移动、缩放、显示名称、隐藏、样式和边路由会反映到兼容 XML。
- 隐藏节点时同步隐藏关联边；样式覆盖使用白名单，只允许颜色、线宽和虚线参数，不能注入任意 XML/CSS。
- 生成阶段自动安全重放最新人工/AI 覆盖，摘要记录 revision 版本、操作数、冲突数和冲突明细。
- 新增 `InternalSvgRenderer`，只使用 Python 标准库解析已校验的 Draw.io XML，输出节点、中文双层标签、语义配色、正交连线和同色箭头。
- 默认生成链路已切换到 `internal-v1`，真实任务在未启动 Draw.io Desktop 的情况下成功生成 `.drawio` 与 `.svg` v5。
- 首轮视觉复核发现内部端点存在短斜线，修正为轴向边界和显式折点；第二轮复核确认主路径与回退路径保持正交。
- 真实架构覆盖 revision v1 的 2 项操作成功重放，冲突数为 0；两张 Draw.io 源继续通过严格布局零告警检查。

下一步：

1. 实现 revision rebase 与冲突解决服务，提供接受新语义、放弃操作和重新指定目标三种动作。
2. 定义资产工作台本地 API：读取图、保存覆盖、列出版本、预览 SVG 和回滚版本。
3. 在桌面纵切中接入可拖拽画布，再将稳定 SVG 插入说明书 DOCX。

### 2026-08-10：资产工作台版本与冲突 API

- `DiagramAssetService` 新增 revision 列表、单版本读取、最新版本 rebase、指定版本回滚和冲突解决接口。
- 冲突必须逐项处理，支持 `drop`、`accept_current` 和 `retarget`；缺少处理项、重复之外的索引或无效目标均拒绝。
- 回滚采用“复制历史操作并创建新版本”，不删除、不覆盖旧 revision，继续保持完整审计链。
- 每个 revision 保存时同步生成独立 `.drawio` 和内置 SVG 预览，并执行产品内结构门禁。
- 新增统一 `workspace_snapshot`，返回两类图表资产、最新 revision、版本数、状态及允许操作，桌面 UI 不需要直接访问 SQLite 或任务目录内部结构。
- 集成测试覆盖 clean revision、版本列表、读取、回滚、语义变化 rebase、`target_changed` 冲突以及接受当前语义后的 clean 新版本。
- 真实架构资产从 revision v1 rebase 到 v2，2 项人工操作均安全保留，冲突数 0，并成功生成独立预览。

下一步：

1. 为资产工作台增加本地 HTTP Schema 与路由，暴露快照、保存、预览、回滚和冲突解决端点。
2. 建立 Tauri 桌面纵切与可拖拽图形画布，先支持位置、尺寸、标签和撤销/重做。
3. 设计 AI 编辑指令到白名单 overlay 操作的结构化输出与审批预览。

### 2026-08-10：本地资产 API v1 契约

- 新增与 Web 框架解耦的 `DiagramAssetApi`，先稳定桌面 UI 所需的请求、响应、错误码和路由；未来 FastAPI 只作为薄传输适配器。
- 路由覆盖工作台快照、保存 revision、版本列表、单版本读取、SVG 预览、rebase、回滚和冲突解决。
- 所有请求在路由分发前验证至少 32 字符的启动会话令牌，并使用常量时间比较；未认证请求返回 401。
- 严格限制两类 diagram key、`manual|ai` 编辑来源、overlay 白名单和单次最多 500 项操作。
- SVG 路径只能来自持久化 revision，解析后必须仍位于对应任务目录内，客户端不能传入任意文件路径。
- 新增 `LOCAL_API.md`，记录安全边界、全部路由、保存/冲突 Schema 与稳定错误结构。
- 定向测试覆盖认证、保存、列表、预览、非法动作、错误方法和未知图表类型。

下一步：

1. 实现只监听 `127.0.0.1` 随机端口的 sidecar 传输适配器、请求体上限和启动握手。
2. 建立 Tauri 桌面壳并消费工作台快照与 SVG 预览端点。
3. 实现首个拖拽保存闭环，再接入撤销/重做与 AI 操作审批。

### 2026-08-10：FastAPI sidecar 真实纵切

- 根据产品依赖边界修正实现策略：FastAPI、Pydantic、Uvicorn 属于应用所需运行依赖，正式加入 `pyproject.toml`；避免捆绑的是 LibreOffice、Draw.io 等完整独立应用，而不是正常代码库。
- 新增真实 FastAPI sidecar 和 `copyright-agent-sidecar` 入口，领域路由继续由 `DiagramAssetApi` 承担，FastAPI 只负责传输与 Schema。
- Pydantic v2 Schema 严格拒绝额外字段，约束编辑来源、操作枚举、目标长度、版本范围、冲突动作和最多 500 项列表。
- sidecar 仅绑定 `127.0.0.1:0`，启动后输出单行 JSON 握手，包含 host、随机端口、PID、协议版本和程序版本。
- 所有业务端点及健康检查都验证 `X-Session-Token`；禁用 Swagger/ReDoc，不添加通配 CORS，并在路由前执行 1 MiB 请求体门禁。
- FastAPI 验证错误统一转换为既有 400 错误契约；SVG 使用正确媒体类型返回。
- 真实进程验证获得随机本地端口 52246，握手字段正确，带令牌 `/api/v1/health` 返回协议版本 1；随后正常终止进程。
- 新增 FastAPI TestClient 测试，覆盖认证健康检查、工作台路由、无 CORS、Pydantic 错误和 413 请求体限制。

下一步：

1. 建立 Tauri v2 桌面项目骨架、sidecar 启动管理和握手校验。
2. 完成资产工作台首屏：资产列表、SVG 预览、revision 历史与状态。
3. 接入拖拽移动并保存 `node.move` revision，形成第一个桌面编辑闭环。

### 2026-08-10：Tauri 桌面壳与资产工作台首屏

- 新增 React 19、TypeScript 与 Vite 前端，形成项目侧栏、图表资产列表、SVG 主预览、revision 状态和 AI/属性检查器四区工作台。
- 新增 Tauri v2 Rust 桌面壳，通过 `invoke("start_sidecar")` 启动和复用 Python sidecar；应用退出时回收子进程。
- 每次启动生成 256 位随机会话令牌，校验 sidecar 的 host、端口、协议版本、程序版本和 PID 握手，再执行带令牌健康检查。
- 前端只从 Rust 获取本次连接信息；工作台快照和 SVG 均通过认证 HTTP 请求读取，SVG 先转为 Blob URL，不把会话令牌放入 URL。
- sidecar CORS 收紧为 Tauri 桌面 WebView 的两个明确 origin，不允许任意网页调用；新增允许与恶意 origin 回归测试。
- 前端生产构建通过，产物主 JS gzip 约 63 KiB；Python 全量 61 项测试通过。
- 当前开发机未安装 Rust，因此本轮尚未执行 `cargo check`；external binary 的跨平台构建与真实桌面进程验证列入下一轮。

下一步：

1. 增加 PyInstaller sidecar 构建脚本，按 macOS、Windows 和 Linux target triple 输出 Tauri external binary。
2. 安装或在 CI 配置 Rust 工具链，执行 `cargo check` 与 Tauri 桌面启动验收。
3. 接入画布拖拽与 `node.move` 保存，增加撤销、重做和 revision 历史切换。

### 2026-08-10：sidecar 跨平台构建与冻结产物验收

- 新增 `packaging` 可选依赖组和 PyInstaller one-file 构建脚本，不把打包工具混入最终运行依赖。
- 构建脚本优先读取 Rust host target triple；无 Rust 时仅对明确支持的 CPU/操作系统组合进行安全推断，未知架构直接拒绝。
- 产物严格采用 Tauri external binary 约定：`copyright-agent-sidecar-<target-triple>[.exe]`，同时生成包含 target、大小和 SHA-256 的 manifest。
- PyInstaller 缓存、work、spec 和 dist 全部使用临时目录，显式注入项目 `src`，不依赖 editable install 或用户目录写权限。
- 新增 macOS、Windows 文件名以及 Apple Silicon 自动探测测试；全量测试从 61 项增加到 64 项并全部通过。
- Apple Silicon 实际生成 `copyright-agent-sidecar-aarch64-apple-darwin`，大小 21,552,720 字节，SHA-256 为 `6981a7f164adc5cccff584a5e8d5fa7b86a8a27181f8b8540872ae362c90d670`。
- 冻结可执行文件真实启动成功，握手端口为随机本地端口，认证健康检查返回版本 `0.1.0`、协议版本 1；进程可被正常终止。
- 前端生产构建继续通过，主 JS gzip 约 63 KiB。生成的二进制与 manifest 默认忽略，不把单平台构建产物提交到源码仓库。

下一步：

1. 配置 Rust/Tauri 编译环境并执行 `cargo check`，修复可能的 Rust API 或配置问题。
2. 使用本机 external binary 完成 Tauri 应用真实启动、sidecar 生命周期和首屏连接验收。
3. 接入画布拖拽与 `node.move` 保存，增加撤销、重做和 revision 历史切换。

### 2026-08-10：Rust 编译与 Tauri 桌面启动验收

- 从 Rust 官方服务器下载并校验 Apple Silicon rustup 安装器，使用 minimal profile 安装 stable `rustc 1.97.1`；Rust 仅为开发构建依赖，不转嫁给最终用户。
- 首次 `cargo check` 发现 Tauri 编译期要求 `src-tauri/icons/icon.png`，补齐可复现的 SVG 图标源以及桌面 PNG、ICNS、ICO 产物。
- 图标采用确定性 code-native SVG，不调用生成模型；只保留 macOS、Windows 和通用桌面所需资产，移除本阶段不使用的移动端/Store 冗余图标。
- 生成并提交 `Cargo.lock`，锁定 481 个 Rust 包；Tauri 桌面 Rust 代码已通过 `cargo fmt --check` 和 `cargo check`。
- 新增 Rust 安全回归测试，覆盖 256 位小写十六进制会话令牌、合法 loopback 握手，以及非 loopback/错误协议拒绝；3 项全部通过。
- Tauri 开发应用成功完成 406 个编译单元并启动桌面主程序；此前独立冻结 sidecar 的握手与健康检查已通过。
- 为桌面运行增加不含会话令牌的 sidecar ready/stop 诊断日志。系统拒绝了第二次 GUI 启动审批，因此本轮没有虚报完整 UI→sidecar 日志验收。
- Python 64 项测试与前端生产构建继续全部通过。

下一步：

1. 实现 SVG 画布节点命中、拖拽预览和坐标换算。
2. 将拖拽结果保存为 `node.move` revision，并在失败时恢复本地预览。
3. 增加撤销、重做和 revision 历史切换，完成首个桌面编辑闭环。

### 2026-08-10：桌面节点拖拽与 revision 保存闭环

- 明确当前桌面端只有“图表资产”纵向切片可用；项目概览、源码材料、说明书和质量检查改为带“待开发”标识的禁用入口，不再提供无响应的伪交互。
- 内置 SVG 导出器为每个可编辑节点增加 `data-node-key`、原始坐标、尺寸和无障碍属性；标题和连线不进入拖拽命中范围。
- 新增前端 `InteractiveDiagram`：把屏幕坐标通过 SVG CTM 换算为画布坐标，支持节点命中、选中高亮、pointer capture 和拖拽预览。
- SVG 在内联前解析并移除 `script`、`foreignObject`、事件属性和外部链接，避免为了交互直接信任任意 SVG 内容。
- 拖动结束后读取当前 revision 的完整 operations，替换同节点已有 `node.move` 并提交全量白名单操作，避免后续移动丢失此前人工调整。
- 保存成功后重新读取 workspace 和新版 SVG；保存失败则撤销本地 transform 并恢复原位置，同时显示后端稳定错误信息。
- 属性栏显示当前选中节点；工具栏明确区分“拖拽编辑已开启”和“正在保存”，保存期间禁止重复编辑。
- SVG 可编辑元数据加入 Python 回归断言；Python 64 项测试和前端 TypeScript/Vite 生产构建全部通过，主 JS gzip 约 64.5 KiB。

下一步：

1. 增加 revision 历史抽屉、版本预览和“复制历史版本为最新”的回滚操作。
2. 在前端加入操作栈，提供保存前撤销/重做和键盘微调。
3. 开始“项目概览”真实页面：选择目录/ZIP、扫描进度、事实摘要和待确认项。

### 2026-08-10：revision 历史预览与审计式恢复

- “历史版本”按钮接入本地 API，按版本倒序显示来源、操作数和冲突状态，当前查看项与当前最新项均有明确标识。
- 支持点击任意 revision 加载其独立 SVG；历史预览期间锁定节点拖拽，避免把旧坐标误保存到当前版本。
- 版本抽屉明确说明恢复语义：调用既有 rollback 服务复制历史 operations 并创建新的最新 revision，不删除、不覆盖旧版本。
- 恢复成功后并行刷新 workspace 和 revision 列表，自动返回最新画布并显示“从 vN 创建 vM”的结果反馈。
- 当前版本的恢复按钮禁用；请求期间锁定重复操作，失败时保留原预览并显示后端错误。
- 前端 TypeScript 与 Vite 生产构建通过，主 JS gzip 约 65 KiB。

下一步：

1. 为拖拽加入保存前撤销/重做、方向键微调和画布边界约束。
2. 增加节点标签、尺寸和白名单颜色的属性编辑表单。
3. 开始“项目概览”真实页面：本地目录/ZIP 选择、扫描任务创建、事实摘要和待确认项。

### 2026-08-10：画布边界、键盘微调与审计式撤销

- 节点落点按 SVG viewBox、节点宽高和标题安全区约束，禁止保存到画布外或覆盖标题区域；拖拽释放时预览也同步吸附到有效位置。
- 节点选中后自动获得焦点，方向键按 1 个画布单位微调，`Shift + 方向键` 按 10 个单位移动，继续复用 `node.move` revision 保存链路。
- 工具栏新增一层撤销/重做：编辑前版本作为撤销目标，撤销前版本作为重做目标；新编辑会清空过期重做目标。
- 撤销和重做都调用审计式 rollback，生成新的最新 revision，不原地改写或删除历史数据。
- 历史预览和保存期间禁用撤销/重做；操作完成后刷新 workspace、历史列表和画布，并显示来源版本与新版本号。
- 前端 TypeScript 与 Vite 生产构建通过，主 JS gzip 约 65.4 KiB。

下一步：

1. 建立“项目概览”真实页面及桌面路由状态，替换第一个禁用导航入口。
2. 增加本地目录/ZIP 选择与扫描任务创建 API，不允许网页任意传入未经桌面授权的路径。
3. 展示扫描进度、项目事实摘要、风险提示和待确认项，形成项目导入纵向闭环。

### 2026-08-10：项目概览与本地扫描纵向闭环

- “项目概览”从禁用占位升级为第二个真实页面，桌面端默认进入项目导入页，并可与图表资产页双向切换。
- 引入 Tauri 官方 dialog 插件，仅授予 `dialog:allow-open`；目录和 ZIP 都必须经系统文件选择器授权，页面不提供任意路径文本输入。
- 新增认证 `POST /api/v1/projects/scan`，复用既有 `ScanProjectService` 完成目录原地扫描或 ZIP 隔离解压、SQLite 任务持久化和确定性事实提取。
- 新增认证 `GET /api/v1/tasks/{task_id}/inspection`，返回任务状态、Fact、Evidence 和待确认项；不存在任务使用稳定 404 错误。
- 项目概览展示有效/过滤文件数、读取体积、敏感特征计数、语言标签、前 10 项事实和全部待确认项；敏感特征只显示数量，不显示命中值。
- 扫描得到的 task ID 自动传给图表资产页的任务输入框，后续材料阶段继续复用同一任务上下文。
- 新增真实 FastAPI 扫描测试，覆盖项目落库、TypeScript 识别、事实返回和 inspection 二次读取；Python 全量测试从 64 增至 65 项并全部通过。
- Tauri `cargo check`、TypeScript 和 Vite 生产构建全部通过；前端主 JS gzip 约 67 KiB。
- 重新冻结 Apple Silicon sidecar 后真实扫描项目内 `tests` 目录：读取 22 个 Python 文件、89,816 字节、0 个敏感值命中，返回 3 项事实和 2 项待确认，证明新接口已进入桌面实际运行产物。

下一步：

1. 在项目概览直接回答待确认项，并刷新任务状态和 confirmed Fact。
2. 增加已持久化任务列表与最近项目恢复，避免每次启动依赖手工记忆 task ID。
3. 启用“源码材料”页面，展示 A/B/C 选择计划、代码页数预算和 DOCX 生成状态。

### 2026-08-10：待确认提交与最近任务恢复

- 新增 `ProjectCatalogService`，按更新时间列出最近 20 个任务；只返回显示名称、目录/ZIP 类型、状态、扫描摘要和 task ID，不返回原始项目绝对路径。
- 新增认证 `GET /api/v1/tasks?limit=N`，限制范围为 1–100；应用启动连接 sidecar 后自动读取最近任务。
- 项目概览可打开最近任务并通过 inspection 恢复事实、待确认项、扫描摘要及后续共用 task ID，不重新扫描项目。
- 新增认证确认端点，复用 `ConfirmationService` 创建用户确认 Evidence、confirmed Fact，并 supersede 旧候选；最后一个必填项提交后任务进入 completed。
- 待确认卡支持候选值快捷填入和手动输入；空值、重复提交及非法任务状态由既有领域规则拒绝，提交成功后刷新事实、任务状态和最近任务列表。
- FastAPI 集成测试扩展到最近任务排序、确认提交、remaining_required、completed 状态和 confirmed Fact；Python 全量 65 项测试全部通过。
- TypeScript/Vite 生产构建通过，主 JS gzip 约 67.6 KiB；Apple Silicon 冻结 sidecar 已更新，大小约 21.56 MB。

下一步：

1. 启用“源码材料”页面并接入 source-plan、code-preview 和 source-docx 状态。
2. 增加任务阶段时间线和错误/警告详情，明确每一步为何可继续或被阻断。
3. 为确认后的项目提供“一键生成下一阶段”入口，但继续保持人工触发、不自动提交材料。

### 2026-08-10：源码材料页与三阶段生成闭环

- “源码材料”从禁用占位升级为第三个可用页面，自动继承项目概览中当前选中的持久化任务。
- 新增聚合服务与 4 个认证 API，统一返回最新 source-plan、code-preview、source-docx、可执行操作和明确阻塞原因。
- 源码筛选、分页预检和 DOCX 生成始终由用户逐步触发；GET 状态请求不会隐式生成任何材料。
- 页面展示入选文件、代码行数、正文页预算、A/B/C 级数量及前 30 个入选文件，方便在生成前审阅。
- 对小项目执行真实分页预检：代码不足时任务进入 `completed_with_warnings`，页面说明缺口并锁定 DOCX 生成，不填充虚假行数。
- FastAPI 集成回归覆盖“确认项目 → 筛选计划 → 分页不足 → DOCX 被拒绝”；Python 65 项测试全部通过，TypeScript 检查和 Vite 生产构建通过，主 JS gzip 约 69 KiB。

下一步：

1. 增加源码分页的页面级预览，在生成 DOCX 前可检查首页、中间页和末页内容。
2. 为已生成 DOCX 增加“在文件管理器中显示”和应用内预览，不暴露可任意读取的本地路径 API。
3. 建立“说明书”真实页面，接入既有手册计划、图表资产和文档生成状态。

### 2026-08-10：源码分页内容预览

- 分页预检完成后可在“源码材料”页直接查看真实代码页，不再只看页数统计。
- 最新分页产物自动抽取首页、中间页和末页；页数较少时自动去重，最多返回 3 页。
- 纸张式预览显示源文件标题、源码行号、视觉折行标记、当前页码和可视行数，便于在 DOCX 生成前发现选材问题。
- 新 API 不接受任意路径或页码，只根据 task ID 从 SQLite 定位最新产物，并对解析后路径执行任务目录边界校验。
- FastAPI 集成测试验证分页中确实返回被扫描的 TypeScript 源码；Python 65 项回归、TypeScript 检查和 Vite 生产构建全部通过，主 JS gzip 约 69.5 KiB。

下一步：

1. 为已生成 DOCX 提供安全的“在文件管理器中显示”操作，并补充产物完整性校验。
2. 建立“说明书”真实页面，接入手册计划、图表资产和文档生成状态。
3. 增加任务阶段时间线和错误详情，把可恢复警告与不可继续错误分开呈现。

### 2026-08-10：说明书 AI 草稿纵向链路

- 移除说明书 AI 按钮的永久禁用占位，当前选中的可用模型可以直接生成说明书草稿。
- Sidecar 从 AES-256-GCM 本地凭据库按需解密 API Key，支持 `messages`、`chat_completions`、`responses` 和 `ollama_chat` 四种已协商接口，不向前端暴露明文凭据。
- 规则章节规划、项目事实和缺失项共同进入提示词；缺失项被降级为“待确认/禁止编造”约束，不再阻止 AI 生成。
- 新增 SQLite v14 `manual_draft_runs`，记录任务、模型配置、版本、摘要、耗时和产物路径；正文版本化保存为本地 Markdown。
- 说明书页面显示生成中状态、明确错误、模型与耗时，并可直接预览和重新生成草稿。
- Python 69 项回归与 TypeScript/Vite 生产构建通过。

下一步：

1. 将单次整篇生成升级为章节级生成与局部重试，避免长输出被模型截断。
2. 从草稿中的图表占位和项目证据生成结构化图表语义，再交给 Draw.io 渲染与人工微调。
3. 增加草稿确认、DOCX 排版、质量检查和自选路径导出。

补充修复：开发模式实际运行冻结的 PyInstaller Sidecar，Python 源码不会热更新。本轮已重新构建并重启 Sidecar，使 `manual_generate` 权限和 v14 迁移真正加载；同时移除“两张图必须全部 ready”的渲染前门禁，证据不足改为产物摘要中的警告，允许先生成可审阅 Draw.io 草稿。

交互收敛：说明书页面改为单一主操作“一键生成说明书与图表”，后台自动完成必要的章节规划、图表规划、AI 正文和 Draw.io 渲染；原三步规则按钮折叠到高级诊断区。下游生成失败的任务允许直接重试，修复 `failed` 状态反向锁死图表生成的问题。

### 2026-08-10：说明书管线纠偏阶段 1

- 以 `copyright-doc` 技能的正式交付契约重写路线，新增 `MANUAL_PIPELINE_PLAN.md`；明确 Markdown 仅为 legacy 中间材料，最终验收对象是 DOCX、Draw.io/SVG/PNG、截图和逐页质检结果。
- 新增 SQLite v15 正式说明书数据模型：版本化生成任务、六阶段步骤状态、结构化章节、图表、截图和最终文档产物。
- 新增 `ManualPipelineService`，可创建和读取版本化任务；研究、正文、图表、截图、DOCX 组装和渲染质检分别持久化，支持后续独立重试。
- 新增正式任务 API：创建任务、列出项目版本、读取任务详情；旧同步 Markdown 接口标记为 legacy。
- Python 回归增加到 70 项并全部通过。

下一步：实现 `research` 阶段的项目线索图谱与代表性源码选择，产出区分事实、推断和待确认项的结构化研究笔记。

### 2026-08-10：说明书章节与图表规划工作台

- “说明书”从禁用占位升级为第四个真实页面，复用项目概览中当前任务。
- 新增说明书聚合服务与 4 个认证 API，统一呈现章节计划、图表语义计划、Draw.io/SVG 产物及三个人工触发动作。
- 章节计划直接读取持久化中间产物，展示 9 个技术说明书章节的目的、小节、就绪状态和缺失信息。
- 页面汇总章节数、已就绪数、待补证据数与缺失信息数，并把待补项以可扫读标签展示。
- 图表语义计划只能在章节计划存在后触发；Draw.io/SVG 仅在架构图和流程图均为 ready 时开放，不会用无证据节点强行生成。
- FastAPI 回归扩展到“小项目 → 生成 9 章计划 → 显示证据缺口”；Python 65 项测试全部通过，TypeScript 检查与 Vite 构建通过，主 JS gzip 约 70.6 KiB。

下一步：

1. 为说明书待补信息建立本地补充表单，将用户回答持久化为 confirmed Fact 并重新计划章节。
2. 提供章节内容草稿预览，明确区分项目证据、模型生成文字和用户手工修改。
3. 接入说明书 DOCX 的显式生成与质量门禁，保持“先预览、再生成、最后导出”。

### 2026-08-10：扫描按钮连接恢复修复

- 根据真实桌面验收反馈，确认系统文件选择器已成功返回路径，但首次 sidecar 连接未就绪时扫描按钮被永久禁用。
- 扫描按钮现在只在未选路径或正在执行时禁用；sidecar 未连接时按钮改为“连接本地服务并扫描”。
- 点击后先调用统一 `ensureConnection`，连接成功后使用同一连接继续扫描和刷新最近任务，无需用户重新选择路径。
- 连接失败时在页面与侧边栏保留具体错误信息，不再以“浏览器预览模式”概括真实桌面错误。
- 内置 sidecar 独立启动与握手诊断通过；Python 65 项回归、TypeScript 检查与 Vite 构建通过。

下一步：

1. 由用户在当前热更新桌面窗口重试扫描，根据页面显示的具体错误继续收敛。
2. 扫描闭环验收后，继续实现说明书待补信息的本地填写与 confirmed Fact 持久化。

### 2026-08-10：桌面开发来源 CORS 修复

- 第二次真实验收已确认 sidecar 成功握手，扫描请求在 WebView 层返回 `Load failed`。
- 根因是 Tauri 开发窗口使用 `http://127.0.0.1:1420`，但 sidecar CORS 只精确放行打包后的 Tauri 来源。
- 白名单增加固定本机开发来源 `http://127.0.0.1:1420`，继续拒绝通配来源、外部域名和其他端口。
- 回归测试同时覆盖打包来源、开发来源和恶意外部来源；Python 65 项测试与前端构建通过。
- Apple Silicon sidecar 已重新冻结，桌面开发窗口已完整重启。

下一步：

1. 由用户在新窗口再次选择目录并执行真实扫描。
2. 根据扫描结果继续验收事实提取、待确认项、源码材料和说明书页。

### 2026-08-10：源码不足时的恢复路径

- 真实验收发现“可用代码不足 59 页”只有说明而没有下一步操作，导致流程断点。
- 警告卡新增“重新扫描当前项目”和“返回项目概览”两个入口；前者无需重新选路径，后者可更换目录或 ZIP。
- 新增认证 `POST /api/v1/tasks/{task_id}/rescan`，只从 SQLite 反查原来由系统选择器授权的项目来源，不接受前端新路径。
- 重新扫描生成新 task 与 snapshot，旧任务、筛选计划和分页产物保留，符合可追溯要求。
- 原任务已确认的软件版本等标量信息会匹配新任务待确认项并自动继承，避免重复输入。
- FastAPI 集成回归已验证“项目新增源码 → 重扫 → 新 task ID → 文件数更新 → 版本确认保留”；Python 65 项测试和前端构建通过。

下一步：

1. 在重启后的桌面窗口验收重新扫描和新任务切换。
2. 继续实现说明书待补信息的本地填写与 confirmed Fact 持久化。

### 2026-08-10：分级源码筛选与任务清理

- 将单一源码筛选策略升级为“标准、宽松、最大覆盖”三档，用户可在同一任务中降级策略并重新生成版本化筛选计划。
- 标准档保持 A/B 主体业务代码；宽松档纳入项目自身通用支持代码；最大覆盖档进一步纳入测试、Mock、Fixture、迁移和示例代码。
- 安全底线不随严格度降级：敏感命中、二进制、第三方 vendor、生成代码和压缩代码始终排除。
- 策略名保存在 source-plan summary 与 rules version 中，每次降级都可追溯；页面继续展示分级、文件和代码行变化。
- 最大覆盖档分页后仍不足 59 页时，页面给出“当前项目材料不足，暂不建议继续”的终止提示。
- 最近任务新增单项删除和“清理全部”；删除前明确确认，只清理 SQLite 关联记录与应用任务目录，不触碰原项目。
- 回归测试覆盖三档入选差异、具有多类子产物的任务删除、原项目保留和旧任务不可再读取。

下一步：

1. 在桌面窗口实测当前项目三档策略的页数变化，确认最合适的默认提示顺序。
2. 继续实现说明书待补信息的本地填写与 confirmed Fact 持久化。

### 2026-08-10：桌面版 DOCX 生成与失败重试修复

- 根据真实桌面验收复现“页数 59/59 但生成提示任务未完成”，确认分页本身成功，实际是 DOCX 生成失败后任务被置为 failed，下一次操作又被状态机拒绝。
- 根因是 PyInstaller 单文件包未完整释放 `python-docx` 的模板访问路径；构建脚本现同时收集 DOCX 模板和运行时 `docx/parts` 目录，不要求用户另装 Python 或文档工具。
- `source_document_error` 现在属于可恢复失败：原任务保留筛选和分页结果，修复后可直接重试“生成 DOCX”，无需重新扫描项目。
- 页面动作快照同步开放该重试入口；接口返回稳定中文提示，不再把底层异常或英文状态机信息直接暴露给用户。
- 使用用户截图中的失败任务 `d47a3847…` 对重建后的冻结 sidecar 做端到端验收，接口返回 200，成功生成 60 页、约 13.7 MB 的 DOCX，SHA-256 完整性校验通过。

下一步：

1. 在重启后的桌面窗口直接重试该任务的“生成 DOCX”，验收文件定位与实际 Word 打开效果。
2. 继续实现说明书待补信息的本地填写与 confirmed Fact 持久化。

### 2026-08-10：项目切换、程序内预览与资产管理

- 源代码 DOCX 生成后在页面顶部立即显示成功卡，并自动打开程序内文档查看器；不再要求用户滚动到超长源码列表底部才能发现结果。
- 文档查看器包含封面与全部 59 页代码正文导航，支持在查看器内直接发起导出；原有分页抽查入口继续保留。
- 新增“导出…”能力，通过系统保存对话框自选文件名和目标路径；Rust 端重新核验 task、登记产物、SHA-256 状态和任务目录边界后再复制文件。
- 各工作页右上角由不易识别的 task ID 改为项目名称下拉框，可在源码、说明书和图表页面直接切换项目。
- 新增“我的资产”页面，按项目聚合源代码文档和说明书位置，统一提供程序内查看、导出、文件夹定位和进入项目入口。
- 第二个项目的 `Load failed` 留痕显示请求未到达 Python 服务，并非跨项目数据污染；API 现会在网络层断开时检查并重启失效 sidecar，再对同一 task ID 安全重试一次。
- Python 65 项回归、Rust 4 项测试、TypeScript 检查和 Vite 生产构建均通过，主 JS gzip 约 73.0 KiB。

下一步：

1. 由用户在桌面窗口验收两个项目之间的快速切换、程序内预览和自选路径导出。
2. 将说明书 DOCX 接入同一资产管理、预览和导出模型。
3. 实现说明书待补信息的本地填写与 confirmed Fact 持久化。

### 2026-08-10：导出命名与跨平台文件定位

- 源码材料快照增加已确认项目名和版本号，导出默认名统一为“项目名-版本号-源代码文档.docx”。
- 文件名会过滤 Windows 禁止的 `< > : " / \\ | ? *`、控制字符以及结尾空格/句点，避免同一默认名在 macOS 可用而 Windows 保存失败。
- 删除源码长页面底部的应用内部产物定位卡；生成成功后的顶部主操作成为唯一导出入口。
- 导出成功后按钮立即切换为“在文件夹中显示”，定位用户实际选择的导出文件，而非应用内部任务目录副本。
- 文件定位由 Rust 分平台处理：macOS 使用 Finder `open -R`，Windows 使用 Explorer `/select,`，Linux 使用 `xdg-open` 打开父目录；定位前验证绝对路径、普通文件和 `.docx` 扩展名。
- Python 65 项回归（含项目名/版本 API 契约）、Rust 4 项测试、TypeScript 检查及 Vite 生产构建全部通过。

下一步：

1. 在 macOS 桌面窗口验收默认文件名、导出后按钮切换和 Finder 定位。
2. 建立 Windows CI 构建并在 Windows 10/11 实机验收保存对话框、Explorer 定位和 Word/WPS 打开。

### 2026-08-10：说明书验收边界与图表预检可见性

- 侧边栏顺序调整为“源码材料 → 说明书 → 图表资产”，反映说明书先规划、图表作为说明书资产产生和维护的关系。
- 明确纠正旧页面语义：现有章节计划与图表计划属于本地确定性“证据预检”，不调用 AI，也不再暗示说明书正文已生成。
- 说明书页新增生成边界提示和独立 AI 生成卡，未配置真实模型前保持禁用，避免用规则结果冒充模型内容。
- 图表语义预检结果现在可见：逐图显示节点数、连线数、证据状态和具体缺口；不再只表现为下一步按钮灰掉而没有原因。
- 可编辑 Draw.io/SVG 已生成时，说明书页提供“进入图表资产查看与修改”入口。
- 对照现有 `copyright-doc` 与 `drawio-professional-diagram` 技能确认目标链路：AI 负责项目化正文和图表语义，Draw.io XML/SVG/PNG 由技能约束的确定性工具生成、校验和视觉复核；不使用不可编辑的图片生成模型替代 Draw.io。
- FastAPI 回归增加图表预检详情契约；TypeScript 检查通过。

下一步：

1. 实现多协议模型配置与连通性测试，API Key 进入操作系统安全存储，SQLite 只保存凭据引用。
2. 接入真实 AI 说明书草稿阶段，记录模型、耗时、输入证据、调用 ID 和版本；页面右上角只展示已验证可用模型。
3. 接入 AI 图表语义草稿，并使用专业 Draw.io 技能规则生成、校验和预览可编辑图表。

### 2026-08-10：多协议模型设置与安全凭据闭环

- 新增“设置”页面，支持 OpenAI 兼容协议、Anthropic 和 Ollama；OpenAI 兼容模式可覆盖多数国内外模型服务和自建网关。
- 模型配置元数据持久化到 SQLite `model_configs`，新增 v11 migration；列表只返回 `has_credential`，不会返回凭据引用或 API Key。
- API Key 通过 Rust `keyring` 写入 macOS Keychain、Windows Credential Manager 或 Linux 系统凭据后端，不写入 SQLite、日志或前端持久化状态。
- 保存配置时执行真实连通性和模型发现测试：OpenAI/Anthropic 读取 `/models`，Ollama 读取 `/api/tags`；远程服务强制 HTTPS，本机回环地址允许 HTTP。
- 只有服务连通且目标模型确实存在时才标记 verified；未验证配置保留在设置页供诊断，但不会进入说明书模型选择器。
- 说明书右上角新增“生成模型”下拉框，只列出已验证且启用的模型，为下一步真实 AI 正文生成提供明确选择。
- 引入 `keyring` 3.6.3；Rust 编译测试、TypeScript 检查和模型元数据 API 回归通过。

下一步：

1. 实现一次性凭据注入的模型调用代理，API Key 只在调用内存中出现，不写入 sidecar 数据库。
2. 生成第一版 AI 说明书结构化草稿，保存模型、提示版本、证据引用、耗时和调用状态。
3. 在草稿预览确认后，再生成 AI 图表语义并交给 Draw.io 技能管线渲染。

### 2026-08-10：Token 权限模型发现与通用设置补全

- 模型接入改为两阶段：先填写连接与 Token 并检测模型列表，再从该 Token 实际有权访问的模型中选择保存；不再要求用户手填固定模型名。
- OpenAI 兼容/Anthropic 从 `/models` 发现权限范围，Ollama 从 `/api/tags` 读取本机模型；服务连通但列表为空时明确提示检查套餐权限。
- 检测到的模型只在当前界面内存中展示，选择后才写入模型配置；API Key 仍只保存在系统安全存储。
- 新增 SQLite v12 `app_settings`，持久化说明书默认模型、图表语义默认模型、生成温度、最大输出 Token、默认源码筛选档位和生成后自动预览。
- 默认源码策略与自动预览已经接入源码材料实际流程；说明书页面会优先选择已保存的默认模型。
- 删除模型时同步清除引用它的默认模型设置，避免留下失效配置。
- Python 66 项、Rust 4 项、TypeScript 和 Vite 生产构建通过。

下一步：

1. 接入一次性凭据模型调用代理，并让说明书“AI 生成”按钮调用右上角所选模型。
2. 保存结构化草稿、模型调用留痕与证据引用，提供章节级预览和重新生成。

### 2026-08-10：DOCX 完整性与安全定位

- 源码材料快照对最新 DOCX 重新计算 SHA-256，区分 `verified`、`missing`、`mismatch` 和 `invalid_path`，并显示文件大小。
- 只有完整性通过的产物才能点击“在文件管理器中显示”；丢失、被篡改或路径异常时按钮锁定。
- 新增 Tauri `reveal_source_document` 命令，前端只传 task ID；Rust 通过已认证 sidecar 重新读取登记产物，不信任前端文件路径。
- 定位前依次验证 task ID 字符集、相对路径组成、规范化后真实路径、任务目录归属和普通文件类型，再调用 macOS Finder、Windows Explorer 或 Linux 文件管理器。
- Python 65 项回归、Rust 4 项测试、TypeScript 检查和 Vite 生产构建全部通过，主 JS gzip 约 69.8 KiB。

下一步：

1. 建立“说明书”真实页面，展示手册章节计划、待补信息、图表资产和文档生成状态。
2. 接入手册 DOCX 的显式生成与质量门禁，保持“先预览、再生成、最后导出”。
3. 增加任务阶段时间线和错误详情，把可恢复警告与不可继续错误分开呈现。
### 2026-08-10：正式说明书阶段 2——证据化项目研究

- 新增正式研究执行器：从任务扫描快照读取确定性 Facts/Evidence，并从最新源码筛选计划中挑选 A/B 级代表性文件，不再把整个仓库无差别交给模型。
- 源码研究包保留相对路径、准确行号、文件 SHA-256 与截取内容；扫描报告命中的敏感文件不会进入模型上下文。
- AI 输出被约束为结构化 JSON，研究结论必须区分 `verified`、`inference`、`pending_confirmation`，并提供逐章写作建议。
- 服务端只接受输入上下文中真实存在的证据引用；模型虚构引用会被剔除，失去依据的“已验证”结论自动降级为待确认。
- 新增 SQLite v16 `manual_research_artifacts`，研究版本、模型、输入指纹、耗时、源码引用和研究结论完整留痕；研究失败可在同一任务独立重试。
- 正式任务状态会在研究完成后推进到 `draft`，而不是继续依赖旧版 Markdown 草稿状态。
- 新增研究执行与读取 API；Python 回归增至 71 项并全部通过，TypeScript/Vite 生产构建通过。
- Apple Silicon Sidecar 已重新冻结；临时独立启动验证健康端点、v16 迁移和研究表均可用。真实桌面数据库也已无损迁移到 v16，原有 3 个任务、6 个模型配置和 1 份加密凭据均保留。

下一步：

1. 将研究成果转换为按章节、按内容块持久化的 AI 正文，而不是单一 Markdown 长文本。
2. 支持章节级重新生成、人工修改、证据查看和版本比较。
3. 在单一主按钮中串联研究、正文、图表、截图与 DOCX，内部步骤只用于进度和诊断。
### 2026-08-10：正式说明书阶段 3——结构化章节正文

- 正式正文不再落为单一 Markdown 长文本，改为 `paragraph`、`list`、`table`、`figure_request` 四类可审阅内容块。
- 默认覆盖引言、总体设计、功能与模块、数据与接口、运行与部署、安全与可靠性、测试与总结；仅在研究证据显示存在界面时加入用户界面与操作说明。
- 每章独立调用所选模型并校验证据引用。事实段落无有效证据时标记 `needs_review`，合理推断必须明确标记，不能伪装成已验证事实。
- 一次批量生成允许部分成功：单章失败保留其他已完成章节并返回具体错误，可仅重试该章。
- 新增 SQLite v17 `manual_section_revisions`；AI 重生成与人工修改都新增版本，不覆盖历史，当前版本投影继续保存在 `manual_section_artifacts`。
- 人工编辑版本标记为 `origin=user` 和 `confirmed`，支持读取逐章版本历史，为后续程序内比较与恢复提供数据基础。
- 正文中的 `figure_request` 已成为下一阶段图表规划的正式输入，而不再从固定规则图表计划反推文章。
- 新增批量生成、章节读取、单章重生成、人工保存和版本历史 API；Python 72 项回归、TypeScript 与 Vite 生产构建全部通过。
- Apple Silicon Sidecar 已重新冻结。真实桌面数据库已无损迁移到 v17，原有 3 个任务、6 个模型配置和 1 份加密凭据均保留。

下一步：

1. 从正文中的图表请求和研究证据生成章节绑定的专业图表语义。
2. 输出每张图的 `.drawio`、`.svg`、高分辨率 `.png`，完成 XML、布局与视觉复核。
3. 将图表编辑入口继续复用现有图表资产工作台，但资产归属切换到正式说明书任务版本。
### 2026-08-10：正式说明书阶段 4——章节驱动专业图表

- 正式图表输入改为结构化正文中的 `figure_request` 与章节证据，不再依赖旧版固定规则先画两张图再反推文章。
- AI 只负责生成受证据约束的图表语义：节点、关系、类型、层级与布局；本地确定性工具负责所有可交付文件。
- 新增通用 Draw.io 构建器，支持架构、模块、流程、时序、ER、部署和数据流语义，采用企业浅色样式、稳定语义 ID、明确阅读方向和显式正交路由。
- 每张图同步输出可编辑 `.drawio`、可预览 `.svg` 和供 Word 插入的 2 倍分辨率 `.png`，中文字体来自随包分发的 Noto CJK 字体。
- 新增 SQLite v18 `manual_figure_revisions`；重新生成创建新文件与新版本，当前资产投影记录章节锚点、语义、三种格式路径和 QA 报告。
- 批量图表生成允许部分成功，单图可以独立重试；资产读取只接受 job ID、figure key 和格式白名单，并重新验证任务目录边界。
- 使用专业 Draw.io skill 的 `validate_drawio.py` 与 `analyze_layout.py --strict` 验证代表性流水线图，结果为 6 个顶点、4 条边、0 项警告；PNG 经人工视觉检查后收紧画布高度并调整语义配色。
- Python 全量回归增至 74 项并全部通过，TypeScript 与 Vite 生产构建通过。
- Apple Silicon Sidecar 已重新冻结并包含 Pillow PNG 渲染依赖，二进制约 32.9 MiB；用户无需安装 Draw.io 或额外图片环境即可生成三种资产。真实桌面数据库已无损迁移到 v18，原有任务、模型与加密凭据均保留。

下一步：

1. 实现安全的本地项目启动探测与界面截图清单，禁止触发付费调用或外部副作用。
2. 支持无法安全启动时由用户导入截图，并为每张截图保存章节锚点与五维说明。
3. 在最终 DOCX 质检时再次检查实际项目图表的版面密度、可读性和图文位置。
### 2026-08-10：正式说明书阶段 5——安全截图与章节绑定

- 新增截图安全评估，明确区分 `auto_available`、`manual_import`、`not_applicable`，并版本化记录判断依据与下一步操作。
- 自动截图策略硬性禁止执行项目任意启动脚本、访问外部网络、触发付费调用或使用真实凭据；当前安装包没有受控浏览器适配器时不会伪装成“自动截图成功”。
- 有 UI 但无法安全自动采集时允许从系统文件选择器导入真实 PNG/JPEG/WebP；无 UI 项目或用户暂不提供截图时，截图阶段可安全跳过，不阻塞 DOCX。
- 导入图片限制为 20 MiB、最小 640×360、最大边 12000 像素；解码后统一转为无元数据 RGB PNG，并登记宽高、SHA-256 与来源。
- 每张截图必须绑定真实章节，并保存页面用途、进入条件、可见区域、典型流程、后台/API/数据交互以及结果、校验、异常恢复六类实质说明。
- 新增 SQLite v19 `manual_capture_assessments` 与 `manual_screenshot_revisions`；截图资产读取时重新验证任务目录边界和 SHA-256 完整性。
- 新增截图评估、导入、完成/跳过、清单和图片读取 API；Python 回归增至 76 项并全部通过。
- TypeScript 与 Vite 生产构建通过；Apple Silicon Sidecar 已重新冻结，包含截图解码与净化能力。
- 真实桌面数据库已无损迁移到 v19；迁移后仍保留 3 个任务、6 个模型配置和 1 份加密凭据，新增截图表为空且未污染既有记录。

下一步：

1. 使用真实 Word 标题、段落、列表、表格和图注装配正式软件说明书 DOCX。
2. 将每张图表 PNG 和截图插入绑定章节，保留 Draw.io/SVG 可编辑资产。
3. 接入程序内文档预览、版本、导出和资产管理。

### 2026-08-10：正式说明书阶段 6——Word 装配、预览与资产交付

- 新增正式说明书 Word 装配器，直接从结构化章节、章节图表和真实截图生成 `.docx`，不再把旧 Markdown 草稿当作最终产物。
- 文档使用 A4 页面、真实 Heading 1/2/3、中文首行缩进、原生项目符号、固定 DXA 表格、图注、页眉页脚和动态页码；每个版本登记 SHA-256、文件大小、生成摘要和任务目录边界。
- 目录改为确定性章节目录，避免 LibreOffice/Word 未更新域时出现空白目录页；正文取消逐章强制分页，短章节会连续排版，修复此前所有页面只占一部分的问题。
- 图表和截图按真实长宽比同时限制宽高，截图后附页面用途、进入条件、可见区域、典型流程、后台交互和异常恢复六维说明，不再把最后一行说明挤到孤立尾页。
- 导出 DOCX 以 OOXML 标准嵌入随应用分发的 Noto CJK 开源字体。独立渲染器确认 PDF 实际使用 `NotoSansCJKsc-Regular`，Windows/macOS 无需额外安装中文字体。
- 说明书主页面改为一个用户动作：内部依次执行研究、结构化正文、章节图表、截图决策和 Word 装配；各阶段仍独立持久化并在“高级”区域留痕，不再要求用户点三次规则按钮。
- 生成结果在页面上方立即显示，可直接程序内查看、生成新版本或通过系统保存对话框导出；默认文件名为“项目名-版本号-软件说明书.docx”。导出后按钮切换为跨平台文件夹定位。
- “我的资产”同步纳入正式说明书版本，复用程序内查看、导出和定位逻辑；图表资产继续保留 Draw.io/SVG/PNG 供后续人工微调。
- 使用确定性 fixture 完成六页 DOCX 全页渲染检查：封面、静态目录、连续正文、原生表格、章节图表、界面截图和六维说明均无方框字、空白页、裁切、断表或孤立说明页。
- Python 78 项回归、Rust 6 项测试、TypeScript 与 Vite 生产构建全部通过；Apple Silicon Sidecar 已重新冻结为约 34.6 MB，并通过临时启动与带令牌健康检查。
- 真实桌面数据库保持 schema v19，验收前后均保留 3 个任务、6 个模型配置和 1 份加密凭据，没有发生清库或配置丢失。

下一步：

1. 实现正式版本的运行时 `render_qa` 服务，保存 PDF/逐页图片、页数和问题清单，并将文档状态推进到 `qa_passed` 或 `qa_failed`。
2. 在页面明确区分“已装配、待质检”和“可交付”，只有通过质量门的版本作为默认导出版本。
3. 用真实项目生成一份完整说明书，逐页复核 AI 内容密度、图表可读性与截图章节位置，再完善章节级人工编辑和图表对话修改入口。

### 2026-08-10：正式说明书阶段 7——运行时逐页质检与交付门禁

- 新增 SQLite v20 `manual_document_qa_runs`，逐次保存质检策略、检查项、问题摘要、A4 页面图片、预览 PDF、报告路径和时间；重试产生新版本，不覆盖既有检查记录。
- 一键生成现在完整执行六个阶段：项目研究、结构化正文、专业图表、截图决策、DOCX 装配和逐页质量检查。主流程完成度推进到 100%，内部步骤继续仅作为高级留痕和独立重试入口。
- 运行时使用随 Sidecar 分发的 Noto CJK 字体与 Pillow 确定性分页器，从和 DOCX 相同的章节、表格、图表、截图及六维说明生成 A4 PNG/PDF；最终用户无需安装 Word、LibreOffice、Draw.io 或额外文档环境。
- 质检同时检查 DOCX SHA-256、A4 页面尺寸、章节标题、表格、插图、显式分页符、内嵌字体部件、中文字形覆盖、页面数量、空白页和正文页密度。阻断问题将文档标为 `qa_failed`，只有 `qa_passed` 才允许 API、说明书页和资产页导出。
- 程序内查看改成逐页 A4 预览，提供上一页、下一页、页码和质检状态；不再用浏览器重新排版的长页面冒充最终文档。界面和 API 明确披露运行时页面是同源 companion render，并非 Word 原生排版引擎。
- 发行基线仍执行真实 DOCX 渲染：同一确定性 fixture 经内置分页器和 LibreOffice 均生成 6 页，逐页人工检查封面、目录、连续正文、表格、图表、截图和六维说明，未发现空白页、裁切、重叠、断表或字体替换。
- Apple Silicon Sidecar 已重新冻结为 34,577,808 字节，并通过独立启动、带会话令牌健康检查和临时 v20 初始化验证。
- Python 79 项回归、Rust 6 项测试、TypeScript 与 Vite 生产构建全部通过。（最终提交前再次复跑并以最终结果为准。）
- 真实桌面数据库已无损迁移到 v20；迁移前后均保留 3 个任务、6 个模型配置和 1 份加密凭据，新质检表为空，没有清库或污染既有记录。

下一步：

1. 在桌面程序用一个真实项目重新执行“一键生成”，验收逐页预览、质检状态、导出门禁和资产页默认交付版本。
2. 根据真实 AI 正文的逐页结果调整章节密度、图表尺寸和截图位置，而不是用规则填充页数。
3. 增加章节级所见内容编辑、图表对话修改和重新装配，使正文与图片资产在正式版本生成后仍可持续修订。

### 2026-08-11：正式说明书阶段 8——章节修订与重新交付

- 正式说明书结果卡新增“编辑内容”入口，打开当前生成任务的章节修订工作台；侧栏按章节导航并明确显示人工确认、AI 状态或未保存状态。
- 段落使用正文编辑框，列表按引导句和逐行条目编辑，表格按 Tab 分列、逐行记录编辑；证据引用和推断标记继续随原内容块保存，不退化为无结构长文本。
- 图表请求在章节编辑器中只读展示图名、类型、语义和稳定 key，防止正文编辑意外破坏 Draw.io/SVG/PNG 资产关联。
- “保存本章修订”调用既有版本化章节接口，生成 `origin=user` 的新 revision，不覆盖 AI 原稿；“AI 重新生成本章”仅调用当前章，并明确由用户触发模型费用。
- 用户可连续修改多章而不逐章提交。装配按钮会先保存所有未落库章节；如果 AI 重写改变了图表请求，则同步图表资产，再生成新的 DOCX 版本并自动逐页质检。
- 后端集成回归覆盖“v1 通过质检 → 修改正文 → 装配 v2 → v2 再次通过质检”，验证两个 DOCX 版本和不同 SHA-256 均保留。
- Python 79 项、Rust 6 项、TypeScript 与 Vite 生产构建全部通过。真实桌面数据库仍为 v20，保留 3 个任务、6 个模型配置和 1 份加密凭据；本轮没有修改真实任务或调用付费模型。

下一步：

1. 将正式说明书任务产生的 Draw.io/SVG/PNG 接入专门图表资产页，而不是只跳转到旧版规则图表页面。
2. 支持节点、连线、文字和布局的可视化微调，并允许用自然语言生成白名单 overlay 修订。
3. 图表或截图修订后自动标记关联 DOCX 为“存在更新”，由用户一键重新装配并质检。

### 2026-08-11：正式说明书阶段 9——正式图表资产修订

- 图表资产页的数据源已从旧版两张固定规则示例图切换为正式说明书任务的真实章节图表；没有正式图表时明确引导返回说明书生成。
- 每张资产可切换查看 SVG、PNG 与 Draw.io XML。SVG 节点带稳定 `data-node-key`，支持直接拖动；节点名称也可在属性栏修改。
- 正式图表修改复用安全 overlay 白名单。人工拖动、改名和历史恢复不调用模型，均创建新的 `.drawio`、`.svg`、`.png` 与数据库 revision，旧版本不会被覆盖。
- AI 对话修改采用两阶段确认：模型只返回 `node.move`、`node.label`、`node.style`、`edge.route` 等白名单操作；服务端校验目标并先渲染未落库 SVG，用户确认后才写入正式修订。
- 历史面板显示 AI 初稿、人工修改与 AI 修改来源；恢复任意历史内容会继续创建新版本，避免破坏审计链。
- 新增 SQLite v21，为正式图表修订记录父版本、修改来源、操作清单、语义指纹和冲突状态。
- DOCX 响应新增 `freshness`：当正文、图表或截图晚于文档时，旧 DOCX 明确标为“内容已更新，待重新装配”，暂停导出并提供“重新装配最新内容”入口；旧版本和质检记录仍完整保留。
- 按 Draw.io 专业图表技能要求，`.drawio` 继续作为可编辑源，SVG/PNG 由本地确定性渲染器生成；用户不需要安装 Draw.io 或额外图片环境。
- Python 79 项回归、Rust 6 项测试、TypeScript 与 Vite 生产构建全部通过。应用内浏览器完成空状态页面视觉检查；纯浏览器不具备 Tauri 本地桥，因此真实数据交互由服务级回归与桌面包验收覆盖。
- Apple Silicon Sidecar 已重新冻结为 34,581,936 字节；本轮最终代码与前一冻结版本使用同一启动链，临时独立启动及带令牌健康检查已通过。
- 真实桌面数据库已无损迁移到 v21；迁移前后均保留 3 个任务、6 个模型配置和 1 份加密凭据。

下一步：

1. 在桌面程序选择已有正式说明书项目，验收拖动、AI 修改预览、历史恢复以及 DOCX 过期提示和重新装配。
2. 增加截图资产的专门管理页：导入、章节绑定、六维说明修订和版本历史。
3. 用真实项目迭代图表视觉模板与布局质量，重点处理节点密度、跨层边、长标签和图文比例，而不是只确认“能生成”。
