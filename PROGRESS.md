# 开发进度

> 本文件采用追加式记录。每轮结束都更新“当前状态”和新增一条轮次记录，确保进度、验证和下一步保存在仓库中。

## 当前状态

- 当前里程碑：M4 说明书与可持续修订资产。
- 总体状态：源码文档与正式说明书已形成一键生成、逐页质检、程序内预览、导出和正文/图表/截图版本化修订闭环。
- 当前分支：`main`。
- 当前阻塞：无。
- 下一轮目标：用真实项目完成 Stage 10 桌面验收，并进入整包安装、Windows 兼容与发布基线检查。

## 里程碑概览

| 里程碑 | 状态 | 说明 |
|---|---|---|
| M0 需求和架构基线 | 暂停 | 核心详细设计完成，迁移与运行时评估随实现推进 |
| M1 领域内核与 CLI | 进行中 | 已完成至代码正文分页预览的纵向链路 |
| M2 模型适配与项目理解 | 未开始 | 依赖核心接口与状态机 |
| M3 源代码文档 | 未开始 | 依赖扫描、模型和领域内核 |
| M4 说明书与图表 | 未开始 | 依赖模型、DOCX 和 Draw.io 内核 |
| M5 桌面应用与发布 | 未开始 | 可在 M1 后建立薄纵切验证 |
| M6 1.1 自动截图 | 未开始 | MVP 后续阶段 |

## 轮次记录

### 2026-08-10：规划基线

本轮目标：

- 将已经对齐的产品边界转化为可维护的仓库文档。
- 建立每轮留痕和下一步同步机制。

完成内容：

- 确认产品完全不依赖 Codex。
- 确认产品为单机、本地工具，不包含多用户和自动申报。
- 确认使用 SQLite 进行轻量持久化。
- 确认模型层需要支持多协议并兼容未来国内模型。
- 制定 M0 至 M6 的开发计划。
- 建立开发进度和架构决策记录。

验证：

- 核对当前仓库仅包含交接文档，没有待兼容的应用代码。
- 核对本地 `main` 与 `origin/main` 同步后再建立规划基线。

风险与待确认：

- LibreOffice、字体、Draw.io 渲染和 Chromium 的分发方式仍需在 M0/M5 形成许可与体积评估。
- 页面自动启动和截图暂定为 1.1，MVP 仅支持手动导入截图并预留接口。
- 首批原生模型协议数量需要在 M0 接口设计后结合测试成本确认。

下一步：

1. 编写领域对象和接口草案。
2. 设计 SQLite 表、版本迁移和数据保留策略。
3. 设计任务状态机、阶段重试、取消和恢复语义。
4. 设计任务目录、产物版本和哈希规则。
5. 完成设计评审后再决定 M1 工程骨架。

### 2026-08-10：M0 核心详细设计

本轮目标：

- 冻结跨平台架构和进程边界。
- 定义领域对象、模型协议、Tool、Artifact 和 QA 接口。
- 设计 SQLite、任务状态机、恢复语义和任务目录。
- 不创建应用工程骨架或业务实现。

完成内容：

- 新增 `ARCHITECTURE.md`，明确 Tauri、FastAPI sidecar、Python 内核和适配器职责。
- 明确 sidecar 只监听本机随机端口，使用一次性会话令牌。
- 新增 `DOMAIN_INTERFACES.md`，定义 ProjectSnapshot、Evidence、Fact、Artifact、QAResult 等对象。
- 将 `ProtocolAdapter` 与 `ModelProvider` 分层，规定能力探测和结构化输出降级策略。
- 定义受控 Tool、应用服务、存储端口和质量策略接口。
- 新增 `DATA_AND_WORKFLOW.md`，设计 SQLite 核心表和并发版本控制。
- 固定 MVP 十二阶段工作流、重试、取消、SSE 续传和启动恢复语义。
- 固定任务目录、内部 ASCII 命名、产物版本与哈希协议。
- 新增 ADR-007 至 ADR-009。

验证：

- 检查新增 Markdown 的标题结构和文件完整性。
- 执行 `git diff --check` 检查空白及补丁格式。
- 对照 `DEVELOPMENT_PLAN.md` 的 M0 交付物核对覆盖范围。

风险与待确认：

- SQLite 表目前是逻辑 Schema，M1 前需转为首版 migration 并通过约束测试。
- FastAPI 本地令牌的安全传递方式需在 Tauri 薄纵切中用真实打包环境验证。
- 大型运行时组件的许可证、体积、下载和升级策略尚未定案。
- 现有两个 Skill 的脚本、规则、依赖和可迁移边界尚未形成清单。

下一步：

1. 只读盘点 `copyright-doc` 与 `drawio-professional-diagram` 的可迁移资产。
2. 区分可直接迁移、需要重构、仅作规范参考和不可带入产品的内容。
3. 评估 LibreOffice、Draw.io 渲染、字体和后续 Chromium 的分发策略。
4. 冻结首批模型协议与协议契约测试范围。
5. 汇总 M0 评审清单，请用户确认后进入 M1。

### 2026-08-10：M1 首个可运行纵向切片

本轮目标：

- 停止继续扩展纯设计文档，开始可运行实现。
- 打通“扫描项目 → 写入 SQLite → 生成 manifest → CLI 输出摘要”。
- 保持零第三方运行依赖，兼容当前 Python 3.9 环境。

完成内容：

- 建立 `src/software_copyright_agent` Python 包和 `pyproject.toml`。
- 实现任务、阶段、文件分类和扫描结果领域对象。
- 实现跨平台项目扫描器，采用稳定排序和 SHA-256 项目指纹。
- 过滤常见版本控制、依赖、构建、虚拟环境目录和敏感文件。
- 默认跳过符号链接和超过 10 MiB 的文件。
- 建立首版 SQLite migration，包含项目、快照、任务、阶段和事件表。
- 实现事务化扫描服务和原子 `manifest.jsonl` 写入。
- 实现 `copyright-agent scan` CLI 与 JSON 输出。
- 添加 README、`.gitignore` 和单元测试。

验证：

- 使用 Python 3.9 标准库运行全部单元测试。
- 使用当前仓库完成端到端 CLI 扫描。
- 验证 SQLite 任务状态、阶段和事件已经落库。
- 验证任务目录生成 manifest，数据库启用 foreign keys 和 WAL。

已知限制：

- 扫描规则暂未读取项目自身的 `.gitignore`。
- 当前任务只表达 scan-only 纵向切片，尚未实现完整十二阶段调度器。
- ZIP 导入、秘密内容扫描和可配置 ignore 规则尚未实现。
- migration runner 当前只有 v1，尚未实现多版本 checksum 校验。

下一步：

1. 把 SQL 操作拆分为 repository 和 UnitOfWork，避免应用服务直接写 SQL。
2. 实现真实任务状态转换器和乐观并发版本检查。
3. 实现 ZIP 安全解压及输入摄取阶段。
4. 增加 `.gitignore`、二进制识别、秘密内容和超大项目预算。
5. 为 Windows 路径、保留名和大小写差异增加测试。

### 2026-08-10：Repository、UnitOfWork 与任务状态机

本轮目标：

- 移除扫描应用服务中的直接 SQL。
- 实现真实任务生命周期、失败留痕和乐观并发控制。
- 保持现有 CLI 行为可用。

完成内容：

- 新增 Source、Snapshot、Task、Stage 和 Event Repository。
- 新增 SQLite `UnitOfWork`，统一事务提交和回滚。
- 新增任务状态机和显式合法转换表。
- 使用 `row_version` 与原状态共同约束任务更新。
- 每次任务状态转换在同一事务中追加结构化事件。
- 扫描流程调整为 `created → running → completed`。
- 扫描异常时将阶段和任务持久化为 `failed`，不记录原始敏感异常内容。
- 扫描成功事件序列固定为 `task.running → stage.succeeded → task.completed`。

验证：

- 9 项单元测试全部通过。
- 覆盖非法状态跳转、过期版本写入、扫描失败落库和正常扫描。
- 使用当前仓库完成真实 CLI 扫描，任务最终 `row_version=3`。
- 核对 SQLite 中三条生命周期事件顺序正确。
- Python 编译检查和 `git diff --check` 通过。

已知限制：

- `UnitOfWork` 当前采用单 SQLite 写入事务，后续 sidecar 任务执行器需要串行化写入调度。
- 阶段自身尚未增加独立 `row_version`，目前依靠状态条件更新。
- 失败后的阶段重试和下游 `stale` 传播尚未实现。
- 输入摄取仍仅支持目录。

下一步：

1. 实现目录与 ZIP 统一的 `InputIngestor`。
2. 防止 ZIP Slip、绝对路径、符号链接和解压膨胀。
3. 将 ZIP 解压到任务专属 `input/extracted` 目录。
4. 增加文件数量、单文件和总解压体积预算。
5. 为恶意 ZIP 和中文路径编写回归测试。

### 2026-08-10：ZIP 安全摄取

本轮目标：

- 统一目录与 ZIP 项目输入。
- 不依赖外部解压命令，安全解压 ZIP 到任务隔离目录。
- 对常见恶意归档和解压膨胀建立确定性阻断。

完成内容：

- 新增 `InputIngestor`、`IngestedProject` 和可配置解压预算。
- 支持目录直接扫描与 ZIP 任务内解压扫描。
- ZIP 使用逐条目校验和流式复制，不调用 `extractall`。
- 阻止 `..` 穿越、绝对路径、Windows 盘符路径和反斜杠变体。
- 拒绝 ZIP 内符号链接和加密条目。
- 按大小写折叠检测重复路径，避免 Windows/macOS 文件覆盖冲突。
- 限制文件数量、单文件解压大小和总解压大小。
- 实际写入量不得超过 ZIP 声明大小或预算。
- 解压先写临时目录，完整成功后原子移动到 `input/extracted`。
- 单一顶层项目目录会自动成为扫描根目录，并忽略 `__MACOSX` 包装目录。
- CLI 帮助和扫描服务已经接受目录或 ZIP。

验证：

- 16 项单元测试全部通过。
- 覆盖中文路径、Zip Slip、符号链接、大小写冲突、文件数量和体积预算。
- 使用真实 ZIP 运行 CLI，正确扫描 2 个文件。
- SQLite 正确保存 `kind=zip`、任务 `completed` 和 `row_version=3`。
- 解压文件、manifest 和任务 ID 位于同一隔离任务目录。

已知限制：

- 当前只支持 ZIP，不支持 tar、7z 或远程 Git；这些均不属于当前 MVP。
- ZIP 原始压缩包不会复制到任务目录，只保存原始路径和解压结果。
- 暂未处理项目自身 `.gitignore` 和嵌套 ignore 规则。
- 扫描器会计算全部受控文件哈希，超大项目尚缺总扫描预算。

下一步：

1. 实现项目根与嵌套 `.gitignore` 的核心匹配规则。
2. 在哈希前识别二进制文件并限制读取。
3. 增加秘密文件名和内容特征检测报告。
4. 增加最大扫描文件数、总字节数和取消检查点。
5. 输出按文件类别、语言和忽略原因聚合的扫描报告。

### 2026-08-10：可审计项目扫描

本轮目标：

- 支持项目自身 `.gitignore` 的常用规则。
- 在读取内容前后执行资源预算和二进制判断。
- 检测秘密特征但不把秘密值写入数据库、manifest 或报告。
- 输出可供后续事实提取和 QA 使用的扫描报告。

完成内容：

- 实现项目根和嵌套 `.gitignore` 规则加载。
- 支持 glob、目录规则、根锚定、否定规则和最后匹配优先。
- 为默认目录、`.gitignore`、敏感文件、符号链接、超大文件和读取失败记录独立忽略原因。
- 增加常见源码扩展名到语言名称映射。
- 通过 NUL 字节和 UTF-8 解码检测二进制输入。
- 增加私钥、AWS Key、GitHub Token 和常见赋值式秘密特征检测。
- 秘密发现只包含 `rule_id`、相对路径和行号。
- 增加最大文件数、单文件字节数和总扫描字节数预算。
- manifest 新增 `language` 和 `is_binary` 字段。
- SQLite 快照摘要新增类别、语言、忽略原因、二进制和秘密发现计数。
- 原子生成 `qa/scan-report.json`。
- CLI JSON 和文本摘要新增扫描报告及安全统计。

验证：

- 19 项单元测试全部通过。
- 覆盖根/嵌套 ignore、否定与锚定规则、二进制、语言、秘密和预算。
- 测试确认秘密原文不会出现在 `SecretFinding` 中。
- 使用当前仓库完成真实扫描：30 个文件、19 个 Python 文件、0 个秘密发现。
- SQLite 聚合摘要、manifest 和扫描报告内容一致。
- Python 编译与 `git diff --check` 通过。

已知限制：

- `.gitignore` 是面向项目扫描的安全子集，不覆盖 Git 的全部转义和父目录重包含边缘语义。
- 当前文本识别以 UTF-8 为主，其他编码暂按二进制处理。
- 秘密检测是阻断前的第一层规则，后续还需要熵检测和可配置白名单。
- 扫描暂不支持协作式取消，预算超限会立即失败并持久化任务状态。

下一步：

1. 从 README、依赖清单和部署配置确定性提取项目元数据。
2. 根据文件语言、框架依赖和目录结构形成技术栈候选。
3. 生成模块候选及 Evidence 路径引用。
4. 将事实候选写入 SQLite，而不是只写 JSON 摘要。
5. 增加 `inspect` CLI，展示事实、证据和需要用户确认的字段。

### 2026-08-10：确定性事实与 Evidence

本轮目标：

- 从项目现有配置和文档提取可追溯事实，不引入模型猜测。
- 将事实、Evidence 和待确认项写入 SQLite。
- 增加 CLI 检查入口，验证持久化结果可被桌面层消费。

完成内容：

- 新增 SQLite v2 migration：`evidence`、`facts` 和 `confirmation_requests`。
- 支持从 v1 数据库幂等升级到 v2。
- 从 `package.json`、`pyproject.toml` 和 `Cargo.toml` 提取名称及版本。
- 配置清单中的明确名称和版本标记为确定性确认。
- README 一级标题仅作为较低置信候选，并生成用户确认请求。
- 未发现版本号时生成必填确认请求，不虚构默认版本。
- 根据扫描 manifest 聚合语言和源码模块候选。
- 根据依赖清单识别 React、Next.js、Vue、Tauri、FastAPI 等框架候选。
- 每个事实保存来源、置信度和 Evidence ID。
- Evidence 保存相对路径、定位器、内容哈希和提取器版本。
- 扫描工作流新增 `03_extract_facts` 阶段。
- 存在待确认项时任务进入 `waiting_for_user`，否则进入 `completed`。
- 新增 `inspect [task_id] --json`，省略 ID 时读取最近任务。

验证：

- 23 项单元测试全部通过。
- 覆盖 package、TOML、README、语言、框架、模块和确认请求。
- 覆盖既有 SQLite v1 数据库升级至 v2。
- 使用当前仓库完成端到端扫描和 inspect。
- 当前仓库确定性识别名称 `software-copyright-agent`、版本 `0.1.0`、Python 与模块候选。
- SQLite 中 Fact 的 Evidence ID 均可解析到对应 Evidence。

已知限制：

- TOML 当前使用受控正则读取标准 name/version 字段，M2 引入依赖后应换成正式 TOML 解析器。
- 框架识别当前基于依赖清单标记，尚未分析具体版本约束和运行方式。
- 模块候选根据源码目录聚合，仍需要后续语义模型判断其业务含义。
- 待确认项目前只能查看，尚不能通过 CLI 回答并恢复任务。
- 事实尚未支持 superseded 和用户修改历史。

下一步：

1. 实现 `confirm` CLI 写回答案。
2. 将用户答案保存为新的 Evidence 和 confirmed Fact。
3. 把旧候选事实标记为 superseded，保留完整历史。
4. 所有必填确认完成后恢复 `waiting_for_user → running`。
5. 增加后续阶段 checkpoint，为核心源码筛选做准备。

### 2026-08-10：用户确认与任务恢复闭环

本轮目标：

- 允许用户回答确定性程序无法安全确认的项目元数据。
- 保留候选事实历史，不直接覆盖原记录。
- 最后一个必填确认完成后合法恢复任务状态。

完成内容：

- 新增 `confirm TASK_ID FIELD_KEY VALUE` CLI。
- 确认服务在单一 UnitOfWork 中完成所有写入。
- 确认请求由 `pending` 原子更新为 `answered`，重复回答被拒绝。
- 每个用户答案生成 `user_confirmation` Evidence。
- 旧 candidate/confirmed Fact 标记为 `superseded`。
- 用户答案生成 `source=user`、`status=confirmed`、`confidence=1.0` 的新 Fact。
- 事实历史与 Evidence ID 均保留，inspect 可查看新旧版本。
- 工作流新增 `04_confirm_metadata` 等待阶段。
- 部分确认后任务保持 `waiting_for_user`。
- 最后一个必填确认完成后执行 `waiting_for_user → running → completed`。
- 确认阶段同步更新为 `succeeded`，任务版本递增至 5。

验证：

- 26 项单元测试全部通过。
- 覆盖部分确认、最终恢复、重复回答拒绝和 CLI 闭环。
- 真实测试项目从 README 候选生成两个确认请求。
- 回答名称和版本后，任务、三个阶段和所有确认项均完成。
- SQLite 保留旧名称为 `superseded`，新名称和版本为用户 confirmed Fact。
- 两条用户 Evidence 均能回溯到对应 confirmation ID。

已知限制：

- 当前确认值按文本保存，后续需要按字段 Schema 支持枚举、数组和结构化值。
- 当前 CLI 使用字段键定位确认请求；桌面 API 应使用 confirmation ID 和 expected row version。
- 当前纵向切片在元数据确认后标记完成，完整产品后续会继续进入源码筛选阶段。
- 尚未实现修改已回答确认项；未来修改应创建新版本而不是原地改写。

下一步：

1. 实现 `05_select_source` 阶段。
2. 确定性排除测试、mock、vendor、migration、生成代码和敏感文件。
3. 为候选源码计算业务相关性、入口性、依赖中心度和基础设施必要性评分。
4. 输出每个文件的选择/排除理由及 Evidence。
5. 增加 `source-plan` CLI，展示 A/B/C 分级和预计可用代码量。

### 2026-08-10：核心源码 A/B/C 计划

本轮目标：

- 建立 `05_select_source` 确定性阶段。
- 对所有受控源码给出选择、排除、等级、评分和理由。
- 计划可重复生成且历史版本不可覆盖。

完成内容：

- 新增 SQLite v3 migration 和扫描根引用。
- 新增 `source_plan_runs` 与 `source_candidates`。
- 支持目录项目和 ZIP 任务解压目录重新定位源码。
- 确定性排除 test、mock、fixture、vendor、migration、demo、sample、example 和 generated。
- 排除二进制源码及扫描阶段发现秘密的文件。
- 根据业务目录/文件名、应用入口、必要基础设施、主源码树、实现规模和通用辅助属性计算评分。
- A 级为核心优先，B 级为必要支撑，C 级为低相关或确定性排除。
- 每个候选保存 `reasons` 和可选 `exclusion_code`。
- 统计入选文件数、非空代码行数和 A/B/C 分布。
- 原子生成 `intermediate/source-selection/plan.vN.json`。
- 新增 `source-plan TASK_ID --json` CLI。
- 重复生成计划时 plan version 和 stage attempt 分别递增，不覆盖旧记录。
- 待确认任务不能提前执行源码计划。

验证：

- 31 项单元测试全部通过。
- 覆盖排除规则、A/B/C 分级、文件名业务语义、重复计划版本和 CLI。
- 第二次计划最初暴露固定 attempt 冲突，已改为数据库计算下一 attempt 并保留回归测试。
- 当前仓库真实计划识别 29 个源码文件：A 4、B 13、C 12。
- 入选 17 个文件、2769 个非空代码行；11 个测试文件被确定性排除。
- 当前任务阶段为 `05_select_source`，计划 JSON 与 SQLite 汇总一致。

已知限制：

- 当前评分基于路径、文件名和实现规模，不等同于业务语义理解。
- 暂未解析跨文件 import 图或调用关系，依赖中心度仍是后续能力。
- 非空物理行数只是容量近似，尚未执行视觉宽度换行和 Word 分页估算。
- C 级低相关候选不会自动进入代码文档，但仍保留供用户或模型复核。
- 模型语义重排不得恢复秘密、测试、vendor 等确定性排除项。

下一步：

1. 对 A/B 入选文件执行编码识别和确定性代码行提取。
2. 增加视觉宽度计算、长行硬换行和页容量配置。
3. 估算 59 页代码正文所需行数，并报告源码不足或超量。
4. 按 A 优先、B 补充和去重规则形成稳定代码序列。
5. 输出代码文档的分页预览数据，为 DOCX 生成做准备。

### 2026-08-10：代码视觉换行与 59 页分页预览

本轮目标：

- 将最新 A/B 源码计划转换为稳定代码序列。
- 在 Word 出件前确定性计算视觉换行和页面容量。
- 源码不足时如实报告，禁止填充和重复。

完成内容：

- 新增 SQLite v4 migration 和 `code_preview_runs`。
- 新增 `06_prepare_source_doc` 阶段。
- 只读取最新 source plan 中 selected 的 A/B 文件。
- 每个文件读取前使用 manifest SHA-256 复核，源码变化立即阻断。
- 使用 Unicode East Asian Width 计算中英文视觉宽度。
- 组合字符宽度为 0，中日韩宽字符宽度为 2，其他字符为 1。
- Tab 按固定宽度展开，长行在进入 Word 前硬分段。
- 默认参数：100 视觉列、每页 50 行、59 页代码正文、Tab 4 空格。
- 文件按 A 优先、B 补充、评分降序和路径稳定排序。
- 每个代码段保留原文件、源行号、segment 和 continuation 标记。
- 精确生成 page/entry JSON，不依赖 Word 自动换行估页。
- 源码超量时稳定截取前 2950 个视觉行；不足时生成实际页数。
- 新增 `code-preview TASK_ID --json` CLI。
- 重复预览创建新版本和新 stage attempt，不覆盖历史。
- 代码不足时任务为 `completed_with_warnings`，不是失败。

验证：

- 35 项单元测试全部通过。
- 覆盖中文双宽字符、Tab、硬换行、分页、不足警告和源码哈希变化。
- 覆盖预览版本化、SQLite v4 升级和 CLI。
- 当前仓库实际 source plan：32 个源码文件，A 5、B 14、C 13。
- 入选 19 个文件，预处理得到 3606 个视觉行。
- 稳定使用 2950 行生成完整 59 页正文，每页均为 50 行。
- 预览实际覆盖前 13 个文件，其余内容因达到目标容量而截断。
- 任务完成于 `06_prepare_source_doc`，`row_version=7`。

已知限制：

- 当前视觉宽度模型尚未与最终 Word 字体的真实字形度量做校准。
- 文件标题和分隔行计入页面容量；最终模板必须保持相同约定。
- 当前只支持 UTF-8 入选源码，其他编码会被阻断而非猜测转换。
- 当前按计划顺序截断，尚未实现保证每个核心模块最低覆盖量的配额算法。
- 分页预览是 DOCX 输入数据，不是最终可交付文档。

下一步：

1. 引入 DOCX 生成依赖并创建 1 页封面＋59 页代码正文。
2. 固定页面尺寸、页边距、等宽字体、字号、行距、页眉和页脚。
3. 使用分页预览逐页写入，禁止 Word 自动重排改变行数。
4. 通过 LibreOffice 转 PDF 并核对实际总页数为 60。
5. 增加空白页、异常换行、页眉页脚和字体回退 QA。

### 2026-08-10：首版 60 页源代码 DOCX 与真实渲染闭环

本轮目标：

- 将最新完整分页预览生成可打开的 Word 源代码文档。
- 固定 A4、封面、代码行距、页眉页脚和显式分页。
- 使用真实仓库完成 DOCX → PDF → 逐页 PNG 视觉验证。

完成内容：

- 新增 `python-docx` 运行依赖和 `source-docx TASK_ID --json` CLI。
- 新增 SQLite v5 migration 与 `source_document_runs`，保存预览版本、模板参数、摘要、相对路径和 SHA-256。
- 新增 `SourceDocumentBuilder` 和 `SourceDocumentService`。
- 使用 `compact_reference_guide` 预设、`editorial_cover` 封面模式，并将纸张明确覆盖为 A4。
- 固定 1 页封面、59 页正文、每页 50 个预处理视觉行。
- 使用显式分节和分页符，正文页眉、PAGE/NUMPAGES 字段及等宽代码字体。
- 生成前要求最新预览 `sufficient=true`，禁止短项目通过空白或重复内容凑页。
- 每次重复生成创建新版本，旧 DOCX 和数据库记录均不覆盖。
- 首轮视觉检查发现中文静态标签缺字；通过最小字体样本确认是 LibreOffice 与 macOS 系统 CJK 字体兼容问题。
- 最终 QA 模板改用无缺字的英文固定标签；用户确认的软件名称和版本仍保持原值。
- 新增 ADR-010，要求跨平台发布前内置可再分发的 CJK 字体并设置字体探测门禁。

验证：

- 37 项完整单元测试全部通过，新增固定页数、行数和不完整预览阻断覆盖。
- 真实仓库扫描 46 个文件，无秘密命中。
- 源码计划识别 35 个源码文件，A 6、B 15、C 14；选中 21 个，排除 13 个。
- 预处理得到 4131 个视觉行，稳定使用前 2950 行生成 59 个正文页。
- 最终 DOCX v3 SHA-256：`d5d5ce765e1dcb04b2034f3af3007adeaf360e00c7403411f77c0106696f5f25`。
- LibreOffice 实际渲染为 60 页，PDF 页面尺寸为 A4。
- 已检查全部 60 页缩略图，并以原始分辨率复核封面、第 2 页和第 60 页；未发现空白页、溢出、重叠、截断或异常分页。

已知限制：

- 本轮 QA 项目名称和源码为拉丁字符；中文项目在内置开源 CJK 字体完成前不能宣称跨平台渲染通过。
- PAGE 字段按整个文档计数，因此代码正文显示第 2–60 页；尚未提供“正文从 1 重新编号”的模板选项。
- 渲染 QA 当前是开发流程，尚未封装为应用内自动质量门禁和数据库 QA 记录。

下一步：

1. 增加 DOCX 自动 QA 服务：实际页数、A4、空白页、页眉页脚、产物哈希和报告持久化。
2. 选定并内置许可兼容的 CJK 字体，增加中文软件名和中文源码黄金样例。
3. 将渲染失败或页数不等于 60 设为出件阻断项。
4. 再开始说明书章节规划和 Draw.io 图表阶段，不提前进入桌面 UI。

### 2026-08-10：源代码 DOCX 自动 QA 门禁

本轮目标：

- 将上一轮开发期人工渲染步骤封装为应用自身可执行的质量门禁。
- 自动验证 DOCX 结构、实际页面、A4 尺寸和空白页。
- 将每次 QA 结果、检查明细和渲染路径持久化。

完成内容：

- 新增 SQLite v6 migration 和 `source_document_qa_runs`。
- 新增 `qa-source-docx TASK_ID --json` CLI；通过返回 0，质量阻断返回 3，运行故障返回 2。
- 新增 `LibreOfficeRenderer`，直接调用可配置的 LibreOffice 与 `pdftoppm`，不依赖 Codex 文档脚本。
- 支持 `COPYRIGHT_AGENT_SOFFICE` 指定应用内置或外部 headless LibreOffice。
- 新增 11 项自动检查：DOCX SHA-256、2 个分节、58 个正文分页符、2 个 A4 分节、页眉部件、PAGE/NUMPAGES 字段、60 个渲染页、页面尺寸一致、A4 宽高比和空白页。
- 使用 Pillow 检查纯白页面；自动结果始终保留 `visual_review_required=true`，不将像素阈值伪装为完整审美判断。
- QA 报告写入任务目录，SQLite 保存策略版本、通过状态、检查明细、摘要、报告路径和渲染路径。
- QA 未通过时任务结束为 `completed_with_warnings`；渲染器等运行故障记录为 `failed`，允许在修复环境后重试。

验证：

- 39 项完整单元测试全部通过。
- 新增完整 60 页通过测试，以及人为制造第 17 页空白时阻断测试。
- 首次真实运行发现 Homebrew LibreOffice 26.2 headless 进程无诊断 abort，任务安全记录失败而未产生伪通过结果。
- 增加通用 soffice 路径配置后，使用独立 headless LibreOffice 从失败状态成功重试。
- 真实 DOCX v3 自动 QA 结果为 11/11 通过，渲染 60 页，尺寸 827×1170，无空白页。
- QA run v1、检查明细和报告路径已写入 SQLite，任务完成于 `06_qa_source_doc`。
- 已再次检查自动 QA 产生的全部 60 页缩略图，未发现溢出、重叠、缺页或异常空白。

已知限制：

- 当前发布包尚未真正捆绑 LibreOffice 和 Poppler，只完成可配置路径与领域门禁；桌面打包阶段必须补齐两平台运行时。
- 空白页检查只能发现全白或近全白页面，不能自动判断字符缺失、排版美感或语义完整性。
- QA PNG 和 PDF 当前全部保留在任务目录，尚未提供按策略清理中间渲染物的生命周期管理。

下一步：

1. 实现 CJK 字体探测器和字体门禁，先覆盖中文软件名与中文源码黄金样例。
2. 选定许可兼容的内置字体，并定义 macOS/Windows 字体解析优先级。
3. 将字体检查加入 `source-docx-qa-v2`，中文缺字风险必须阻断。
4. 完成后再进入说明书内容规划和 Draw.io 图表纵向切片。

### 2026-08-10：内置 CJK 字体、DOCX 内嵌与中文黄金样例

本轮目标：

- 消除中文软件名、固定标签和中文源码对系统字体的依赖。
- 对字体文件、许可、字形覆盖和 DOCX 内嵌结构建立确定性门禁。
- 使用 60 页中文黄金样例完成真实渲染闭环。

完成内容：

- 从 notofonts 官方仓库引入未经修改的 `NotoSansCJKsc-Regular.otf`，随项目附带 SIL OFL 1.1 原文和第三方声明。
- 字体 SHA-256 固定为 `2c76254f6fc379fddfce0a7e84fb5385bb135d3e399294f6eeb6680d0365b74b`，许可证 SHA-256 同样进入代码门禁。
- 新增无第三方解析依赖的 OpenType cmap format 4/12 读取器。
- 生成 DOCX 前收集软件名称、版本、固定标签和全部入选源码中的非 ASCII 字符；字体缺少任何所需字形时阻断出件。
- 使用确定性 font key 将字体混淆为 OOXML `.odttf` 部件，并写入 `fontTable.xml.rels`、`embedRegular`、`embedTrueTypeFonts` 和 content type。
- 生成器恢复中文“源程序代码”、文档构成、页眉和页脚标签。
- QA 策略升级为 `source-docx-qa-v2`，增加字体 SHA、缺字、内嵌部件、embedRegular、嵌入设置和字体内容类型检查。
- 新增 `THIRD_PARTY_NOTICES.md`，记录上游、文件、哈希和许可证路径。

验证与迭代：

- 41 项完整单元测试全部通过。
- 覆盖字体/许可证哈希、常用中文 cmap、扩展字符缺字阻断，以及 DOCX 确实包含 `.odttf` 和 `embedRegular`。
- 第一版仅设置 `SAL_FONTPATH`，自动结构门禁通过但真实页面仍显示方框；人工视觉检查成功捕获该假阳性。
- 第二版复制字体到隔离用户字体目录仍无效。
- 第三版首次整体重序列化 OOXML，因丢失 `mc:Ignorable` 关联命名空间而被 LibreOffice 拒绝；改为局部字节插入后修复。
- 最终中文黄金样例使用“软著材料助手”、中文文件标题和 2950 行中文源码，实际渲染 60 页 A4。
- 自动 QA 17 项全部通过，所需 35 个非 ASCII 字形无缺失、无空白页。
- 已检查中文黄金样例全部 60 页缩略图，并以原始分辨率复核封面和正文页；中文无方框、无缺字、无溢出。
- 当前真实仓库重新生成 source DOCX v4，SHA-256 为 `4c4960fd070f77bd2d7647059cac4429d1ff18531ec543481971ed4ab727d1fe`；QA run v2 的 17 项检查全部通过。
- 已检查真实 v4 的全部 60 页渲染，中文固定标签正常，正文布局未发生回归。

已知限制：

- 当前只内置 Regular 字重；标题设置为粗体时由渲染器合成，后续若版式要求严格可再引入官方 Bold 字重。
- Noto Sans CJK SC 不覆盖所有 Unicode CJK 扩展字符；当前策略是确定性阻断并报告缺字样例。
- 字体使每个未子集化 DOCX 增加约 16MB；后续可评估安全的字体子集化，但不得破坏可编辑性和许可留痕。
- 自动门禁仍不能用像素规则证明所有字形语义正确，最终交付继续保留人工视觉检查。

下一步：

1. 开始说明书内容规划：从 Fact/Evidence 生成确定性章节骨架和缺失信息清单。
2. 先实现章节计划 JSON 与持久化，不立即大规模生成长篇正文。
3. 定义说明书所需架构图、核心流程图和模块关系图的 Draw.io 输入契约。
4. 保持模型输出只能提供候选内容，事实和图表节点必须可追溯到证据。

### 2026-08-10：修复源码文档页面未铺满

本轮目标：

- 修复每页虽有 50 行但正文仅占页面约四分之三的版式缺陷。
- 将正文纵向覆盖率纳入自动质量门禁，避免“页数正确但页面未铺满”再次回归。

完成内容：

- 将默认视觉列宽由 100 收窄到 90，为放大字号保留横向安全余量。
- 将源码字号由 8pt 调整为 9pt，精确行高由 11pt 调整为 15pt；继续保持每页 50 行和总计 60 页。
- QA 策略升级为 `source-docx-qa-v3`，逐页检测源码正文最末墨迹位置，最低纵向覆盖率要求为页面高度的 86%。
- QA 报告新增 `minimum_body_fill_ratio` 和 `underfilled_pages`，覆盖不足会直接阻断出件。
- 新增人为制造第 17 页正文不足的回归测试。

验证与迭代：

- 14pt 行高首轮真实渲染仍发现第 6 页覆盖率仅 85.38%，新门禁正确阻断，没有误放行。
- 调整为 15pt 后重新生成并渲染真实仓库，得到 60 页 A4，18 项自动检查全部通过，无空白页、无覆盖不足页。
- 最低正文纵向覆盖率提升到 89.23%，页面尺寸为 827×1170。
- 已检查全部 60 页缩略总览，并以原始分辨率抽检第 2、30、60 页；未发现裁切、溢出、重叠或异常换页。

下一步：

1. 进入说明书章节计划的确定性 JSON 与持久化实现。
2. 定义架构图、核心流程图和模块关系图的 Draw.io 输入契约。
3. 延续每轮自动检查、真实渲染和人工视觉抽检留痕。

### 2026-08-10：说明书章节计划与证据缺口

本轮目标：

- 进入 M4，但只实现说明书章节计划，不直接生成长篇正文。
- 让每个章节明确引用已有 Fact/Evidence，并暴露当前无法安全撰写的内容缺口。

完成内容：

- 新增 `manual-plan-v1` 确定性规划器，生成软件概述、运行环境、总体设计、功能设计、数据设计、接口设计、安全与可靠性、用户操作说明、测试部署维护共 9 个章节。
- 章节记录用途、子章节、生成模式、Fact ID、Evidence ID、缺失信息、状态和所需图表；每项缺口映射到稳定 Fact key，后续补齐证据后可自动转为 `ready`。
- 固定规划系统总体架构图和核心业务流程图，并关联到对应章节。
- 新增 SQLite v7 migration 和 `manual_plan_runs`，保存规则版本、摘要、产物路径和内容指纹。
- 新增 `manual-plan TASK_ID --json` CLI，计划原子写入 `intermediate/manual-planning/plan.vN.json`。
- 任务阶段新增 `07_plan_manual`，执行、失败、重试和事件均进入现有状态机留痕。

验证：

- 45 项完整测试全部通过，覆盖章节结构、模块动态子章节、事实和证据引用、缺失信息、状态自动收敛、图表需求、SQLite migration、CLI 与版本化持久化。
- 使用真实仓库任务生成最新 plan v3：9 个章节、2 个图表需求、27 项去重后的稳定事实缺口。
- 当前 9 个章节均为 `needs_evidence`；这是现有扫描只提取名称、版本、语言和模块目录所导致的真实结果，没有将章节骨架误报为可写正文。

已知限制：

- 当前确定性事实提取尚未覆盖数据实体、接口、状态转换、安全机制、部署方式和测试策略，因此不能开始可信的说明书正文生成。
- `project.modules` 目前来自目录候选，只能用于章节导航，不能直接证明模块业务职责。
- 图表阶段当前仅输出需求契约，还没有节点、边和证据绑定结构。

下一步：

1. 扩展项目理解，确定性提取数据存储、接口入口、任务/状态模型、配置和部署证据。
2. 为架构图和核心流程图定义节点、边、容器、证据引用及完整性检查契约。
3. 证据覆盖达到门槛后，再接入模型协议生成受 Schema 约束的章节候选内容。

### 2026-08-10：结构事实提取第一阶段

本轮目标：

- 从仓库中确定性提取数据存储、SQL 实体、HTTP 接口、状态模型和部署方式。
- 直接补充说明书章节规划所需 Fact key，不依赖模型推断业务语义。

完成内容：

- 新增结构分析预算：最多 2,000 个文件、16MB 总文本、单文件 512KB，只读取扫描清单中的非二进制源码和配置。
- 识别配置依赖声明及源码 import/调用模式中的 SQLite、PostgreSQL、MySQL、Redis、MongoDB 和常见 ORM。
- 从 `CREATE TABLE` 提取表名及来源文件，生成 `data.entities`。
- 识别 FastAPI/Flask 风格装饰器、Express 路由和 Spring Mapping，生成带方法、路径、文件和行号的 `interfaces.catalog`。
- 识别 Python `*Status` / `*State` Enum 及其显式字符串状态，生成 `data.lifecycle`。
- 识别 Dockerfile、Docker Compose、Tauri、Vercel、Netlify、Fly.io 描述文件，生成 `deployment.method`。
- 数据库、接口、状态和部署证据只保存命中摘要、定位信息与原文件 SHA-256，不复制整份配置。
- 排除 test、fixture、mock、demo、example 和 sample 路径，避免把测试夹具当成产品能力。

真实验证与纠偏：

- 初版全文关键词扫描在真实仓库中误报 MongoDB、MySQL 等，并把测试路由 `/orders` 当成真实接口。
- 将规则收紧为“配置依赖声明＋源码 import/调用模式＋测试路径排除”，重新扫描后只识别真实 SQLite、15 个 SQLite 表、TaskStatus/StageStatus，无虚假 HTTP 接口和外部数据库。
- 真实说明书 plan v1 的缺失信息由 27 项降至 24 项，数据设计章节首次从 `needs_evidence` 自动变为 `ready`；其余 8 章仍保持待证据状态。

验证：

- 47 项完整测试全部通过。
- 新增正向结构提取测试和关键词/测试夹具误报回归测试。
- 正向样例覆盖 SQLite、Redis、SQL 表、POST 路由、状态枚举和 Dockerfile，全部事实均关联证据引用及文件哈希。

已知限制：

- 当前接口识别不覆盖动态路径、集中式路由表、RPC、GraphQL Schema 和自定义框架。
- SQL 实体只提取显式 `CREATE TABLE`，尚未解析 ORM 模型字段和表关系。
- 状态事实能证明枚举集合，不能仅凭枚举证明允许的状态转换边。
- Dockerfile 等文件只能证明存在部署描述，不能证明当前环境已成功部署。

下一步：

1. 提取模块职责候选、接口请求响应 Schema 和显式错误处理证据。
2. 提取配置项、启动入口、安装升级与测试策略，继续降低说明书缺口。
3. 定义图表节点、边、容器和 Evidence ID 的确定性契约，状态转换边必须来自代码证据。

### 2026-08-10：接口契约、入口、配置与测试证据

本轮目标：

- 在不读取配置值的前提下补充运行入口和配置项证据。
- 提取显式接口请求响应模型、HTTP 错误状态和测试策略。

完成内容：

- 对 FastAPI/Flask 风格路由提取处理函数、参数类型、返回注解和 `response_model`，生成 `interfaces.contracts`。
- 识别显式 `HTTPException`、`abort` 和响应状态调用，生成只含状态码、文件和行号的 `interfaces.errors`。
- 识别 Python、Node.js 和 Java 的环境变量读取；`configuration.items` 只保存变量名和来源文件，不保存默认值、实际值或完整表达式。
- 从 `pyproject.toml` 的 `[project.scripts]`、`package.json` 的脚本名和 Dockerfile 的 CMD/ENTRYPOINT 提取 `runtime.entrypoints`；Node/Docker 脚本不保存命令正文。
- 从测试路径、文件名和显式框架导入生成 `testing.strategy`，记录框架列表和测试文件数量。
- 运行配置和测试内容分析沿用 2,000 文件、16MB 总量、单文件 512KB 的读取预算。
- 说明书运行环境章节新增“启动入口”和“运行配置项”两个稳定事实契约。

验证：

- 47 项完整测试全部通过。
- 正向样例验证 `OrderInput`、`OrderOutput`、HTTP 409、`SERVICE_TOKEN` 键名、npm `start` 入口和 unittest 策略均能回链证据。
- 真实仓库只识别 `COPYRIGHT_AGENT_SOFFICE` 配置键、`copyright-agent` 入口和 16 个 unittest 测试文件；没有因提取器规则文本虚报接口或错误状态。
- 最新真实说明书 plan v1 保持数据设计章节 `ready`，总缺口从上一轮 24 项降至 23 项；其余章节未因弱证据被错误放行。

已知限制：

- 请求响应模型只覆盖显式静态注解，不解析运行时依赖注入、动态 Schema 或 GraphQL 类型系统。
- HTTP 状态码能证明存在错误分支，但不能单独证明触发条件和恢复流程。
- 测试文件数量和框架只能证明测试资产存在，不等同于测试已执行或验收通过。
- 环境变量名可能暗示配置用途，但正文仍不得虚构其默认值、是否必填或安全存储方式。

下一步：

1. 提取显式状态转换调用、异常恢复和事务边界，为核心流程图提供有方向的边证据。
2. 定义架构图和流程图的节点、边、容器、Evidence ID 与完整性检查 Schema。
3. 再扩展 README/manifest 中的软件用途、运行环境和安装命令候选，并保留用户确认门槛。

### 2026-08-10：流程边证据与图表语义计划

本轮目标：

- 为流程图提取有方向、可定位的状态转换边。
- 在生成 Draw.io 前建立节点、边和证据引用的持久化语义契约。

完成内容：

- 使用 Python AST 解析 `ALLOWED_TRANSITIONS`，只提取显式源状态、目标状态、文件和行号，生成 `workflow.transitions`。
- 识别 `BEGIN IMMEDIATE`、commit 和 rollback，生成 `data.transactions`。
- 识别原子文件替换和失败临时文件清理，生成 `reliability.recovery`；不把普通异常捕获自动解释为恢复能力。
- 新增 `diagram-plan-v1`：系统架构图节点来自入口、模块和存储 Fact，核心流程图节点及边来自状态转换 Fact。
- 每个节点和边保存 Fact ID、Evidence ID；每条流程边额外保存源文件和行号定位。
- 图表计划校验重复节点键、重复边键、悬空边和无证据边。
- 系统架构图在缺少模块依赖边时保持 `needs_evidence`，不会凭节点顺序自动连线。
- 新增 SQLite v8 migration 和 `diagram_plan_runs`，关联最新说明书计划并保存版本、规则、摘要、路径和指纹。
- 新增 `diagram-plan TASK_ID --json` CLI 和任务阶段 `08_plan_diagrams`。

真实验证：

- 从 `state_machine.py` 提取 8 个任务状态和 13 条允许转换。
- 从 SQLite UnitOfWork 提取即时事务、commit 和 rollback；从各原子写入路径提取替换与失败清理机制。
- 最新真实 diagram plan v2 共 2 张图、11 个节点、13 条边，结构校验通过，13 条边均包含源文件和行号。
- 核心业务流程图状态为 `ready`；系统架构图包含 CLI、核心模块和 SQLite 三个节点，但因缺少模块依赖边保持 `needs_evidence`。
- 说明书缺口从 23 项降至 22 项；安全可靠性章节仍缺安全控制与审计备份，未被错误放行。

验证：

- 49 项完整测试全部通过。
- 定向测试覆盖 AST 状态边、事务/恢复机制、边证据、悬空引用校验、SQLite v8 migration、CLI 与版本化持久化。

已知限制：

- 当前只解析 Python 常量形式的状态转换表，尚未覆盖 TypeScript、Java 和数据库驱动的动态状态机。
- 允许转换表能证明合法状态边，但不说明每条边由哪个用户操作或业务条件触发。
- 架构图尚无 import/依赖关系证据，因此本轮不生成 Draw.io 文件。

下一步：

1. 提取 Python import 图及模块归属，形成有证据的架构依赖边。
2. 为状态转换边关联调用位置、阶段名称和触发条件候选。
3. 两张语义图达到门槛后，再使用 Draw.io 生成器输出可编辑 XML、SVG 并执行路由与视觉检查。

### 2026-08-10：Python 内部依赖图与架构图门槛

本轮目标：

- 以 AST 提取 Python 项目内部模块依赖，补齐系统架构图的有向边证据。
- 控制真实项目图规模，使语义计划可进入后续 Draw.io 布局阶段。

完成内容：

- 将 `src`、`app`、`lib` 布局统一转换为点分模块标识，正确处理普通模块和 `__init__.py` 包。
- 使用 Python AST 解析绝对导入和相对导入，并将目标限制在同一项目的已扫描 Python 模块集合内。
- 排除 test、fixture、mock、demo、example 和 sample 路径，标准库与第三方包不会形成内部依赖边。
- 新增 `architecture.modules` 和 `architecture.dependencies`；每条依赖包含源模块、目标模块、源文件、行号及文件哈希 Evidence。
- 系统架构语义计划改为使用内部模块图；存在有效依赖边后才从 `needs_evidence` 转为 `ready`。
- 针对大图建立确定性核心视图：按内部度数选择最多 12 个模块，再用高权重最大生成森林保留覆盖核心模块的依赖骨架。
- metadata 同时记录完整模块/边数量、入选前边数、最终节点/边数和选择规则，图视图裁剪不会修改完整事实。
- DiagramPlanService 根据边的源文件和 locator 类型匹配精确 Evidence ID；每条架构边和流程边只携带对应文件证据，不再附带整个 Fact 的证据集合。

真实验证与迭代：

- 完整 import 事实包含 29 个内部模块和 83 条内部依赖，未发现第三方模块误边。
- 首版选取 12 个模块后仍有 45 条架构边，视觉风险过高；最终改为生成森林，避免仅截断边导致局部过密。
- 最新 diagram plan v4：系统架构图 14 个节点、11 条边；核心流程图 8 个节点、13 条边；两张图均为 `ready`。
- 每条边只有 1 个精确 Evidence ID，并保留源文件和行号；整体结构校验通过。

验证：

- 49 项完整测试全部通过。
- 相对导入黄金样例验证 `orders.service -> orders.repository` 的模块解析、文件定位和证据引用。
- 图表测试验证架构依赖边、精确 Evidence 映射和流程边继续满足完整性门禁。

已知限制：

- 当前 import 图只覆盖 Python；JavaScript/TypeScript、Java、Go 和 Rust 需要各自的语法解析器。
- import 关系证明代码依赖，不等同于运行时调用频率或业务重要性；度数筛选只是确定性核心视图，不替代语义说明。
- CLI 和 SQLite 作为上下文节点尚未与模块边连接，因为当前没有跨 Fact 的直接边证据。

下一步：

1. 将两张 `ready` 语义图转换为未压缩、可编辑的 Draw.io XML。
2. 输出 SVG 预览并执行节点重叠、连线穿越、交叉和标签背景检查。
3. 通过自动检查后进行视觉复核，再决定是否插入说明书 DOCX。

### 2026-08-10：可编辑 Draw.io 图件生成与持久化

本轮目标：

- 将证据化语义计划转换为可编辑图件，而不是只停留在 JSON 规划。
- 为生成、导出和质量门禁建立本地持久化留痕。

完成内容：

- 新增确定性 Draw.io XML 生成器，输出未压缩 XML、稳定节点 ID、显式正交路径点和 SVG 预览。
- 系统架构图采用依赖森林树形布局；核心流程图采用状态分区布局与外围回退通道。
- 新增产品内置质量检查：验证 XML 可解析、ID 唯一、节点不重叠、边端点存在、每条边包含显式路径点。
- 新增 SQLite v9 migration 和 `diagram_artifact_runs`，保存计划关联、版本、生成器版本、四个产物路径、摘要与组合指纹。
- 新增任务阶段 `09_generate_diagrams` 和 `drawio TASK_ID --json` CLI；失败会记录安全错误并进入失败状态。
- SVG 导出通过本机 Draw.io Desktop 完成，可由 `COPYRIGHT_AGENT_DRAWIO` 配置，不依赖 Codex 或在线服务。

真实验证：

- 对本仓库真实任务生成架构图 14 节点/11 边、流程图 8 节点/13 边，并成功导出两份 SVG。
- 两份 `.drawio` 均通过专业制图 XML 校验；严格布局分析分别为 `vertices=15 edges=11 warnings=0` 与 `vertices=9 edges=13 warnings=0`，计数包含标题节点。
- 完成 PNG 视觉抽检；架构主体清晰，流程回退路径使用外围通道，不遮挡状态节点。

验证：

- 新增生成器结构测试、质量门禁测试和带假渲染器的服务持久化集成测试。
- 完整回归测试共 51 项，最终结果见本轮提交前验证记录。

已知限制：

- SVG 导出目前需要安装 Draw.io Desktop；未来桌面应用需把兼容导出器作为可检测的本地依赖打包或引导安装。
- 架构上下文节点没有无证据连线；它们只表达已知入口与存储，不虚构调用关系。
- 状态图展示允许转换，不代表每条转换的用户动作或业务条件，后续仍需触发证据候选。

下一步：

1. 将已验收 SVG 按章节插入说明书 DOCX，并验证图片清晰度、标题、分页和交叉引用。
2. 基于章节计划生成第一版证据约束正文，缺失字段继续保留确认门槛。
3. 为多模型协议定义统一请求、流式响应、能力探测和本地密钥引用接口。

### 2026-08-10：运行依赖边界与图表视觉模板 v2

- 接受 ADR-011：核心 DOCX 出件、图表预览、资产微调和导出不得要求用户安装 LibreOffice 或 Draw.io；两者降级为开发验证或可选兼容工具。
- 确定图表资产使用“证据语义、自动布局、人工/AI 覆盖、导出版本”四层模型，后续重新生成不得静默覆盖人工调整。
- 图表模板加入中文职责主标签、英文技术副标签、领域配色以及成功、警告、失败和回退路径的语义样式。
- 视觉复核后移除架构画布中没有关系证据的孤立入口/存储节点；事实保留在 metadata，不通过虚构边强行连接。
- 压缩依赖树的节点宽度和叶节点间距，以改善后续插入纵向说明书时的可读性。

下一步：

1. 为资产工作台定义 SQLite 覆盖层 Schema、冲突检测与版本合并规则。
2. 建立不依赖 Draw.io Desktop 的内部 SVG 导出器，保留 `.drawio` 作为兼容格式。
3. 完成桌面纵切后，在内置画布实现拖拽、属性修改、撤销与 AI 修改操作。

### 2026-08-10：图表资产非破坏覆盖层

- 新增 SQLite v10 migration 和 `diagram_asset_revisions`，按任务、图表和版本保存基础图件、父修订、编辑来源、操作、冲突、状态及审计文件。
- 定义首版覆盖操作：节点移动、缩放、样式、显示名称、隐藏，以及边路由、样式和显示名称；未知动作直接拒绝。
- 原始语义 `label`、节点/边关系、Fact ID 与 Evidence 不被修改，显示调整写入 `visual_override` 或 `display_label`。
- 每个操作绑定目标语义指纹；新基础图中目标缺失时报告 `target_missing`，目标语义改变时报告 `target_changed`，冲突操作不会自动应用。
- 人工编辑与 AI 编辑共用同一安全模型，以 `edit_source=manual|ai` 区分来源；每次保存创建不可覆盖的新版本并记录任务事件。
- 真实任务成功保存 `system_architecture` revision v1，包含节点移动和显示名称覆盖，状态为 `clean`、冲突数为 0。

下一步：

1. 让 Draw.io 与内部 SVG 生成器消费 `visual_override`，使保存的覆盖操作真正反映在预览与导出中。
2. 增加 revision rebase 服务和冲突解决操作，支持接受新语义、放弃修改或重新指定目标。
3. 再建立桌面资产工作台 API，避免 UI 直接操作 SQLite 或任务文件。

### 2026-08-10：覆盖层出图与内置 SVG 导出

- Draw.io 生成器开始消费最新资产 revision：移动、缩放、显示名称、隐藏、样式和边路由会反映到兼容 XML。
- 隐藏节点时同步隐藏关联边；样式覆盖使用白名单，只允许颜色、线宽和虚线参数，不能注入任意 XML/CSS。
- 生成阶段自动安全重放最新人工/AI 覆盖，摘要记录 revision 版本、操作数、冲突数和冲突明细。
- 新增 `InternalSvgRenderer`，只使用 Python 标准库解析已校验的 Draw.io XML，输出节点、中文双层标签、语义配色、正交连线和同色箭头。
- 默认生成链路已切换到 `internal-v1`，真实任务在未启动 Draw.io Desktop 的情况下成功生成 `.drawio` 与 `.svg` v5。
- 首轮视觉复核发现内部端点存在短斜线，修正为轴向边界和显式折点；第二轮复核确认主路径与回退路径保持正交。
- 真实架构覆盖 revision v1 的 2 项操作成功重放，冲突数为 0；两张 Draw.io 源继续通过严格布局零告警检查。

下一步：

1. 实现 revision rebase 与冲突解决服务，提供接受新语义、放弃操作和重新指定目标三种动作。
2. 定义资产工作台本地 API：读取图、保存覆盖、列出版本、预览 SVG 和回滚版本。
3. 在桌面纵切中接入可拖拽画布，再将稳定 SVG 插入说明书 DOCX。

### 2026-08-10：资产工作台版本与冲突 API

- `DiagramAssetService` 新增 revision 列表、单版本读取、最新版本 rebase、指定版本回滚和冲突解决接口。
- 冲突必须逐项处理，支持 `drop`、`accept_current` 和 `retarget`；缺少处理项、重复之外的索引或无效目标均拒绝。
- 回滚采用“复制历史操作并创建新版本”，不删除、不覆盖旧 revision，继续保持完整审计链。
- 每个 revision 保存时同步生成独立 `.drawio` 和内置 SVG 预览，并执行产品内结构门禁。
- 新增统一 `workspace_snapshot`，返回两类图表资产、最新 revision、版本数、状态及允许操作，桌面 UI 不需要直接访问 SQLite 或任务目录内部结构。
- 集成测试覆盖 clean revision、版本列表、读取、回滚、语义变化 rebase、`target_changed` 冲突以及接受当前语义后的 clean 新版本。
- 真实架构资产从 revision v1 rebase 到 v2，2 项人工操作均安全保留，冲突数 0，并成功生成独立预览。

下一步：

1. 为资产工作台增加本地 HTTP Schema 与路由，暴露快照、保存、预览、回滚和冲突解决端点。
2. 建立 Tauri 桌面纵切与可拖拽图形画布，先支持位置、尺寸、标签和撤销/重做。
3. 设计 AI 编辑指令到白名单 overlay 操作的结构化输出与审批预览。

### 2026-08-10：本地资产 API v1 契约

- 新增与 Web 框架解耦的 `DiagramAssetApi`，先稳定桌面 UI 所需的请求、响应、错误码和路由；未来 FastAPI 只作为薄传输适配器。
- 路由覆盖工作台快照、保存 revision、版本列表、单版本读取、SVG 预览、rebase、回滚和冲突解决。
- 所有请求在路由分发前验证至少 32 字符的启动会话令牌，并使用常量时间比较；未认证请求返回 401。
- 严格限制两类 diagram key、`manual|ai` 编辑来源、overlay 白名单和单次最多 500 项操作。
- SVG 路径只能来自持久化 revision，解析后必须仍位于对应任务目录内，客户端不能传入任意文件路径。
- 新增 `LOCAL_API.md`，记录安全边界、全部路由、保存/冲突 Schema 与稳定错误结构。
- 定向测试覆盖认证、保存、列表、预览、非法动作、错误方法和未知图表类型。

下一步：

1. 实现只监听 `127.0.0.1` 随机端口的 sidecar 传输适配器、请求体上限和启动握手。
2. 建立 Tauri 桌面壳并消费工作台快照与 SVG 预览端点。
3. 实现首个拖拽保存闭环，再接入撤销/重做与 AI 操作审批。

### 2026-08-10：FastAPI sidecar 真实纵切

- 根据产品依赖边界修正实现策略：FastAPI、Pydantic、Uvicorn 属于应用所需运行依赖，正式加入 `pyproject.toml`；避免捆绑的是 LibreOffice、Draw.io 等完整独立应用，而不是正常代码库。
- 新增真实 FastAPI sidecar 和 `copyright-agent-sidecar` 入口，领域路由继续由 `DiagramAssetApi` 承担，FastAPI 只负责传输与 Schema。
- Pydantic v2 Schema 严格拒绝额外字段，约束编辑来源、操作枚举、目标长度、版本范围、冲突动作和最多 500 项列表。
- sidecar 仅绑定 `127.0.0.1:0`，启动后输出单行 JSON 握手，包含 host、随机端口、PID、协议版本和程序版本。
- 所有业务端点及健康检查都验证 `X-Session-Token`；禁用 Swagger/ReDoc，不添加通配 CORS，并在路由前执行 1 MiB 请求体门禁。
- FastAPI 验证错误统一转换为既有 400 错误契约；SVG 使用正确媒体类型返回。
- 真实进程验证获得随机本地端口 52246，握手字段正确，带令牌 `/api/v1/health` 返回协议版本 1；随后正常终止进程。
- 新增 FastAPI TestClient 测试，覆盖认证健康检查、工作台路由、无 CORS、Pydantic 错误和 413 请求体限制。

下一步：

1. 建立 Tauri v2 桌面项目骨架、sidecar 启动管理和握手校验。
2. 完成资产工作台首屏：资产列表、SVG 预览、revision 历史与状态。
3. 接入拖拽移动并保存 `node.move` revision，形成第一个桌面编辑闭环。

### 2026-08-10：Tauri 桌面壳与资产工作台首屏

- 新增 React 19、TypeScript 与 Vite 前端，形成项目侧栏、图表资产列表、SVG 主预览、revision 状态和 AI/属性检查器四区工作台。
- 新增 Tauri v2 Rust 桌面壳，通过 `invoke("start_sidecar")` 启动和复用 Python sidecar；应用退出时回收子进程。
- 每次启动生成 256 位随机会话令牌，校验 sidecar 的 host、端口、协议版本、程序版本和 PID 握手，再执行带令牌健康检查。
- 前端只从 Rust 获取本次连接信息；工作台快照和 SVG 均通过认证 HTTP 请求读取，SVG 先转为 Blob URL，不把会话令牌放入 URL。
- sidecar CORS 收紧为 Tauri 桌面 WebView 的两个明确 origin，不允许任意网页调用；新增允许与恶意 origin 回归测试。
- 前端生产构建通过，产物主 JS gzip 约 63 KiB；Python 全量 61 项测试通过。
- 当前开发机未安装 Rust，因此本轮尚未执行 `cargo check`；external binary 的跨平台构建与真实桌面进程验证列入下一轮。

下一步：

1. 增加 PyInstaller sidecar 构建脚本，按 macOS、Windows 和 Linux target triple 输出 Tauri external binary。
2. 安装或在 CI 配置 Rust 工具链，执行 `cargo check` 与 Tauri 桌面启动验收。
3. 接入画布拖拽与 `node.move` 保存，增加撤销、重做和 revision 历史切换。

### 2026-08-10：sidecar 跨平台构建与冻结产物验收

- 新增 `packaging` 可选依赖组和 PyInstaller one-file 构建脚本，不把打包工具混入最终运行依赖。
- 构建脚本优先读取 Rust host target triple；无 Rust 时仅对明确支持的 CPU/操作系统组合进行安全推断，未知架构直接拒绝。
- 产物严格采用 Tauri external binary 约定：`copyright-agent-sidecar-<target-triple>[.exe]`，同时生成包含 target、大小和 SHA-256 的 manifest。
- PyInstaller 缓存、work、spec 和 dist 全部使用临时目录，显式注入项目 `src`，不依赖 editable install 或用户目录写权限。
- 新增 macOS、Windows 文件名以及 Apple Silicon 自动探测测试；全量测试从 61 项增加到 64 项并全部通过。
- Apple Silicon 实际生成 `copyright-agent-sidecar-aarch64-apple-darwin`，大小 21,552,720 字节，SHA-256 为 `6981a7f164adc5cccff584a5e8d5fa7b86a8a27181f8b8540872ae362c90d670`。
- 冻结可执行文件真实启动成功，握手端口为随机本地端口，认证健康检查返回版本 `0.1.0`、协议版本 1；进程可被正常终止。
- 前端生产构建继续通过，主 JS gzip 约 63 KiB。生成的二进制与 manifest 默认忽略，不把单平台构建产物提交到源码仓库。

下一步：

1. 配置 Rust/Tauri 编译环境并执行 `cargo check`，修复可能的 Rust API 或配置问题。
2. 使用本机 external binary 完成 Tauri 应用真实启动、sidecar 生命周期和首屏连接验收。
3. 接入画布拖拽与 `node.move` 保存，增加撤销、重做和 revision 历史切换。

### 2026-08-10：Rust 编译与 Tauri 桌面启动验收

- 从 Rust 官方服务器下载并校验 Apple Silicon rustup 安装器，使用 minimal profile 安装 stable `rustc 1.97.1`；Rust 仅为开发构建依赖，不转嫁给最终用户。
- 首次 `cargo check` 发现 Tauri 编译期要求 `src-tauri/icons/icon.png`，补齐可复现的 SVG 图标源以及桌面 PNG、ICNS、ICO 产物。
- 图标采用确定性 code-native SVG，不调用生成模型；只保留 macOS、Windows 和通用桌面所需资产，移除本阶段不使用的移动端/Store 冗余图标。
- 生成并提交 `Cargo.lock`，锁定 481 个 Rust 包；Tauri 桌面 Rust 代码已通过 `cargo fmt --check` 和 `cargo check`。
- 新增 Rust 安全回归测试，覆盖 256 位小写十六进制会话令牌、合法 loopback 握手，以及非 loopback/错误协议拒绝；3 项全部通过。
- Tauri 开发应用成功完成 406 个编译单元并启动桌面主程序；此前独立冻结 sidecar 的握手与健康检查已通过。
- 为桌面运行增加不含会话令牌的 sidecar ready/stop 诊断日志。系统拒绝了第二次 GUI 启动审批，因此本轮没有虚报完整 UI→sidecar 日志验收。
- Python 64 项测试与前端生产构建继续全部通过。

下一步：

1. 实现 SVG 画布节点命中、拖拽预览和坐标换算。
2. 将拖拽结果保存为 `node.move` revision，并在失败时恢复本地预览。
3. 增加撤销、重做和 revision 历史切换，完成首个桌面编辑闭环。

### 2026-08-10：桌面节点拖拽与 revision 保存闭环

- 明确当前桌面端只有“图表资产”纵向切片可用；项目概览、源码材料、说明书和质量检查改为带“待开发”标识的禁用入口，不再提供无响应的伪交互。
- 内置 SVG 导出器为每个可编辑节点增加 `data-node-key`、原始坐标、尺寸和无障碍属性；标题和连线不进入拖拽命中范围。
- 新增前端 `InteractiveDiagram`：把屏幕坐标通过 SVG CTM 换算为画布坐标，支持节点命中、选中高亮、pointer capture 和拖拽预览。
- SVG 在内联前解析并移除 `script`、`foreignObject`、事件属性和外部链接，避免为了交互直接信任任意 SVG 内容。
- 拖动结束后读取当前 revision 的完整 operations，替换同节点已有 `node.move` 并提交全量白名单操作，避免后续移动丢失此前人工调整。
- 保存成功后重新读取 workspace 和新版 SVG；保存失败则撤销本地 transform 并恢复原位置，同时显示后端稳定错误信息。
- 属性栏显示当前选中节点；工具栏明确区分“拖拽编辑已开启”和“正在保存”，保存期间禁止重复编辑。
- SVG 可编辑元数据加入 Python 回归断言；Python 64 项测试和前端 TypeScript/Vite 生产构建全部通过，主 JS gzip 约 64.5 KiB。

下一步：

1. 增加 revision 历史抽屉、版本预览和“复制历史版本为最新”的回滚操作。
2. 在前端加入操作栈，提供保存前撤销/重做和键盘微调。
3. 开始“项目概览”真实页面：选择目录/ZIP、扫描进度、事实摘要和待确认项。

### 2026-08-10：revision 历史预览与审计式恢复

- “历史版本”按钮接入本地 API，按版本倒序显示来源、操作数和冲突状态，当前查看项与当前最新项均有明确标识。
- 支持点击任意 revision 加载其独立 SVG；历史预览期间锁定节点拖拽，避免把旧坐标误保存到当前版本。
- 版本抽屉明确说明恢复语义：调用既有 rollback 服务复制历史 operations 并创建新的最新 revision，不删除、不覆盖旧版本。
- 恢复成功后并行刷新 workspace 和 revision 列表，自动返回最新画布并显示“从 vN 创建 vM”的结果反馈。
- 当前版本的恢复按钮禁用；请求期间锁定重复操作，失败时保留原预览并显示后端错误。
- 前端 TypeScript 与 Vite 生产构建通过，主 JS gzip 约 65 KiB。

下一步：

1. 为拖拽加入保存前撤销/重做、方向键微调和画布边界约束。
2. 增加节点标签、尺寸和白名单颜色的属性编辑表单。
3. 开始“项目概览”真实页面：本地目录/ZIP 选择、扫描任务创建、事实摘要和待确认项。

### 2026-08-10：画布边界、键盘微调与审计式撤销

- 节点落点按 SVG viewBox、节点宽高和标题安全区约束，禁止保存到画布外或覆盖标题区域；拖拽释放时预览也同步吸附到有效位置。
- 节点选中后自动获得焦点，方向键按 1 个画布单位微调，`Shift + 方向键` 按 10 个单位移动，继续复用 `node.move` revision 保存链路。
- 工具栏新增一层撤销/重做：编辑前版本作为撤销目标，撤销前版本作为重做目标；新编辑会清空过期重做目标。
- 撤销和重做都调用审计式 rollback，生成新的最新 revision，不原地改写或删除历史数据。
- 历史预览和保存期间禁用撤销/重做；操作完成后刷新 workspace、历史列表和画布，并显示来源版本与新版本号。
- 前端 TypeScript 与 Vite 生产构建通过，主 JS gzip 约 65.4 KiB。

下一步：

1. 建立“项目概览”真实页面及桌面路由状态，替换第一个禁用导航入口。
2. 增加本地目录/ZIP 选择与扫描任务创建 API，不允许网页任意传入未经桌面授权的路径。
3. 展示扫描进度、项目事实摘要、风险提示和待确认项，形成项目导入纵向闭环。

### 2026-08-10：项目概览与本地扫描纵向闭环

- “项目概览”从禁用占位升级为第二个真实页面，桌面端默认进入项目导入页，并可与图表资产页双向切换。
- 引入 Tauri 官方 dialog 插件，仅授予 `dialog:allow-open`；目录和 ZIP 都必须经系统文件选择器授权，页面不提供任意路径文本输入。
- 新增认证 `POST /api/v1/projects/scan`，复用既有 `ScanProjectService` 完成目录原地扫描或 ZIP 隔离解压、SQLite 任务持久化和确定性事实提取。
- 新增认证 `GET /api/v1/tasks/{task_id}/inspection`，返回任务状态、Fact、Evidence 和待确认项；不存在任务使用稳定 404 错误。
- 项目概览展示有效/过滤文件数、读取体积、敏感特征计数、语言标签、前 10 项事实和全部待确认项；敏感特征只显示数量，不显示命中值。
- 扫描得到的 task ID 自动传给图表资产页的任务输入框，后续材料阶段继续复用同一任务上下文。
- 新增真实 FastAPI 扫描测试，覆盖项目落库、TypeScript 识别、事实返回和 inspection 二次读取；Python 全量测试从 64 增至 65 项并全部通过。
- Tauri `cargo check`、TypeScript 和 Vite 生产构建全部通过；前端主 JS gzip 约 67 KiB。
- 重新冻结 Apple Silicon sidecar 后真实扫描项目内 `tests` 目录：读取 22 个 Python 文件、89,816 字节、0 个敏感值命中，返回 3 项事实和 2 项待确认，证明新接口已进入桌面实际运行产物。

下一步：

1. 在项目概览直接回答待确认项，并刷新任务状态和 confirmed Fact。
2. 增加已持久化任务列表与最近项目恢复，避免每次启动依赖手工记忆 task ID。
3. 启用“源码材料”页面，展示 A/B/C 选择计划、代码页数预算和 DOCX 生成状态。

### 2026-08-10：待确认提交与最近任务恢复

- 新增 `ProjectCatalogService`，按更新时间列出最近 20 个任务；只返回显示名称、目录/ZIP 类型、状态、扫描摘要和 task ID，不返回原始项目绝对路径。
- 新增认证 `GET /api/v1/tasks?limit=N`，限制范围为 1–100；应用启动连接 sidecar 后自动读取最近任务。
- 项目概览可打开最近任务并通过 inspection 恢复事实、待确认项、扫描摘要及后续共用 task ID，不重新扫描项目。
- 新增认证确认端点，复用 `ConfirmationService` 创建用户确认 Evidence、confirmed Fact，并 supersede 旧候选；最后一个必填项提交后任务进入 completed。
- 待确认卡支持候选值快捷填入和手动输入；空值、重复提交及非法任务状态由既有领域规则拒绝，提交成功后刷新事实、任务状态和最近任务列表。
- FastAPI 集成测试扩展到最近任务排序、确认提交、remaining_required、completed 状态和 confirmed Fact；Python 全量 65 项测试全部通过。
- TypeScript/Vite 生产构建通过，主 JS gzip 约 67.6 KiB；Apple Silicon 冻结 sidecar 已更新，大小约 21.56 MB。

下一步：

1. 启用“源码材料”页面并接入 source-plan、code-preview 和 source-docx 状态。
2. 增加任务阶段时间线和错误/警告详情，明确每一步为何可继续或被阻断。
3. 为确认后的项目提供“一键生成下一阶段”入口，但继续保持人工触发、不自动提交材料。

### 2026-08-10：源码材料页与三阶段生成闭环

- “源码材料”从禁用占位升级为第三个可用页面，自动继承项目概览中当前选中的持久化任务。
- 新增聚合服务与 4 个认证 API，统一返回最新 source-plan、code-preview、source-docx、可执行操作和明确阻塞原因。
- 源码筛选、分页预检和 DOCX 生成始终由用户逐步触发；GET 状态请求不会隐式生成任何材料。
- 页面展示入选文件、代码行数、正文页预算、A/B/C 级数量及前 30 个入选文件，方便在生成前审阅。
- 对小项目执行真实分页预检：代码不足时任务进入 `completed_with_warnings`，页面说明缺口并锁定 DOCX 生成，不填充虚假行数。
- FastAPI 集成回归覆盖“确认项目 → 筛选计划 → 分页不足 → DOCX 被拒绝”；Python 65 项测试全部通过，TypeScript 检查和 Vite 生产构建通过，主 JS gzip 约 69 KiB。

下一步：

1. 增加源码分页的页面级预览，在生成 DOCX 前可检查首页、中间页和末页内容。
2. 为已生成 DOCX 增加“在文件管理器中显示”和应用内预览，不暴露可任意读取的本地路径 API。
3. 建立“说明书”真实页面，接入既有手册计划、图表资产和文档生成状态。

### 2026-08-10：源码分页内容预览

- 分页预检完成后可在“源码材料”页直接查看真实代码页，不再只看页数统计。
- 最新分页产物自动抽取首页、中间页和末页；页数较少时自动去重，最多返回 3 页。
- 纸张式预览显示源文件标题、源码行号、视觉折行标记、当前页码和可视行数，便于在 DOCX 生成前发现选材问题。
- 新 API 不接受任意路径或页码，只根据 task ID 从 SQLite 定位最新产物，并对解析后路径执行任务目录边界校验。
- FastAPI 集成测试验证分页中确实返回被扫描的 TypeScript 源码；Python 65 项回归、TypeScript 检查和 Vite 生产构建全部通过，主 JS gzip 约 69.5 KiB。

下一步：

1. 为已生成 DOCX 提供安全的“在文件管理器中显示”操作，并补充产物完整性校验。
2. 建立“说明书”真实页面，接入手册计划、图表资产和文档生成状态。
3. 增加任务阶段时间线和错误详情，把可恢复警告与不可继续错误分开呈现。

### 2026-08-10：说明书 AI 草稿纵向链路

- 移除说明书 AI 按钮的永久禁用占位，当前选中的可用模型可以直接生成说明书草稿。
- Sidecar 从 AES-256-GCM 本地凭据库按需解密 API Key，支持 `messages`、`chat_completions`、`responses` 和 `ollama_chat` 四种已协商接口，不向前端暴露明文凭据。
- 规则章节规划、项目事实和缺失项共同进入提示词；缺失项被降级为“待确认/禁止编造”约束，不再阻止 AI 生成。
- 新增 SQLite v14 `manual_draft_runs`，记录任务、模型配置、版本、摘要、耗时和产物路径；正文版本化保存为本地 Markdown。
- 说明书页面显示生成中状态、明确错误、模型与耗时，并可直接预览和重新生成草稿。
- Python 69 项回归与 TypeScript/Vite 生产构建通过。

下一步：

1. 将单次整篇生成升级为章节级生成与局部重试，避免长输出被模型截断。
2. 从草稿中的图表占位和项目证据生成结构化图表语义，再交给 Draw.io 渲染与人工微调。
3. 增加草稿确认、DOCX 排版、质量检查和自选路径导出。

补充修复：开发模式实际运行冻结的 PyInstaller Sidecar，Python 源码不会热更新。本轮已重新构建并重启 Sidecar，使 `manual_generate` 权限和 v14 迁移真正加载；同时移除“两张图必须全部 ready”的渲染前门禁，证据不足改为产物摘要中的警告，允许先生成可审阅 Draw.io 草稿。

交互收敛：说明书页面改为单一主操作“一键生成说明书与图表”，后台自动完成必要的章节规划、图表规划、AI 正文和 Draw.io 渲染；原三步规则按钮折叠到高级诊断区。下游生成失败的任务允许直接重试，修复 `failed` 状态反向锁死图表生成的问题。

### 2026-08-10：说明书管线纠偏阶段 1

- 以 `copyright-doc` 技能的正式交付契约重写路线，新增 `MANUAL_PIPELINE_PLAN.md`；明确 Markdown 仅为 legacy 中间材料，最终验收对象是 DOCX、Draw.io/SVG/PNG、截图和逐页质检结果。
- 新增 SQLite v15 正式说明书数据模型：版本化生成任务、六阶段步骤状态、结构化章节、图表、截图和最终文档产物。
- 新增 `ManualPipelineService`，可创建和读取版本化任务；研究、正文、图表、截图、DOCX 组装和渲染质检分别持久化，支持后续独立重试。
- 新增正式任务 API：创建任务、列出项目版本、读取任务详情；旧同步 Markdown 接口标记为 legacy。
- Python 回归增加到 70 项并全部通过。

下一步：实现 `research` 阶段的项目线索图谱与代表性源码选择，产出区分事实、推断和待确认项的结构化研究笔记。

### 2026-08-10：说明书章节与图表规划工作台

- “说明书”从禁用占位升级为第四个真实页面，复用项目概览中当前任务。
- 新增说明书聚合服务与 4 个认证 API，统一呈现章节计划、图表语义计划、Draw.io/SVG 产物及三个人工触发动作。
- 章节计划直接读取持久化中间产物，展示 9 个技术说明书章节的目的、小节、就绪状态和缺失信息。
- 页面汇总章节数、已就绪数、待补证据数与缺失信息数，并把待补项以可扫读标签展示。
- 图表语义计划只能在章节计划存在后触发；Draw.io/SVG 仅在架构图和流程图均为 ready 时开放，不会用无证据节点强行生成。
- FastAPI 回归扩展到“小项目 → 生成 9 章计划 → 显示证据缺口”；Python 65 项测试全部通过，TypeScript 检查与 Vite 构建通过，主 JS gzip 约 70.6 KiB。

下一步：

1. 为说明书待补信息建立本地补充表单，将用户回答持久化为 confirmed Fact 并重新计划章节。
2. 提供章节内容草稿预览，明确区分项目证据、模型生成文字和用户手工修改。
3. 接入说明书 DOCX 的显式生成与质量门禁，保持“先预览、再生成、最后导出”。

### 2026-08-10：扫描按钮连接恢复修复

- 根据真实桌面验收反馈，确认系统文件选择器已成功返回路径，但首次 sidecar 连接未就绪时扫描按钮被永久禁用。
- 扫描按钮现在只在未选路径或正在执行时禁用；sidecar 未连接时按钮改为“连接本地服务并扫描”。
- 点击后先调用统一 `ensureConnection`，连接成功后使用同一连接继续扫描和刷新最近任务，无需用户重新选择路径。
- 连接失败时在页面与侧边栏保留具体错误信息，不再以“浏览器预览模式”概括真实桌面错误。
- 内置 sidecar 独立启动与握手诊断通过；Python 65 项回归、TypeScript 检查与 Vite 构建通过。

下一步：

1. 由用户在当前热更新桌面窗口重试扫描，根据页面显示的具体错误继续收敛。
2. 扫描闭环验收后，继续实现说明书待补信息的本地填写与 confirmed Fact 持久化。

### 2026-08-10：桌面开发来源 CORS 修复

- 第二次真实验收已确认 sidecar 成功握手，扫描请求在 WebView 层返回 `Load failed`。
- 根因是 Tauri 开发窗口使用 `http://127.0.0.1:1420`，但 sidecar CORS 只精确放行打包后的 Tauri 来源。
- 白名单增加固定本机开发来源 `http://127.0.0.1:1420`，继续拒绝通配来源、外部域名和其他端口。
- 回归测试同时覆盖打包来源、开发来源和恶意外部来源；Python 65 项测试与前端构建通过。
- Apple Silicon sidecar 已重新冻结，桌面开发窗口已完整重启。

下一步：

1. 由用户在新窗口再次选择目录并执行真实扫描。
2. 根据扫描结果继续验收事实提取、待确认项、源码材料和说明书页。

### 2026-08-10：源码不足时的恢复路径

- 真实验收发现“可用代码不足 59 页”只有说明而没有下一步操作，导致流程断点。
- 警告卡新增“重新扫描当前项目”和“返回项目概览”两个入口；前者无需重新选路径，后者可更换目录或 ZIP。
- 新增认证 `POST /api/v1/tasks/{task_id}/rescan`，只从 SQLite 反查原来由系统选择器授权的项目来源，不接受前端新路径。
- 重新扫描生成新 task 与 snapshot，旧任务、筛选计划和分页产物保留，符合可追溯要求。
- 原任务已确认的软件版本等标量信息会匹配新任务待确认项并自动继承，避免重复输入。
- FastAPI 集成回归已验证“项目新增源码 → 重扫 → 新 task ID → 文件数更新 → 版本确认保留”；Python 65 项测试和前端构建通过。

下一步：

1. 在重启后的桌面窗口验收重新扫描和新任务切换。
2. 继续实现说明书待补信息的本地填写与 confirmed Fact 持久化。

### 2026-08-10：分级源码筛选与任务清理

- 将单一源码筛选策略升级为“标准、宽松、最大覆盖”三档，用户可在同一任务中降级策略并重新生成版本化筛选计划。
- 标准档保持 A/B 主体业务代码；宽松档纳入项目自身通用支持代码；最大覆盖档进一步纳入测试、Mock、Fixture、迁移和示例代码。
- 安全底线不随严格度降级：敏感命中、二进制、第三方 vendor、生成代码和压缩代码始终排除。
- 策略名保存在 source-plan summary 与 rules version 中，每次降级都可追溯；页面继续展示分级、文件和代码行变化。
- 最大覆盖档分页后仍不足 59 页时，页面给出“当前项目材料不足，暂不建议继续”的终止提示。
- 最近任务新增单项删除和“清理全部”；删除前明确确认，只清理 SQLite 关联记录与应用任务目录，不触碰原项目。
- 回归测试覆盖三档入选差异、具有多类子产物的任务删除、原项目保留和旧任务不可再读取。

下一步：

1. 在桌面窗口实测当前项目三档策略的页数变化，确认最合适的默认提示顺序。
2. 继续实现说明书待补信息的本地填写与 confirmed Fact 持久化。

### 2026-08-10：桌面版 DOCX 生成与失败重试修复

- 根据真实桌面验收复现“页数 59/59 但生成提示任务未完成”，确认分页本身成功，实际是 DOCX 生成失败后任务被置为 failed，下一次操作又被状态机拒绝。
- 根因是 PyInstaller 单文件包未完整释放 `python-docx` 的模板访问路径；构建脚本现同时收集 DOCX 模板和运行时 `docx/parts` 目录，不要求用户另装 Python 或文档工具。
- `source_document_error` 现在属于可恢复失败：原任务保留筛选和分页结果，修复后可直接重试“生成 DOCX”，无需重新扫描项目。
- 页面动作快照同步开放该重试入口；接口返回稳定中文提示，不再把底层异常或英文状态机信息直接暴露给用户。
- 使用用户截图中的失败任务 `d47a3847…` 对重建后的冻结 sidecar 做端到端验收，接口返回 200，成功生成 60 页、约 13.7 MB 的 DOCX，SHA-256 完整性校验通过。

下一步：

1. 在重启后的桌面窗口直接重试该任务的“生成 DOCX”，验收文件定位与实际 Word 打开效果。
2. 继续实现说明书待补信息的本地填写与 confirmed Fact 持久化。

### 2026-08-10：项目切换、程序内预览与资产管理

- 源代码 DOCX 生成后在页面顶部立即显示成功卡，并自动打开程序内文档查看器；不再要求用户滚动到超长源码列表底部才能发现结果。
- 文档查看器包含封面与全部 59 页代码正文导航，支持在查看器内直接发起导出；原有分页抽查入口继续保留。
- 新增“导出…”能力，通过系统保存对话框自选文件名和目标路径；Rust 端重新核验 task、登记产物、SHA-256 状态和任务目录边界后再复制文件。
- 各工作页右上角由不易识别的 task ID 改为项目名称下拉框，可在源码、说明书和图表页面直接切换项目。
- 新增“我的资产”页面，按项目聚合源代码文档和说明书位置，统一提供程序内查看、导出、文件夹定位和进入项目入口。
- 第二个项目的 `Load failed` 留痕显示请求未到达 Python 服务，并非跨项目数据污染；API 现会在网络层断开时检查并重启失效 sidecar，再对同一 task ID 安全重试一次。
- Python 65 项回归、Rust 4 项测试、TypeScript 检查和 Vite 生产构建均通过，主 JS gzip 约 73.0 KiB。

下一步：

1. 由用户在桌面窗口验收两个项目之间的快速切换、程序内预览和自选路径导出。
2. 将说明书 DOCX 接入同一资产管理、预览和导出模型。
3. 实现说明书待补信息的本地填写与 confirmed Fact 持久化。

### 2026-08-10：导出命名与跨平台文件定位

- 源码材料快照增加已确认项目名和版本号，导出默认名统一为“项目名-版本号-源代码文档.docx”。
- 文件名会过滤 Windows 禁止的 `< > : " / \\ | ? *`、控制字符以及结尾空格/句点，避免同一默认名在 macOS 可用而 Windows 保存失败。
- 删除源码长页面底部的应用内部产物定位卡；生成成功后的顶部主操作成为唯一导出入口。
- 导出成功后按钮立即切换为“在文件夹中显示”，定位用户实际选择的导出文件，而非应用内部任务目录副本。
- 文件定位由 Rust 分平台处理：macOS 使用 Finder `open -R`，Windows 使用 Explorer `/select,`，Linux 使用 `xdg-open` 打开父目录；定位前验证绝对路径、普通文件和 `.docx` 扩展名。
- Python 65 项回归（含项目名/版本 API 契约）、Rust 4 项测试、TypeScript 检查及 Vite 生产构建全部通过。

下一步：

1. 在 macOS 桌面窗口验收默认文件名、导出后按钮切换和 Finder 定位。
2. 建立 Windows CI 构建并在 Windows 10/11 实机验收保存对话框、Explorer 定位和 Word/WPS 打开。

### 2026-08-10：说明书验收边界与图表预检可见性

- 侧边栏顺序调整为“源码材料 → 说明书 → 图表资产”，反映说明书先规划、图表作为说明书资产产生和维护的关系。
- 明确纠正旧页面语义：现有章节计划与图表计划属于本地确定性“证据预检”，不调用 AI，也不再暗示说明书正文已生成。
- 说明书页新增生成边界提示和独立 AI 生成卡，未配置真实模型前保持禁用，避免用规则结果冒充模型内容。
- 图表语义预检结果现在可见：逐图显示节点数、连线数、证据状态和具体缺口；不再只表现为下一步按钮灰掉而没有原因。
- 可编辑 Draw.io/SVG 已生成时，说明书页提供“进入图表资产查看与修改”入口。
- 对照现有 `copyright-doc` 与 `drawio-professional-diagram` 技能确认目标链路：AI 负责项目化正文和图表语义，Draw.io XML/SVG/PNG 由技能约束的确定性工具生成、校验和视觉复核；不使用不可编辑的图片生成模型替代 Draw.io。
- FastAPI 回归增加图表预检详情契约；TypeScript 检查通过。

下一步：

1. 实现多协议模型配置与连通性测试，API Key 进入操作系统安全存储，SQLite 只保存凭据引用。
2. 接入真实 AI 说明书草稿阶段，记录模型、耗时、输入证据、调用 ID 和版本；页面右上角只展示已验证可用模型。
3. 接入 AI 图表语义草稿，并使用专业 Draw.io 技能规则生成、校验和预览可编辑图表。

### 2026-08-10：多协议模型设置与安全凭据闭环

- 新增“设置”页面，支持 OpenAI 兼容协议、Anthropic 和 Ollama；OpenAI 兼容模式可覆盖多数国内外模型服务和自建网关。
- 模型配置元数据持久化到 SQLite `model_configs`，新增 v11 migration；列表只返回 `has_credential`，不会返回凭据引用或 API Key。
- API Key 通过 Rust `keyring` 写入 macOS Keychain、Windows Credential Manager 或 Linux 系统凭据后端，不写入 SQLite、日志或前端持久化状态。
- 保存配置时执行真实连通性和模型发现测试：OpenAI/Anthropic 读取 `/models`，Ollama 读取 `/api/tags`；远程服务强制 HTTPS，本机回环地址允许 HTTP。
- 只有服务连通且目标模型确实存在时才标记 verified；未验证配置保留在设置页供诊断，但不会进入说明书模型选择器。
- 说明书右上角新增“生成模型”下拉框，只列出已验证且启用的模型，为下一步真实 AI 正文生成提供明确选择。
- 引入 `keyring` 3.6.3；Rust 编译测试、TypeScript 检查和模型元数据 API 回归通过。

下一步：

1. 实现一次性凭据注入的模型调用代理，API Key 只在调用内存中出现，不写入 sidecar 数据库。
2. 生成第一版 AI 说明书结构化草稿，保存模型、提示版本、证据引用、耗时和调用状态。
3. 在草稿预览确认后，再生成 AI 图表语义并交给 Draw.io 技能管线渲染。

### 2026-08-10：Token 权限模型发现与通用设置补全

- 模型接入改为两阶段：先填写连接与 Token 并检测模型列表，再从该 Token 实际有权访问的模型中选择保存；不再要求用户手填固定模型名。
- OpenAI 兼容/Anthropic 从 `/models` 发现权限范围，Ollama 从 `/api/tags` 读取本机模型；服务连通但列表为空时明确提示检查套餐权限。
- 检测到的模型只在当前界面内存中展示，选择后才写入模型配置；API Key 仍只保存在系统安全存储。
- 新增 SQLite v12 `app_settings`，持久化说明书默认模型、图表语义默认模型、生成温度、最大输出 Token、默认源码筛选档位和生成后自动预览。
- 默认源码策略与自动预览已经接入源码材料实际流程；说明书页面会优先选择已保存的默认模型。
- 删除模型时同步清除引用它的默认模型设置，避免留下失效配置。
- Python 66 项、Rust 4 项、TypeScript 和 Vite 生产构建通过。

下一步：

1. 接入一次性凭据模型调用代理，并让说明书“AI 生成”按钮调用右上角所选模型。
2. 保存结构化草稿、模型调用留痕与证据引用，提供章节级预览和重新生成。

### 2026-08-10：DOCX 完整性与安全定位

- 源码材料快照对最新 DOCX 重新计算 SHA-256，区分 `verified`、`missing`、`mismatch` 和 `invalid_path`，并显示文件大小。
- 只有完整性通过的产物才能点击“在文件管理器中显示”；丢失、被篡改或路径异常时按钮锁定。
- 新增 Tauri `reveal_source_document` 命令，前端只传 task ID；Rust 通过已认证 sidecar 重新读取登记产物，不信任前端文件路径。
- 定位前依次验证 task ID 字符集、相对路径组成、规范化后真实路径、任务目录归属和普通文件类型，再调用 macOS Finder、Windows Explorer 或 Linux 文件管理器。
- Python 65 项回归、Rust 4 项测试、TypeScript 检查和 Vite 生产构建全部通过，主 JS gzip 约 69.8 KiB。

下一步：

1. 建立“说明书”真实页面，展示手册章节计划、待补信息、图表资产和文档生成状态。
2. 接入手册 DOCX 的显式生成与质量门禁，保持“先预览、再生成、最后导出”。
3. 增加任务阶段时间线和错误详情，把可恢复警告与不可继续错误分开呈现。
### 2026-08-10：正式说明书阶段 2——证据化项目研究

- 新增正式研究执行器：从任务扫描快照读取确定性 Facts/Evidence，并从最新源码筛选计划中挑选 A/B 级代表性文件，不再把整个仓库无差别交给模型。
- 源码研究包保留相对路径、准确行号、文件 SHA-256 与截取内容；扫描报告命中的敏感文件不会进入模型上下文。
- AI 输出被约束为结构化 JSON，研究结论必须区分 `verified`、`inference`、`pending_confirmation`，并提供逐章写作建议。
- 服务端只接受输入上下文中真实存在的证据引用；模型虚构引用会被剔除，失去依据的“已验证”结论自动降级为待确认。
- 新增 SQLite v16 `manual_research_artifacts`，研究版本、模型、输入指纹、耗时、源码引用和研究结论完整留痕；研究失败可在同一任务独立重试。
- 正式任务状态会在研究完成后推进到 `draft`，而不是继续依赖旧版 Markdown 草稿状态。
- 新增研究执行与读取 API；Python 回归增至 71 项并全部通过，TypeScript/Vite 生产构建通过。
- Apple Silicon Sidecar 已重新冻结；临时独立启动验证健康端点、v16 迁移和研究表均可用。真实桌面数据库也已无损迁移到 v16，原有 3 个任务、6 个模型配置和 1 份加密凭据均保留。

下一步：

1. 将研究成果转换为按章节、按内容块持久化的 AI 正文，而不是单一 Markdown 长文本。
2. 支持章节级重新生成、人工修改、证据查看和版本比较。
3. 在单一主按钮中串联研究、正文、图表、截图与 DOCX，内部步骤只用于进度和诊断。
### 2026-08-10：正式说明书阶段 3——结构化章节正文

- 正式正文不再落为单一 Markdown 长文本，改为 `paragraph`、`list`、`table`、`figure_request` 四类可审阅内容块。
- 默认覆盖引言、总体设计、功能与模块、数据与接口、运行与部署、安全与可靠性、测试与总结；仅在研究证据显示存在界面时加入用户界面与操作说明。
- 每章独立调用所选模型并校验证据引用。事实段落无有效证据时标记 `needs_review`，合理推断必须明确标记，不能伪装成已验证事实。
- 一次批量生成允许部分成功：单章失败保留其他已完成章节并返回具体错误，可仅重试该章。
- 新增 SQLite v17 `manual_section_revisions`；AI 重生成与人工修改都新增版本，不覆盖历史，当前版本投影继续保存在 `manual_section_artifacts`。
- 人工编辑版本标记为 `origin=user` 和 `confirmed`，支持读取逐章版本历史，为后续程序内比较与恢复提供数据基础。
- 正文中的 `figure_request` 已成为下一阶段图表规划的正式输入，而不再从固定规则图表计划反推文章。
- 新增批量生成、章节读取、单章重生成、人工保存和版本历史 API；Python 72 项回归、TypeScript 与 Vite 生产构建全部通过。
- Apple Silicon Sidecar 已重新冻结。真实桌面数据库已无损迁移到 v17，原有 3 个任务、6 个模型配置和 1 份加密凭据均保留。

下一步：

1. 从正文中的图表请求和研究证据生成章节绑定的专业图表语义。
2. 输出每张图的 `.drawio`、`.svg`、高分辨率 `.png`，完成 XML、布局与视觉复核。
3. 将图表编辑入口继续复用现有图表资产工作台，但资产归属切换到正式说明书任务版本。
### 2026-08-10：正式说明书阶段 4——章节驱动专业图表

- 正式图表输入改为结构化正文中的 `figure_request` 与章节证据，不再依赖旧版固定规则先画两张图再反推文章。
- AI 只负责生成受证据约束的图表语义：节点、关系、类型、层级与布局；本地确定性工具负责所有可交付文件。
- 新增通用 Draw.io 构建器，支持架构、模块、流程、时序、ER、部署和数据流语义，采用企业浅色样式、稳定语义 ID、明确阅读方向和显式正交路由。
- 每张图同步输出可编辑 `.drawio`、可预览 `.svg` 和供 Word 插入的 2 倍分辨率 `.png`，中文字体来自随包分发的 Noto CJK 字体。
- 新增 SQLite v18 `manual_figure_revisions`；重新生成创建新文件与新版本，当前资产投影记录章节锚点、语义、三种格式路径和 QA 报告。
- 批量图表生成允许部分成功，单图可以独立重试；资产读取只接受 job ID、figure key 和格式白名单，并重新验证任务目录边界。
- 使用专业 Draw.io skill 的 `validate_drawio.py` 与 `analyze_layout.py --strict` 验证代表性流水线图，结果为 6 个顶点、4 条边、0 项警告；PNG 经人工视觉检查后收紧画布高度并调整语义配色。
- Python 全量回归增至 74 项并全部通过，TypeScript 与 Vite 生产构建通过。
- Apple Silicon Sidecar 已重新冻结并包含 Pillow PNG 渲染依赖，二进制约 32.9 MiB；用户无需安装 Draw.io 或额外图片环境即可生成三种资产。真实桌面数据库已无损迁移到 v18，原有任务、模型与加密凭据均保留。

下一步：

1. 实现安全的本地项目启动探测与界面截图清单，禁止触发付费调用或外部副作用。
2. 支持无法安全启动时由用户导入截图，并为每张截图保存章节锚点与五维说明。
3. 在最终 DOCX 质检时再次检查实际项目图表的版面密度、可读性和图文位置。
### 2026-08-10：正式说明书阶段 5——安全截图与章节绑定

- 新增截图安全评估，明确区分 `auto_available`、`manual_import`、`not_applicable`，并版本化记录判断依据与下一步操作。
- 自动截图策略硬性禁止执行项目任意启动脚本、访问外部网络、触发付费调用或使用真实凭据；当前安装包没有受控浏览器适配器时不会伪装成“自动截图成功”。
- 有 UI 但无法安全自动采集时允许从系统文件选择器导入真实 PNG/JPEG/WebP；无 UI 项目或用户暂不提供截图时，截图阶段可安全跳过，不阻塞 DOCX。
- 导入图片限制为 20 MiB、最小 640×360、最大边 12000 像素；解码后统一转为无元数据 RGB PNG，并登记宽高、SHA-256 与来源。
- 每张截图必须绑定真实章节，并保存页面用途、进入条件、可见区域、典型流程、后台/API/数据交互以及结果、校验、异常恢复六类实质说明。
- 新增 SQLite v19 `manual_capture_assessments` 与 `manual_screenshot_revisions`；截图资产读取时重新验证任务目录边界和 SHA-256 完整性。
- 新增截图评估、导入、完成/跳过、清单和图片读取 API；Python 回归增至 76 项并全部通过。
- TypeScript 与 Vite 生产构建通过；Apple Silicon Sidecar 已重新冻结，包含截图解码与净化能力。
- 真实桌面数据库已无损迁移到 v19；迁移后仍保留 3 个任务、6 个模型配置和 1 份加密凭据，新增截图表为空且未污染既有记录。

下一步：

1. 使用真实 Word 标题、段落、列表、表格和图注装配正式软件说明书 DOCX。
2. 将每张图表 PNG 和截图插入绑定章节，保留 Draw.io/SVG 可编辑资产。
3. 接入程序内文档预览、版本、导出和资产管理。

### 2026-08-10：正式说明书阶段 6——Word 装配、预览与资产交付

- 新增正式说明书 Word 装配器，直接从结构化章节、章节图表和真实截图生成 `.docx`，不再把旧 Markdown 草稿当作最终产物。
- 文档使用 A4 页面、真实 Heading 1/2/3、中文首行缩进、原生项目符号、固定 DXA 表格、图注、页眉页脚和动态页码；每个版本登记 SHA-256、文件大小、生成摘要和任务目录边界。
- 目录改为确定性章节目录，避免 LibreOffice/Word 未更新域时出现空白目录页；正文取消逐章强制分页，短章节会连续排版，修复此前所有页面只占一部分的问题。
- 图表和截图按真实长宽比同时限制宽高，截图后附页面用途、进入条件、可见区域、典型流程、后台交互和异常恢复六维说明，不再把最后一行说明挤到孤立尾页。
- 导出 DOCX 以 OOXML 标准嵌入随应用分发的 Noto CJK 开源字体。独立渲染器确认 PDF 实际使用 `NotoSansCJKsc-Regular`，Windows/macOS 无需额外安装中文字体。
- 说明书主页面改为一个用户动作：内部依次执行研究、结构化正文、章节图表、截图决策和 Word 装配；各阶段仍独立持久化并在“高级”区域留痕，不再要求用户点三次规则按钮。
- 生成结果在页面上方立即显示，可直接程序内查看、生成新版本或通过系统保存对话框导出；默认文件名为“项目名-版本号-软件说明书.docx”。导出后按钮切换为跨平台文件夹定位。
- “我的资产”同步纳入正式说明书版本，复用程序内查看、导出和定位逻辑；图表资产继续保留 Draw.io/SVG/PNG 供后续人工微调。
- 使用确定性 fixture 完成六页 DOCX 全页渲染检查：封面、静态目录、连续正文、原生表格、章节图表、界面截图和六维说明均无方框字、空白页、裁切、断表或孤立说明页。
- Python 78 项回归、Rust 6 项测试、TypeScript 与 Vite 生产构建全部通过；Apple Silicon Sidecar 已重新冻结为约 34.6 MB，并通过临时启动与带令牌健康检查。
- 真实桌面数据库保持 schema v19，验收前后均保留 3 个任务、6 个模型配置和 1 份加密凭据，没有发生清库或配置丢失。

下一步：

1. 实现正式版本的运行时 `render_qa` 服务，保存 PDF/逐页图片、页数和问题清单，并将文档状态推进到 `qa_passed` 或 `qa_failed`。
2. 在页面明确区分“已装配、待质检”和“可交付”，只有通过质量门的版本作为默认导出版本。
3. 用真实项目生成一份完整说明书，逐页复核 AI 内容密度、图表可读性与截图章节位置，再完善章节级人工编辑和图表对话修改入口。

### 2026-08-10：正式说明书阶段 7——运行时逐页质检与交付门禁

- 新增 SQLite v20 `manual_document_qa_runs`，逐次保存质检策略、检查项、问题摘要、A4 页面图片、预览 PDF、报告路径和时间；重试产生新版本，不覆盖既有检查记录。
- 一键生成现在完整执行六个阶段：项目研究、结构化正文、专业图表、截图决策、DOCX 装配和逐页质量检查。主流程完成度推进到 100%，内部步骤继续仅作为高级留痕和独立重试入口。
- 运行时使用随 Sidecar 分发的 Noto CJK 字体与 Pillow 确定性分页器，从和 DOCX 相同的章节、表格、图表、截图及六维说明生成 A4 PNG/PDF；最终用户无需安装 Word、LibreOffice、Draw.io 或额外文档环境。
- 质检同时检查 DOCX SHA-256、A4 页面尺寸、章节标题、表格、插图、显式分页符、内嵌字体部件、中文字形覆盖、页面数量、空白页和正文页密度。阻断问题将文档标为 `qa_failed`，只有 `qa_passed` 才允许 API、说明书页和资产页导出。
- 程序内查看改成逐页 A4 预览，提供上一页、下一页、页码和质检状态；不再用浏览器重新排版的长页面冒充最终文档。界面和 API 明确披露运行时页面是同源 companion render，并非 Word 原生排版引擎。
- 发行基线仍执行真实 DOCX 渲染：同一确定性 fixture 经内置分页器和 LibreOffice 均生成 6 页，逐页人工检查封面、目录、连续正文、表格、图表、截图和六维说明，未发现空白页、裁切、重叠、断表或字体替换。
- Apple Silicon Sidecar 已重新冻结为 34,577,808 字节，并通过独立启动、带会话令牌健康检查和临时 v20 初始化验证。
- Python 79 项回归、Rust 6 项测试、TypeScript 与 Vite 生产构建全部通过。（最终提交前再次复跑并以最终结果为准。）
- 真实桌面数据库已无损迁移到 v20；迁移前后均保留 3 个任务、6 个模型配置和 1 份加密凭据，新质检表为空，没有清库或污染既有记录。

下一步：

1. 在桌面程序用一个真实项目重新执行“一键生成”，验收逐页预览、质检状态、导出门禁和资产页默认交付版本。
2. 根据真实 AI 正文的逐页结果调整章节密度、图表尺寸和截图位置，而不是用规则填充页数。
3. 增加章节级所见内容编辑、图表对话修改和重新装配，使正文与图片资产在正式版本生成后仍可持续修订。

### 2026-08-11：正式说明书阶段 8——章节修订与重新交付

- 正式说明书结果卡新增“编辑内容”入口，打开当前生成任务的章节修订工作台；侧栏按章节导航并明确显示人工确认、AI 状态或未保存状态。
- 段落使用正文编辑框，列表按引导句和逐行条目编辑，表格按 Tab 分列、逐行记录编辑；证据引用和推断标记继续随原内容块保存，不退化为无结构长文本。
- 图表请求在章节编辑器中只读展示图名、类型、语义和稳定 key，防止正文编辑意外破坏 Draw.io/SVG/PNG 资产关联。
- “保存本章修订”调用既有版本化章节接口，生成 `origin=user` 的新 revision，不覆盖 AI 原稿；“AI 重新生成本章”仅调用当前章，并明确由用户触发模型费用。
- 用户可连续修改多章而不逐章提交。装配按钮会先保存所有未落库章节；如果 AI 重写改变了图表请求，则同步图表资产，再生成新的 DOCX 版本并自动逐页质检。
- 后端集成回归覆盖“v1 通过质检 → 修改正文 → 装配 v2 → v2 再次通过质检”，验证两个 DOCX 版本和不同 SHA-256 均保留。
- Python 79 项、Rust 6 项、TypeScript 与 Vite 生产构建全部通过。真实桌面数据库仍为 v20，保留 3 个任务、6 个模型配置和 1 份加密凭据；本轮没有修改真实任务或调用付费模型。

下一步：

1. 将正式说明书任务产生的 Draw.io/SVG/PNG 接入专门图表资产页，而不是只跳转到旧版规则图表页面。
2. 支持节点、连线、文字和布局的可视化微调，并允许用自然语言生成白名单 overlay 修订。
3. 图表或截图修订后自动标记关联 DOCX 为“存在更新”，由用户一键重新装配并质检。

### 2026-08-11：正式说明书阶段 9——正式图表资产修订

- 图表资产页的数据源已从旧版两张固定规则示例图切换为正式说明书任务的真实章节图表；没有正式图表时明确引导返回说明书生成。
- 每张资产可切换查看 SVG、PNG 与 Draw.io XML。SVG 节点带稳定 `data-node-key`，支持直接拖动；节点名称也可在属性栏修改。
- 正式图表修改复用安全 overlay 白名单。人工拖动、改名和历史恢复不调用模型，均创建新的 `.drawio`、`.svg`、`.png` 与数据库 revision，旧版本不会被覆盖。
- AI 对话修改采用两阶段确认：模型只返回 `node.move`、`node.label`、`node.style`、`edge.route` 等白名单操作；服务端校验目标并先渲染未落库 SVG，用户确认后才写入正式修订。
- 历史面板显示 AI 初稿、人工修改与 AI 修改来源；恢复任意历史内容会继续创建新版本，避免破坏审计链。
- 新增 SQLite v21，为正式图表修订记录父版本、修改来源、操作清单、语义指纹和冲突状态。
- DOCX 响应新增 `freshness`：当正文、图表或截图晚于文档时，旧 DOCX 明确标为“内容已更新，待重新装配”，暂停导出并提供“重新装配最新内容”入口；旧版本和质检记录仍完整保留。
- 按 Draw.io 专业图表技能要求，`.drawio` 继续作为可编辑源，SVG/PNG 由本地确定性渲染器生成；用户不需要安装 Draw.io 或额外图片环境。
- Python 79 项回归、Rust 6 项测试、TypeScript 与 Vite 生产构建全部通过。应用内浏览器完成空状态页面视觉检查；纯浏览器不具备 Tauri 本地桥，因此真实数据交互由服务级回归与桌面包验收覆盖。
- Apple Silicon Sidecar 已重新冻结为 34,581,936 字节；本轮最终代码与前一冻结版本使用同一启动链，临时独立启动及带令牌健康检查已通过。
- 真实桌面数据库已无损迁移到 v21；迁移前后均保留 3 个任务、6 个模型配置和 1 份加密凭据。

下一步：

1. 在桌面程序选择已有正式说明书项目，验收拖动、AI 修改预览、历史恢复以及 DOCX 过期提示和重新装配。
2. 增加截图资产的专门管理页：导入、章节绑定、六维说明修订和版本历史。
3. 用真实项目迭代图表视觉模板与布局质量，重点处理节点密度、跨层边、长标签和图文比例，而不是只确认“能生成”。

### 2026-08-11：正式说明书阶段 10——界面截图资产修订

- 侧栏新增独立“界面截图”入口，顺序位于“说明书”和“图表资产”之间；页面采用截图清单、真实图片预览、章节与六维说明编辑三栏工作台，不再把截图埋在生成流程底部。
- 导入仍使用系统文件选择器；图片经过解码、尺寸限制、元数据移除与 PNG 规范化后才进入任务目录。标题、绑定章节和六维说明每次修改都创建新版本。
- 支持替换真实截图且保留旧文件；支持归档和恢复使用，归档只影响下一版说明书是否采用，不删除图片、说明或历史记录。
- 历史版本展示修改来源和时间；回滚会创建新 revision，不覆盖既有版本。即使在归档状态回滚内容，也不会绕过明确的“恢复使用”操作。
- 新增 SQLite v22，为截图当前投影增加更新时间与归档状态，为 revision 增加修改来源、父版本、变更摘要和历史归档状态。
- DOCX 新鲜度已纳入截图 `updated_at`：标题、章节、说明、图片、归档或恢复发生变化后，既有说明书变为 `outdated`，须返回说明书重新装配并逐页质检后才能导出。
- 新增截图编辑、替换、版本历史、历史恢复、归档/恢复 API；服务级回归覆盖导入 v1、信息修订、图片替换、归档、归档中回滚、恢复和再次回滚的完整链路。
- Python 79 项回归、Rust 6 项测试、TypeScript 与 Vite 生产构建全部通过；Python 3.9 发行基线兼容性已验证。
- Apple Silicon Sidecar 已重新冻结为 34,585,008 字节，SHA-256 为 `5d14f96600dc7c63f8c29819095508ea7dc292798c5b693397815115f790f2ca`；冻结二进制独立启动并通过带会话令牌的回环健康检查。
- 真实桌面数据库已确认 schema v22，仍保留 3 个任务、6 个模型配置和 1 份加密凭据；迁移没有清库或暴露 API Key。

下一步：

1. 在桌面程序选择已有正式说明书项目，验收真实截图导入、六维说明、章节换绑、图片替换、归档/恢复、历史回滚和 DOCX 过期提示。
2. 进入跨平台发布基线：分别构建 macOS 与 Windows 安装包，验证系统文件选择器、保存对话框、文件夹定位、SQLite 数据目录和嵌入字体。
3. 用真实项目生成最终说明书版本，逐页复核正文密度、图表视觉质量、截图位置和六维说明篇幅，再进入质量检查页与安装包验收。

### 2026-08-11：桌面发布阶段 11——原生安装包与进程生命周期基线

- Tauri 正式构建钩子改为直接执行仓库内锁定的 TypeScript/Vite，修复依赖已经安装时仍因包管理器联网切换失败的问题；依赖安装阶段的锁文件和签名校验不降级。
- `tauri.conf.json` 明确声明 PNG、ICNS、ICO 图标；最终 macOS `.app` 已包含 `Contents/Resources/icon.icns` 和正确的 `CFBundleIconFile`，不再生成无图标应用。
- 源代码与说明书导出统一要求绝对 `.docx` 目标路径；Finder、Explorer 和 Linux 文件管理器命令由可测试的分平台适配器生成，中文和空格路径保持为单一参数。
- 新增 macOS/Windows 原生 GitHub Actions 构建：每个平台现场冻结对应架构 Sidecar，构建 `.app/.dmg` 或 NSIS/MSI，并上传安装产物；Linux 作业继续运行完整 Python、TypeScript 与 Rust 回归。
- 桌面壳启动 Sidecar 时传入父进程 PID；Sidecar 在 POSIX 使用进程存活探测，在 Windows 使用 `OpenProcess/WaitForSingleObject`。桌面崩溃或强退时，Sidecar 会主动退出，不再只依赖 Rust 析构清理。
- 新增跨平台冻结 Sidecar 父进程退出验收脚本，并在本机真实 PyInstaller 二进制上通过：父进程结束后 Sidecar 在 8 秒门限内以 0 退出。
- Apple Silicon Sidecar 最终为 34,585,264 字节，SHA-256 为 `7c6b8ad5831dd4e67f28aace1d8c9291b3585712e392b4c70d84da8a3bf70877`。
- 最终 macOS DMG 大小约 38 MiB，SHA-256 为 `8cd1e1589fb0f7be746c44e4f5644677adc739808ad555fc448c75f2b597138b`，`hdiutil verify` 完整通过。
- 从只读 DMG 实际启动桌面壳和内嵌 Sidecar；Sidecar 使用正式 `app_data_dir`。启动前后真实数据库均为 v22、3 个任务、6 个模型、1 份加密凭据。强制终止临时桌面壳后未残留 DMG Sidecar 进程。
- 当前 macOS 包仅为 ad-hoc 签名；Apple 公证、Windows 代码签名和 Windows 10/11 实机安装仍是公开发布门禁，未被虚报为已完成。

下一步：

1. 推送并等待 GitHub Actions 的 macOS/Windows 原生构建，以 CI 产物证明 Windows 编译与安装包结构；失败项在同一发布阶段修复。
2. 下载 Windows NSIS/MSI，在 Windows 10/11 实机验收中文/空格路径、Explorer 定位、Word/WPS 打开、升级数据保留和任务管理器强退清理。
3. 回到真实项目成品验收：运行一键说明书流程，逐页复核 AI 正文、Draw.io 图表、真实截图和最终 DOCX，再完善质量检查总览。

### 2026-08-11：验收纠偏——新项目源码 DOCX 与模型验证状态

- 按用户验收反馈暂停发布收口，重新以开发版真实项目流程为当前主线；本轮不推送安装包或宣布交付。
- `seek-pitch-creator` 新任务的源码 DOCX 失败已复现到确定原因：源码含 Unicode 变体选择符 `U+FE0F`，它是不可见组合标记，不应要求字体提供独立字形；旧字体门禁将其误判为缺失字形。
- 字体预检现忽略 `Cf/Mn/Me` 类不可见或组合字符；预检也已移入源码文档任务失败保护，异常必须记录为可重试业务失败，不得再表现为无信息的 `Load failed`。
- 对同一真实任务重试已成功生成 `source-code.v1.docx`：59 页源码正文、60 页总页数、2950 个可视行，中文字体嵌入与 SHA-256 摘要正常。
- 模型页的“连接成功”原先只保存 `endpoint_mode`，却未保存 `verified_at`；说明书页严格筛选 `verified_at` 后就得到“尚无已验证模型”。现在测试成功会同时原子更新接口模式和验证时间。
- 新增 SQLite v23 无损迁移，为已有 `endpoint_mode` 的成功模型回填验证时间；真实库中 6 个商汤模型均已恢复为说明书可选模型，凭据仍为加密存储。
- 前端本地请求重连会动态替换新会话令牌；重连仍失败时显示中文可诊断错误，不再直接暴露 WebView 的 `Load failed`。

下一步：

1. 由用户在已重启的开发版验收源码文档程序内预览与说明书模型下拉框。
2. 选择已验证模型对真实项目执行一键说明书，按正文、图表、截图、DOCX 和逐页预览分段验收。
3. 只有功能效果通过用户验收后，才恢复 macOS/Windows 打包与发布验证。

### 2026-08-11：验收纠偏——源码文档真实分页与长任务可恢复进度

- 对用户导出的《霓裳云枢-V1.0-源代码文档.docx》执行 LibreOffice 真实渲染：文档实际为 69 页，第 3、8、13、24、29、33、37、41、50 页几乎只有一行，与系统声称的 60 页不一致，验收不通过。
- 根因为每个逻辑页的 50 行被配置为 15pt 固定行距，总高度超出 A4 正文可用空间；Word 先自动把末行挤到新页，程序随后又插入强制分页，从而产生稀疏页。
- 源码 DOCX 生成器升级为 `source-docx-v2`，经 12pt 与 14pt 两轮真实渲染校准，最终选定 14pt：12pt 虽然消除稀疏页，但最低填充率只有 75.38%，仍未达质量门禁。
- 同一“霓裳云枢”真实任务已生成 `source-code.v3.docx`；应用自身的 LibreOffice/Poppler 逐页 QA 结果为 60 页 A4、空白页 0、稀疏页 0、最低正文填充率 86.92%，18 项门禁全部通过。
- 说明书页不再仅依赖组件内的 `generating` 布尔值。每次进入页面均从 SQLite 恢复 `queued/running` 任务，并每 1.5 秒轮询阶段、百分比、耗时、完成与失败结果。
- 新增常驻生成进度面板，显示项目研究、结构化正文、专业图表、界面截图、Word 装配和逐页质检六个阶段；切换页面再返回时继续显示，不得伪装成未开始。
- 前端在存在活动任务时禁止重复生成；后端 `ManualPipelineService.create` 也增加同项目活动任务唯一性门禁，防止并发点击绕过界面。
- 真实库确认此前因页面状态丢失已产生两个重复任务：v1 已完成 6/6 阶段并生成通过 QA 的 DOCX，v2 正处于结构化正文阶段。新界面会显示此事实并禁止创建第三个任务，未删除用户已生成的版本。

下一步：

1. 由用户验收说明书常驻进度面板、切页恢复以及 v1 成品预览。
2. 待正在运行的 v2 结束后重建 Sidecar，使后端防重复门禁进入当前验收开发版，避免中断正在进行的 AI 调用。
3. 源码文档导出与正式说明书继续以真实渲染页数、稀疏页与逐页视觉检查为验收标准。

### 2026-08-11：验收纠偏——说明书长任务细粒度进度

- 正文与图表阶段不再只在阶段结束时从 17% 跳到 33%、再跳到 50%。后端会在每个章节、每张图表开始和结束时，把当前序号、总数、标题和阶段内百分比写入 SQLite。
- 说明书页面从持久化任务读取这些字段，显示“结构化正文（3/8 章 · 当前章节）”或“专业图表（5/8 张图 · 当前图名）”；切换页面或重新进入后仍可恢复，不依赖 React 组件内存。
- 阶段卡片同步显示 `正在执行 N/总数`；完成后的阶段摘要保留处理总数，便于高级留痕查看，而不是完成后抹掉过程数据。
- 真实 v2 任务最终完成 6/6 阶段并保留 6 张正式图表；任务结束后才重建冻结 Sidecar，没有中断该次模型调用。v2 的首份 DOCX 因装配时图表数量与最终资产不一致而被 QA 判为失败，系统保留失败记录；此前 v1 通过版仍可预览，未将失败伪装为成功。
- Python 82 项回归与 TypeScript/Vite 生产构建全部通过；新增测试覆盖正文 7/7、图表 2/2 的持久化阶段摘要。
- 新 Sidecar 为 34,586,640 字节，SHA-256 为 `edb628bdaaf533a24d7b0979909c1f03cfcf588ecccdf09b456a36dc388c6030`；Tauri 开发版已自动重启并加载。重启后真实库仍保留 5 个任务、6 个模型、1 份加密凭据、4 份说明书和 7 份源码文档。

下一步：

1. 在程序内导出修正后的源码文档，确认 60 页均为有效内容。
2. 新建一次说明书任务，切换页面后返回，验收逐章节/逐图表进度恢复和完成结果提示。
3. 对 v2 最终 6 张图表执行重新装配并复核质检失败原因；未通过效果验收前不进入打包。

### 2026-08-11：真实素材调优——说明书内容、任务隔离与图表可读性

- 按用户提供的“霓裳云枢”源码文档、软件说明书和真实任务数据库做成品复核；旧说明书虽然能打开，但正文偏模板化、图表尺寸小且缺少真实界面截图，不能作为效果验收通过版。
- 项目研究输入由单纯高分排序改为入口、界面、接口、业务逻辑、数据和配置分层轮询，采样上限提升到 18 个文件/约 72,000 字符；避免 Java 后端文件垄断上下文，使 AI 同时看到 Vue 页面、前端 API、控制器、服务和持久化证据。
- 正文提示词升级为每章 5–8 个结构化内容块、至少 3 个实质段落、约 900–1600 字，并要求引用真实模块、接口、数据结构、状态和异常分支；不再接受只有标题和占位句的 Markdown 骨架。
- 说明书质量门禁升级为 v2：章节内容块与正文深度不足会直接阻止交付；存在界面操作章节却没有真实截图时也会阻止交付，并在程序内列出中文问题及“去补充界面截图”入口。
- 研究文件、图表、截图和 DOCX 全部按说明书任务版本命名空间隔离；新任务不再覆盖旧任务的同名中间文件或图表资产。此前共享路径造成的跨任务串稿被确定为旧版核心缺陷。
- 任务启动后立即把 `running` 状态和阶段进度写入并回显，不等待长 HTTP 请求结束；页面切换后从 SQLite 恢复。应用异常退出留下的任务会在重启时标记为中断，90 分钟无心跳的任务也会自动从“假运行”状态退出并保留已有阶段结果。
- 真实库中 `seek-pitch-creator` 长时间停在 research 的任务已改为 `failed`，错误明确为“上次应用退出时生成被中断”；配置、历史任务和旧成品均未清空。
- Draw.io 编辑改为暂存模式：拖动和改名先在页面内预览，用户明确“保存为新版本”后才落库；支持缩放、放弃修改和切图防误操作，历史版本不被覆盖。
- 新图表限定最多 10 个节点、最多 `节点数 + 2` 条主关系，并使用避让节点的显式折线路由。对真实库 10 张历史语义重新规范化后，每张均通过 Draw.io XML 校验；最复杂图限制为 9 个节点、11 条边，不再渲染成不可读线团。
- 对项目的可启动网页执行真实浏览器检查：当前前端依赖后端鉴权，只有静态服务器时会出现白屏，因此程序不会自动把白屏当作说明书截图。嵌套 `dist/index.html` 入口已能识别，但最终截图仍要求用户导入或确认可用运行环境。
- 用真实“霓裳云枢”和 qwen3.6-27b 生成 job v3：研究阶段完成，8/8 章正文全部成功且无章节错误，生成 3 张正式图表，旧 job 与旧文档未覆盖。
- 首次 v3 DOCX 真实 LibreOffice 渲染为 15 页，正文页无“几行一页”；但复核发现自动规划的模块协作图虽然已生成，却因 AI 正文没有对应占位块而漏装，横向五阶段流程图也因 A4 缩放过扁。
- DOCX 装配器现按章节补插未被正文占位引用的确定性规划图；五层以上横向流程自动改为纵向布局。无额外模型调用地重排后，v2 DOCX 插入 3/3 张图，真实 LibreOffice 渲染为 16 页，正文与最终页均有实质内容，纵向流程标签可读。
- v2 仍被明确标记为 `qa_failed`，唯一阻断项为 `content.ui_screenshot`：项目存在真实界面章节但尚未导入截图；文档不会在截图缺失时伪装成交付通过版。内置同源渲染器对末页给出低填充警告，但 LibreOffice 实际末页为密集正文与表格，记录为运行时预览与 Word 基线的差异，未掩盖。
- Python 86 项、Rust 8 项和 Vite 生产构建全部通过；`git diff --check` 通过。Apple Silicon Sidecar 已按最终调优代码重新冻结，SHA-256 为 `c5d54d6b3129db1f958a7c75896ea62f61f75e0e72ae2c9ad5a30e5187132fb9`。
- 当前没有进入安装包发布；下一轮以程序内 v3/v2 结果、截图补充和用户视觉验收为准。

下一步：

1. 在已重启的开发版选择“霓裳云枢”，验收 job v3 的 8 章正文、3 张图、失败原因明细和 v2 文档预览。
2. 从“界面截图”页导入 1 张或多张真实截图并绑定“用户界面与操作说明”，重新装配后复核质量门槛是否转为通过。
3. 在图表资产页验收暂存拖动、保存新版本、放弃修改、缩放和历史恢复；根据用户指出的具体图继续调布局与配色。

### 2026-08-11：真实素材调优——真实截图、Word 末页与跨页任务状态

- 使用“霓裳云枢”项目自带静态页面实际启动并由浏览器采集两张真实界面：云肩文化详情和四大名绣导览；白屏的 Vue 壳页未被当成截图。两张图均绑定 `ui_operations` 章节，并补齐页面用途、进入条件、可见区域、典型流程、后台交互、结果校验与恢复六维说明。
- 说明书 job v3 重新装配到文档 v5：8 章、3 张可编辑 Draw.io 图、2 张真实界面截图，内置结构/内容门禁 19 项通过；唯一非阻断警告为正文仍含“待确认”字样。
- 内置伴随渲染器报告 15 页，但真实 LibreOffice 首次将 DOCX 渲染为 18 页，最后一页只有一段结论。该差异证明只依赖程序内预览仍会漏掉 Word/WPS 分页问题，因此未把 v3/v4 视为验收通过。
- A4 中文说明书覆盖参数调整为 1.20 倍正文行距、4.5pt 段后距和 3pt 列表项间距，生成器升级为 `formal-manual-docx-v3`。真实 LibreOffice 最终将 v5 渲染为 17 页，结论回收至包含测试表和异常说明的末页，不再出现“几行一页”；逐页联系表与重点图表/截图页均已人工复核。
- 说明书启动接口由“保持长 HTTP 请求直到全部完成”改为“先持久化任务并立即返回，再由后台线程执行既有 job”。页面切换或组件卸载不再决定任务生命周期，应用退出仍由既有中断恢复机制保留阶段结果并标记可诊断失败。
- 左侧栏新增全局说明书进度：任意页面均显示当前任务版本、研究/正文/图表/截图/Word/质检阶段和百分比，点击可直接回到说明书详情；说明书页仍从 SQLite 每 1.5 秒恢复细粒度进度。
- Python 应用环境 87 项回归全部通过，文档运行时专项测试通过，TypeScript/Vite 生产构建通过。Apple Silicon Sidecar 重新冻结为 34,594,464 字节，SHA-256 为 `89e149bb2a7131390fb74fee9885875412309175f25380f3b4251cf6c95b7ba4`。
- 更新后的 Tauri 开发版已启动供验收；本轮没有构建或发布安装包。

下一步：

1. 用户在开发版选择“霓裳云枢”，打开说明书文档 v5，验收 17 页正文、三张图和两张截图的实际阅读效果。
2. 新建一次说明书任务后切换到源码材料或设置页，确认左侧全局进度持续更新，再返回说明书核对同一 job 与阶段留痕。
3. 进入图表资产页逐张验收节点布局、连线标签、缩放、暂存修改、保存新版本与历史恢复；根据视觉反馈继续调图，不进入打包。

### 2026-08-11：真实素材调优——图表碰撞质检、工作台导出与说明书重装配

- 对“霓裳云枢”说明书现有三张正式图逐张查看原始 PNG，确认旧版的主要视觉缺陷不是配色，而是连线标签压住节点边框、关系线穿过无关节点以及交叉关系阅读顺序不清。
- Draw.io 生成器升级为 `drawio-generator-v4`：扩大节点与层级间距，根据横向/纵向关系选择标签候选位置，并对节点碰撞、画布越界和文字视觉宽度进行评分；SVG 与 PNG 使用同一布局逻辑，避免预览与导出位置不一致。
- 运行时图表检查新增“关系线穿过无关节点”和“关系标签覆盖节点”两类错误，不再只检查节点互相重叠。三张真实图重新生成后，通过专业 Draw.io 严格分析：节点重叠、越界、边穿节点和标签压框警告均为 0。
- 图表工作台增加单步撤销、未保存位置同步、导出 Draw.io/SVG/PNG 以及导出后在文件夹显示；有暂存修改时禁止导出旧版本，并明确提示连线将在保存新版本后按节点位置重新避让。
- 真实任务中的系统架构图、模块协作图和数据流图分别生成新版本并逐张查看原始 PNG；随后无额外模型调用地重新装配 `software-manual.v6.docx`。
- v6 通过应用结构门禁，并由 LibreOffice 按真实 Word 分页渲染为 17 页；第 6、8、10 页三张图和相邻正文已逐页查看，标签与节点不再重叠，末页包含测试表和多段正文，没有“几行一页”。应用伴随预览仍显示 15 页，与 Word 实际 17 页的差异继续作为待解决项保留。
- 全量 Python 88 项、Rust 9 项、Vite 生产构建和 `git diff --check` 全部通过。Apple Silicon Sidecar 已重新冻结为 34,596,192 字节，SHA-256 为 `9ccc518aa353372cff9c9de34845eeaa9d886cb201028224e49fb71435031d7b`。
- 更新后的 Tauri 开发版已重新启动，加载新的 sidecar、图表生成器和桌面导出命令；本轮仍未构建发布安装包。

下一步：

1. 用户在开发版复测新项目源码 DOCX、说明书模型下拉与跨页面长任务恢复，确认此前两个主链路问题不再复现。
2. 在图表资产页查看三张真实新图，测试拖动、改名、单步撤销、保存新版本以及 Draw.io/SVG/PNG 自选路径导出。
3. 继续以 Word/WPS 实际渲染为基线调说明书正文密度与图文比例，并修正程序内伴随预览 15 页与真实 Word 17 页的计数差异；用户验收前不进入打包。

### 2026-08-11：真实成品纠偏——源码满页、零推断正文与五类证据图

- 源码筛选策略升级为 v2：排除历史成品、模板仓库、Agent 素材、构建元数据与低价值生成代码；同时补充符号字体回退，避免代码文档中的特殊符号被替换或丢失。
- 使用真实 `seek-pitch-creator` 任务重新生成源码文档 v2。LibreOffice 实际渲染为 60 页，空白页 0，最低正文填充率 86.84%；19 项结构与内容检查全部通过，并逐页检查十组联系表，没有再出现只有几行代码的页面。
- 说明书正文改为“零推断正式材料”：每个内容块都必须绑定真实源码证据，模型标记为推断的块会被丢弃；章节不足 3 个实质内容块、简介不足 650 字或其他章节不足 800 字时自动携带错误原因重试，最多 3 次。
- 图表不再因为模型漏写占位而阻断整条任务。系统会依据已经通过的章节正文和证据引用，自动补齐系统架构、模块关系、数据流、部署和业务流程五类必要图表任务；所有图表均保留可编辑 Draw.io 源文件。
- 使用真实“霓裳云枢”任务重新生成 8 章正文：共 39 个结构化内容块，缺失证据引用 0，推断内容 0，占位语句 0；五张当前图表全部重新生成，历史错误图仍作为版本留痕保留但不会混入新 Word。
- 说明书 DOCX 只装配当前章节引用的图表，横向图按宽高比限制最大高度，使正文表格、图和图注能够合理共页；纵向流程图保留更高的可读空间。
- 最新 `software-manual.v8.docx` 通过 19 项应用内检查。LibreOffice 实际渲染为 18 页，重点复核了原先稀疏的第 5 页：现已包含完整表格、系统架构图、图注与说明，没有裁切或半页空白；第 6 页正常开始下一章节。
- 本轮仍处于开发验收阶段，没有构建发布安装包。下一步先跑全量回归、重建 Sidecar 并启动开发版，复测模型验证状态、源码生成、说明书跨页面任务恢复和五张当前图表。

下一步：

1. 重建并启动开发版，确认已验证模型在说明书页面与设置页面的状态一致。
2. 发起真实说明书长任务，切换到其他页面再返回，检查全局进度、章节进度、失败原因和最终成品是否都从 SQLite 恢复。
3. 在图表资产页继续验收五张当前图的编辑、撤销、保存新版本与自选路径导出体验；根据实机反馈继续调优，不进入打包。

补充启动验收记录：

- 说明书页面不再把“模型列表”和“生成偏好”作为一个全有或全无的请求。偏好读取临时失败时，6 个已验证模型仍会正常显示并默认选择首个可用模型，避免设置页显示连接成功而说明书页误报“尚无已验证模型”。
- SQLite 只读复核确认：5 个项目、6 个已验证模型和历史任务均存在；霓裳云枢 job v3 为完成状态，说明书 v8 为 `qa_passed`，没有清库或丢失配置。
- 最终开发版检查通过 Python 89 项、Rust 9 项、前端生产构建和 `git diff --check`；新 Sidecar 为 34,898,672 字节，SHA-256 为 `a62733a53250ee5c522d596e685dc28c1e506191125d02d71dcde3c733815a89`。
- Tauri 开发版已重新启动并连接实际数据目录，保持运行供用户验收；仍未打包。

### 2026-08-11：真实成品纠偏——源码坏版本识别与说明书图表层级化

- 对用户提供的《霓裳云枢-V1.0-源代码文档.docx》重新执行 LibreOffice 全页渲染，确认该早期导出件实际为 69 页；第 3、8、13、24、29、33、37、41、50 页正文填充率约 7%，属于视觉换行溢出后叠加强制分页形成的伪空白页，不能交付。
- 同条件复核应用数据库中的霓裳云枢源码文档 v3：真实 60 页，59 个代码页最低正文填充率 87.1%，低于 86% 的页面为 0。源码页和“我的资产”现直接显示当前文档版本与逐页 QA 状态；明确质检失败的版本禁用导出，避免“已生成”被误解成“可交付”。
- Draw.io 生成器升级为 `drawio-generator-v7`：架构、部署和模块图增加层级标签，节点增加角色副标题；工作流和数据流增加稳定步骤号与菱形判断节点；修复 PNG 箭头始终朝右、双向边重合和键盘微调不累计等问题。
- 真实霓裳云枢五张当前图已保留历史并生成新版本。系统功能模块图由单排五模块改为 3+2 分行，并单独增大主标签与副标题；曾尝试 2+2+1 近方形布局，但因九条跨模块关系被迫大范围绕线，验证器拦截且未写入数据库，最终保留关系更清晰的 3+2 方案。
- 说明书重新装配为 `software-manual.v11.docx`：8 章、5 张当前 Draw.io 图、2 张真实界面截图、39 个证据绑定内容块；22 项应用内检查通过，LibreOffice 实际渲染 17 页，第 5、7、9、11、13 页图文页面均已按原始尺寸复核，无空白页或“几行一页”。
- 图表工作台不再向普通用户直接倾倒 Draw.io XML；格式切换改为“可视编辑 / PNG 成品 / Draw.io 源文件”，源文件页解释可编辑节点、关系和导出用途。方向键可连续 1px 微调，Shift+方向键为 10px。
- 全量 Python 93 项、Rust 9 项和 Vite 生产构建全部通过。Apple Silicon Sidecar 已重建为 34,901,056 字节，SHA-256 为 `6d41739daa9364d70fa22902e60665287b099539dc9901e3984104508339647d`。本轮仍不打安装包，只启动开发版供实际验收。

下一步：

1. 在源码材料页确认霓裳云枢显示 v3、真实 60 页和“逐页质量检查通过”，重新导出后不应再得到早期 69 页坏文件。
2. 在说明书页打开 v11，重点验收第 5、7、9、11、13 页图表与正文比例，以及切换页面后的任务进度恢复。
3. 在图表资产页测试可视编辑、连续键盘微调、保存新版本、历史恢复和 Draw.io/PNG 自选路径导出；继续按实机反馈调优，不进入打包。

### 2026-08-11：验收盲区修复——源码 DOCX 真实逐页预览

- 复核确认源码页原有“程序内查看”并未打开最终 DOCX，而是重新读取分页预检 JSON、拼接模拟封面和代码纸张；这种假预览无法暴露 Word/WPS 的换页、溢出和空白页问题，是早期 69 页坏文件看似正常的重要原因。
- 新增最终源码文档真实预览接口：只读取当前 DOCX 最新逐页 QA 的渲染目录，返回实际文档版本、QA 版本、质量状态和真实页码；逐页 PNG 通过受限任务路径读取，越界和目录逃逸均会被拒绝。
- 源码材料页把“分页内容预览”和“最终文档预览”拆成独立状态。前者继续用于生成前抽查首页/中间页/末页，后者现在展示真实 Word 渲染图、1–60 页导航、上一页/下一页、文档版本和质检状态，不再生成模拟封面。
- 对没有逐页 QA 渲染结果的历史版本明确提示“尚无真实 Word 渲染结果”，不再偷偷退回到视觉不等价的代码 JSON；已明确质检失败的版本继续禁止导出。
- 使用实际应用数据库副本和霓裳云枢任务验证：识别源代码文档 v3、QA v2、真实 60 页；第 60 页读取为有效 PNG，大小 112,508 字节。
- 新增 2 项真实预览服务测试；全量 Python 95 项、Vite 生产构建和 `git diff --check` 通过。Apple Silicon Sidecar 重建为 34,902,336 字节，SHA-256 为 `279345f72c548a7d1f5a93416cf4fa8d7112a64f84148253354661e183774343`。
- Tauri 开发版已重启并继续使用原应用数据目录，任务、说明书历史和模型凭据未清空；本轮仍未打安装包。

下一步：

1. 用户在源码材料页选择霓裳云枢，点击“程序内查看”，逐页验收真实 60 页并与导出 DOCX 对照。
2. 新建一次说明书任务，切换到其他页面，确认左侧全局阶段、百分比和返回后的同一任务状态持续存在。
3. 对没有真实 QA 渲染的历史源码版本补充用户可控的“执行逐页质检”入口，并明确外部 LibreOffice 可用性，不把模拟渲染冒充 Word 基线。

### 2026-08-11：说明书真实 Word 基线与跨页面进度收口

- 说明书目录改为章节书签加 `PAGEREF` 页码域，生成的 v12 在 LibreOffice/Word 实际分页中显示点引导线和正确章节页码，不再只是无页码的文字清单。
- Draw.io 说明书插图按 A4 纵向阅读重新布局：架构、模块和部署图采用更易读的分行结构，增加纵向间距并修复长连线路径；真实模块图重新检查后，节点重叠、边穿节点和标签压框均为 0。
- 说明书研究阶段增加可持久化子进度：整理项目事实、读取代表性源码、模型分析源码片段、校验证据引用。前端继续轮询 SQLite 中同一个 job；切换到源码、设置或资产页后，左侧全局进度仍显示当前阶段，返回说明书恢复同一任务与耗时，而不是创建空白页面状态。
- 真实霓裳云枢任务重新装配 `software-manual.v12.docx`：8 章、5 张当前 Draw.io 图、2 张截图，LibreOffice 实际渲染 19 页。22 项检查通过，空白页 0、缺失图表 0；最低正文页填充率 33.9%，该页包含 3 个接口条目和状态处理正文，不再出现只有几行内容的中间页。
- 淘汰说明书的模拟分页验收。应用内逐页预览和质量报告现在直接渲染当前 DOCX，v12 的程序内预览与外部 Word 基线统一为 19 页；数据库保存实际渲染器 `libreoffice_word`，不再出现程序显示 16 页、外部打开 19 页的矛盾。
- SQLite schema 无损升级到 v24，保留实际应用中的 5 个项目、6 个模型配置和 6 个已验证模型；旧 QA 记录根据报告内容迁移真实渲染器类型，未清库、未重置任务或凭据。
- 全量 Python 回归 97/97 通过，`git diff --check` 通过。Apple Silicon Sidecar 已重新冻结为 34,916,064 字节，SHA-256 为 `16dbc7395278b23ef47fbb67a3587c71afb8c4aa08572124e0c29857d2558c75`。
- Tauri 开发版已经重新启动并连接原应用数据目录，保持运行供用户验收；本轮没有打包安装包。

下一步：

1. 用户在源码材料页选择霓裳云枢，打开“最终文档预览”，按真实 60 页检查中间页密度并重新导出，确认不再得到早期 69 页坏版本。
2. 用户在说明书页选择霓裳云枢并查看 v12 的真实 19 页；另起一个长任务后切换页面，核对侧栏进度与返回后的 job、阶段、耗时持续存在。
3. 继续根据实机验收处理截图选择质量和图表工作台编辑体验；用户确认前不进入打包。

### 2026-08-11：真实全页视觉收口——低密度阻断与图文重排

- 对霎裳云枢说明书 v12–v14 的 LibreOffice 真实页面逐页复核，确认两个既有门禁漏洞：中间低密度页只记为 warning，且末页被无条件豁免，因此“半页空白”和“末页仅两行”仍被错标为通过。
- 说明书 QA 升级到 `manual-docx-qa-v5`：中间正文页填充率低于 50% 直接阻断，末页低于 25% 同样阻断。v14 因第 5、17 页低密度被正确记为 `qa_failed`，不再假通过。
- 正式 Word 装配器升级到 `formal-manual-docx-v5`：系统架构、模块与数据接口章的总览图移到职责表之前，结论章单独使用紧凑但可读的行距，避免表格或最后两行被孤立推到新页。
- Draw.io 生成器升级到 `drawio-generator-v8`：六步及以上工作流程采用两列蛇形布局，减少细长图两侧的大面积空白，保留可编辑 Draw.io 源文件。
- 使用实际应用 SQLite 和现有五张图、两张截图重新装配 `software-manual.v16.docx`。LibreOffice 实际渲染 16 页，22 项检查全部通过，中间页最低填充率 68.4%，末页 92.64%，空白页、低密度页和缺失图表均为 0。
- v16 的 16 张真实页面已全部人工视觉检查：目录页码正确，架构图、模块图、数据流图、部署图和操作流程可读，表格无裁切，两张真实界面截图与对应操作说明相邻。
- 说明书长任务仍以 SQLite job/step/progress 为唯一状态源：页面每 1.5 秒恢复同一 job，左侧栏每 1.8 秒显示全局阶段与百分比；切换到其他页面不会取消后台任务或丢失阶段记录。
- 全量 Python 回归 97/97、Vite 生产构建和 `git diff --check` 全部通过。Apple Silicon Sidecar 已重建为 34,914,064 字节，SHA-256 为 `bc2572d7c1cd37fef46106d89f375f5047dc3898abbfc007fc3f0d3da1665db8`。
- Tauri 开发版已加载新 Sidecar 重启，连接原应用数据目录并保持运行供验收；本轮仍未打包安装包。

下一步：

1. 用户在开发版选择霎裳云枢，打开说明书 v16 的真实 16 页预览，重点验收第 5、8、9、13、15 页图文比例。
2. 发起一次新说明书任务，生成中切换到源码或设置页，确认侧栏进度持续变化，返回后仍为同一 job、同一阶段与耗时。
3. 继续验收图表工作台的拖动、键盘微调、撤销、保存新版本和自选路径导出；用户确认前不进入打包。

### 2026-08-11：真实界面走查——预览 404、长列表收口与分层图表 v17

- 使用原应用 SQLite、真实霓裳云枢任务和浏览器驱动的本地前端逐页操作，不再只依赖接口或单元测试。源码最终预览复现出“元数据显示 60 页、画布却全灰”的假成功：渲染文件实际命名为 `page-01.png`，读取端却固定请求 `page-1.png`。源码与说明书两套逐页读取已统一改为按数值页码匹配真实文件名。
- 两个预览器都增加了画布内加载状态、明确失败原因和“重新加载本页”；源码 60 页侧栏改为页码输入 + 邻近页快捷跳转，不再要求滚动 60 个按钮。真实界面回归确认源码 v3 第 1/60 页和说明书 v16 第 1/16 页均成功显示实际 PNG，不再 404。
- 源码候选区由一次渲染 264 个文件改为默认 30 个，增加路径/语言搜索、A/B/C 等级筛选和分批展开。项目事实中的数组、对象默认只显示数量与摘要，需要时再展开 JSON 详情，避免页面被结构化原始数据无限拉长。
- 说明书跨页面恢复再次实机确认：返回说明书后仍恢复霓裳云枢任务、6 个已验证模型、当前文档和全部历史版本；长任务以 SQLite job 为状态源，侧栏全局阶段/百分比和说明书页内部阶段持续轮询，离开页面不会取消任务。
- 图表工作台完成真实交互回归：节点选择、方向键微调、未保存修改提示、“撤销本次修改”、保存新版本入口和历史版本抽屉均可用；测试中的 1px 移动已撤销，没有污染正式图表版本。
- Draw.io 生成器升级为 `drawio-generator-v9`。架构、模块、部署图增加浅色分层容器与克制节点阴影，保留正交连接线、证据语义和可编辑 Draw.io 源文件；工作流继续使用清晰的两列蛇形/流程布局。
- 在真实任务中为系统架构、模块交互和部署图各创建一个可回滚新版，并装配 `software-manual.v17.docx`。LibreOffice 实际渲染 16 页，22 项检查全部通过，低密度页 0；重点复核第 5、11、13 页，分层图表、图注、表格和正文无裁切或孤立页。应用界面已恢复显示 v17。
- 新增零补齐页名回归覆盖；全量 Python 回归 97/97、Vite 生产构建和 `git diff --check` 均通过，临时本地走查连接钩子已移除。本轮仍处于验收调优阶段，没有打包。

下一步：

1. 用户在开发版验收说明书 v17 与源码 v3 的真实逐页预览，重点确认中间页密度、图表阅读性和错误重试提示。
2. 发起一次新的说明书长任务，生成中切换到源码或设置页，确认侧栏进度持续变化，返回后恢复同一任务与阶段。
3. 继续检查界面截图选择、我的资产和导出后的文件夹定位流程；用户确认文档与图表体验前不进入发布打包。

### 2026-08-11：用户成品复核——旧源码坏页对照、截图成章与数据流图重排

- 将用户实际导出的《霓裳云枢-V1.0-源代码文档.docx》重新交给 LibreOffice 全页渲染，确认该旧文件为 69 页且存在多张近空白代码页；同时复核当前源码 v3 为真实 60 页、空白页 0、最低正文填充率 86.92%。旧导出问题真实存在，但当前内部产物已经不是同一坏版本。
- 对说明书 v17 的 16 张真实 Word 页面逐页检查后，没有因 22 项 QA 通过就停止：发现数据流图仍是窄长“邮票”、截图与说明跨页断裂、第二张截图后直接接结论章三项视觉缺陷。
- Draw.io 数据流布局新增两种 A4 友好策略：纯串行长流程采用双列蛇形；包含一个双分支层的数据流采用紧凑梯形。真实“服饰数据上传与审核流程图”生成 v13，画布由窄长结构收敛为 760×686，8 条连线全部通过节点穿越和标签碰撞检查。
- 正式 Word 装配器将每张真实界面截图提升为带编号的二级小节，并从新页开始；截图、标题、六维操作说明不再跨页失联。截图章节结束后，下一主章节强制另起页，避免正文挤在截图说明下方。
- 使用原应用 SQLite 和现有素材生成 `software-manual.v19.docx`：17 张真实 Word 页面、8 章、5 张图、2 张截图。22 项检查全部通过，空白页和低密度页均为 0；第 8、15、16、17 页已按原始 PNG 复核。
- 说明书长任务继续以 SQLite job/step/progress 为唯一状态源，后台线程不依赖页面组件；说明书页每 1.5 秒恢复细分阶段，左侧栏每 1.8 秒跨页面展示进度。全局轮询现在优先显示任何仍在 queued/running 的任务，不会被列表首个已完成版本遮住。
- 全量 Python 99/99 项、Vite 生产构建和 `git diff --check` 通过；Apple Silicon Sidecar 重建为 34,917,696 字节，SHA-256 为 `5be8a2b9fbdcdddafb0b9b75e542db554d12af6bb85b9556e12b59ed68c4c9a9`。Tauri 开发版已连接原应用数据目录启动供验收；本轮只新增可回滚图表/说明书版本，没有清库、覆盖旧产物或构建安装包。

下一步：

1. 重建 Sidecar 并启动 Tauri 开发版，让用户验收源码 v3 真实 60 页和说明书 v19 真实 17 页。
2. 用户在说明书生成过程中切换到源码或设置页，核对侧栏任务版本、阶段、百分比持续变化，返回后仍为同一 job。
3. 继续按实机反馈调正文信息层级、图表资产编辑和导出体验；未验收前不打包。

### 2026-08-11：说明书从“文字墙”改为真正章节层级，v22 通过真实 Office 验收

- 不再把段落间距当作信息层级：新增可版本化的 `subheading` 结构块，贯通 AI 输出约束、证据规范化、章节编辑器、Word Heading 2、截图小节续号和 QA 同源渲染。
- 基于当前霿裳云枢真实任务，为 8 章补入 28 个有语义的二级小节；原正文、表格、图表、截图和证据引用全部保留，每一章修订都通过 `manual_section_revisions` 留存历史版本。
- QA 新增“推断性措辞”阻断项，正式材料中出现“根据项目证据推断”、“据此推断”或“合理推断”时直接不予通过；小节标题不被误计入正文深度或证据覆盖分母。
- v20 真实 LibreOffice 渲染发现第 16 页仅 3 行尾段、第 20 页仅测试结论，被新密度门禁正确标记为 `qa_failed`；没有降低阈值，而是对界面操作章和测试总结章应用紧凑但可读的局部版式。
- 内容人工复核继续清理 3 处“当前代码库未发现……”和裸露的 `placeholderInterval`，改为正面、可验证的实现边界及“搜索提示轮换定时器（placeholderInterval）”。
- 最终生成 `software-manual.v22.docx`：LibreOffice 真实渲染 18 页，23 项检查全部通过，空白页、低密度页、未装配图像、未绑定证据块和推断性措辞均为 0。18 页总览已逐页观察，并放大复核受影响的模块、界面操作和测试总结页。
- 全量 Python 回归 99/99、Vite 生产构建和 `git diff --check` 通过。Apple Silicon Sidecar 已重建为 34,918,336 字节，SHA-256 为 `97ec540015fda6ecd6f6fdd4c289c2a88bbe53e5f28f751f4eb4b88aea5168af`。Tauri 开发版已重启供验收，本轮没有打安装包。

下一步：

1. 用户在说明书页选择霿裳云枢，验收 v22 的 18 页真实预览，重点检查二级小节、图表字号和两张界面截图的章节位置。
2. 新发起一次说明书任务，生成中切换到源码或设置页，确认侧栏阶段、百分比和返回后的同一 job 持续恢复。
3. 继续验收图表工作台的拖动、文字编辑、撤销、保存新版和导出；用户确认前不进入打包。

### 2026-08-11：源码取样纠偏、图表实时连线与 Word 尺寸复核

- 重新审计当前源码 v3：60 页排版虽然通过，但 2950 行只来自 9 个后端 Java 文件，属于“版式合格、选材失败”。分页器升级为 `code-preview-v2`，按前端页面、组件、客户端、后端控制器、服务、领域和基础设施分层轮转，避免分数排序后直接截断前 2950 行。
- 源码预检和 DOCX 生成增加代表性门禁：持久化实际覆盖文件、层级和语言；旧的 9/264 集中样本必须重新预检，不能继续生成看似 60 页但结构失真的材料。真实项目临时模拟覆盖 19 个文件、9 类代码层、3 种语言。
- 源码 Word 生成器升级为 `source-docx-v4`，代码行高调至 14.2 磅。使用霓裳云枢当前源码生成临时 60 页平衡样稿并全部渲染复核：空白页 0，最低正文填充率 86.41%，文件切换覆盖前后端与基础设施，不再由少量 Controller/Service 垄断全文。
- Draw.io SVG 增加边的来源、目标、标签中心语义标记；图表工作台拖动节点时连接线和标签现在实时重排，撤销、轻微点击和保存失败时会恢复原路线，不再出现“节点已移动、连线留在原地”的半成品状态。
- 架构图允许三列数据层并补充端点中心候选路线，消除跨层长边绕过标题区的问题；模块图保持两列并提高 Word 最大展示高度。用现有 5 张真实图重新装配审计样稿，实际 Office 渲染仍为 18 页。
- 审计样稿 23 项检查全部通过：缺失图像、空白页、低密度页均为 0；正文页最低填充率 57.87%。架构图在 Word 中使用更多页面宽度，节点和连线标签比 v22 更易读，没有通过增加独立空白图页来换取字号。
- 说明书长任务继续以 SQLite 中的 job、step 和细分进度为唯一状态源；说明书页面重新挂载时主动读取正在运行的 job，应用侧栏独立轮询并显示全局百分比，离开页面不会取消后台线程或丢失任务状态。
- 全量 Python 回归 102/102、Vite 生产构建和 `git diff --check` 全部通过。Apple Silicon Sidecar 已重建为 34,919,024 字节，SHA-256 为 `e4e3a29f8c5f03bcf8a0067ddaab0e59326cf17aca466b127fccb17dc48f5f12`；Tauri 开发版已连接原应用数据目录重新启动。本轮没有构建安装包。

下一步：

1. 用户直接在当前开发版复验：启动说明书长任务后切到源码或设置页，确认侧栏仍显示同一 job 的阶段与百分比，返回后进度和历史版本不丢失。
2. 在应用里对霓裳云枢重新扫描并执行新版源码分页预检，确认界面显示实际覆盖层级/语言，再生成正式新版源码文档。
3. 继续收紧 AI 正文中的绝对化测试结论和证据粒度；用户确认文档、图表及长任务体验前不进入打包。

### 2026-08-11：用户导出件溯源、旧分页硬阻断与跨页任务即时恢复

- 对用户提供的《霓裳云枢-V1.0-源代码文档.docx》计算 SHA-256 并与应用历史产物比对，确认它精确对应 `source-code.v1.docx`（`463804…651e`），而不是当前平衡取样版本。真实 LibreOffice 渲染为 69 页，预检文件记录为 `code-preview-v1`；第 3、8、13 等页只剩一行，根因是旧分页结果被 Word 自动换行再次撑页。
- 源码 DOCX 生成增加分页器版本硬门禁：数据库记录和 JSON 产物都必须等于当前 `code-preview-v2`；旧预检不能再直接生成文档。源码材料快照同步暴露旧版本阻塞原因，界面明确提示重新执行分页预检，避免继续导出历史坏版。
- 说明书页面不再在重新挂载时先清空任务：应用级轮询的同一 job 会作为即时恢复快照传入说明书页，随后再与 SQLite 完整版本列表合并。生成中切换页面返回时，任务版本、阶段、百分比、运行时长和最近更新时间立即可见。
- 完成或失败的任务也不再从主页面消失：新增“最近一次生成任务”状态卡，持久显示最终阶段、百分比、完成时间和安全错误；完整六阶段留痕仍保留在高级区域。
- 长任务阶段卡补充当前子步骤标题，例如模型分析源码、具体章节和图表名称，不再只显示抽象的 `2/8`；侧栏继续显示跨页面全局进度。
- 说明书提示词和 QA 升级：没有测试报告、日志或验收记录时，禁止“指标达到预期、均通过验证、具备上线条件、已全面验证”等结论；`manual-docx-qa-v6` 将命中的无证据测试/上线结论直接阻断。
- 新增旧分页阻塞和无证据结论回归覆盖；全量 Python 103/103、Vite 生产构建通过。Apple Silicon Sidecar 重建为 34,921,360 字节，SHA-256 为 `121364b4bc1cc50ebd4310cd1aa21b08fcabbb11ce32acea7e3ae7a631a1e2d8`。Tauri 开发版已加载新 Sidecar 启动，继续使用原应用数据库；没有打安装包。

下一步：

1. 用户在源码材料页对霓裳云枢重新执行“代码分页预检”，确认旧版本提示消失、实际取样文件/层级/语言更新，再生成并逐页验收新版 DOCX。
2. 发起说明书长任务后切到设置或源码页，观察侧栏进度；返回说明书确认同一任务立即显示阶段、当前子步骤、耗时和最近更新时间。
3. 继续根据新版真实导出件检查正文事实密度、图表编辑反馈和截图选择流程；用户确认前不进入打包。

### 2026-08-11：真实项目稳定快照复测——源码跨文件取样与图表标签避碰

- 继续使用用户实际项目复测。由于该目录同时被其他任务修改，先制作排除 `.git`、依赖和构建产物的只读稳定快照，避免测试期间源码指纹漂移，也不干扰用户正在进行的开发工作。
- 源码分页器升级为 `code-preview-v3`：每个文件首轮最多连续抽取 100 个可视代码行，并按业务层轮转；只有首轮仍不足 2950 行时才进入同一文件的后续片段。相同项目从旧版仅 9 个文件扩展为 35 个文件、9 类代码层和 4 种语言，单文件不再连续占据六七页。
- 源码筛选规则升级为 `source-selection-v3`，明确排除 Lottie 动画数据和 assets 中的生成式数据脚本，避免大段坐标/关键帧数组挤占软著源码材料。
- 用稳定快照实际生成 `source-code.v3.docx` 并进行正式 Office 渲染：共 60 页，空白页 0、低密度页 0、最低正文填充率 88.03%，19 项源码文档检查全部通过。该产物仅用于隔离验证，没有覆盖用户应用里的历史版本。
- 对原应用数据库中真实模块图、系统架构图和数据流图重新渲染审计，定位到模块图中“验证用户凭证”和“存储消息记录”两条边标签实际重叠。Draw.io 内部 SVG/PNG 渲染器升级为 `drawio-generator-v10`，边标签会把已经放置的标签也作为障碍物；真实模块图复渲染后两处文字已分离。
- 新增“大文件不能垄断分页”和“边标签之间保持间距”的回归覆盖。全量 Python 104/104、Vite 生产构建和 `git diff --check` 全部通过。Apple Silicon Sidecar 已重建为 34,922,096 字节，SHA-256 为 `71869e4aad7f551d9734d7df6e8e0f46dd5cb6f82c31509b754e210d7105959b`。
- 用户说明 Tauri 窗口仍在另一个 macOS 桌面空间；后续测试不再主动切屏或抢占焦点。当前仍是验收调优阶段，没有构建安装包。

下一步：

1. 无打扰重启当前开发版 Sidecar，使源码筛选与图表渲染新版本生效；不切换用户桌面空间。
2. 用户有空返回应用后，对目标项目重新扫描、重新生成筛选计划与分页预检，再生成新源码 DOCX；旧 v1 产物不会自动冒充新版。
3. 继续以实际说明书生成任务检查跨页面进度恢复、图表工作台修改反馈和最终 Word 的内容密度；用户确认前不进入打包。

### 2026-08-11：后台实机复测——冷启动修复、长任务恢复与读者化图表

- 用户确认开发窗口实际已在另一个 macOS 桌面空间打开；后续不再把“当前屏幕不可见”误判为启动失败，也不主动抢占用户正在使用的桌面空间。
- 定位侧栏偶发 `sidecar handshake timed out` 的冷启动竞态：桌面端握手等待由 12 秒调整为 45 秒，持续收集 Sidecar 标准错误，超时或提前退出时主动清理子进程并返回诊断信息，避免残留孤儿进程。
- 使用隔离数据库和后台浏览器实际操作说明书长任务：切换到源码页后侧栏继续显示同一 job 的阶段、当前子步骤和百分比；返回说明书后从 SQLite 恢复详细步骤。版本区默认只展示最近 6 个版本，可按需展开全部历史，避免二十多个旧版本挤满页面。
- 修复图表资产入口的真实阻塞：当最新说明书任务尚未生成章节或已失败时，前端继续查找更早的可用正式图表；Sidecar 图表列表接口将业务异常稳定转换为 HTTP 400 JSON，不再产生 ASGI 未捕获异常。
- 在隔离数据中完整操作图表工作台：选择节点、方向键 1 px/10 px 微调、未保存提示、撤销、保存新版本和历史版本均成功；旧版本没有被覆盖。
- 真实检查五张 PNG 后发现主要视觉问题不是 Draw.io 可编辑性，而是将 `UserAuthPage`、`loginUserStore`、`doSearch` 和接口路径直接显示在正式图中。图表语义现采用双层标签：原始标识保留用于证据追踪，画面使用“用户认证页面”“登录用户状态”“搜索与筛选处理”等中文业务名称；提示词同步禁止把源码路径和代码标识当作可见节点名。
- 以现有语义重新渲染“服饰数据上传与审核流程图”和“用户认证与首页浏览操作流程”，接口路径、Page/Store/方法名已从画面移除，连线、节点和步骤说明保持可读。
- 说明书正文质量门禁升级到 `manual-docx-qa-v8`：每章至少 4 个实质块、2 个完整正文段落、至少 320 字连续叙述；引言不少于 750 字，其他章节不少于 950 字。提示词要求 1000–1800 字并明确禁止用短列表或表格代替技术叙述。旧的短提纲版本将明确失败，不能再作为正式通过版导出。
- 源码隔离实测继续采用 `code-preview-v5`：60 页、33 个文件、9 个代码层级，空白页和低密度页为 0，19 项检查全部通过。说明书装配 v23 的 18 张真实 Office 页面已复核，最后一张截图后可继续利用页面剩余空间；旧正文中的无证据上线结论和内部文件名被新门禁正确阻断。
- 全量 Python 回归 109/109、TypeScript、Vite 生产构建、Rust `cargo check` 和 `git diff --check` 全部通过。Apple Silicon Sidecar 已重建，SHA-256 为 `95cb9223fa109ced0e81217f55acbafb145b45af689a68d9773bb72c116f83d6`；仍未构建安装包。

下一步：

1. 无打扰重启一次 Tauri 开发版，使最新 Sidecar、正文门禁和图表标签策略加载到用户验收环境。
2. 用户有空时对目标项目重新执行源码分页预检并生成新版；旧分页版本会被明确阻断，不能冒充当前合格版本。
3. 发起一轮新说明书任务，重点验收正文事实密度、图表中文业务标签、跨页面进度恢复和最终 Word 逐页效果；确认前继续调优，不打包。
### 2026-08-11：交付标准版本化与历史产物降级

- 修复旧产物误显示为当前“质检通过”的问题：源码文档同时核对 `source-docx-v4` 与 `source-docx-qa-v4`，说明书同时核对 `formal-manual-docx-v6` 与 `manual-docx-qa-v8`。
- 历史生成器或历史质检策略下曾通过的文件现在标记为“历史标准已过期”；仍可在程序内预览，但不能导出为当前交付件。
- 源码材料、说明书和“我的资产”三处采用同一质量状态；桌面端导出命令及说明书下载接口也加入服务端守卫，不能仅靠前端按钮绕过。
- 使用正式数据库只读副本复核：现有源码 v2/v3 与说明书 v21/v22 均被正确识别为历史版本；未修改正式数据库或用户文件。
- 完整回归通过：110 项 Python 测试、TypeScript 类型检查、Rust `cargo check` 与 `git diff --check` 均通过。
- 重建并重启开发版 sidecar：`c03f2e39bb70a6a1f6ef30d94590d8381f92b339cd39308c24787291c42254cf`；冷启动握手成功。
- 尝试在隔离副本中用真实模型生成单章做质量复测时，因该操作会将项目证据发送到外部模型服务而被安全策略阻止；未绕过、未产生外部调用，等待用户明确授权后再执行。
### 2026-08-11：验收环境重置

- 停止开发进程后备份正式 SQLite 数据库，再清空项目来源、扫描快照、任务、阶段、源码材料、说明书、截图和图表等业务记录。
- 清理结果复核：项目、任务、说明书任务和源码文档记录均为 0；6 个模型配置与 1 份加密 API 凭据完整保留，通用设置表未改动。
- 数据库备份位于应用数据目录的 `app.before-task-cleanup-20260811.db`；历史任务文件目录未删除，避免扩大用户要求的清理范围。
- Tauri 开发版已重新启动，Sidecar 冷启动和健康握手成功，等待用户从空任务列表连续执行两轮验收。

### 2026-08-11：失败图表防崩溃、说明书工作台重构与质检恢复路径

- 定位图表页白屏根因：失败图表被列表接口作为 `v0` 返回，前端仍按可编辑资产访问不存在的 `semantic.nodes`，导致 React 页面同步崩溃。列表接口现返回稳定语义结构和 `available` 状态；失败图表显示“生成失败 / 可重试”，可单图重新生成，不再读取资产或拖垮窗口。
- 图表 SVG 预览增加格式异常边界。即使单个 SVG 损坏，也只在画布内显示错误提示，不再造成整个桌面页面白屏。
- 说明书逐页预览改用与源码文档相同的桌面文档外壳：左侧页码导航、快速跳页、固定上一页/下一页、真实 Word 渲染页；质量问题收纳为紧凑处理栏，不再与顶部控制区重叠。
- 说明书编辑器改为完整的章节导航、主编辑区和固定操作栏；状态由 `generated` 等内部值改为“AI 初稿 / 人工确认 / 已修订”，未保存章节数量显式提示，段落输入区和信息层级重新排版。
- 质检失败不再形成死路：主页面新增恢复卡，可直接导入真实截图、查看未通过项、继续编辑或导出明确标注的“审阅稿”。正式 DOCX 仍要求当前内容、完整性和质量检查全部通过；审阅稿不会伪装成正式材料。
- 截图资产页返回按钮改为“返回说明书并重新装配”；截图保存后说明书会识别资产已更新并提示生成新版本。
- 使用用户当前真实数据库的只读副本复核失败图表：`data_flow_diagram` 被稳定识别为 `available=false / failed / v0 / 0 nodes`，其余三张真实图继续保持可编辑。
- 验证通过：110 项 Python 回归、Vite 生产构建、Rust `cargo check` 与 `git diff --check`。Tauri 开发版已重新启动，Sidecar 地址为本次动态本地端口；没有构建安装包。

下一步：

1. 用户在图表资产页点击原第二张失败图，确认页面不再白屏，并看到单图重试入口。
2. 用户打开说明书“编辑内容”和“逐页预览”，验收新的章节编辑器与源码同款页码导航。
3. 对当前缺截图、质检失败版本分别测试“导入真实界面截图 → 返回说明书重新装配”和“导出审阅稿”；正式导出门槛保持不变。

### 2026-08-11：统一项目删除与资产清理入口

- 复现“最近任务清理失败”：含说明书流程的任务仍被 `manual_draft_runs` 和 `manual_generation_jobs` 直接外键引用，旧删除逻辑遗漏这两张后加表，SQLite 因此拒绝删除父任务。
- 删除服务现先清理说明书草稿和生成任务，生成任务下的章节、图表、截图、文档与质检记录继续通过外键级联删除；模型配置、加密 API Key 和原始项目目录不受影响。
- 删除请求改用带 Sidecar 自动重连的本地请求封装；连接端口变化时不再只显示 WebView 的 `Load failed`，后端错误也会返回可读信息。
- “我的资产”项目卡新增“删除”入口，与项目概览使用同一删除接口、同一确认语义；删除成功后立即移除资产卡，并清理当前选中的任务状态。
- 新增包含说明书草稿与正式生成任务的删除回归测试。使用用户真实数据库只读副本对 `nichangyunshu / 50be4e46…` 实测删除成功，正式数据库没有在测试中修改。
- 验证通过：111 项 Python 回归、Vite 生产构建与 `git diff --check`。Tauri 开发版已经重新启动供验收，没有构建安装包。

下一步：

1. 在项目概览点击单个 × 或“清理全部”，确认任务与应用内产物删除且原项目目录保留。
2. 在“我的资产”点击项目卡右侧“删除”，确认资产卡即时消失；模型配置应继续保留。

### 2026-08-11：说明书有限并发、持久化执行节点与真实导出回执

- 重新审计说明书长任务后确认主要耗时来自 8 个章节和多张图表严格串行调用模型；图表失败还把模型语义、本地 Draw.io 构建和 SVG/PNG 渲染混成同一种错误，造成无效重试和模糊反馈。
- 正文分章与图表生成改为有限并发：应用默认并发 3，实际并发取“任务设置”和“连接并发上限”的较小值。商汤 SenseAudio Ultra 预设连接上限为 10，其余常用连接默认 3；设置页可独立调整连接上限和任务并发数。
- 新增 SQLite `manual_execution_nodes`：每章、每张图、项目研究、截图决策、Word 装配和逐页质检均记录节点状态、依赖、模型、尝试次数、心跳、耗时、输出摘要、错误类别和安全错误信息。页面切换后直接读取持久化节点，不依赖 React 临时状态。
- 说明书页的实时任务卡增加模型、任务心跳、阶段耗时、尝试次数与并发子任务列表；子任务逐项显示等待、运行、完成或失败及耗时。旧任务没有节点数据时继续兼容阶段摘要。
- 图表错误拆分为模型请求、语义解析、语义校验、Draw.io 构建、Draw.io 校验、SVG 渲染和 PNG 渲染。本地构建/渲染失败会保存已经生成的语义，单图重试时只重做本地步骤，不再重复消耗模型；图表资产页每 2 秒自动刷新后台结果。
- 说明书导出改为同目录临时文件写入、同步、原子替换和逐字节落盘校验；覆盖已有文件时验证失败会恢复备份。Sidecar 重新计算文件大小和 SHA-256，与文档产物校验和匹配后写入 `manual_export_records`，前端展示实际路径、大小、摘要和导出回执。
- 数据库迁移升级到 v26，新增并发设置、导出记录与执行节点回归测试。最终全量验证为 Python 117/117、前端生产构建、Rust `cargo check` 和 `git diff --check` 全部通过。
- Apple Silicon Sidecar 已重建，大小 34,939,072 字节，SHA-256 为 `49e3ef66192551b84238c682b2dafdce3c42bf011dd09f5396ab2564c6a15769`。Tauri 开发版已重新启动，Sidecar 在新的随机本地端口完成握手；仍未构建安装包。

### 2026-08-11：显式授权的项目启动与真实界面采集

- 新增受控截图启动方案：Sidecar 只从项目 `package.json` 的 `dev`、`start`、`serve`、`preview` 脚本生成不可篡改的候选 ID，不接受前端传入任意命令；工作目录、完整命令、脚本原文和预计本机地址均在用户授权前展示。
- Tauri 新增项目采集会话，支持 npm、pnpm、yarn 与 bun 的已识别脚本。用户必须勾选授权后才能启动；会话记录 PID、持续时间和日志尾部，切换页面后仍可恢复查看，并能跨平台停止整个启动进程树。
- 截图地址严格限制为不含凭据的 `http://127.0.0.1`、`http://localhost` 或 `http://[::1]`，且采集地址必须与已授权会话使用相同主机和端口；采集前先验证页面返回成功状态，避免把服务错误页写入说明书。
- 采集使用本机 Chrome、Edge、Chromium 或 Brave 的隔离无界面配置，不复用用户浏览器资料。PNG 完成即回收浏览器进程组，不再等待 Chrome 更新器；在本机 Chrome 上实际生成 1440×1000、82,159 字节的 PNG 并完成视觉检查。
- 自动采集的图片仍进入原有图片净化、尺寸校验、SHA-256 完整性、版本历史、章节绑定和六维说明流程；保存后会把原先跳过的截图执行节点更新为已完成，并触发旧 DOCX 的内容过期提示，要求重新装配。
- 商汤 Ultra 连接仍采用“能力上限 10、当前说明书任务默认 3”的双层控制：多模态能力最多可配置 10 路，而正文与图表的单任务默认保留 3 路，避免一个长任务耗尽共享积分池与连接额度。
- 验证通过：Python 119/119、Rust 11/11、前端生产构建、`cargo check` 与 `git diff --check`。Apple Silicon Sidecar 大小 34,942,928 字节，SHA-256 为 `ecbdcbdbbfd84bf31180add74ab8f7ebd829751070b6858dfa768e90438dfdff`；仍未构建安装包。

下一步：

1. 在真实 Node/Web 项目上验收授权启动、日志、页面采集、停止进程和截图入库五个操作。
2. 基于说明书正文和已识别路由增加“建议采集页面清单”，让用户逐项授权采集，而不是手工填写每个 URL。
3. 对无法由本机浏览器访问的桌面端或移动端项目保留人工导入，并补充平台专用采集适配器，不以模拟截图冒充真实界面。

下一步：

1. 重建并无打扰重启开发版供用户发起新任务验收。
2. 用真实任务验证并发下的章节事实密度、图表成功率、执行节点恢复和审阅稿真实落盘，不在用户确认前打包。
3. 自动截图采用显式授权适配器：先展示将执行的启动命令、端口和停止方式，用户确认后才启动项目并采集；没有适配器时继续使用人工导入，不默认执行陌生项目代码。

### 2026-08-11：说明书阶段审阅稿、非阻塞资产节点与任务驾驶舱

- `ManualDocumentService` 新增正文阶段检查点：章节正文完成后立即装配独立的 `review_checkpoint` DOCX，只读取已完成正文，不等待图表或截图，不推进正式装配与 QA 阶段；产物使用独立文件名，可在任务仍运行时读取、预览正文并按审阅稿真实落盘。
- `ManualWorkflowService` 将阶段审阅稿、章节图表和截图决策改为并行兄弟节点。图表全部失败、截图不可用或规划异常会保留失败/警告节点，但不再阻断当前可用资产的正式候选稿装配；阶段审阅稿失败也不会抹掉正文或其他节点结果。
- 截图节点新增持久化 `waiting_for_authorization` 状态。没有真实截图且需要人工导入或授权采集时，节点明确显示等待授权和下一动作；本轮没有自动启动任何用户项目。
- 图表模型调用收敛为“首次生成＋最多一次结构修复”，执行节点同步记录真实模型尝试次数。失败语义与错误分类继续保留，资产页可单图重试；本地 Draw.io、SVG、PNG 失败优先复用已有语义，不整单重跑。
- 任务总进度改为从 `manual_execution_nodes` 实际聚合，返回排队、运行、完成、警告、失败、等待授权的节点计数；节点补充持续时间、模型、依赖、产物路径和下一动作。数据库迁移升级到 v27。
- 说明书页升级为任务驾驶舱：按研究、正文章节、章节图表、截图与授权、审阅稿/候选稿、真实渲染 QA 分组，展示模型、并发、心跳、预计剩余时间、耗时和尝试次数；已完成产物可直接切换查看，失败章节/图表提供单项重试，截图只进入显式授权或人工导入入口。
- 阶段审阅稿不能调用正式逐页 QA 或冒充正式交付件；正式导出仍要求当前生成器、当前 QA 策略、内容新鲜度和完整性全部通过。
- 完整验证通过：Python 122/122、Rust 11/11、`cargo check`、前端生产构建与 `git diff --check` 全部通过；未构建安装包、未提交、未推送、未清理数据库。
- Apple Silicon 开发 Sidecar 已重建为 34,947,104 字节，SHA-256 为 `7f47c2e96c85bea75f0a100ff26a5e199e37990b6b14bee7547a19544c3a8754`；旧开发进程按已确认 PID 受控退出后，Tauri 开发版已在原应用数据目录完成握手并启动供验收。

下一步：

1. 在开发版发起一轮新说明书任务，验收正文完成后阶段审阅稿是否在图表仍运行时立即出现，并实际导出核对落盘回执。
2. 验收图表失败、截图等待授权时正式候选稿与任务驾驶舱是否保持可操作；分别执行单图重试和人工补截图后重新装配。
3. 继续按真实项目逐页检查正文事实密度、图表中文业务标签和最终 Word 质量；用户确认前不打包。

### 2026-08-11：真实长任务复盘——截图误判、任务收尾交互与内容质量门禁

- 复盘真实任务 `718a7787…` 及用户提供的全程截图，确认旧界面把“节点已处理 100%”错误呈现得像产品验收完成：实际仍有 1 个正文章节超时、2 张图表本地校验失败、0 张界面截图。终态页面现在只保留紧凑的遗留问题摘要，并明确显示“已产出，仍有问题”和单项下一动作；完整驾驶舱仅在任务运行时展开，双列卡片不再被最高列强行拉出大片空白。
- 修复“查看正文”误触发单章重生成：已完成章节始终打开独立只读正文浏览器；重生成只存在于编辑区或失败节点的“重试此项”。失败章节和失败图表在运行中及任务结束后均保留单项重试按钮，不会整单重跑。
- 截图未触发有两个真实根因：界面章节生成超时后，截图服务只看章节结果而把整个 Web 项目判成“不适用”；启动候选只扫描项目根目录 `package.json`，遗漏了 `nichangyunshu-fronted/package.json`。现在项目清单中的 Vue/React/Vite/TSX 等界面证据可独立判定适用性，并在不进入 `node_modules` 或构建目录的前提下扫描最多三层子项目启动脚本。
- 截图候选规划新增为研究完成后的独立异步节点，与正文分章生成同时运行；该节点只读项目清单，绝不启动用户项目。识别到安全启动候选后仍进入“等待授权”，只有用户明确确认命令、工作目录和本机地址后才可能启动；章节失败、无可启动脚本或单页失败只产生警告/等待，不阻断阶段审阅稿和当前资产候选稿。
- 逐页阅读用户导出的正式稿 13 页和阶段审阅稿 12 页后，确认基础排版可读，但本轮内容不能作为正式合格材料：正式稿缺失整章“用户界面与操作说明”、0 张截图、2/4 图表失败，结尾页大面积空白；正文还存在较多泛化叙述和无法仅凭源码证明的“测试验证完整链路”等结果性结论。阶段稿此前也没有在正文内醒目标识资产与质检尚未完成。
- 说明书 QA 升级为 `manual-docx-qa-v9`：根据研究产物校验必要章节、必要章节图表和 UI 截图覆盖；缺章、缺必要图、识别到 UI 却无真实截图均为 blocker。结果性测试/验收/上线结论识别范围扩展；正文提示词禁止在证据未直接支持时补写缓存 TTL、端口、盐值、重试次数、设备数、审计、备份等精确实现配置，也禁止把“存在测试代码”写成“测试已验证通过”。
- 阶段审阅稿封面和目录现在明确写明“仅供正文审阅；图表、界面截图与最终质量检查尚未完成”，避免与正式候选稿混淆。正式候选稿即使成功落盘，只要覆盖门禁未通过，也只能导出为带风险提示的审阅稿。
- 新增浅层前端包识别、界面章节失败但项目 UI 证据仍需截图、共享并发槽、阶段稿标识及异步截图规划顺序等回归。完整验证通过：Python 125/125、Rust 11/11、`cargo check`、TypeScript、Vite 生产构建和 `git diff --check`。
- Apple Silicon Sidecar 已重建为 34,950,448 字节，SHA-256 为 `338469b081c3d7aec76802571f7c754381c7792a580fecedd77a5456326c6859`。旧 Tauri、Vite 残留进程按已确认 PID 受控退出，开发版已在本地随机端口完成新 Sidecar 握手；未构建安装包、未提交、未推送、未清理业务数据，也未启动用户项目。

下一步：

1. 用户先验收已完成章节“查看正文”只读浏览、失败章节/图表单项重试，以及终态只展示遗留问题摘要的交互。
2. 新建一轮说明书任务，确认“界面截图候选规划”在正文生成期间出现；待授权节点应展示 `nichangyunshu-fronted` 的明确启动候选，但未经用户确认不得启动。
3. 补齐真实截图并修复失败章节/图表后重新装配，重点检查 v9 QA 是否正确阻断缺章、缺图、缺截图和无证据结果性结论；用户确认文档内容前仍不打包。

### 2026-08-11：按章即时派图、紧凑任务驾驶舱与前后端路由采集

- 复盘用户新一轮真实长任务与 20 页审阅稿后确认：正文分章虽已并发，但 `ManualWorkflowService` 仍等待全部章节完成后才调用整批图表生成；“尝试 3/3”也把仍在等待返回的第三次请求表达得像已经失败。现在正文持久化单章后立即回调派发该章图表，图表与尚未完成的其他正文共享同一任务并发上限，不再存在全正文到全图表的串行栅栏。
- 图表增量执行新增开始、按章生成和汇总收尾接口。每个图表继续只允许首次语义 JSON 请求加一次结构修复，本地 Draw.io/SVG/PNG 构建失败仍保留节点供单项重试；阶段审阅稿只等待正文，图表失败不阻断正文和当前资产候选稿。
- 正文和图表自动重试会把上次解析/校验失败原因写入持久化执行节点。驾驶舱不再显示含混的 `3/3`，改为“第 3 次请求进行中（已自动重试 2 次）”“已尝试 3 次，可单项重试”等状态。慢模型读取上限由 180 秒调整为 300 秒，避免 20B+ 长正文在正常生成时被过早断开并制造无效重试。
- 运行中任务页重新组织为六段状态条、“当前正在处理”和“最近完成与待办”两个紧凑面板；全部真实节点、依赖、模型、产物和下一动作保留在按需展开区。移除了只有一个研究节点却占据半页高度的左右等高布局，首屏可直接看到并发执行与异常。
- 截图启动方案新增安全的多服务候选：浅层 Vue/Vite 前端和带 Spring Boot 插件的 Maven 后端可在一次明确授权后共同启动，任一服务退出会显示“部分服务已退出”并停止采集；应用仍不会注入凭据、启动 MySQL/Redis 或执行任意命令。对 `nichangyunshu` 实际识别结果为 `mvn spring-boot:run；npm run dev`。
- 截图页从 Vue Router 等静态源码提取无参数本机路由，点击推荐页面即可用隔离浏览器自动导航、等待 5 秒渲染并采集，保存前仍需核对六维说明。新截图会用 64×64 灰度差异与现有资产比较，高度重复的空首页被拒绝；QA v10 也会阻断标题和说明完全相同的重复截图。
- 逐页检查用户审阅稿确认 8 章、4 图、4 个截图位置，但压缩包中三张 1440×1000 截图的低分辨率视觉差异为 0，文档实际反复使用同一空首页；图表第 4 张字号偏小，最后一页填充不足，因此本稿仍不能作为正式质量验收通过件。
- 完整验证通过：Python 128/128、Rust 12/12、`cargo check`、TypeScript、Vite 生产构建和 `git diff --check`。Apple Silicon 开发 Sidecar 已重建为 34,957,904 字节，SHA-256 为 `51b404f16ad0c20bea77c271c3683cd56fb9d81e1049445d69c4824ce25df80a`；未构建安装包、未提交、未推送、未清理业务数据，也未自动启动用户项目。

下一步：

1. 在开发版新建说明书任务，验收某章完成后其图表节点是否在其他正文仍运行时立即出现，并观察新的重试原因与状态文案。
2. 在截图页明确授权“前端 + 后端”方案；确认项目自身的 MySQL、Redis 与密钥配置已准备后，再逐项点击推荐路由采集。重复空首页应被拒绝，真实不同页面才可保存。
3. 补齐不同业务页面截图、修复失败图表并重新装配；继续人工审阅事实质量和页面布局，用户确认前不打包。

### 2026-08-11：Draw.io 确定性失败恢复与正文证据驱动的截图候选

- 复盘真实任务 `8792bbd3…` 的三张失败图确认：模型已经生成并持久化有效语义 JSON，失败全部发生在本地连线标签布局。`cos_download_flow` 有 3 个标签压住节点，`nichangyunshu_module_arch` 和 `ui_search_waterfall_workflow` 各有 1 个；旧单项重试复用相同语义和固定 10px 标签偏移，因此必然重复同一失败。
- 本地标签布局现在沿同一连线段依次尝试 10/34/58/82px 安静通道，在不重新请求 AI 的前提下避开节点。三个真实失败语义均已在本地重新构建并通过结构校验；失败消息也保留具体碰撞原因，不再只显示无法定位的统一文案。
- 截图候选不再把所有源码路由显示为同等“推荐”。启动方案会解析路由名称、组件路径和登录要求，再与持久化的“用户界面与操作说明”正文及证据引用匹配，分成“正文对应”“正文关键词”和“仅路由”。
- 对当前霓裳云枢任务实测，只有 `/waterfall` 通过 `WaterFullPage.vue` 与正文“瀑布流布局与内容检索交互”建立直接证据关系；首页和其他路由明确标为“仅路由”，不能盲目保存为软著截图。候选仍必须在用户授权启动后采集，保存前人工核对六维说明；需要登录的页面明确提示先准备测试账号和数据。
- 已对真实任务的三个失败节点执行原位本地恢复，任务当前共有 5/5 张 rendered 图表；首次结构恢复后目视检查又发现下载数据流的双向标签互相覆盖，因此校验器继续补上“标签—标签”和画布边界检查，水平图为回路线增加顶部通道。三张恢复图随后创建本地 v2，重新目视确认标签可分辨。
- 使用最新图表装配正式候选稿 v4 并重新执行 LibreOffice QA。`content.figure_coverage` 已清除，当前只剩 `content.ui_screenshot` 与末页密度 `render.page_density`，证明单图恢复链路已真实闭环且没有整单重跑。
- 完整验证通过：Python 130/130、Rust 12/12、`cargo check`、TypeScript、Vite 生产构建和 `git diff --check`。Apple Silicon Sidecar 已重建为 34,959,808 字节，SHA-256 为 `6d6f069f42cb44f7ff71dfadd581b2b49e7678dc22344607f8dbdec424c414bf`；未构建安装包、未提交、未推送、未清理数据库，也未自动启动用户项目。

下一步：

1. 在开发版对任务 `8792bbd3…` 的三张失败图分别执行“重试此项”，确认只做本地构建与渲染，并用最新资产重新装配以清除 `content.figure_coverage`。
2. 后端启动失败时先由用户准备项目自身依赖；应用只报告部分服务退出，不以无数据前端页面冒充真实业务截图。
3. 采集 `/waterfall` 等“正文对应”页面后继续人工检查截图内容、图表字号和 Word 分页；用户确认文档质量前仍不打包。

### 2026-08-11：阶段稿资产回填入口、单列节点流与稳定验收进程

- 核对真实任务 `ff643ab2…` 后确认：阶段审阅稿 v1 在正文完成时按设计以 0 图落盘；5 张图随后全部生成，最后一张在桌面进程重启后通过单项重试恢复，但任务已标记为失败，自动正式装配没有继续。旧界面又只给“过期正式稿”显示重新装配按钮，导致阶段稿没有任何可发现的补救入口。
- 阶段审阅稿结果卡新增明确的资产回填说明和“用当前资产装配候选稿”动作。该动作使用当前正文、图表与截图生成新的正式候选稿并继续逐页 QA，原阶段稿保持不变；同时显示当前已完成图表数，并保留补充真实截图入口。
- “查看全部执行节点”改为严格单列顺序流：阶段占满整行，阶段内节点逐条排列，消除短研究卡与长正文卡左右并排产生的大面积空白。
- 源码材料页明确说明源码 DOCX 是确定性取样而非模型随机写作：同一项目快照、同一严格度和同一分页规则会得到相同源码正文；项目源码变化后必须先重新扫描。当前霓裳云枢数据库只有一个扫描任务与一个源码 DOCX 版本，两份历史导出正文取样一致但封面软件名不同，不是串任务复用旧文件。
- 复盘闪退终端确认：Tauri 开发监视器报告 `src-tauri/binaries/copyright-agent-sidecar-aarch64-apple-darwin.json changed` 后主动重建并重启桌面进程，不是采集 HTTP 或浏览器截图异常导致主进程崩溃。新增 `tauri:dev:stable`，关闭 Rust/Sidecar 文件热监视但保留前端热更新，后续长任务与采集验收统一使用稳定模式，避免开发资产事件中断 Sidecar 和说明书任务。
- 验证通过：Python 130/130、Rust 12/12、`cargo check`、TypeScript、Vite 生产构建和 `git diff --check`。本批未清理数据库、未启动或停止用户项目、未构建安装包、未提交、未推送；用户仍需人工验收说明书内容与截图质量。

下一步：

1. 以稳定开发模式重新打开应用，在任务 `ff643ab2…` 的阶段稿下点击“用当前资产装配候选稿”，确认新稿包含现有 5 张图并进入逐页 QA。
2. 用户自行确认当前 5173 端口上的前端进程是否继续保留；后端依赖未满足时不采集空白前端页，也不由应用擅自补启数据库或缓存服务。
3. 补充与正文直接对应的真实业务截图后再次装配，继续人工审阅事实质量和版式；用户确认前不打包。

### 2026-08-11：说明书执行依赖图

- 将任务驾驶舱“查看全部执行节点”从阶段纵向列表改为真实依赖图。图中以项目研究为根节点，研究完成后并行派生正文章节与截图授权分支；八个正文章节以紧凑网格展示，每个图表节点依据持久化 `dependencies` 挂载到实际来源章节下，不由前端按标题猜测关系。
- 依赖图明确展示“章节完成即派发本章图表”和“当前可用资产汇合”：正文可先形成阶段审阅稿，图表、截图随后进入正式候选稿，最后执行逐页 QA。失败、警告、等待授权、运行和完成使用独立状态色，失败图表与章节仍保留单项重试。
- 图节点默认只显示标题、真实状态、耗时和尝试情况；模型、依赖、上次失败原因、产物路径和下一动作收进单节点详情，避免重新退化成长列表。任务结束后仍保留“查看本次执行依赖图”，便于复盘，不只在运行期间可见。
- 依赖图对窄窗口自动从“正文主分支＋截图旁支”收敛为单列，交付节点的横向箭头同步改为纵向箭头；本轮只通过 Vite 热更新刷新前端，没有重启 Tauri 或 Sidecar。
- 验证通过：Python 130/130、Rust 12/12、`cargo check`、前端生产构建和 `git diff --check`。稳定开发进程持续运行；未构建安装包、未提交、未推送、未清理数据库，也未启动或停止用户项目。

下一步：

1. 在当前开发版展开“查看本次执行依赖图”，核对八章与五张图的父子归属是否符合产品认知。
2. 下一轮长任务中观察章节完成后图表子节点是否立即从排队切换为运行，并核对失败节点的单项重试按钮。
3. 继续人工验收说明书正文、图表和真实截图质量；用户确认前不打包。

#### 依赖图首轮验收修正

- 用户实测发现图节点内部标题、状态与耗时重叠。根因是旧 `.cockpit-all-nodes article` 三列列表规则继续命中新图卡片，把图卡片自身的块级布局覆盖；现已对依赖图卡片显式恢复块级排版，标题可正常换行，状态徽标保持单行，详情区不再挤占标题宽度。
- 取消“图表直接嵌套在章节卡下面”的不等高树形布局，改为分层泳道：研究根节点、八章固定网格、截图授权并行泳道、五张衍生图表独立网格、交付与 QA。每张图表直接显示实际来源章节，既保留依赖语义，又避免父子卡片交错堆叠。
- 宽屏下八章优先 4 列排布，随窗口宽度依次收敛为 3、2、1 列；图表层为 3、2、1 列。桌面开发版通过 Vite 热更新生效，Tauri 与 Sidecar 未重启。
- 再次验证通过：Python 130/130、Rust 12/12、`cargo check`、前端生产构建和 `git diff --check`；未打包、提交、推送或操作用户项目。

### 2026-08-11：Draw.io 分层布局、端口寻路与节点尺寸编辑

- 对照用户导出的 13 页审阅稿和真实任务 `8cf979b6…` 的四份图表语义复现质量问题：成功数据流为 1120×526 的扁图、工作流为 760×868 的瘦图，插入竖版 Word 后分别出现文字缩小与大面积留白；模块图的 8 个节点按两列排成 828px 高，单走廊路由无法让 `user_to_db` 绕过中间节点而确定性失败。
- Draw.io 生成器升级为 `drawio-generator-v11`。本地布局改为稳定的分层重心排序，模块层最多三列，分支工作流与数据流使用 912px 文档友好画布；稀疏中间层会为跨层长边保留通道，真实数据流收敛为 912×566、模块图为 912×564、工作流为 912×690。
- 连线生成改为节点端口＋障碍物可见性网格寻路：短、无阻挡关系先布线，长边再依据已有路径选择安静通道；节点碰撞、业务边交叉和重叠分别使用不同代价，并把校验与 SVG/PNG 最终渲染统一到同一条显式 waypoint 路径。真实四图均通过应用结构校验，原 `user_to_db` 穿节点失败已复现并加入固定回归。
- 图形样式移除默认阴影，保留克制的节点类别色、层标签与不透明连线标签；边标签同时避让标题、节点、其他标签和画布边界。未压缩 mxGraph XML 新增语义节点/关系、类别、层级与原始标签元数据，导出的 `.drawio` 仍可逐节点、逐关系编辑。
- 图表资产页新增 10px 网格吸附，按住 Option 可精细拖动；属性区可暂存节点宽高，与名称和位置一起保存为新版本，保存后由本地路由器重新避让。没有复制 Draw.io 图标、模板或商标资源，也没有引入远程编辑器；本轮只实现可审计的兼容 XML、分层布局、正交寻路和网格交互。
- 完整验证通过：Python 131/131、Rust 12/12、`cargo check`、前端生产构建与 `git diff --check`。未构建安装包、未提交、未推送、未清理数据库，也未启动用户项目；说明书缺章、缺图与截图问题仍需用户逐项修复和验收。

下一步：

1. 在现有任务对失败的 `modules` 图点击“仅重试本地构建与渲染”，确认复用原语义生成 v11 图表，不再请求模型；随后用当前资产重新装配候选稿。
2. 在图表资产页验收节点网格拖动和宽高调整，确认保存产生新版本且 `.drawio`、SVG、PNG 同步更新；需要完整 Draw.io 编辑体验时再单独评估官方编辑器的离线自托管范围与许可证清单。
3. 分别单项重试缺失的 `runtime`、`testing_summary` 正文及 `runtime` 图表；暂不处理截图，但 QA 应继续如实保留截图 blocker，不能宣称文档质量通过。

### 2026-08-11：完整 Draw.io 内嵌工作台与 XML 专用 AI 上下文

- 经用户授权，在其已登录的 Smart AI Flow 参考页只读核对交互结构；结合 Draw.io 官方 Embed Mode 协议，将图表资产页改为固定三列工作台：左侧资产列表、中间常驻 `embed.diagrams.net` 完整 Draw.io、右侧 AI 图表助手。移除旧 `InteractiveDiagram.tsx` 和前端遗留的简化节点编辑分支，不再要求点击按钮打开另一个编辑器。
- 官方编辑器使用 `configure → init → load` 会话加载现有未压缩 XML；保存时由编辑器导出 SVG 和 2× PNG，并把原始 XML、SVG、PNG 作为同一个正式版本原样落盘。历史恢复也复制完整三件套，用户手工增加的图层、分组、形状、连接线和样式不会再被本地语义重建覆盖。
- AI 编辑改以当前画布 XML 为唯一基线。后端从 XML 提取包含真实单元 id、父子关系、源/目标、坐标、尺寸、样式和折点的受限清单，使用专用 `drawio-xml-editor-v1` 系统提示词，仅接受最多 12 个白名单局部操作；本地再次校验目标、单元类型、颜色、尺寸、坐标和 SVG/XML 安全边界后才回写 XML，模型不直接生成或覆盖整份 Draw.io 文件。
- 按用户提供的 Mermaid 专业提示词结构，将 Draw.io 提示词组织为核心任务、强制规则、规模控制、严格输出和静默自检。前端聊天记录按图表隔离；Sidecar 上下文缓存按任务、图表、当前 XML 指纹和提示词版本隔离，AI 连续修改可沿用最近上下文，手工改图或切换版本造成指纹变化时自动开启新上下文，避免旧坐标污染当前画布。
- 编辑结果仍需用户点击“保存为新版本”后才写入正式资产；保存后明确提示重新装配说明书。图表失败节点保留单项重试，资产导出与“显示导出文件”拆成独立动作。CSP 仅新增 `https://embed.diagrams.net` 的 `frame-src`，编辑 XML、SVG、PNG 请求分别执行大小与主动内容校验。
- 完整验证通过：Python 132/132、Rust 12/12、`cargo check`、前端 Vite 生产构建（43 modules）和 `git diff --check`。已重新冻结本地 Sidecar 并用 `tauri dev --no-watch` 稳定重启，当前 Vite 为 `127.0.0.1:1420`、Sidecar 为 `127.0.0.1:56830`；未构建安装包、未提交、未推送、未清理数据库，也未启动用户项目。

下一步：

1. 用户在当前开发版进入“图表资产”，验证三列内嵌编辑、官方工具栏、手工改图、AI 连续对话、撤销和“保存为新版本”。
2. 保存图表后回到说明书，用当前资产重新装配候选稿，核对新 PNG 是否真实插入 Word；用户确认前仍不得宣称说明书质量或产品验收完成。
3. 网络不可用时应明确显示官方 Draw.io 无法加载；后续若要求离线完整编辑，再单独评估自托管官方 Draw.io 的体积、更新和许可证清单，不能静默退回旧简化编辑器。

### 2026-08-11：Draw.io AI 模型切换与真实流式可观测

- AI 图表助手标题区新增已启用模型选择器，默认跟随当前说明书任务模型，也可在每次对话前切换。前端对话缓存和 Sidecar XML 上下文均纳入模型 ID，切换模型不会混用上一个模型的图表修改语境。
- 模型调用增加原生流式适配：OpenAI-compatible Chat Completions SSE、OpenAI Responses SSE、Anthropic Messages SSE 和 Ollama NDJSON。Sidecar 对前端输出 NDJSON 事件，包含真实 `delta`、阶段、心跳、最终结果和错误；不伪造 token 或百分比进度。不支持流式的供应商仍只显示其实际一次性返回。
- 右侧对话气泡会持续显示模型正在返回的受限操作 JSON 和已接收字符数；完整 JSON 结束且通过本地 XML/白名单校验后才一次性载入 Draw.io，避免半截操作破坏画布。如用户在模型流式返回期间手工改图，结果会被拦截并提示重新发起，不覆盖用户的新 XML。
- 回归验证通过：Python 133/133、Rust 12/12、`cargo check`、前端 Vite 生产构建（43 modules）和 `git diff --check`。已重新冻结 Sidecar 并启动 `tauri dev --no-watch`，当前 Vite 为 `127.0.0.1:1420`、Sidecar 为 `127.0.0.1:59698`。本轮未构建安装包、未提交、未推送、未清理数据库，也未启动用户项目；文档与产品质量仍待用户验收。

下一步：

1. 在图表资产页切换两个不同模型，确认原始 JSON 随返回持续增长，最后才载入画布，且各模型上下文独立。
2. 手工修改画布后再发起 AI 修改，验证流式期间的手工变更不会被迟到结果覆盖；确认后再点“保存为新版本”。
3. 保存后回到说明书重新装配，继续验收图表是否真实插入 Word；用户确认前不宣称说明书或产品完成。

### 2026-08-12：Draw.io 单一确认入口与自动文档装配

- 根据用户实测核对两个“保存”：官方 Draw.io 蓝色按钮和应用顶部按钮原本都会触发同一个 Embed Mode `save` 事件，并非一个真一个假。现按 Draw.io 官方 Embed Mode 的 `saveAndExit=0 + noSaveBtn=1 + noExitBtn=1` 配置隐藏编辑器内部按钮，只保留应用级业务入口。
- 唯一入口改为“确认并装配说明书”。点击后先从当前画布导出 XML、SVG 和 2× PNG；XML 相比已持久化版本确有变化时才创建新图表版本，避免重试装配时制造重复图版本。
- 图表确认后自动使用当前全部正文、图表和截图生成新的 Word 候选版本，并继续执行逐页质检。如说明书整单仍在运行，不抢占当前流程，仅确认图表并由后续装配节点自动使用；如自动装配或质检失败，已确认图表仍安全保留，界面显示原因和说明书页重试入口。
- 完整回归通过：Python 133/133、Rust 12/12、`cargo check`、前端 Vite 生产构建（43 modules）和 `git diff --check`。本轮仅修改前端交互与既有 API 编排，不需要重新冻结 Sidecar；未构建安装包、未提交、未推送、未清理数据库，也未启动用户项目。

下一步：

1. 在当前开发版进入图表资产，确认 Draw.io 内部蓝色保存已消失，顶部仅保留“确认并装配说明书”。
2. 对一张图做可识别的小修改并确认，回到说明书检查是否自动产生新文档版本、嵌入新 PNG，并如实显示 QA 通过或未通过。
3. 继续人工验收说明书内容和图表质量；用户确认前不宣称产品完成。

### 2026-08-12：Draw.io AI 操作 JSON 容错与一次格式修复

- 用户实测“线条较乱，帮我调整一下”时出现“模型未返回可解析的图表语义”。核对后确认交互编辑错误复用了首次建图的通用语义解析器；模型只要返回说明文字、Markdown 围栏、顶层操作数组或供应商包装对象，就会在载入画布前直接失败。
- Draw.io 交互提示词升级为 `drawio-xml-editor-v2`，明确要求首尾为 JSON 对象且禁止解释、思考过程和代码围栏；提示词版本变化同时隔离旧上下文缓存，避免把不符合新协议的历史返回继续带入请求。
- 交互编辑改用专用受限解析器，可读取代码围栏、说明文字中的平衡 JSON、顶层操作数组、单项操作和常见 `result/data/output` 包装，但解析后仍必须经过既有动作白名单、真实 XML 单元 ID、节点/连线类型和参数安全校验，不放宽画布写入边界。
- 首次返回仍不可用时，只向同一所选模型追加一次“JSON 格式修复”请求，不重新生成整图；前端显示真实的 `repair` 阶段并清空首次无效流，再继续展示修复流。第二次仍失败时保留当前画布，提示切换模型或拆小修改要求，不覆盖已保存版本，也不无限重试。
- 回归覆盖解释文字＋围栏数组和首次截断、第二次修复成功的完整流式链路。完整验证通过：Python 133/133、Rust 12/12、`cargo check`、前端 Vite 生产构建（43 modules）和 `git diff --check`。未构建安装包、未提交、未推送、未清理数据库，也未启动用户项目；说明书与产品质量仍待用户验收。

下一步：

1. 在重启后的开发版用同一句“线条较乱，帮我调整一下”复测，观察首次格式异常时是否明确进入一次修复，并最终载入受控操作。
2. 若同一模型连续两次仍失败，切换右侧模型或把要求拆成“先整理连线折点”“再调整节点间距”两步，确认失败不会改变当前画布。
3. 确认图表效果后点击唯一的“确认并装配说明书”，继续核对新 PNG 是否自动进入新的 Word 候选版本；用户确认前不宣称产品完成。

### 2026-08-12：Draw.io 职责收敛与应用侧快速恢复

- 用户实测 AI 整理连线后图面反而更乱，且官方 Draw.io 在应用重新载入候选 XML 后不会保留可用的编辑器撤销栈；已有“历史版本”又只覆盖已确认落盘版本，无法恢复尚未确认的 AI 候选。现明确职责边界：Draw.io 只负责渲染、人工微调和导出当前 XML/SVG/PNG；AI 候选、未确认撤销与正式版本历史全部由本地应用管理。
- 应用在每次 AI 候选载入画布前保存当前 XML，按当前图表维护最多 8 步会话撤销栈。画布标题区常驻“撤销本次 AI”和“恢复已确认版本”，右侧对话区同时显示醒目的恢复卡；逐步撤销不请求模型、不创建新版本，恢复已确认版本会放弃全部未确认修改。
- 用户点击“确认并装配说明书”后，会话撤销栈清空，完整 XML、SVG、PNG 进入既有 SQLite 图表修订历史；历史恢复继续由应用复制三件套并创建新版本，不依赖 Draw.io 内部历史，也不覆盖旧版本。
- Draw.io AI 提示词升级为 `drawio-xml-editor-v3`。纯“连线、线条、路径、交叉”等请求只能返回 `edge.route/edge.style/edge.label`，不得移动、缩放或改名节点；单次最多 6 项、单线最多 4 个折点。若用户明确同时要求节点、间距或整体布局，才开放节点动作。越权动作进入一次格式修复，仍越权则拒绝载入画布。
- 完整验证通过：Python 133/133、Rust 12/12、`cargo check`、前端 Vite 生产构建（43 modules）和 `git diff --check`。未构建安装包、未提交、未推送、未清理数据库，也未启动用户项目；图表、文档与产品质量仍待用户验收。

下一步：

1. 在重启后的开发版对同一图连续执行两次 AI 修改，确认标题区和右侧恢复卡均可逐次撤销，最后可一键回到当前已确认版本。
2. 再次发送“线条较乱，帮我调整一下”，确认候选只改连线而不移动模块；效果仍不满意时直接撤销，再由人工使用 Draw.io 微调。
3. 人工微调满意后确认并装配，检查应用历史新增完整图表版本且新 PNG 真实进入 Word；用户确认前不宣称产品完成。

### 2026-08-12：Draw.io 连续格式修复误判消除

- 用户连续两次发送“当前图表结构不是很清晰，线条看不出条理性，帮我重新整理”后都进入格式修复。核对确认并非连续两次单纯 JSON 语法错误，而是请求同时包含整体结构整理和连线路由，却被旧策略误分为“只允许改线”；模型返回合理的节点重排操作后被策略拒绝，界面又把策略拒绝笼统显示为格式修复。
- Draw.io 交互提示词升级为 `drawio-xml-editor-v4`。识别“结构、重排、重新整理、重新布局、层次、条理、整体”等意图时开放受控的节点与连线混合操作；只有明确的纯线条、路径、交叉请求继续保持连线专用边界。
- 一次格式修复现在只用于真正无法解析 JSON 的返回。JSON 可以解析但动作越权、目标不存在或参数不安全时，直接显示精确校验原因并保留当前画布，不再浪费第二次模型请求，也不再误导为格式问题。
- 本地解析器补充常见协议别名归一化：兼容 `actions/changes/edits`、`op/type`、`id/cell_id`、`params/data` 以及 `route_edge/move_node` 等动作名，再统一进入既有动作白名单、真实单元 ID 和参数安全校验；兼容格式不等于放宽画布写入边界。
- 完整验证通过：Python 133/133、Rust 12/12、`cargo check`、前端 Vite 生产构建（43 modules）和 `git diff --check`。数据库中无排队或运行中的说明书任务；未构建安装包、未提交、未推送、未清理数据库，也未启动用户项目。图表、文档与产品质量仍待用户验收。

下一步：

1. 在重启后的开发版用原句复测，整体整理请求应直接接收受控的节点与连线操作；只有返回文本确实无法提取 JSON 时才显示一次格式修复。
2. 若模型返回越权或无效目标，界面应直接显示具体校验原因并保留当前画布，可切换模型、缩小修改范围或使用应用侧“撤销本次 AI / 恢复已确认版本”。
3. 图表满意后点击“确认并装配说明书”，继续核对新 PNG 是否进入新的 Word 候选版本；用户确认前不宣称产品完成。

### 2026-08-12：修复“确认并装配说明书”无响应

- 用户实测应用级唯一入口“确认并装配说明书”点击无效。运行日志没有进入图表保存或 Word 装配请求；代码核对确认外层按钮发送了非可靠的 `action: save`，同时 Draw.io 内部保存按钮又通过 `noSaveBtn=1` 被隐藏，因此官方嵌入页没有产生应用等待的 `save` 回调，界面也没有任何即时状态。
- 对照 diagrams.net 官方 Embed Mode 集成流程后，应用按钮不再模拟编辑器内部保存。Draw.io 继续只负责渲染、人工微调和导出；应用使用 `autosave` 持有的当前 XML 作为事实源，点击后直接按官方 `export` 动作依次导出 SVG 与 2× PNG，再调用既有本地版本保存、Word 自动装配和逐页 QA。
- 点击后立即进入“正在读取当前画布并导出 SVG”状态；SVG、PNG 各自设置 30 秒超时。空导出、官方编辑器错误或超时都会解除按钮忙碌态、保留当前画布，并给出可重试原因，不再静默卡死。
- 保留对未来 Draw.io 自身 `save` 事件的兼容处理，但应用业务入口不再依赖它。新增前端协议回归契约，明确禁止把外层按钮重新接回 `action: save`，并要求当前 XML、SVG 导出与超时恢复逻辑存在。
- 完整验证通过：Python 134/134、Rust 12/12、`cargo check`、前端 Vite 生产构建（43 modules）和 `git diff --check`。数据库中无排队或运行中的说明书任务；本轮未修改 Sidecar 后端，不需重新冻结。未构建安装包、未提交、未推送、未清理数据库，也未启动用户项目。图表、文档与产品质量仍待用户验收。

下一步：

1. 在重启后的开发版点击“确认并装配说明书”，先确认按钮立刻显示忙碌态，随后依次显示 SVG、PNG、图表版本保存、Word 装配与 QA 状态。
2. 回到说明书页核对是否新增 Word 候选版本，以及所确认图表的最新 PNG 是否真实插入对应章节。
3. 若导出或装配失败，记录界面显示的具体阶段与错误；当前 XML 和既有正式版本均应保留，可直接重试而不整单重跑。

### 2026-08-12：说明书 QA 定向闭环、目录页码落盘与连续图号

- 针对用户当前审阅稿和 SQLite 真实任务核对 QA：`runtime` 正文中已保存“系统运行与部署架构图”请求，但本次任务既没有对应图表产物，也没有生成过 `runtime` 图表执行节点，因此 `content.figure_coverage` 不是误报；旧界面只展示既有失败节点，确实没有为“从未派发的缺图”提供恢复入口。
- 质量报告改为逐项闭环。缺图项可直接按正文中保存的图表请求创建并执行缺失节点，随后使用当前全部资产重新装配并复跑 QA；章节深度、适用章节缺失和无证据结论可定位到实际命中章节，由用户选择仅重生成这些章节并替换、打开命中章节手工编辑，或“本轮忽略（留痕）”。单章修复保留旧修订，不整单重跑。
- “本轮忽略”只保存 QA 决策、原因和时间，不篡改检测结果，也不放开正式导出门槛；下次 QA 仍会重新检查。真实界面截图问题本轮按用户要求不实现自动修复，仍可单独导入或留痕忽略，并继续如实阻断正式质量门槛。
- Word 装配新增真正写入文档的连续编号：表格使用 `表 1、表 2…`，Draw.io 图表使用 `图 1、图 2…`，真实界面截图独立使用 `界面 1、界面 2…`。编号基于最终装配顺序，不依赖 Word 客户端重新计算域。
- 逐页 QA 在 LibreOffice 首次渲染后，从真实 PDF 页面对八个一级章节建立页码映射，将 TOC 的 `PAGEREF` 显示结果回写原 DOCX、更新产物 SHA-256，再二次渲染最终审阅稿。导出的文件在不打开 Word、不手工“更新域”时也能直接显示本次版式对应的目录页码；域代码仍保留，后续 Word 中可继续更新。
- 数据库 schema 升级到 v28，增加 QA 决策留痕表；QA 策略升级为 v11，报告返回当前版本的忽略记录。新增 DOCX 目录页码回写、连续编号、QA 忽略不放行和缺图恢复上下文测试。
- 完整验证通过：Python 134/134、Rust 12/12、`cargo check`、TypeScript＋Vite 生产构建（43 modules）和 `git diff --check`。本机 `pnpm` 启动器因注册表签名校验网络失败未进入构建，改为直接运行仓库既有 `scripts/build_frontend.mjs` 完成同一套 `tsc + vite build`，未下载或修改依赖。未构建安装包、未提交、未推送、未清理数据库，也未启动用户项目；截图、正文事实质量和最终文档质量仍待用户验收。

下一步：

1. 在说明书质量报告中对 `runtime` 缺图点击“生成缺失图表并装配”，确认创建独立图表节点、生成资产并形成新的 Word 候选版本，不重跑其他章节。
2. 对“无证据的测试与上线结论”和“章节内容深度”分别验收三种路径：AI 仅修复命中章节、打开命中章节手工编辑、本轮忽略并留痕；忽略后正式导出门槛不得伪装为通过。
3. 导出新审阅稿，核对目录页码无需手工刷新即可与实际章节页一致，并检查表、图、界面编号连续；截图问题继续暂缓，但用户确认文档质量前不宣称产品完成。

### 2026-08-12：质量报告操作的弹层内可观测反馈

- 用户实测在质量报告中点击“本轮忽略”或定向重试后，操作可能已生效但看不到响应。核对确认：旧实现只更新说明书主页面顶部的全局消息，而质量报告使用全屏弹层，消息被弹层遮住；忽略操作也未进入统一忙碌态，因此按钮没有过程反馈。
- 质量报告弹层新增常驻、可访问的操作状态条（`role=status`、`aria-live=polite`）。点击定向修复后依次显示问题定位、缺图或章节的真实序号进度、Word 新版本装配和逐页复检；完成后明确显示新候选稿版本以及是否仍有未通过项，失败原因保留在弹层内，不再要求用户关闭报告寻找提示。
- “本轮忽略”在用户填写原因后立即显示审计留痕保存中，相关按钮进入禁用和“正在保存”状态；成功后明确说明留痕已保存但仍阻止正式质量门槛，失败则显示具体原因。取消确认也会显示“本次没有修改”，避免静默返回。
- 状态与具体 QA 项和动作类型绑定：修复时只把对应修复按钮标为“正在处理”，忽略时只把对应忽略按钮标为“正在保存”；切换项目、打开其他文档版本或关闭报告时清理旧提示，避免跨版本误导。
- 新增前端契约回归，确保弹层内状态区、逐项进度、Word 复检状态和忽略完成提示不会被后续改动移除。完整验证通过：Python 135/135、Rust 12/12、`cargo check`、前端 TypeScript＋Vite 生产构建（43 modules）和 `git diff --check`。本批未修改 Sidecar 后端，不需重新冻结；未构建安装包、未提交、未推送、未清理数据库，也未启动用户项目。

下一步：

1. 在质量报告中对任一正文问题点击“AI 修复命中章节并装配”，观察弹层顶部是否连续显示定位、章节进度、装配和复检结果。
2. 对另一问题选择“本轮忽略（留痕）”，确认按钮立即显示保存状态，完成后问题卡显示原因且顶部给出成功提示；该问题仍不得伪装为通过。
3. 用户继续审阅新候选稿正文和图表质量；截图问题仍按当前约定暂缓，最终验收前不打包。

### 2026-08-12：真实截图驱动用户界面说明章节

- 将截图证据前移为项目级资产，不再要求先创建说明书任务。数据库升级到 v30，新增项目概要修订、截图导入批次、图片修订、结构化解读修订、说明书采用快照、第 7 章来源快照及“等待真实截图／源码推断版／项目不适用截图”用户决策；旧任务级截图以兼容迁移保留，不覆盖旧图片、旧解读、旧章节或旧 Word 候选稿。
- 项目研究结果被压缩为可预览、可编辑、可版本化的截图理解概要。多图、文件夹、递归文件夹、拖放和剪贴板导入支持自然排序、格式与尺寸检查、20MB 安全上限、重复与近重复提示及单图失败不中断批次；图片替换生成不可变新版本，并使旧 AI 解读、审核采用集和第 7 章状态过期。
- 增加真实视觉模型调用链，按模型协议发送图片和受限项目上下文，强制返回结构化字段；支持一次 JSON 修复、有限并发、逐图失败原因与重试、按图片 SHA＋概要版本＋模型＋提示版本缓存。无法确认图片能力的模型不会被静默调用；首次发送前在界面提示第三方供应商与隐私风险。
- 截图工作台改为左侧页面组与状态、中间大图预览、右侧结构化解读审核。人工可编辑页面用途、角色、条件、可见区域、控件、流程、成功与恢复、后台证据、路由推测、证据引用、置信度、警告、分组顺序和图注，逐图确认敏感信息后采用，也可排除或替换。实验性自动采集默认折叠，标记“开发测试中 · 不稳定 · 不推荐”，必须显式授权且失败不阻断正文。
- 正式说明书依赖改为项目研究与其他七章先行；第 7 章只在存在已审核采用的真实截图时并发生成，采用图片与解读均固定到明确修订。正文按页面组组织并逐图引用，Word 使用与 Draw.io 图统一的连续 `图 N` 编号；一键“确认采用并更新说明书”只更新第 7 章、装配一次新候选版本并执行截图、敏感信息、正文引用和来源一致性 QA，失败保留上一个可用版本。
- 没有真实截图时不再伪装成完成：默认节点停在“等待真实截图”；用户可明确选择源码推断版或项目不适用截图并填写原因，该决策进入执行节点、文档占位和 QA。源码推断版会明确声明不是真实界面证据，正式质量仍需人工确认。
- 对参考文件 `/Users/zhouwei5/Downloads/霓裳云枢-v1.0-软件说明书-审阅稿.docx` 只读渲染 17 页并逐页核对；参考稿版式稳定，但原第 7 章只有源码推断文字和流程图，因此本轮没有改写参考文件，而是用新的真实证据链替代产品逻辑。
- 完整验证通过：Python 144/144、Rust 12/12、`cargo check`、TypeScript＋Vite 生产构建（43 modules）和 `git diff --check`。新增回归覆盖失败分析可单图重试、图片替换版本与失效、显式三态决策、真实 HTTP 批量导入路由和第 7 章单次装配／失败保留。未构建安装包、未提交、未推送、未清理数据库、未自动启动任何用户项目；文档内容与产品体验仍待用户验收。

下一步：

1. 在开发版选择一个真实项目，于生成说明书前批量导入 3～5 张真实界面截图，确认项目概要、有限并发 AI 解读、逐图失败重试、人工审核和页面组拖动均符合预期。
2. 创建或打开说明书任务，点击“确认采用并更新说明书”，核对驾驶舱只更新第 7 章、形成一个新的 Word 候选版本并显示截图一致性 QA，不重跑项目研究和其他章节。
3. 预览新候选稿，逐一核对正文中的 `图 N` 引用、图片、图注、页面组顺序、敏感信息和目录页码；用户确认产品与文档质量前不打包。

### 2026-08-12：截图驱动章节完成审计补强

- 按原始 A～F 验收场景重新审计后补齐此前测试证据不足的边界：视觉分析上下文现在包含同组少量真实图片、同组已审核结构化解读和当前截图最近三次已确认修订；相关上下文指纹进入缓存键，因此只有图片、概要、模型、提示词和确认上下文均相同时才复用缓存。
- 项目级截图增加完整图片与解读历史读取、图片替换和历史恢复。恢复操作总是创建新修订，不覆盖历史；已采用截图可独立撤回为待审核，也可排除，随后会明确使第 7 章过期。批量主操作在未勾选单图时采用全部已审核截图，避免按钮可点击但没有采用对象。
- 第 7 章生成器现在校验每张截图首次引用顺序必须等于人工确认的页面组与截图顺序；QA 新增当前截图解读引用和页面组顺序检查。项目级截图资产更新时间纳入 Word 候选稿 freshness，替换或恢复图片后候选稿会真实显示需要重新装配，不再仅更新驾驶舱节点。
- Sidecar 启动恢复扩展到无说明书任务的项目级截图分析、导入批次，以及已完成说明书任务上的独立第 7 章更新节点。上次进程遗留的排队或运行状态会转为带原因的可重试失败，图片、成功解读、旧章节和旧候选稿全部保留。
- DOCX 自动化回归由旧任务级截图切换为真实项目截图资产、图片修订、已审核解读和任务固定采用引用，直接证明第 7 章插入正确图片、连续 `图 3`、审核图注及正文图号引用；另保留专门的 v28→v29 兼容迁移回归，证明旧图片、旧六字段说明和旧任务关系迁移后仍可读取并形成已审核解读。
- 最终完整验证通过：Python 150/150、Rust 12/12、`cargo check`、TypeScript＋Vite 生产构建（43 modules）和 `git diff --check`。开发 Sidecar 需要在本批改动后再次冻结；桌面 Tauri 开发窗口仍因系统权限审查要求用户在当前对话直接确认而尚未启动。未构建安装包、未提交、未推送、未清理数据库、未启动任何用户项目，产品最终质量仍待用户验收。
- 随后已重新冻结当前开发 Sidecar：`copyright-agent-sidecar-aarch64-apple-darwin`，SHA-256 为 `3670a20a8942dd0634b6b21b3a555ab5fe53449b0f2d658be9f7ecc48eb31b6c`。沙箱内直接执行冻结二进制的额外冒烟因 macOS 同步信号量权限被拒绝，Python TestClient 真实 HTTP 路由回归已通过；桌面开发窗口启动仍等待用户直接授权。
- 用户随后明确授权启动开发版。`./node_modules/.bin/tauri dev --no-watch` 已成功运行：Vite 前端监听 `http://127.0.0.1:1420/`，Rust 桌面壳完成编译并启动 `target/debug/software-copyright-agent-desktop`，冻结 Sidecar 完成握手（启动时 PID `38309`、地址 `http://127.0.0.1:62362`、版本 `0.1.0`）；持续轮询无退出或新增错误。开发版现已可供用户验收，未启动任何用户项目。

### 2026-08-12：图表空文档回归与视觉模型配置闭环

- 用户验收发现四张已生成图表在内嵌 Draw.io 中统一报 `Document is empty`，但 Word 审阅稿仍有图。只读核对真实任务后确认：四份 `.drawio`、SVG、PNG 均存在且非空，15 页审阅稿中的四张图也分别在第 4、7、9、12 页正常渲染；故障不是 Draw.io 渲染器，也不是图表生成失败。
- 根因是 Sidecar 图表资产读取路由在鉴权后提前结束，本应执行的 `ManualFigureService.read_asset` 代码被误移到截图图片路由的返回语句后成为死代码，前端因此把空响应交给 Draw.io。现已恢复图表路由的真实二进制响应、删除死代码，并新增鉴权、XML 字节与媒体类型回归。
- 截图页没有视觉模型并非用户未配置模型，而是现有模型都没有明确的 `supports_vision` 标记，设置页此前又没有写入入口。现增加逐模型“启用/关闭图片能力”控制、供应商能力确认提示、能力状态展示，并在截图页无可用模型时提供“去设置视觉模型”入口；仍不凭模型名称静默假定供应商端点支持图片。
- 对霓裳云枢原项目只读扫描显示仓库内有大量 `public/images`、构建产物、服饰照片、背景、二维码和模型素材，没有可直接确信为真实页面截图的专用目录。这证明后续自动发现必须采用“候选目录评分、排除素材目录、展示原因与缩略图、用户确认后导入”，不能按扩展名或尺寸静默导入。
- 本轮验证通过：Python 152/152、Rust 12/12、`cargo check`、TypeScript 与 Vite 生产构建（43 modules）、`git diff --check`。Sidecar 已重新冻结，SHA-256 为 `2b957ac852c2882a1d9ff8a5648fb4f70db2dcd864dc5f8507501e9e53dc9e29`；开发版已重启，Sidecar PID `51633`、地址 `http://127.0.0.1:57517`。未启动用户项目、未导入任何项目图片、未修改或覆盖参考 DOCX、未提交、未推送、未打包。
# 2026-08-12 图表、截图与第 7 章稳定性收口

- Draw.io 内部渲染器对边标签执行画布边界钳制，修复短终止边标签越界导致整张图失败；架构图在模型连续返回无关系节点时，改用章节证据生成保守、可编辑的确定性关系图。
- 截图分析增加运行中原子去重与重启恢复；供应商明确返回“非多模态模型”时会自动撤销错误能力标记，停止无意义重试，并展示可理解的失败原因。
- 设置页启用图片能力时会生成带随机验证码的测试图，真实调用所选模型并核对返回值；只有挑战成功才保存为“已验证并启用”。旧的手工能力标记不再被视为可用。
- 第 7 章仅由已审核采用的真实截图装配：移除内部等待说明和未获截图支持的 AI 层级，标题按 `7.1 / 7.2` 连续编号并清理重复数字前缀；每个页面增加一段模块用途叙述，再保留结构化操作说明。
- 截图工作台保留用户当前选中项，失败项支持就地重试；更新说明书展示生成第 7 章、装配 Word、一致性 QA 的实际进度，并避免把旧 QA 完成状态误判为本次成功；常用操作改为直接可见。
- 回归结果：前端生产构建通过，Rust `cargo check` 通过，Python 全量 160 项测试通过；Draw.io 源文件通过专业校验，Word 候选稿完成 6 页渲染复检。
- 当前应用数据库中的任务、说明书任务和截图资产均为 0 条，无法在不伪造数据的情况下恢复此前截图中的旧失败节点；需在开发版中重新导入项目/截图验证新流程。
# 2026-08-12 最终稿阻断闭环

- 修复真实任务 `核心数据流图` 的 `flow_dress_to_cos` 标签碰撞：生成器在构建 XML 时预先执行最终标签布局；若某条边确实没有安全标签走廊，则仅隐藏显示标签，保留连线、`dataSemanticLabel` 原语义和 Draw.io 可编辑性，不再让可选注释阻断整图与文档。
- 用当前数据库中的 8 节点、9 边真实语义回归：内部校验通过、专业 `validate_drawio.py` 通过、`analyze_layout.py --strict` 为 0 警告，并完成 PNG 目视检查。
- “忽略并留痕”现在对允许豁免的版式密度、章节深度和图表覆盖真正生效：从待处理列表消失，保留原始失败检查和原因，并按剩余未豁免阻断项重新计算交付门槛；真实性、证据完整性和安全类检查不可豁免。底层动作仍保存为 `deferred`，兼容现有数据库 CHECK 约束。
- QA 策略升级至 `manual-docx-qa-v12`：正常章节尾页/末页密度不足改为非阻断 warning；截图驱动的第 7 章由截图真实性、已审核六维解读、顺序和敏感信息检查负责，不再机械套用普通正文 950 字门槛。
- 已在当前任务中恢复失败图表，装配正式候选稿 v5，并用 LibreOffice 逐页渲染 17 页；QA 通过、阻断项 0、非阻断 warning 1。17 页联系表目视复检无裁切、重叠或空白页。
- 已正式导出 `/Users/zhouwei5/Downloads/霓裳云枢-V1.0-软件说明书.docx`，大小 21,044,367 bytes，SHA-256 `42d6d1c5067d456b26776a41a8bc022b79a00cbdd8d96a9df5930621c76855d8`，导出回执已写入数据库。
- 全量回归：Python 161 项、前端 production build、Rust cargo check 均通过；sidecar 已重新打包并启动。

# 2026-08-13 快速开始就地预览与关系聚焦

- 快速开始顶部流程改为自适应三段网格，取消临界宽度下多余的横向滚动；阶段卡统一紧凑比例、居中对齐，并用细线与小折角替换突兀三角箭头。
- 总进度不再只按九个大阶段整数计数：长耗时说明书阶段复用真实执行节点百分比，源码、截图、装配与交付采用显式权重，运行中最高保持 99%，完成后才显示 100%。
- 快速开始的“查看正文”和“查看图表”改为页内轻量模态预览：正文复用结构化章节块渲染，图表直接读取当前 PNG；需要修改时再从模态框跳转说明书或图表工作台。
- 问号按钮移除浏览器原生 `title`，只保留产品自定义说明，避免同时出现两层提示。
- 执行画布将依赖聚焦扩展到所有节点：悬浮任一节点时仅绘制它的一跳上游与下游，并高亮关联节点；默认不铺开全依赖图，继续避免蜘蛛网式杂乱。
- 前端生产构建、快速开始 7 项 UI 契约测试与 `git diff --check` 通过；本地服务冷启动失败后会自动重连两次，不再要求用户手工刷新。开发版重启命令已两次提交，但均被桌面权限审查超时拦截，本轮没有把旧安装版窗口误报为新开发版。

### 2026-08-13 快速画布关系线收口与文档就地预览

- 执行关系线改为纯悬浮状态：取消“上次点击关系”和“运行中图表关系”的隐式保持；指针离开画布、移到节点空白区或滚动画布时立即清空，快速跨节点不再残留旧连线。
- 正文快照、Word 装配和逐页 QA 节点接入说明书文档版本解析：优先按节点输出版本定位，快速流程完成后 QA 节点直接命中 `final_document`，装配节点命中审阅稿，正文快照命中 `review_checkpoint`。
- 审阅稿与终稿在快速开始中直接打开页内真实渲染预览，支持上一页/下一页；未形成 QA 渲染时回退到结构化成品正文。修改或深度审阅仍通过模态框按钮进入说明书工作台。
- Vite 生产构建、快速开始 7 项 UI 契约测试和 `git diff --check` 再次通过。开发版已完整重启，Sidecar 在 `http://127.0.0.1:51161` 输出 ready；实机自动导航因 Mac 锁屏暂停，窗口已保留供用户解锁后验收。

### 2026-08-13 快速开始研究产物与截图就地浏览

- 项目证据研究、截图理解项目概要节点不再把“查看产物”直接跳转成另一工作区；快速开始页会读取已版本化的项目概要，连同节点输出、状态、耗时、执行次数和下游用途形成结构化弹窗，完整编辑仍保留跳转入口。
- 截图规划与授权节点的“查看截图”接入项目级截图证据工作台：弹窗左侧列出真实截图及审核/采用状态，右侧显示当前大图、标题、图注与序号；若项目级资产为空则兼容读取旧说明书任务截图，只有确实无图时才展示节点执行摘要。
- 图片预览沿用现有鉴权接口和对象 URL 回收机制，没有复制图片或建立第二套截图数据；弹窗提供“进入截图工作台”用于审核、替换和深度编辑。
- 前端生产构建、Python 全量 179 项、Rust `cargo check`、快速开始 7 项 UI 契约测试与 `git diff --check` 通过；开发版已完整重启，Sidecar 地址为 `http://127.0.0.1:56203`，供本轮验收。
- 实机核对发现 macOS 会把同 bundle identifier 的 `/Applications` 旧安装版与 `target/debug` 开发进程归并，辅助控制及用户点击可能命中旧窗口，表现为侧栏没有“快速开始”、旧握手超时或修复看似未生效。新增开发专用 Tauri 配置和 `com.local.software-copyright-agent.dev` 标识，窗口运行时强制显示“软著材料助手 · 开发版”；开发 runner 通过 `COPYRIGHT_AGENT_DATA_DIR` 显式复用正式数据目录，历史项目、截图、文档和模型配置不迁移也不丢失。
- 研究概要和截图弹窗增加历史任务兼容回退：项目级工作台缺失时不让按钮无响应，截图继续回退到旧说明书截图；两类资产都不存在时至少打开节点执行摘要。相关前端构建、Rust 检查、快速开始 8 项契约测试和 `git diff --check` 通过。

### 2026-08-13 快速开始详情与截图入口实机闭环

- 快速画布的项目研究与概要节点统一显示“查看详情”；产物按钮增加指针和点击事件隔离，避免画布悬浮关系追踪抢占产物浏览操作，忙碌文案改为“正在打开…”。
- 使用真实快速任务数据库的临时副本做完整前端点击验证：“查看详情”打开页内结构化产物弹窗；“查看截图”打开 3 张项目截图画廊，图片实际解码尺寸为 2936×1670，而非只显示空容器。
- 真实 HTTP 路由副本验证返回项目概要 v1、3 张已审核且已采用截图；首张 PNG 为 1,224,823 字节并通过 PNG 文件头检查。测试全程不修改真实数据库。
- 前端生产构建、快速开始 8 项 UI 契约测试和 `git diff --check` 通过；接下来必须使用标题为“软著材料助手 · 开发版”的窗口验收，避免命中 `/Applications` 内的旧安装版。

### 2026-08-13 快速开始全类型产物预览回归

- 冷重启开发版后，使用真实快速任务数据库的只读临时副本逐项点击 19 个完成节点；项目证据研究与截图理解概要均显示“查看详情”，不再沿用含糊的“查看产物”。
- “查看详情”在当前页打开结构化结果弹窗；“查看截图”打开 3 张真实截图画廊，首张实际解码为 2936×1670，切换后的第二张实际解码为 2934×1666。
- 同轮验证“查看正文”“查看图表”“查看审阅稿”“查看终稿”：正文直接呈现生成内容，图表 PNG 实际解码为 2736×2544，审阅稿显示 20 页逐页预览，终稿显示 19 页逐页预览。
- 19 个节点产物按钮均为可见、可用状态；按钮点击与画布悬浮关系追踪已隔离。开发版已重新启动，验收时应使用标题为“软著材料助手 · 开发版”的窗口，旧安装版窗口不代表当前代码。

### 2026-08-13 最终文档空白页与快速交付状态收口

- 逐页审计真实快速任务的双文档：源码文档为 60 页、QA 通过且无空白页；旧软件说明书终稿 v3 为 19 页，确认第 15 页只有页眉页脚。根因是第 7 章被强制另起页，前一节分页恰好留下空页。
- 软件说明书生成器升级到 `formal-manual-docx-v12`，取消第 7 章的额外强制分页；在真实任务只读副本上重新装配后为 18 页，第 7 章、3 张真实截图和第 8 章连续排版，逐页渲染复检无空白页。
- QA 策略升级到 `manual-docx-qa-v15`，新增“正文区域完全空白页”阻断检查；页眉页脚存在但正文为空不再漏检。终稿 OOXML 同时确认 4 张图表、3 张截图、30 个标题，正文颜色统一为黑色，且不含“终稿”“审阅稿”“等待用户”等过程性文案。
- 快速开始交付缓存改为保存终稿 QA 后的文档对象，而不是 QA 前快照；交付门槛改用 `failed_check_count`、失败检查数组和 QA `passed` 的真实结果，避免只读取兼容字段 `blocker_count` 而误放行或显示陈旧质量状态。
- 回归通过：Python 全量 183 项、前端 Vite 生产构建、相关 12 项定向测试和 `git diff --check`。开发版已重新启动，Vite 地址 `http://127.0.0.1:1420/`，Sidecar ready 地址 `http://127.0.0.1:59436`；当前窗口可继续验收，新生成的终稿将采用 v12/v15。

### 2026-08-13 快速开始 B 线时序语义修正

- 顶部软件说明书线不再把整个生成过程压缩为“处理界面截图 → 生成软件说明书”，也不额外重复“装配软件说明书”。现在按真实时序展示为“项目证据研究 → 处理界面截图 → 正文撰写与专业图表并行生成”，最终统一汇入右侧“装配双文档 → 交付完成”。
- 项目研究、正文和图表三个可视节点直接聚合说明书任务中已有的 `research/profile`、`section`、`figure` 实节点状态，展示真实完成数、当前运行节点和失败原因，不新增虚构后台阶段。
- 快速开始 UI 契约测试 10/10、Vite 生产构建和 `git diff --check` 通过；开发版已完整重启，Sidecar ready 地址为 `http://127.0.0.1:64831`。

### 2026-08-13 定稿候选提交与项目文档校准

- 用户确认当前版本功能无问题并授权提交推送；创建定稿分支 `codex/finalize-automation-workflow`，功能基线提交为 `142655f`，已推送到 GitHub 并反查远端哈希一致。
- 提交前完成 Python 全量 183 项、前端 Vite production build、Rust 12 项、格式与缓存差异检查；93 个文件共 19,769 行新增、1,732 行删除纳入功能基线。
- 审计发现 README 仍写“尚未接入模型协议/正文/画布”，开发计划仍停在 M0，交接提示仍把已经完成的串行栅栏、截图阻塞和任务驾驶舱列为下一步；这些描述会误导后续维护，因此按实际实现统一校准。
- README、架构、数据流、开发计划、说明书流水线、本地 API、交接、新会话提示与发布清单现已记录 SQLite v31、快速开始持久化任务、源码/说明书并行支线、真实截图模型实测、节点产物就地预览、人工终稿和发布门禁。
- 定稿不等于公开发行：剩余工作只有 GitHub Actions 原生构建、干净环境安装与 Word/WPS 抽检、两平台签名/公证、SBOM 和升级卸载数据保留验证；除发布阻断问题外不再扩大功能范围。
- 新增 ADR-012/013，固定“快速任务身份＋双交付分支”和“视觉能力实测＋截图明确授权”两项已验收设计，避免后续维护把它们退回页面临时状态或纯用户声明。
