# 跨平台总体架构

## 1. 设计目标

- 完全独立运行，不依赖 Codex 或其他通用 Agent 运行时。
- 同一领域内核同时服务 CLI、FastAPI sidecar 和测试。
- macOS arm64 与 Windows x64 从路径、进程、字体和打包层共同设计。
- 模型输出必须经过结构化校验和确定性质量门禁。
- 任务可以取消、重试，并在应用异常退出后恢复。
- 项目文件、API Key 和生成材料默认只保存在本机。

## 2. 分层结构

```text
┌─────────────────────────────────────────────┐
│ Tauri v2 桌面应用                            │
│ 项目选择 / 模型配置 / 信息确认 / 任务 / 产物 │
└───────────────────┬─────────────────────────┘
                    │ HTTP + SSE
                    │ 127.0.0.1 / 随机端口 / 会话令牌
┌───────────────────▼─────────────────────────┐
│ FastAPI Sidecar                             │
│ API 映射 / 输入校验 / SSE / 生命周期与健康检查 │
└───────────────────┬─────────────────────────┘
                    │ 调用应用服务
┌───────────────────▼─────────────────────────┐
│ Python 领域与应用内核                        │
│ 工作流 / 项目分析 / 模型编排 / 出件 / QA      │
├─────────────────────────────────────────────┤
│ 端口与适配器                                 │
│ SQLite / 文件系统 / Key 存储 / 模型协议 / 渲染 │
└─────────────────────────────────────────────┘
```

依赖方向始终指向领域内核：领域模型不得导入 FastAPI、Tauri 或具体模型 SDK。

## 3. 模块职责

### 桌面层

- 启动、监控和关闭 sidecar。
- 生成一次性会话令牌并传给 sidecar。
- 选择项目目录、ZIP 和产物导出目录。
- 调用系统安全存储保存和读取 API Key。
- 展示扫描事实、证据、待确认项、阶段进度和 QA。
- 打开或导出产物，不直接修改任务数据库。

### FastAPI sidecar

- 只监听 `127.0.0.1` 随机端口。
- 验证会话令牌、请求 Schema 和任务版本。
- 将 HTTP 命令转换为应用服务调用。
- 通过 SSE 推送持久化事件，不自行维护另一套任务状态。
- 提供健康检查、版本和运行时诊断。
- 不持有长期 API Key；需要时通过凭据端口读取。

### 领域与应用内核

- 创建任务和执行阶段状态机。
- 扫描项目、过滤文件、生成事实与证据。
- 调用模型能力，验证结构化输出并执行修复循环。
- 生成代码文档、说明书和图表。
- 注册产物、执行 QA、判定是否可交付。
- 写入 checkpoint、事件和错误，恢复未完成任务。

### 适配器

- SQLite repository。
- 本地任务目录和原子文件写入。
- Keychain / Credential Manager。
- OpenAI Responses、Chat Completions 及后续协议。
- LibreOffice、Draw.io 渲染和字体发现。
- 1.1 阶段的 Playwright 和受控项目进程。

## 4. 进程与通信

桌面应用是父进程，sidecar 是唯一受管子进程。启动协议：

1. Tauri 生成随机会话令牌和期望的 sidecar 版本。
2. Tauri 启动 sidecar，令牌通过继承句柄或短生命周期环境变量传递。
3. sidecar 绑定随机本地端口，并通过标准输出发送单行启动握手。
4. Tauri 校验进程 PID、端口、版本和健康检查结果。
5. UI 才允许发送业务请求。

sidecar 不对局域网开放，不启用 CORS 通配符。每次桌面应用启动都更换令牌。

正常退出时先请求 sidecar 停止接收新任务，等待 checkpoint 写入后终止。异常退出时：

- macOS 终止 sidecar 进程组。
- Windows 使用 Job Object 管理并清理进程树。
- 下次启动根据 SQLite 状态执行恢复审计。

## 5. 数据边界

应用级 SQLite 保存结构化元数据，任务目录保存大型文件。任何数据库记录不得把临时绝对路径作为跨设备或长期稳定标识；任务内路径使用相对路径，打开时再由任务根目录解析。

项目默认采用“引用＋摘要”模式：

- 保存用户选择的原始路径。
- 保存扫描时的项目指纹、文件哈希和必要证据片段。
- 不默认复制整个源码项目。
- 恢复前重新检查源项目是否存在、是否变化。
- 输入变化时任务进入待确认，不静默复用旧结果。

ZIP 输入先解压到任务输入区。解压必须阻止绝对路径、`..` 穿越、符号链接逃逸和超额膨胀。

## 6. 安全与信任边界

### 模型请求

- 仅发送完成当前阶段所需的最小代码和证据。
- 在发送前应用敏感文件与秘密扫描规则。
- UI 明确展示模型服务目的地和即将使用的项目。
- 模型响应视为不可信输入，必须通过 Schema、长度和引用校验。
- 模型不能决定任意本地路径或命令。

### 本地文件

- 工具只能访问任务根目录和经用户明确选择的项目根目录。
- 路径必须规范化后再做父子关系检查。
- 不跟随越界符号链接。
- 中间文件先写临时文件，`fsync` 后原子替换。
- 导出到已有目标时由 UI 明确确认覆盖。

### 日志与秘密

- 请求头、Key、认证参数和疑似秘密统一脱敏。
- 原始模型请求/响应是否保留由设置控制，默认只保存结构化摘要。
- 错误报告不包含完整环境变量和用户主目录清单。
- Key 只由系统安全存储管理，数据库保存 `credential_ref`。

### 本地命令

MVP 不提供通用命令工具。LibreOffice、渲染器等命令使用固定可执行文件、固定参数模板和任务内路径。项目自动启动留到 1.1，并必须经过用户确认。

## 7. 跨平台约束

- Python 全部使用 `pathlib.Path`，前端使用 Tauri Path API。
- 数据库和任务目录使用系统标准应用数据目录。
- 文件名内部采用 ASCII 稳定标识；用户可见导出名再使用中文名称。
- 不依赖文件系统大小写敏感性。
- 不将 POSIX 权限位作为唯一安全机制。
- 长路径、中文路径、空格、Windows 保留名和文件锁均进入测试集。
- 时间存储为 UTC ISO 8601；展示时转换到本地时区。
- 进程退出码、信号与 Windows 异常统一映射为领域错误。

## 8. 运行时与打包边界

MVP 包含：Tauri 应用、sidecar、Python 领域依赖和内置规则。大型第三方组件单独版本化：

- LibreOffice：优先评估首次安装组件包，同时允许检测用户已有安装。
- Draw.io：源文件由内核生成；渲染后端必须可替换，避免永久绑定 Desktop。
- 字体：维护明确的授权清单和回退顺序，不隐式依赖某台机器字体。
- Chromium：1.1 阶段按版本下载并校验哈希，不进入 MVP 基础包承诺。

组件管理器需要保存组件版本、来源、哈希、安装状态和诊断结果。任何下载都由用户明确触发。

## 9. 可观测性

每个任务、阶段、模型调用和工具调用都有独立 ID。结构化事件至少包含：

- 时间、级别、事件类型。
- `task_id`、`stage_run_id`、可选 `model_call_id` 或 `tool_call_id`。
- 用户可见消息和机器可读数据。
- 可重试性和错误分类。

日志不是状态真相；SQLite 中的任务、阶段和 checkpoint 才是恢复依据。

## 10. 架构验收点

- CLI 无需启动 FastAPI 即可运行领域内核。
- sidecar 关闭后 SQLite 和产物仍可独立检查。
- 更换模型协议不修改项目扫描和出件模块。
- 更换 DOCX 或 Draw.io 渲染器不修改工作流语义。
- 模型不可越过 QA 门禁把产物标记为可交付。
- macOS 与 Windows 使用同一状态机和数据库 Schema，仅进程与系统能力适配器不同。
