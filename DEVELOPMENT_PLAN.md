# 软著 Agent 开发计划

## 1. 产品目标

开发一个完全不依赖 Codex 的本地桌面工具。用户选择软件项目并配置模型服务后，应用辅助分析项目、筛选核心源码、生成软件著作权申请材料，并通过确定性程序检查产物质量。

目标平台为 macOS arm64 和 Windows x64。产品不包含账号、多用户、团队协作、云端任务管理或自动提交申请材料。

## 2. 已确认的技术方向

- 桌面层：Tauri v2。
- 本地服务层：FastAPI sidecar。
- 领域内核：与 FastAPI 解耦的 Python 包，可由 CLI 和桌面应用共同调用。
- 持久化：SQLite 保存结构化状态，任务目录保存文档、图表、渲染文件和大体积日志。
- 模型层：以协议适配器和能力声明为核心，不与单一国外或国内厂商绑定。
- 安全存储：macOS Keychain、Windows Credential Manager；数据库不保存明文 API Key。
- 工作流：受控阶段状态机，不提供模型任意执行 Shell 的能力。
- 质量策略：模型负责理解与写作，分页、格式、路由、渲染、质量门禁和恢复由确定性程序负责。

## 3. MVP 范围

### 包含

- 本地目录和 ZIP 项目输入。
- 项目扫描、技术栈识别、事实提取和证据索引。
- 用户确认软件名称、版本、用途等不可安全推断的信息。
- 多个模型服务配置。
- OpenAI Chat Completions 兼容协议、OpenAI Responses API。
- 可扩展的模型协议适配器和能力探测机制。
- 核心源码过滤、分级和语义排序。
- 约 60 页源代码 DOCX；源码不足时如实报告，不填充虚假内容。
- 内容完整的软件说明书 DOCX。
- 一张系统架构图和一张核心业务流程图。
- `.drawio`、`.svg` 和 Word 插图用 `.png`。
- DOCX、Draw.io 和产物完整性质量检查。
- 任务进度、日志、取消、阶段重试和应用重启后恢复。
- 产物查看、打开和导出。
- 手动导入界面截图。
- 快速开始一次性配置与无人值守双文档生成。
- 真实截图文件夹批量导入、视觉模型实测、解读、审核采用和版本复用。
- 文档/绘图风格提示词外置配置及专业用户修改确认。
- 依赖画布、节点级产物预览、失败节点恢复和项目资产集中管理。

### 暂不包含

- Codex 或 Codex CLI 运行时依赖。
- 登录、多用户、租户、团队协作和云端同步。
- 自动提交软件著作权申请。
- Git URL 拉取和私有仓库认证。
- 所有模型厂商专有协议同时首发。
- 未经用户授权自动启动项目、登录系统或执行有外部副作用的操作。
- 把普通图片素材自动猜测为界面截图；快速开始只读取用户明确选择的截图文件夹。

## 4. 开发里程碑

### M0：需求和架构基线

交付：

- MVP PRD 和非目标。
- 跨平台总体架构。
- 核心领域接口草案。
- SQLite 数据模型。
- 任务状态机、事件和恢复协议。
- 任务目录与产物命名规则。
- 现有 Skill 规则和脚本迁移清单。

完成标准：主要模块职责、数据边界、失败恢复语义和首批协议范围不存在阻塞性歧义。

### M1：领域内核与 CLI 纵向链路

交付：

- Python 项目骨架及跨平台配置。
- SQLite migration 基线。
- 项目扫描、ignore 和敏感信息过滤。
- Evidence、Artifact、QAResult 等领域模型。
- 最小任务状态机和 CLI。
- “扫描项目 → 保存事实和证据 → 输出扫描报告”的完整链路。

完成标准：进程退出后可从 SQLite 恢复任务；相同输入可得到稳定扫描结果；路径处理通过 macOS 和 Windows 单元测试。

### M2：模型适配和项目理解

交付：

- `ProtocolAdapter`、`ModelProvider` 和能力声明。
- OpenAI Chat Completions 兼容适配器。
- OpenAI Responses 适配器。
- 结构化输出校验、修复、重试和错误标准化。
- 模型配置测试和最小能力探测。
- 事实候选、证据引用和用户确认数据流。

完成标准：至少两个不同服务端点通过同一工作流；模型输出不能绕过 Schema 校验；日志中不存在完整 Key。

### M3：源代码文档

交付：

- 核心源码过滤、分级和语义排序。
- 视觉宽度换行、确定性分页和 DOCX 生成。
- 代码不足、敏感内容和重复内容报告。
- DOCX 转 PDF、页面渲染和确定性 QA。

完成标准：正常项目生成总计 60 页且包含封面；不使用依赖、测试、mock、重复或伪造代码凑页；黄金样例页面回归通过。

### M4：说明书与图表

交付：

- 说明书内容规划、证据约束和章节生成。
- DOCX 样式、表格、图片、页眉页脚和分页。
- Draw.io 架构图及核心流程图生成。
- 图表结构、路由和渲染检查。
- 综合 QA 报告和阻断门禁。

完成标准：关键事实可追溯；无孤立表头、异常空白页、图片溢出和明显图表穿线；阻断项未清零时不可标记为可交付。

### M5：桌面应用与跨平台发布

交付：

- Tauri 桌面骨架和 FastAPI sidecar 管理。
- 模型配置、项目选择、确认项、任务进度、日志和产物页面。
- SSE 事件流、取消和阶段重试。
- macOS arm64、Windows x64 构建。
- 运行时依赖、字体、签名和故障诊断方案。

完成标准：两平台完成安装到出件的端到端测试；取消后无残留进程；应用重启后任务状态和产物可恢复。

### M6：快速开始与真实截图证据

交付：

- 一次性确认项目、名称、版本、三类模型、并发、重试和截图授权。
- 源码文档与说明书两条并行支线及统一交付门禁。
- 截图文件夹批量导入、真实视觉请求能力测试、审核采用与版本复用。
- 可视执行画布、节点关系聚焦、节点产物就地预览、失败恢复和清空重来。

完成标准：真实项目可在不逐页点击的情况下生成两个 Word 文档；切页与轮询不重复创建任务；成功节点可查看产物，失败节点可从原任务恢复。

## 5. 测试策略

- 单元测试：路径、过滤、分页、Schema、状态转换、QA 规则。
- 契约测试：每种模型协议适配器使用统一测试集。
- 集成测试：SQLite 恢复、任务目录、DOCX、Draw.io 和渲染工具链。
- 黄金样例：至少两个真实项目，保存结构摘要和页面渲染基线。
- 故障注入：网络失败、限流、模型格式错误、进程退出、磁盘异常、取消和重试。
- 跨平台测试：macOS arm64 和 Windows x64 均进入持续验证范围，不留到发布末期补做。
- 安全测试：Key 脱敏、路径越界、ZIP 穿越、符号链接、敏感文件和危险命令。

## 6. 每轮工作协议

每轮开发遵循以下顺序：

1. 阅读 `PROGRESS.md` 的当前状态和下一步。
2. 明确本轮目标、非目标、预期变更文件和验证方法。
3. 实施小范围、可验证的变更。
4. 运行与风险相匹配的检查。
5. 更新 `PROGRESS.md`，记录完成项、验证结果、风险和下一步。
6. 出现架构性决定时追加 `DECISIONS.md`。
7. 向用户同步实际结果，不把计划描述成已完成工作。

默认每轮保持一个清晰主题；未经用户确认，不跨越多个里程碑进行大规模编码。

## 7. 当前推荐顺序

M0–M4 与 M6 的功能开发验收链路已经贯通；M5 的桌面功能已完成，但“两平台原生安装包端到端通过”的公开发布标准尚未满足。定稿阶段不再扩大功能范围，推荐顺序为：

1. 在干净数据目录完成一次快速开始端到端回归，并核对双文档、图表、截图和资产记录。
2. 由 GitHub Actions 在 macOS 与 Windows 原生 runner 验证冻结 Sidecar 和安装包。
3. 完成 Word/WPS 抽检、签名、公证、SBOM、升级/卸载和数据保留测试。
4. 仅修复发布阻断问题；新的自动浏览器采集、云同步等需求进入后续版本。
