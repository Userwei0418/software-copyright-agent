# 真实模型端到端验收与说明书人工复核（2026-09-11）

本目录记录隔离审计样例“软著材料助手审计样例 / 0.1.1”。数据目录为 `/tmp/copyright-audit-20260911`，未操作正式应用数据库。两张截图来自真实应用页面，仅作为本次回归样例；自动采用授权仅在这个审计任务中模拟确认。

## 运行及当前结果

- 唯一 QuickStart：`6b94e538-227f-46e8-aae2-0bdba9e54d93`
- 项目任务：`99f67040-d30c-4019-8eef-1670be1eeda7`
- 说明书作业：`d749c750-47af-4704-8685-6cb59969cb4d`
- 首轮完整生成：137.11 秒、16 次真实调用，14 次 `senseaudio-s2`、2 次 `senseaudio-vl-1.0-260319`，全部完成。并发 3，QuickStart retry_limit=0。
- 发现图表角色未生效后，只新增 1 次真实 `glm-5.3-flash` 架构图调用，14.316 秒；未重跑完整任务。后续截图/正文修订、部署图与架构图重渲染、文档重装配均为本地操作，无模型调用。
- 源码 DOCX：60 页；源码代理另行完成逐页复核，见 `source-review.json`。
- 当前说明书：终稿 **v7**，产品真实 LibreOffice QA **20 页 / 43 项检查 / 0 阻断 / 2 类普通提醒 / 无豁免**。提醒为第 16 页章尾留白，以及截图未展示失败/恢复场景。原始 v3 和所有中间文档版本仍在隔离库，原 v3 文件 SHA 未改变。
- 当前生成器 `formal-manual-docx-v15`，QA 策略 `manual-docx-qa-v18`。61 项说明书和 QuickStart 聚焦回归通过，24.657 秒。

最终 DOCX 位于：
`/tmp/copyright-audit-20260911/tasks/99f67040-d30c-4019-8eef-1670be1eeda7/artifacts/manual/jobs/job-v1/software-manual-final.v7.docx`

产品检查的 PDF 位于同一任务目录下：
`qa/manual/d749c750-47af-4704-8685-6cb59969cb4d/doc-v7/render-v1/software-manual-final.v7.pdf`

QuickStart 的首次完成输出保存其当时的 v3，是原始运行记录。后续人工修订产生的 v7 通过说明书工作台/我的资产查看，不把修订结果冒充第一次全自动输出。

## 首轮实际缺陷与修复

| 实际证据 | 修复及恢复行为 |
| --- | --- |
| 四张表题均出现“表 n 表1”双重编号 | 文档装配统一去掉图表标题已有编号；新增图表编号 QA 检查。 |
| 源码截图解读写入 `generate_manual`、`scan_project`，并自己声明“仅依据界面按钮推断”，仍被自动采用并通过旧 QA | 新增 `unresolved_claims`；兼容识别旧 warnings 中的推断/未验证主张。QuickStart 自动采用、人工采用、截图快照及正式 QA 均阻止未解决事实推断。普通观察提醒保持独立 warning。 |
| 设置截图实际为“0 个连接”的服务商选择页，解读却声称保存了凭证 | 本次基于原图人工修订，保留旧 AI 解读版本，删除不可见输入/保存步骤与后台函数名；当前结果明确写成 0 连接。未来视觉提示词要求 success_state 只描述当前可见结果。 |
| 后台字段为空会形成不完整六字段说明 | 明确写出“当前截图未展示后台接口或数据写入过程”，不为满足字段完整性编造后台动作。 |
| 第 7 章人工编辑误用源码 ref 校验器，截图引用会被剔除 | 人工保存走截图版本引用校验，记录当前来源集合并更新章节节点，能本地修订恢复，无须付费重写。 |
| 正文把局部机制扩大为“所有业务写入”“失败信息不存原始细节”，以及把静态文件/建表统计当运行时精确数量 | 当前样例按源码和统计口径局部人工修订，逐项 before/after 留痕；正文提示词增加范围约束。 |
| 图表角色配置为 GLM，首轮图表仍使用正文模型 | 源码代理修复角色路由，并仅重生成一张架构图验证 GLM。 |
| 图内出现“第5层/第6层”，外绕线贴近标题 | 源码代理修复默认层名和标题避让；架构图 v3、部署图 v2 为本地重渲染版本。部分外绕线仍较长。 |
| 图表改版后，图示说明仍直接输出旧生成请求；新图不含 SQLite，旧说明却声称图中有 SQLite | 图示说明改用当前图形语义。完整 Draw.io 编辑后不假设旧语义仍准确，说明以当前图内标注为准。 |
| 六字段说明拼接产生“。；” | 本地统一句尾标点，分析提醒保留。 |
| `finalize_with_warnings=false` 仍可能放过 QA warning，因为 QA passed 只表示没有 blocker | 按 warning_count 判断自动定稿偏好；提示可到说明书工作台处理或审阅后手动定稿。 |
| 恢复优先读取旧终稿，可能遮住新候选或拿旧文档重查新材料 | 只复用当前内容与当前生成器版本；新候选优先，旧版本保留历史，必要时重新装配。 |

## 人工审阅与渲染范围

原始 v3：产品和独立渲染共 21 页，已查看全页联系表及图表、表格、截图关键页原图。没有发现缺字、正文裁切或页脚覆盖，但上述内容/编号/图表缺陷真实存在，旧 QA 通过没有替代人工复核。

最终 v7：查看产品 20 页全页联系表及图表、截图页原图；4 张表、4 张图表、2 张真实截图、8 章目录，连续图号 1–6、表号 1–4。最终 Word 文本检查不再出现已识别的双表号、假保存成功、推断后台函数、精确 47 数量或“。；”。图示说明列举当前实际图形对象，不再沿用旧请求描述。

独立 Documents 技能使用 **LibreOfficeDev 26.8.0.0.alpha0**，系统产品渲染使用 **LibreOffice 26.2.5.2**。同一 v7 在独立环境为 **19 页**；指定相同字体路径后仍为 19 页。独立 PDF 的目录自动更新，后两章实际为第 16/17 页；产品 PDF 中为第 17/18 页。两边目录各自匹配实际分页，全页联系表均已查看，未发现内容缺失。不能把某个 Office 环境的页数当作所有 Word/WPS 环境的固定承诺。产品正式 QA 基线是当前 20 页 PDF；没有进行 Microsoft Word/WPS 原生验收。

截图事实守卫用于拦截模型已经声明的不确定主张，不能证明每句话与图片完全一致。原始设置截图没有声明 warning，其错误通过人工看图识别；更新后的视觉提示词没有再发起第二轮付费视觉请求，不声称此后模型绝不推断。正文的 ref 关联也不能代替逐句语义复核或真实运行证据。

## 输入冻结边界

首次扫描已开始后才收到冻结目录指令。为避免第二次完整付费，保留实际运行，不改写原清单。后来安全冻结副本位于 `/tmp/copyright-audit-20260911/project`；与原扫描 190 项比较，185 项哈希一致，4 个 UI 文件在并行修复中变化，1 个 `.gitkeep` 被安全规则排除。差异为 `ui/App.tsx`、`ui/ManualWorkspace.tsx`、`ui/Settings.tsx`、`ui/SourceMaterials.tsx`。因此本轮证据是原扫描/研究/源码分页所保存的输入及其已说明差异，不是所有后续修改完成后的整库冻结验收。完整明细见 `freeze-comparison.json`、`frozen-input-files.json`。

## 证据入口

- `run-summary.json` / `model-calls.json`：首轮调用数量、结果与时长；没有凭证、请求正文或响应原文。
- `diagram-role-routing.json`：唯一新增 GLM 调用、版本和本地重渲染。
- `manual-human-corrections.json`：原始 v3 新策略缺陷复现、人工修改前后与理由、旧文件 SHA 核对。
- `manual-original-assets.json` / `manual-text.txt`：首轮章节、截图、图表及正文材料。
- `manual-repaired-final-qa.json` / `manual-repaired-summary.json`：当前 v7 产品真实 QA。
- `manual-final-review.json`：当前 SHA、目录、图表数、两种渲染环境、61 项回归和未验证边界。
- `manual-final-v7-contact-*.jpg`：产品 20 页全页联系表。
- `manual-independent-v7-contact-*.jpg`：独立 Office 环境 19 页全页联系表。
- `manual-final-v7-render/`：Documents 技能的独立渲染；较早的 v3/v5 目录与图片用于复现历史，不是最新交付基线。

仓库保留本目录的摘要、调用统计、质量结论和人工修订记录。以上完整正文、完整 QA、逐文件冻结清单及二进制渲染文件保存在本地 artifacts 和隔离数据目录，未全部提交到 Git；仓库内证据文件以 Git 清单为准。
