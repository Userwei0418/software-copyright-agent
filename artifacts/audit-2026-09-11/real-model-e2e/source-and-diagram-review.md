# 源码与图表角色实测补充

本报告对应真实 QuickStart 隔离任务 `99f67040-d30c-4019-8eef-1670be1eeda7`，数据目录 `/tmp/copyright-audit-20260911`。不同于 `../source/` 中早期长路径合成样例和独立源码快照，不能混用任务编号或产物哈希。

## 源码 Word

- 源码方案选入 74 个文件、30,983 行；摘录涉及 33 个文件，语言包括 Python、Rust 和 TypeScript TSX。
- 59 页源码正文，每页 50 个分页条目，加 1 页说明页，真实渲染共 60 页。条目为 2,883 行代码、35 行文件路径标题、32 行分隔。
- 19 项产物 QA 全部通过；最低正文填充率 0.8812；未发现空白页、缺字或系统字体回退；DOCX 的 SHA-256 与数据库记录一致。
- 已查看全部 60 页联系表及第 2、45、60 页原尺寸图。未发现裁切、页眉页脚重叠或溢出；这不是逐字核读全部页面，也不代表 Windows Word 验证。
- 首段包含本仓库 landing/main.tsx，后续包含应用 Python/Rust/TSX；不声称覆盖所有模块。

完整计数、哈希和图像路径见 [source-review.json](source-review.json)。

## 图表角色与分组名称

发现 `ManualFigureService._context` 仅取说明书任务的正文模型，导致已配置的 GLM 图表角色被忽略。选择顺序已修复为：编辑器显式选择、QuickStart 保存的图表角色、当前图表默认值、旧任务正文模型。失效模型给出重新选择后重试的错误提示，避免静默换模型。

另外，Draw.io 绘图器按层序号套用缓存、基础设施等固定名称，超过四层写成“第 5 层 / 第 6 层”。现在优先使用语义中的真实分组名，旧语义则取该组实际节点名称；模型提示和归一化也拒绝编号占位名。

实际只对 `architecture_overview` 进行一次外部模型生成：

- v2：`glm-5.3-flash`，`messages` 端点，实际非空输出 5,419 字符，14,316 ms；仅新增此图一个版本，其余图和说明书 Word 未重跑。
- 实际 PNG 复核发现 CLI 绕线挤到标题区，绘图器已扩大标题避让范围，运行时检查也纳入标题碰撞。
- v3：沿用 v2 语义、本地重新渲染发布，无外部模型调用。层名为“桌面层 / 本地服务层 / 子服务层”，10 个节点、9 条边，标题碰撞已消除。
- 实际 XML 结构校验通过；仅排除装饰背景色块的 QA 副本通过技能严格布局检查，保留所有节点、层名、标题和连线，0 警告。
- 已用 Draw.io Desktop CLI 导出 SVG/PNG 并查看。节点、连线几何一致，但其边标签位置与应用内部渲染器不同：XML 固定偏移与内部标签避让算法尚未完全统一。少量外绕线较长，最终 Word 字号与整页效果由说明书验收另行确认。

回归：`test_drawio_document.py` 19 项、`test_manual_figures.py` 7 项、`test_manual_workflow.py` 5 项通过；`git diff --check` 通过。正文研究/草拟和截图分析/重试角色线路已检查。主代理已同步修复 UI 编辑器首次选项：优先图表默认模型，后续轮询保留用户选择。

模型调用、版本及产物路径见 [diagram-role-routing.json](diagram-role-routing.json)。

## SenseAudio 补测

MiniMax 初次 32 token 预算只得到空正文，不能据此判定模型不可用。一次受限补测改为 1,024 token，实际返回 `OK`，HTTP 200，4,246 ms，175 个 completion tokens。验证端点已通过应用服务保存到正式模型配置及隔离配置，其他默认项不变。目前 15/15 文本模型均有实际非空返回；这代表接口和解析可用，不代表持续可靠性或长文质量排名。

完整无秘密证据见 [senseaudio-validation.json](../senseaudio-validation.json)。
