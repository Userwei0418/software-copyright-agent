# 架构决策记录

## ADR-001：产品完全独立于 Codex

- 状态：已接受
- 日期：2026-08-10

决定：产品运行时不得依赖 Codex、Codex CLI、Codex Skill 或其隐式工具环境。现有 Skill 仅作为领域规则和确定性算法的迁移来源。

原因：产品必须可以作为独立桌面应用安装和运行，并由自身提供模型适配、工作流、本地工具、持久化、重试和质量检查。

## ADR-002：单机本地工具，不设计用户体系

- 状态：已接受
- 日期：2026-08-10

决定：不引入账号、用户、租户、角色、团队协作或云端任务管理概念，也不自动提交申请材料。

原因：产品目标是快速读取本地项目并辅助生成材料，多用户能力会增加与目标无关的复杂度。

## ADR-003：SQLite 与任务目录共同持久化

- 状态：已接受
- 日期：2026-08-10

决定：使用 SQLite 保存任务、阶段、配置引用、证据索引、产物元数据和 QA 结果；使用任务目录保存 Word、图表、渲染文件和大体积日志。

约束：API Key 不得以明文写入 SQLite、任务目录或日志。

## ADR-004：模型按协议和能力抽象

- 状态：已接受
- 日期：2026-08-10

决定：将协议适配器、模型服务配置和模型能力声明分层设计。首期不绑定单一国外厂商，并为国内模型和厂商专有协议保留扩展点。

原因：不同服务在结构化输出、工具调用、视觉、流式输出和上下文限制方面存在差异，不能只抽象一个 `chat()`。

## ADR-005：采用受控阶段工作流

- 状态：已接受
- 日期：2026-08-10

决定：使用可持久化的阶段状态机编排任务。模型只能调用明确注册、受权限和目录限制的工具，不提供任意 Shell 自主执行能力。

原因：软著材料生成的流程和产物相对明确，受控工作流更安全、更可测试、更容易恢复。

## ADR-006：CLI 内核优先，尽早验证桌面纵切

- 状态：暂定接受
- 日期：2026-08-10

决定：先实现与 UI 解耦的领域内核和最小 CLI；在 M1 后尽早建立 Tauri 与 FastAPI sidecar 的薄纵向链路，不等所有出件能力完成后再验证桌面打包。

原因：需要同时降低产物质量风险和跨平台 sidecar 风险。

## ADR-007：FastAPI 只作为本地传输层

- 状态：已接受
- 日期：2026-08-10

决定：Tauri 与 sidecar 通过仅监听 `127.0.0.1` 的随机端口通信，使用每次启动生成的会话令牌。FastAPI 负责 HTTP、SSE、Schema 校验和生命周期接口，不承载领域状态真相。

原因：本地 HTTP 与 SSE 便于调试和跨平台实现，但必须避免开放到局域网或在 Web 层复制工作流状态。

## ADR-008：阶段恢复依赖 checkpoint 和输入指纹

- 状态：已接受
- 日期：2026-08-10

决定：阶段能否复用或恢复由 checkpoint Schema、输入指纹、规则版本及产物哈希共同决定。应用异常退出后不得仅将 `running` 状态改回并盲目续跑。

原因：模型调用、文件出件和外部渲染具有不同的幂等性，恢复前必须证明已有输出完整且仍与当前输入一致。

## ADR-009：产物采用不可覆盖的版本记录

- 状态：已接受
- 日期：2026-08-10

决定：同一逻辑产物重新生成时创建新版本，旧版本保留并标记为 `superseded`。最终交付状态只能由版本化质量策略产生。

原因：保留生成历史有利于审计、回退和故障诊断，也能阻止模型或单一生成阶段绕过质量门禁。

## ADR-010：发布包内置可再分发的 CJK 字体并执行字体探测

- 状态：已接受
- 日期：2026-08-10

决定：macOS 与 Windows 发布包必须内置同一套许可兼容的开源 CJK 字体。DOCX 生成和渲染前先探测字体可用性；缺失时阻断中文项目出件，不允许静默回退为缺字字体。

原因：真实 LibreOffice 渲染验证表明，macOS 已安装的受系统保护中文字体可能被识别但无法渲染字形。仅在 OOXML 中声明字体名不能保证跨平台输出一致。

实施记录：首个实现采用 Noto Sans CJK SC Regular，保持上游文件不变并附带 OFL 1.1。字体通过 OOXML `embedRegular` 内嵌进 DOCX；仅设置系统字体名、`SAL_FONTPATH` 或临时用户字体目录均不能满足当前 macOS headless LibreOffice 的真实渲染门禁。

## ADR-011：核心出件与编辑不得依赖外部办公或绘图软件

- 状态：已接受
- 日期：2026-08-10

决定：最终桌面产品必须自行完成 DOCX 生成、图表预览、资产微调、SVG/PNG 导出和插图。LibreOffice 与 Draw.io Desktop 仅作为开发期真实渲染验证器或用户可选兼容工具，不得成为一键生成的必装前置条件，也不默认随主安装包捆绑。

图表资产采用“证据化语义图 → 自动布局基础版本 → 人工/AI 修改覆盖层 → 导出产物”的分层模型。重新生成时保留可匹配节点的人工布局和样式；语义冲突必须提示用户选择，不得静默覆盖。

原因：完整捆绑通用办公和绘图应用会显著增加跨平台安装包体积、签名与升级复杂度；生成后不可修改又不符合本地材料辅助工具的使用方式。

实施顺序：当前外部工具调用保留为质量基准；在桌面纵切阶段实现内置资产工作台和内部图形导出，再解除核心流程对外部可执行文件的依赖。

## ADR-012：快速开始采用固定任务身份与双交付分支

- 状态：已接受
- 日期：2026-08-13

决定：一次快速开始运行在创建时持久化完整配置快照，并在首次扫描/创建说明书任务后固定 `task_id` 与 `manual_job_id`。共享扫描和用户确认完成后，源代码文档与软件说明书以两个后台分支并行推进；只有两支都达到交付条件后才进入统一装配和交付。页面轮询、路由切换和失败重试只能读取或恢复这条运行记录，不得隐式创建 v2/v3 整单任务。

快速开始可以在用户启动前明确确认截图安全、自动采用和非阻断警告策略后自动生成终稿；专业说明书工作台继续要求人显式点击“生成终稿”。两种路径都不能绕过阻断级 QA。

原因：源码分页与说明书研究不存在必须串行的依赖；固定任务身份可避免重复计费、重复截图解读和版本漂移，也让节点级恢复、可视进度与资产审计具有稳定锚点。

## ADR-013：真实截图必须经能力实测与明确授权

- 状态：已接受
- 日期：2026-08-13

决定：视觉能力不能只依赖用户勾选或模型名称推断。模型配置必须用应用提供的真实测试图片完成一次请求并记录通过结果，才能被保存为默认截图解读模型。快速开始只读取用户通过系统选择器指定的截图文件夹，并要求用户确认敏感信息与自动采用授权；系统不递归猜测整个项目中的普通图片素材，也不为追求自动化隐式启动项目或登录账号。

原因：能力声明与真实 API 行为可能不一致；截图属于用户界面证据且可能包含账号、密钥或身份信息，必须同时满足技术能力验证、来源边界和授权边界。
