# 新会话启动提示词（2026-08-11）

请完整阅读当前目录 `PROJECT_HANDOFF.md` 最前面的“当前真实状态”与“下一轮开发顺序”，再检查工作树和 `PROGRESS.md` 最后 150 行。不要整份读取 1900 多行的进度文件，也不要根据交接文档后半部的早期“尚未编码”描述重新立项。

这是一个已经可运行的本地单用户软著材料桌面应用：Tauri v2＋FastAPI Sidecar＋SQLite，独立于 Codex，支持多厂商/多模型，已有源码 DOCX、说明书、Draw.io、截图资产、预览、导出、版本、QA 和后台持久化任务。当前尚未通过最终产品验收，禁止打包、发布、清库或大范围重构。

本轮只聚焦说明书长任务的执行架构与前端可观测性：

1. 当前章节和图表各自已经并发，但总流程仍是串行栅栏；图表模型调用和最多 3 次校正会拖住截图、Word 和导出。
2. 改成依赖图与可部分交付：正文完成即生成可预览、可真实落盘的阶段审阅稿；图表/截图按章节派发，失败不得阻断正文；当前可用资产再装配正式候选稿。
3. AI 只生成短图表语义 JSON，本地负责 Draw.io、SVG、PNG 和校验。首次失败最多修复一次，随后保留失败节点供单项重试，不整单重跑。
4. 截图候选可异步规划，但启动项目必须用户明确授权；未授权、不可启动或单页失败只能标记为等待/警告，不能阻断审阅稿。
5. 前端改成任务驾驶舱，清晰显示真实节点：排队、运行、完成、警告、失败、等待授权；显示模型、并发、心跳、节点耗时、尝试次数、产物和下一动作。进度按实际节点聚合，不只显示 6 个粗阶段。

请先用代码和测试核对现状，然后按 `PROJECT_HANDOFF.md` 的 1→5 顺序小步实现并在 `PROGRESS.md` 留痕。优先修改 `manual_document.py`、`manual_workflow.py`、`manual_pipeline.py`、`manual_execution.py`、`ManualWorkspace.tsx` 及相关测试。保留工作树全部现有修改；不要 reset、清空配置、自动启动用户项目、构建安装包、提交或推送。

每批完成后运行 Python 全量测试、Rust 测试/检查、前端生产构建和 `git diff --check`，然后启动开发版供用户验收。用户尚未确认文档质量，因此不能宣称产品完成。
