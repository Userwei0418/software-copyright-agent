# 软著 Agent 项目交接上下文

> 用途：将本文完整提供给新的 Codex / AI 会话，使其无需阅读旧会话即可理解项目背景并继续工作。
>
> 当前阶段：产品化立项与架构设计。尚未在本目录开始正式编码。

## 新会话启动指令

请先完整阅读本文件，然后检查当前工作目录。我们准备开发一个真正独立的“软著 Agent”桌面应用，不依赖 Codex 才能运行，目标平台至少覆盖 macOS 和 Windows。先与我确认产品边界、跨平台技术架构、MVP 范围和开发顺序；未经确认不要直接铺开全部编码。后续设计必须区分模型智能、Agent 编排、本地工具和确定性质检，避免把关键质量仅寄托在提示词上。

## 一、项目目标

开发一个本地优先的桌面应用：用户选择或拖入一个软件项目，配置模型 API Key，应用自动生成一套中国软件著作权申请材料。

主要交付物：

1. 软著源代码文档 `.docx`
2. 软件说明书 `.docx`
3. 可编辑的 Draw.io 架构图、流程图等 `.drawio`
4. 图表的高清 `.svg` 和用于 Word 插入的高分辨率 `.png`
5. 项目可运行时的功能页面截图
6. 生成过程记录、假设、缺失信息和成品质检报告

期望用户体验：

1. 安装应用，不要求用户单独安装 Python 或 Node.js。
2. 选择项目目录或压缩包。
3. 配置模型供应商、Base URL、模型名和 Key。
4. 应用扫描项目并展示识别到的名称、版本、技术栈、模块和可运行方式。
5. 用户确认少量无法安全推断的信息。
6. 一键执行分析、代码筛选、写作、绘图、截图、Word 出件和 QA。
7. 失败可重试，任务可暂停、恢复并查看阶段进度。
8. 最终文件可直接打开和导出，不要求用户手工复制 Markdown 或调整 Word。

## 二、为什么要做成独立应用

现有能力已经以 Codex Skill 形式验证，但完整任务不仅依赖 Skill：

- Skill 提供领域规则、工作流说明、格式约束和脚本。
- 模型负责理解项目、筛选核心代码、推断架构和撰写项目特定内容。
- Codex 当前隐式提供 Agent 循环、Shell、文件读写、浏览器、视觉检查、权限控制、重试和任务持续执行。
- 独立应用必须自行补齐这些运行时能力。

产品方向已确定优先考虑“路线二”：自研真正独立的软著 Agent，而不是给 Codex CLI 套一个 UI。可以把 Codex CLI 作为可选高级执行后端，但不能成为产品运行的必要条件。

## 三、现有 Skills

### 1. `copyright-doc`

位置：`~/.codex/skills/copyright-doc/`

触发示例：

> 使用 Software Copyright Documents，读取这个项目，生成软著源代码文档和软件说明书；根据系统功能自动绘制架构图和业务流程图，并插入相应章节。

已经固化的核心能力与规则：

- 读取项目 README、依赖清单、部署文件、架构文档和代表性源码。
- 将源码分成 A/B/C 三档，优先选择核心业务代码和必要基础设施代码。
- 排除依赖、构建产物、测试、fixture、vendor、migration、lockfile、mock、demo、sample、legacy 和敏感信息。
- 不允许根据文件名虚构系统行为，技术结论必须有仓库证据。
- 自动推断系统名称、版本、用途、用户、技术栈、模块、存储、接口和部署方式。
- 默认生成源代码文档和软件说明书两份 Word。
- 源代码文档默认目标为总计 60 页：1 页封面＋59 页代码。
- 长代码行先按视觉宽度硬换行，再确定性分页，避免 Word/WPS 自动换行造成 60 页变成 120 页。
- 代码不足时如实输出，不得用依赖、测试、mock、重复代码或伪造代码补页。
- 软件说明书标题为黑色；封面不显示“软件著作权申请材料”字样。
- 正文段落首行缩进两个中文字符，标题、图注、表格、列表和代码不缩进。
- 表格不能在页末只留下表头；表头至少与第一行数据绑定，短表尽量整体分页。
- 自动绘制架构图和业务流程图并插入相关章节。
- 项目能安全启动时，使用浏览器自动化截图，并生成内容充实的“用户界面与操作说明”。
- 每张页面截图的介绍至少覆盖页面目的、区域控件、典型操作、后端数据交互、结果状态、校验异常和恢复方式。
- 软件说明书要具有实现导向的充实内容，而不是图片周围的简短概述。
- DOCX 必须渲染检查，修复分页、表格、字体、图片、空白页、页眉页脚等问题后再交付。

### 2. `drawio-professional-diagram`

位置：`~/.codex/skills/drawio-professional-diagram/`

触发示例：

> 使用 Draw.io Professional Diagram，读取这个项目，根据系统功能和代码结构自动绘制系统架构图与核心业务流程图；交付可编辑的 `.drawio` 和高清 `.svg` 文件。

已经固化的核心能力与规则：

- 生成未压缩、可编辑的 Draw.io XML，并导出 SVG。
- 默认使用专业、克制的企业级视觉风格，但允许根据内容自由发挥。
- XML 是图表源文件，图片只用于渲染检查，不使用图像生成模型替代 Draw.io。
- 生成前规划层级、节点位置、连接端口、路由通道、路径点和标签位置。
- 跨容器、横向、回退和跨多层连线必须显式规划路径点。
- 连线不得穿过无关节点、文字、容器标题或数据库标签。
- 主流程只连接相邻节点，不能使用一条长线贯穿多个流程节点。
- 判断分支从不同侧面出发；异常和回路线走外围通道。
- 边标签必须有不透明背景和明确偏移。
- 已新增自动布局检查器，可检测：节点重叠、文字容纳风险、连线穿节点、连线交叉、复杂连线缺少显式路径、标签无背景或无偏移。
- 严格模式存在警告时返回非零状态，阻止直接交付。
- Draw.io 渲染后仍需进行视觉复核，因为静态几何分析不能完全代替真实渲染。

## 四、已经验证和修复过的问题

源代码文档：

- 曾出现目标 60 页、实际 121 页的问题，原因是依赖 Word 自动换行估页。
- 已改成按视觉宽度硬换行后再分页。
- 曾出现某页大面积空白、代码被推到下一页的问题，已要求内容节顶端对齐并控制分页。
- 明确 60 页包含封面，而不是 60 页代码再加封面。

软件说明书：

- 标题颜色统一为黑色。
- 移除封面的“软件著作权申请材料”。
- 增加正文首行缩进两个中文字符。
- 增加页面截图及页面功能说明。
- 页面说明不能只占半页，应根据项目证据补充实质内容。
- 技术文档应覆盖设计目标、模块协作、主异常流程、状态转换、数据生命周期、接口校验、权限、安全、恢复、部署、性能、维护、测试和验收。
- 表格分页时不能在前一页留下孤立表头。

Draw.io 图表：

- 历史图中出现过连线穿过 SQLite、模块正文、判断菱形和容器标题的问题。
- 已将边路由作为一级质量指标，并加入自动检测与严格阻断。
- 盲测生成过一个包含 16 个节点、18 条连线的复杂流程图，首版严格检查为 `warnings=0`，说明规则对默认直出有改善。

## 五、独立应用建议架构

推荐总体结构：

```text
Tauri v2 桌面壳（macOS / Windows）
        │
        ▼
本地任务引擎（建议 FastAPI sidecar）
        │
        ├── 工作流状态机 / 任务队列 / 断点恢复
        ├── 模型供应商适配层
        ├── 项目扫描与代码筛选工具
        ├── 受控本地命令执行器
        ├── Playwright 浏览器与截图服务
        ├── Draw.io XML、校验和渲染服务
        ├── DOCX 生成与分页服务
        ├── LibreOffice / PDF 渲染服务
        └── 确定性质量检查器
                │
                ▼
代码文档＋软件说明书＋图表＋截图＋QA 报告
```

建议职责划分：

### 模型负责

- 理解陌生项目。
- 识别业务模块和核心流程。
- 对候选源码进行语义排序。
- 生成项目特定的软件说明内容。
- 规划专业图表的语义结构。
- 根据 QA 报告修订内容。

### 本地确定性程序负责

- 文件扫描、ignore 规则和敏感信息过滤。
- 代码视觉宽度计算、换行和页数控制。
- DOCX 样式、页眉页脚、图注、表格和分页。
- Draw.io XML 结构与路由检查。
- 启动进程、端口探测、浏览器截图。
- DOCX/PDF 渲染和页面级检查。
- 字数、页数、图像落位、表格孤立表头、字体和空白页检查。
- 任务状态持久化、重试、超时、取消和恢复。

重要原则：关键质量门禁必须由程序执行，不能只依赖提示词让模型“自觉检查”。

## 六、多模型兼容策略

应用应提供统一的模型适配接口，例如：

```text
ModelProvider
├── OpenAIProvider
├── AnthropicProvider
├── GeminiProvider
├── OpenAICompatibleProvider
└── OptionalLocalModelProvider
```

建议第一阶段支持：

1. OpenAI API
2. OpenAI-compatible Base URL＋Key＋模型名
3. 后续增加 Anthropic 和 Gemini 原生协议

需要抽象的能力，而不是只抽象 `chat()`：

- 普通文本生成
- JSON Schema / 结构化输出
- 工具调用
- 图片输入
- 流式输出
- 超时、重试、限流和错误标准化
- 上下文窗口和输出长度能力声明
- token 与成本统计

不要假设所有模型质量一致。应提供能力探测和最低要求：

- 良好的代码理解和中文长文能力
- 足够的上下文长度
- 稳定结构化输出
- 可靠的工具调用
- 最好支持视觉输入，用于截图和 Word 页面检查

浏览器、Word、Draw.io 和本地文件工具属于应用能力，不属于某一模型供应商。模型通过统一工具协议调用它们。

## 七、macOS 与 Windows 兼容性要求

至少在设计阶段提前处理：

- Tauri 桌面壳分别构建 `.dmg` 和 Windows 安装包。
- 本地引擎分别构建 macOS arm64、必要时 x64，以及 Windows x64 sidecar。
- 所有路径使用跨平台 Path API，不能硬编码 `/`、盘符或用户主目录。
- 任务和产物放在平台标准应用数据目录或用户选择的目录。
- 子进程创建、终止和进程树清理在 macOS/Windows 上分别实现。
- 端口占用、权限、防火墙、SmartScreen、Gatekeeper 和代码签名要进入发布方案。
- 中文字体要随应用合法分发或采用可靠的字体回退方案。
- LibreOffice、Draw.io 和浏览器运行时不能假定用户已安装。
- 优先评估将 Playwright Chromium 随安装包或首次启动下载的体积与升级策略。
- 如果依赖 LibreOffice 和 Draw.io Desktop，应评估许可证、安装包体积、静默调用和多平台兼容；也可以逐步替换为内置渲染实现。
- 项目启动命令必须经过用户确认，并限制在项目工作目录，不能访问或删除无关文件。
- API Key 使用系统安全存储：macOS Keychain、Windows Credential Manager；界面不回显完整 Key。

## 八、参考项目：bid-dog

项目地址：<https://github.com/shandianT/bid-dog>

值得参考的架构思想：

- Tauri 桌面壳。
- FastAPI 本地引擎。
- PyInstaller 单文件 sidecar，用户无需安装 Python。
- SSE 推送阶段、进度和产物事件。
- 执行外壳＋技能包＋确定性出件脚本。
- 支持粘贴 Key，也支持外部 CLI/Agent 运行方式。
- 质检不通过不直接出 Word，由程序审计并重新出件。
- Key 与任务文件本地保存，前端不直接持有真实 Key。

注意：该仓库声明版权保留，未经许可不得商业分发、再发布或制作衍生产品。只借鉴公开的架构思想，不复制其受版权保护实现。

## 九、建议的 MVP 边界

建议不要第一版就实现所有能力。MVP 可限定为：

1. macOS arm64＋Windows x64。
2. 选择本地项目目录。
3. OpenAI 与 OpenAI-compatible 模型接入。
4. 项目扫描、元数据确认和核心源码筛选。
5. 生成稳定分页的源代码 DOCX。
6. 生成基础但内容充实的软件说明书 DOCX。
7. 生成一张系统架构图和一张核心业务流程图。
8. 确定性 DOCX 与 Draw.io QA。
9. 任务进度、日志、取消和失败重试。
10. 页面自动启动与截图作为第二阶段能力，但架构需预留 BrowserAdapter。

不建议 MVP 立即支持：

- 所有模型厂商的原生协议。
- 云端多租户。
- 自动提交软著申请。
- 任意项目都能完全无人值守启动。
- 复杂多人协作、素材库和企业权限体系。

## 十、推荐开发顺序

1. 输出产品需求文档、成功标准和非目标。
2. 设计任务状态机、产物目录和失败恢复协议。
3. 定义 ModelProvider、Tool、Artifact、QAResult 等核心接口。
4. 将现有 `copyright-doc` 的确定性脚本抽成独立 Python 包。
5. 实现 CLI 原型，不先做桌面 UI。
6. 用两个已经验证过的真实项目建立回归测试基线。
7. 实现 Tauri 壳和 FastAPI sidecar。
8. 加入模型配置、任务页面、日志流和产物预览。
9. 加入 Playwright 截图适配器。
10. 完成 macOS/Windows 构建、签名、安装、升级与故障诊断。

建议先证明“同一输入可重复生成合格产物”，再优化 UI。

## 十一、下一会话首先需要对齐的问题

1. 产品面向内部使用、交付客户，还是计划商业销售？这影响签名、升级、授权、日志和数据安全设计。
2. MVP 是否接受“首次运行下载 Chromium/LibreOffice 等组件”，还是必须完全离线单安装包？
3. 第一批模型接口是否确定为 OpenAI＋OpenAI-compatible？
4. 第一版是否先做 CLI 引擎再接 Tauri，还是直接搭建完整桌面骨架？推荐先做 CLI 引擎。
5. 现有两个 Skill 的脚本是否作为初始内核迁移，并继续保持 Skill 与独立应用共用一套规则？推荐共用。
6. 是否允许项目启动与浏览器截图进入 MVP？推荐架构预留，功能放第二阶段。
7. 需要支持哪些输入：目录、ZIP、Git URL？推荐 MVP 先支持目录和 ZIP。
8. 产物是否必须严格保持当前已经验证的 Word 版式？如果是，应尽早建立黄金样例和页面截图回归。

## 十二、历史测试项目与旧产物线索

旧会话曾使用以下项目验证现有 Skill：

- `pitch-creator`
- `seek-pet-customer-service-platform`

曾生成并检查源代码文档、软件说明书、架构图、流程图和页面截图。这些项目适合继续作为回归测试集，但新会话需要重新确认它们在当前设备上的路径和访问权限。

历史工作区曾位于：

`/Users/zhouwei5/Documents/Codex/2026-08-09/https-github-com-liujuntao123-smart-excalidraw-3/`

现阶段新的独立应用工作目录为：

`/Users/zhouwei5/Documents/ChatGPT/软著Agent/`

## 十三、必须保持的设计原则

- 不编造项目事实和源码。
- 不泄露 API Key、环境变量、个人数据和项目秘密。
- 不用测试、mock、依赖或重复代码虚假凑够 60 页。
- 不将模型生成结果未经确定性质检直接交付。
- 不将浏览器截图动作连接到真实付费操作或外部副作用。
- 不因追求“全自动”而掩盖缺失信息；不能安全推断时必须要求用户确认。
- 所有中间状态和产物可追踪、可重试、可恢复。
- macOS 与 Windows 不是发布末期再补，而是从路径、进程、字体、渲染和打包层开始共同设计。

