# 交互、UI 与产物质量审计 · 2026-09-11

用户要求全面优化软著材料助手的交互体验、UI 设计和产物质量，更新 SenseAudio 模型，并完成代码推送和文档同步。本轮以实际运行、文件和请求记录为依据；测试通过、视觉检查、桌面操作与发布验证分别记录。

## 基线与范围

- 开始时工作树干净，提交 `a3e5ee9` 与 `origin/main` 一致；实施分支 `codex/audit-experience-quality`。
- 产品仍为 0.1.1、SQLite v31。本轮没有数据库 Schema 迁移，也未把新代码宣称为旧安装包已包含的能力。
- 模型配置按用户授权更新本机加密凭据与默认模型，其他偏好保留；生成和 UI 回归使用隔离任务。未清理正式任务、未启动用户项目、未提交任何 API Key。

## 交互和界面

| 问题 | 最终行为 |
| --- | --- |
| 多入口并列，用户不知道从哪里开始 | 默认进入快速开始；侧栏突出快速开始与我的资产，专业工作台折叠收纳 |
| 有任务时仍展示完整新建表单，重试和重做混在一起 | 优先展示当前任务与下一步；高级参数、整份重做和完整执行画布按需展开；新建保留历史和资产 |
| 质量问题也鼓励反复重试 | 网络/进程中断提供继续，内容/证据问题先定位处理；仅处理失败节点，不隐式重启整单 |
| 连点、异步乱序和切项目后回调把状态拉回旧任务 | 同步操作锁、单请求轮询和生命周期守卫；旧任务结果不能导航或回填当前任务 |
| 文档保存时仍能编辑，返回后覆盖新输入 | 保存/AI 重写/装配期间锁定标题、正文、表格与章节导航；失败保留待保存内容 |
| 模型测试覆盖未保存偏好；探测新 Key 提前破坏旧 Key | 测试只刷新对应模型状态；发现模型使用临时凭据，成功/失败均清理，仅保存连接才更新正式凭据 |
| 弹窗关闭后被迟到结果重新打开 | 区分数据刷新与预览打开意图；共享 Dialog 限制 Tab、支持 Escape、恢复浏览器焦点与背景交互 |
| 从资产预览一次，后来普通导航也自动弹出 | 预览请求绑定项目并消费一次；关闭、离页、版本变化释放旧图片 URL |
| 图表编辑器加载失败时只有空白画布 | 等待 25 秒后显示手动重新连接；保留当前 XML，不重新调用模型；加载成功后使用配置的图表模型 |
| 断线仍显示在线；重连清掉草稿 | 独立记录健康状态，连续失败后显示重连；保持连接对象与工作台身份，保留未保存章节 |
| 写请求响应丢失后自动重发，可能重复生成或保存 | 只有读取允许自动重连重试；写请求提示结果尚未确认，先刷新后再决定继续 |
| 多处 8–10px 字体，密集操作和状态难读 | 字号下限 12px，正文/操作按层级放大，增加焦点轮廓与可点击尺寸；检查 1440/1080/900px 窗口 |
| 文件损坏、旧 QA 或旧版本仍呈现可交付 | 源码与说明书入口共同核对当前质量和文件完整性；历史运行只表示当次已生成，当前交付状态在资产库查看 |

## 模型配置与实际调用

官方目录于 2026-09-11 核对：[SenseAudio 模型列表](https://docs.senseaudio.cn/guides/account/model-list)。15 个模型统一保存在 [shared/senseaudio-models.json](shared/senseaudio-models.json)，设置页和 Rust 发现回退共用；移除旧型号和无依据的套餐并发承诺。

- 15/15 模型获得真实非空文字响应；MiniMax 初次 32 token 空输出，单次 1024 token 上限补测返回 `OK`，保留前后记录。
- `senseaudio-vl-1.0-260319` 和 `qwen3.8-27b` 使用真实随机字符图片验证；模型名称或用户勾选不作为能力证据。
- 本机默认正文 `senseaudio-s2`、图表 `glm-5.3-flash`、图片 `senseaudio-vl-1.0-260319`。凭据仅存本地加密库。
- 实跑发现图表忽略独立角色。后端改为显式模型 → 本次 QuickStart 图表配置 → 全局图表默认 → 旧任务兼容；失效配置报错而不静默换模型。编辑器首次选项也优先图表默认，刷新保留用户选择。
- 修复后单独重生成一张架构图，确认调用 `glm-5.3-flash/messages`，14.3 秒，返回 5419 字符。没有为此再跑整份说明书。

详见 [模型验证](artifacts/audit-2026-09-11/senseaudio-validation.json) 与 [图表角色实测](artifacts/audit-2026-09-11/real-model-e2e/diagram-role-routing.json)。接口测试代表当次可用，不代表长文质量排名或持续稳定性。

## 源码文档

长路径 FILE 标题原先不参与换行预算，合成复现预检 59 页正文、实际渲染 89 页，29 页正文稀疏。采用 `code-preview-v6` 后真实渲染 60 页、0 稀疏页；旧预检需重新生成。筛选和分页失败可从原阶段继续，复用扫描；候选列表移除后端 30 条截断，搜索覆盖完整集合。

渲染在干净临时目录完成后替换，失败保留上次完整结果，成功时不混入旧页面。源码原生导出复用完整性门禁及写后逐字节校验，不将同名目录当成文件覆盖。

两组源码样本分别为早期独立快照（71 入选/33 取样）和真实 QuickStart 扫描输入（74 入选/33 取样），均为 1 页封面＋59 页正文、19 项 QA 通过。查看全部页面联系表和代表页原图，无缺字、裁切、空白或溢出；不声称逐字核读全部源码。

证据：[源码复现与对照](artifacts/audit-2026-09-11/source/README.md)、[真实任务源码](artifacts/audit-2026-09-11/real-model-e2e/source-review.json)。

## 说明书、截图和图表

- 表格/嵌套表格参与占位、无证据结论和字体检查；目录依据实际章节 ordinal 定位，并与最终 PDF 页码比对。PDF 登记使用实际渲染路径。
- 旧 DOCX 固化自己的名称、版本和资产引用，不得用当前元数据给旧文件套上新规则标签；终稿生成、正式下载和导出核对终稿自身当前 QA。
- 统一生成图表/表格编号，去掉模型标题重复序号；图注绑定当前图形语义，避免图已修改而说明仍沿用旧请求。
- 截图保留已审核用途原句，检查六项说明与安全确认。普通观察提醒可保留；明确声明推断/待核实的事实不能自动采用或进入终稿。规则检查不能替代人眼比对。
- 第 7 章人工修订使用截图版本引用校验与来源记录；可以本地修正并重装配，无需重新调用模型。
- `finalize_with_warnings=false` 按实际提醒数阻止自动定稿。恢复优先采用最新有效候选，旧终稿不能遮住人工修订；同版本反复质检不累加重复提醒。
- 架构分组以真实语义或该组节点命名，取消第 5 层/第 6 层等占位名；绕线纳入标题区域避让。

当前生成器 `formal-manual-docx-v15`，质量策略 `manual-docx-qa-v18`。旧标准文档保留，可预览并在重新装配后按新标准检查。

## 真实任务与人工修订

唯一完整 QuickStart 为 `6b94e538-227f-46e8-aae2-0bdba9e54d93`，任务 `99f67040-d30c-4019-8eef-1670be1eeda7`，说明书任务 `d749c750-47af-4704-8685-6cb59969cb4d`。

- 使用本项目源码及两张实际产品界面截图，明确标记审计样例，不作为客户项目或版权提交材料。
- 首次整单 137.11 秒、16 次真实 HTTP 请求（正文 14、视觉 2），源码 60 页与说明书 21 页。整单自动重试上限 0；仍保留已有章节/图表内部修复策略。
- 初次程序 QA 通过，人工逐页检查仍发现重复表号、把空模型列表写成已配置成功、截图推断后台操作和泛化层名。问题已进入产品修复与回归，不能把初稿 passed 当成内容验收。
- 原终稿 v3 完整保留、哈希不变。截图与相关章节通过服务层保存人工修订；架构新增一次 GLM 版本后仅本地重渲染，其余修订装配无新增模型调用。
- 最终候选 v6 / 终稿 v7 为 20 页，43 项检查、0 blocker、2 类普通提醒：第 16 页章节尾留白，以及截图未展示失败场景。无豁免；目录按最终 PDF 回填。
- 扫描后并行开发发生 4 个 UI 文件变化，后补冻结目录与原扫描不完全相同。记录差异，不将该任务声称为最终 Git 提交的完全冻结快照。

详见 [首次真实运行](artifacts/audit-2026-09-11/real-model-e2e/run-summary.json) 及 [最终说明书复核](artifacts/audit-2026-09-11/real-model-e2e/manual-final-review.json)。早期 [六页合成排版回归](artifacts/audit-2026-09-11/manual/README.md) 单独记录，不能替代真实模型产物验证。

## 可重复验证

```bash
PYTHONDONTWRITEBYTECODE=1 PYTHONPATH=src .venv/bin/python -m unittest discover -s tests -v
npm run test:ui
npm run build
cargo test --manifest-path src-tauri/Cargo.toml
```

浏览器脚本需要本地 Vite、隔离 Sidecar 和 Playwright：`scripts/verify_browser_ui.mjs URL OUTPUT [--populated]`。完整审计数据可用 `scripts/verify_manual_ui.mjs URL OUTPUT` 检查真实手册预览、编辑锁和断线草稿保留；后者拦截所有写请求，不修改后端。URL 只接受回环地址，禁止把正式会话 Token 提交为参数样例。

浏览器记录 9 页×3 窗口的布局、可见字最小字号、运行异常，以及源码真实图片、Tab/Escape、一次性资产预览和重连。编辑器额外检查真实控件禁用与保存失败后草稿保留。handler 测试运行真实 TSX 逻辑，但不替代浏览器 DOM。

最终结果：Python 214 项、UI handler 18 项、连接行为 7 项、UI 契约 16 项、Rust 15 项通过，前端构建通过；浏览器主流程 30 项和说明书交互 3 项通过且无运行异常。真实嵌入 Draw.io 已加载架构图，选中 `glm-5.3-flash`；使用早期真实任务的 UI 审阅快照，手动重连分支由受控测试覆盖，未模拟第三方真实停机。见 [验证总表](artifacts/audit-2026-09-11/verification-summary.json)、[Draw.io 加载记录](artifacts/audit-2026-09-11/diagram-browser.json) 和 [编辑器截图](artifacts/audit-2026-09-11/diagram-browser.png)。

## 桌面与未验证范围

- 自建 macOS 开发桌面壳＋冻结 Sidecar 实际启动，以隔离数据完成源码预览、跳转第 41 页、原生保存窗口和导出；哈希与原文件一致。见 [原生生命周期](artifacts/audit-2026-09-11/native-lifecycle.json)。
- 之后屏幕锁定，未继续执行 Word/WPS 和正常菜单退出；命令行结束开发壳后确认 Sidecar 子进程退出；最终冻结 Sidecar 另用真实父进程终止脚本验证，17.5 秒启动、父进程退出后正常结束。验证脚本的冷启动等待已与桌面 45 秒上限一致。
- 同一终稿在产品 LibreOffice 26.2.5.2 渲染 20 页，在 Documents 技能的 LibreOfficeDev 26.8 alpha 渲染 19 页；相同字体路径仍有差异，两份 PDF 的目录均匹配各自实际页码。不宣称 Microsoft Word/WPS、Windows 安装版或不同 Office 引擎分页完全相同。
- 原生 Draw.io 导出已检查，节点和连线几何一致；边标签位置与应用内部渲染器仍有差异，少量外绕线偏长，不声称像素一致。
- 本轮不创建新发行版本，不修改已发布安装包，也不宣称完成签名/公证。代码推送与正式发布分别核对。
